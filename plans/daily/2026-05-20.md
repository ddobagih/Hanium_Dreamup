# Hanium Dreamup / WalkSafe Assist 일일 계획 - 2026-05-20

## 기준
- 작성일: 2026-05-19 KST
- 대상일: 2026-05-20 KST
- 참고한 daylog/문서:
  - `plans/.work/2026-05-19/daily/*.md` 5개 lane note
  - `plans/daily/2026-05-20.md`: 없음 확인
  - `plans/daily/2026-05-19.md`, `plans/weekly/2026-W21.md`
  - `daylog/2026-05-19.md`
  - `docs/execution/2026-05-19_*.md` 5개
  - `docs/current_status.md`, `docs/pwa_backend_status.md`, `docs/voice_stt_tts_status.md`, `docs/model_v2_status.md`
  - `docs/neck_worn_phone_test_checklist.md`, `docs/report_operations.md`
  - `product/decisions.md`, `product/backlog.md`
- 저장소 내부 `AGENTS.md`는 없음. 사용자 제공 AGENTS 지침 기준 적용.
- 통합 작업 자체에서는 하위/병렬 에이전트를 사용하지 않았다. 5개 lane note와 관련 문서를 병렬 shell 조회로 읽고 단일 통합했다.

## 현재 상태 요약
- 계획 통합 시점 `git status`: `daylog/2026-05-19.md` 수정, `plans/.work/2026-05-19/daily/*.md` 5개 미추적. 내일 실행 전 작업트리를 다시 확인해야 한다.
- PWA는 카메라/센서 재연결, GPS watch 재시작, 방향 센서 상태, 신고 접근성 보강이 반영됐고 lint/typecheck/build/server-mode build는 PASS다. Android 실폰, TalkBack, offline, 브라우저/실폰 마이크는 미확인이다.
- Backend는 detect/uploads no-DB 테스트 `13 passed`, 전체 backend `13 passed, 17 skipped` 상태다. PostGIS/Alembic/reports no-skip/HTTP smoke는 Docker·DB·loopback 제한으로 미완료다.
- Model v2는 class `0 damaged_tactile_block` baseline이다. test mAP50 `0.657`, mAP50-95 `0.481` 근거는 있으나 class `1..3` 한국 GT와 4-class metric은 없다. v3 후보 index 61행, min-box 보류 9행, privacy/location audit이 남아 있다.
- Voice는 direct handler/ASGI 기준 health, intent, CORS, TTS cache 계약은 확인됐지만 실제 HTTP contract, 브라우저/실폰 마이크, TTS HTTP cache header와 청취 평가는 미완료다.
- Integration은 PWA 정적/build와 server-mode build만 PASS다. server-mode headless E2E는 loopback 제한으로 FAIL, `/admin` 운영 흐름은 runtime 신고 생성이 없어 PENDING이다.
- product 결정 기준은 ADB reverse 우선, disposable DB 우선, demo/MVP detector는 `server(.pt)` 우선, fake는 fallback, 공식 field smoke 착용 방식은 사원증형 목걸이다.

## 내일 핵심 목표
- 공통 runtime gate를 먼저 고정한다: DB `5432`, backend `8000`, web `3000`, voice `9001`, Android 접속 방식, 주요 env, 테스트 데이터 cleanup 정책.
- disposable DB 기준 PostGIS/Alembic/reports runtime을 skip 없이 재검증한다.
- Android ADB reverse 기준 fake mode 목걸이 smoke로 카메라, GPS/heading, TTS/진동, 신고 흐름을 확인한다.
- fake 신고 1건을 `/admin` 목록/상세/이미지/위치 품질/상태 변경까지 report ID 기준으로 추적한다.
- voice HTTP contract, CORS, TTS HTTP cache, 데스크톱/실폰 마이크 intent를 direct-call 근거와 분리해 확인한다.
- model lane은 새 학습을 하지 않고 v3 train gate, privacy/location audit, class `1..3` 데이터 확보 계획을 정리한다.
- 실행 결과는 `docs/execution/2026-05-20_*.md`와 `daylog/2026-05-20.md`에 PASS/FAIL/BLOCKED/PENDING으로 분리 기록한다.

## 분야별 상세 체크리스트
### 1. PWA/Accessibility/Sensors
- [ ] 작업트리와 PWA env 확인 → 검증: `git status --short --branch --untracked-files=all`, `NEXT_PUBLIC_API_BASE_URL`, `NEXT_PUBLIC_DETECTOR_MODE`, `NEXT_PUBLIC_VOICE_API_BASE` 기록.
- [ ] PWA 정적 회귀 재확인 → 검증: `cd apps/web && npm run lint && npm run typecheck && npm run build`, `node --check apps/web/public/sw.js`.
- [ ] Android 접속 경로 확정 → 검증: ADB reverse/LAN/HTTPS 중 실제 경로, 기기명, Android/Chrome 버전, 카메라/위치/마이크 권한 기록.
- [ ] fake mode 후면 카메라 smoke → 검증: 프리뷰, bbox, 4개 fake 위험 순환, `데모 탐지 모드`, `source=fake` 확인.
- [ ] 목걸이 착용 통제 테스트 → 검증: 전방 1~3m와 바닥 일부, 렌즈 가림, 흔들림, 화면 미주시 인지성 기록.
- [ ] GPS/방향 센서 확인 → 검증: 위치 허용/거부, accuracy m, heading 값 또는 `방향 센서 미지원/권한 필요/수신 중` 상태와 신고 metadata 반영 확인.
- [ ] TTS/진동 체감 확인 → 검증: 4개 위험 문구, 6초 쿨다운, 음성 on/off별 진동, 신고 성공/실패/유사 신고 패턴 구분 기록.
- [ ] fake 신고와 `/admin` 운영 흐름 확인 → 검증: 신고 ID, 이미지, 위치 품질, `fake_source`, duplicate 후보, `new -> reviewed -> resolved` 상태 변경 기록.
- [ ] PWA 설치/offline shell 확인 → 검증: standalone 실행, 네트워크 차단 후 `/` fallback, manifest/icon/static cache 확인. 오프라인 신고 큐로 표현하지 않음.
- [ ] 접근성 수동 점검 → 검증: TalkBack 또는 DevTools로 `aria-live`, `aria-pressed`, 신고 버튼 disabled reason, bbox `aria-hidden`, 터치 타깃 확인.
- [ ] PWA 실행 문서 작성 → 검증: `docs/execution/2026-05-20_pwa_accessibility_sensors.md`에 실행한 항목만 PASS로 기록.

### 2. Backend/PostGIS/API
- [ ] backend runtime gate 기록 → 검증: `DATABASE_URL`, `UPLOAD_DIR`, `MODEL_ARTIFACT_PATH`, `MODEL_VERSION`, 포트 `5432/8000`, 실행 전 reports count와 upload 파일 수 기록.
- [ ] disposable DB 준비 → 검증: 운영/공유 DB가 아닌지 확인하고 cleanup 또는 rollback 절차를 문서화.
- [ ] PostGIS/Alembic 재검증 → 검증: `docker compose up -d db`, db healthy, `python -m alembic -c backend/alembic.ini upgrade head`, `current == 202605120001 (head)`.
- [ ] DB schema 확인 → 검증: PostGIS extension, `reports` 테이블/index, `Find_SRID('public','reports','location') == 4326`.
- [ ] reports 테스트 no-skip 실행 → 검증: `python -m pytest backend/tests/test_reports.py -q -rs`가 skip/fail 없이 PASS.
- [ ] 전체 backend suite 실행 → 검증: `python -m pytest backend/tests -q -rs` 결과에서 skip/fail/timeout 여부 기록.
- [ ] reports HTTP smoke 실행 → 검증: `/health`, `/detect/health`, `/reports?limit=1`, `POST /reports`, 상세 조회, 필터, `PATCH /status` 응답 기록.
- [ ] upload matrix 확인 → 검증: JPEG/PNG/WebP 성공과 `unsupported_image_type`, `image_extension_mismatch`, `empty_image`, `image_content_mismatch`, `upload_too_large` status/detail.code 기록.
- [ ] duplicate/radius 확인 → 검증: 같은 class/25m/±10분 duplicate ID 반환, `lat/lng/radius_m` 일부 누락 시 HTTP 400 확인.
- [ ] `/detect` 회귀 확인 → 검증: 모델 미설정 `503 model_unavailable`, `best.pt` 설정 시 `/detect/health ready`, known-positive `/detect` `200`과 `source=server`.
- [ ] cleanup 수행 → 검증: 생성 report row와 upload 파일 삭제 후 count/file 상태가 실행 전 기준으로 돌아왔는지 기록.
- [ ] migration 후보는 설계 메모만 작성 → 검증: 코드 변경 없이 `status/source/class_name` constraint, `updated_at` trigger, status history, auth/rate-limit/storage를 C 작업으로 분리.

### 3. Vision Model/Data/MLOps
- [ ] 시작 gate 확인 → 검증: `df -h /home/ddobagi/Code/hanium-dreamup /home/ddobagi/Downloads`, `sha256sum best.pt best.onnx`, `git ls-files`로 대형 산출물 미추적 확인.
- [ ] v3 후보 61행 재검산 → 검증: `walksafe_kr_v3_candidate_index_2026-05-19.csv` row count와 summary JSON policy count 일치 확인.
- [ ] min-box 보류 9행 정책 결정안 작성 → 검증: `hold_until_min_box_policy_review` 9행을 `exclude`, `manual relabel`, `small-object include` 후보로 분류하되 학습 편입은 보류로 기록.
- [ ] bbox 보정 대상 12행 점검 → 검증: `include_after_box_review` 12행의 GT/pred bbox 차이와 수동 라벨 보정 필요 여부 기록.
- [ ] hard-negative 후보 분리 → 검증: 일반 hard-negative 33행과 threshold/augmentation 우선 4행을 별도 bucket으로 유지하고 split 혼합 금지 확인.
- [ ] privacy/location audit 초안 작성 → 검증: 얼굴, 차량번호, 신발, 민감 위치, 동일 장소 연속 프레임 확인 항목 작성. 현재는 contact sheet 수준 검수였음을 명시.
- [ ] full test split sampling 실행/보류 판단 → 검증: `/` 가용 10GB 미만 또는 장시간/RSS 위험이면 보류. 실행 시 `--save` 없이 CSV/checkpoint만 생성하고 이미지 파일 0개 확인.
- [ ] class `1..3` 데이터 확보 manifest 초안 작성 → 검증: 킥보드/자전거, 공사 장애물, 포트홀별 positive/negative 목표, source, 제외 기준, split key, privacy 상태 필드 정의.
- [ ] AI Hub 159 소형 subset 보유/다운로드 가능성 재확인 → 검증: `School`, `Building_area`, `Bridge` label/image zip 존재 여부와 예상 용량 기록. 실제 다운로드는 디스크 gate 통과 전 보류.
- [ ] ONNX browser latency 실행 가능 여부 판단 → 검증: 실행 시 기기/브라우저/p50/p95 기록, 미실행 시 `PENDING/BLOCKED` 기록.
- [ ] Model 실행 문서 작성 → 검증: `docs/execution/2026-05-20_model_data_mlops.md`에 새 학습/4-class metric/field 성능을 완료로 쓰지 않음.

### 4. Voice STT/TTS
- [ ] voice/web env 확인 → 검증: `NEXT_PUBLIC_VOICE_API_BASE`, `VOICE_CORS_ORIGINS`, web `3000`, voice `9001` 접속 주소 기록.
- [ ] voice 서버 기동 → 검증: `PYTHONPATH=. python -m uvicorn voice.server:app --host 127.0.0.1 --port 9001`, `GET /health` 응답 기록.
- [ ] HTTP contract smoke 실행 → 검증: `python scripts/check_voice_contract.py --base-url http://127.0.0.1:9001 --timeout 5` PASS.
- [ ] PWA CORS 확인 → 검증: 브라우저 Network 또는 curl header로 `/speech/stt` OPTIONS/POST CORS 오류 없음 기록.
- [ ] TTS 7문구 HTTP cache 확인 → 검증: 각 문구 2회 `POST /speech/tts`, 두 번째 `X-Voice-Cached: true`, WAV 크기 기록.
- [ ] 데스크톱 마이크 E2E → 검증: `신고해`, `음성 켜`, `음성 꺼`, `다시 말해줘`, `지금 어디야`, `목적지 서울역으로 설정해`, `길 안내 시작해` transcript/intent/confidence/UI action 기록.
- [ ] 음성 신고 경로 확인 → 검증: `create_report`가 현재 위험 신고 버튼과 같은 조건, duplicate check, `POST /reports` 흐름을 쓰는지 확인.
- [ ] fallback 회귀 확인 → 검증: server down, timeout, 권한 거부, `unknown`, confidence `< 0.7`, `speechEnabled=false`, `repeat_last`에서 앱 전체가 막히지 않음.
- [ ] Android 실폰 마이크/스피커 smoke → 검증: ADB reverse/LAN/HTTPS 중 실제 방식, 기기명, Android/Chrome 버전, 마이크 권한, 핵심 명령 4개 이상 UI 변화, TTS 시작 지연/명료도/음량 기록.
- [ ] 자연어 목적지 표현 결정 → 검증: `서울역으로 안내해줘`를 `set_destination`으로 확장할지 확인 필요. 채택 시 rule/test 후보 명시, 미채택 시 `unknown` fallback 문서화.
- [ ] Voice 실행 문서 작성 → 검증: `docs/execution/2026-05-20_voice_stt_tts.md`에 HTTP/PWA/실폰 검증과 direct-call 근거를 분리.

### 5. Integration/Field Test/Report
- [ ] 공통 runtime gate 기록 → 검증: DB/backend/web/voice/Android 접속 방식, env, 권한 상태를 PASS/BLOCKED 표로 기록.
- [ ] 테스트 데이터 격리 기준 확정 → 검증: disposable DB 또는 테스트 전용 `UPLOAD_DIR`, 실행 전후 report row 수와 upload 파일 수 기록.
- [ ] fake/server 근거 분리 → 검증: fake 신고는 `source=fake`, server 신고는 `source=server`로 별도 report ID 기록.
- [ ] fake mode 목걸이 smoke 통합 → 검증: PWA lane 결과에서 카메라/GPS/heading/TTS/진동/신고 ID를 통합 표로 정리.
- [ ] `/admin` 운영 흐름 확인 → 검증: 신고 ID 기준 목록/상세/이미지/위치품질/검토플래그/상태 변경 확인.
- [ ] server mode 선행 조건 확인 → 검증: `/detect/health ready`, 실제 `/detect` timeout 없는 응답, `source=server` 또는 정상 빈 detections 기록.
- [ ] server mode PWA/headless smoke 재실행 → 검증: `scripts/check_pwa_server_e2e.py --web-mode dev|start`, bbox, `source=server`, `metadata.source=server`, report ID, cleanup 기록.
- [ ] 음성 신고 통합 확인 → 검증: `신고해` transcript/intent/confidence/UI action과 버튼 신고 조건, duplicate check, `POST /reports` 경로 일치 여부 기록.
- [ ] 테스트 데이터 cleanup 확인 → 검증: 생성 report row와 upload 파일 삭제 결과 또는 보존 사유 기록.
- [ ] 통합 실행 문서 작성 → 검증: `docs/execution/2026-05-20_integration_field_report.md`에 PASS/FAIL/BLOCKED/PENDING을 분리하고 실행하지 않은 항목은 PASS로 쓰지 않음.
- [ ] daylog 통합 입력 제공 → 검증: `daylog/2026-05-20.md`에 변경 파일, 실제 검증, 미완료/막힘, 병렬 에이전트 결과를 단일 통합으로 기록할 수 있게 요약 제공.

## 병렬 실행 가능 항목
- [ ] Backend PostGIS/reports 재검증과 Model v3 정책/audit 정리는 병렬 가능 → 검증: backend는 `backend/**`, model은 `model/**`, `data_sources/**`만 사용.
- [ ] PWA 정적 회귀와 Voice HTTP contract/TTS cache는 병렬 가능 → 검증: PWA는 `apps/web/**`, voice는 `voice/**`, `outputs/voice/**`로 산출물 분리.
- [ ] Model class `1..3` 데이터 manifest 초안과 Backend upload negative matrix는 병렬 가능 → 검증: model은 문서/manifest, backend는 API error contract로 기록.
- [ ] TalkBack/DevTools 접근성 점검과 TTS HTTP cache 확인은 병렬 가능 → 검증: 접근성 결과와 TTS header/cache 결과를 별도 execution 문서에 기록.
- [ ] 데스크톱 마이크 E2E와 Android fake mode 목걸이 smoke는 장비가 분리되면 병렬 가능 → 검증: 같은 Android 실폰 1대를 쓰면 순차로 전환.
- [ ] `/admin` 운영 확인과 hard-negative FP 정책 검토는 병렬 가능 → 검증: `/admin`은 report ID, model은 CSV row 기준으로 추적.
- [ ] 문서 stale 표현 검색과 runtime smoke는 병렬 가능 → 검증: 문서 담당자는 실행하지 않은 항목을 PASS로 쓰지 않음.

## 병렬 에이전트 배정안
### Batch 1
- Agent Backend Runtime
  - 책임 범위: PostGIS/Alembic, reports tests, HTTP smoke, upload matrix, duplicate/radius, `/detect`, cleanup
  - 참고 입력: `plans/.work/2026-05-19/daily/backend-postgis-api.md`, `docs/execution/2026-05-19_backend_postgis_api.md`, `docs/pwa_backend_status.md`, `docs/report_operations.md`
  - 수정 가능 파일/폴더: `backend/**`, `backend/tests/**`, `backend/alembic/**`, `docs/execution/2026-05-20_backend_postgis_api.md`
  - 검증: Alembic head, `pytest backend/tests`, 실제 HTTP smoke, row/upload cleanup 전후 count

- Agent PWA/Accessibility
  - 책임 범위: PWA 정적 회귀, Android fake mode, 목걸이 착용, GPS/heading, TTS/진동, offline shell, TalkBack/DevTools
  - 참고 입력: `plans/.work/2026-05-19/daily/pwa-accessibility-sensors.md`, `docs/execution/2026-05-19_pwa_accessibility_sensors.md`, `docs/neck_worn_phone_test_checklist.md`, `docs/pwa_backend_status.md`
  - 수정 가능 파일/폴더: `apps/web/**`, `docs/execution/2026-05-20_pwa_accessibility_sensors.md`
  - 검증: `npm run lint/typecheck/build`, `node --check apps/web/public/sw.js`, Android Chrome 수동 기록

- Agent Voice STT/TTS
  - 책임 범위: voice HTTP health/contract, CORS, TTS HTTP cache, desktop mic E2E, fallback, 자연어 목적지 판단안
  - 참고 입력: `plans/.work/2026-05-19/daily/voice-stt-tts.md`, `docs/execution/2026-05-19_voice_stt_tts.md`, `docs/voice_stt_tts_status.md`
  - 수정 가능 파일/폴더: `voice/**`, `scripts/check_voice_contract.py`, `scripts/test_stt.py`, `scripts/test_tts.py`, ignored `outputs/voice/**`, `docs/execution/2026-05-20_voice_stt_tts.md`
  - 검증: `/health`, contract script, CORS header, transcript/intent/confidence, `X-Voice-Cached: true`

- Agent Model/Data/MLOps
  - 책임 범위: disk/hash gate, v3 후보 재검산, min-box/FP 정책, privacy/location audit, class `1..3` 데이터 manifest, sampling 실행/보류 판단
  - 참고 입력: `plans/.work/2026-05-19/daily/vision-model-data-mlops.md`, `docs/execution/2026-05-19_model_data_mlops.md`, `docs/model_v2_status.md`
  - 수정 가능 파일/폴더: `model/**`, `data_sources/**`, ignored `runs/**`, ignored `datasets/**`, `docs/execution/2026-05-20_model_data_mlops.md`
  - 검증: `df -h`, `sha256sum`, `git ls-files`, manifest row count, sampler CSV/checkpoint, generated image files 0

### Batch 2
- Agent Integration/Field
  - 책임 범위: Batch 1 결과 통합, server mode E2E, 실폰/통제 입력, 음성 신고와 버튼 신고 비교, `/admin` 운영 확인, cleanup 확인
  - 참고 입력: Batch 1 산출 문서, `plans/.work/2026-05-19/daily/integration-field-report.md`, `docs/execution/2026-05-19_integration_field_report.md`, `scripts/check_pwa_server_e2e.py`
  - 수정 가능 파일/폴더: `scripts/check_pwa_server_e2e.py`, `docs/execution/2026-05-20_integration_field_report.md`
  - 검증: `/detect/health ready`, 실제 `/detect`, bbox, `POST /reports`, `source=server`, `/admin` 상태 변경, cleanup

- Agent Docs/Daylog Integrator
  - 책임 범위: lane 결과 통합, stale 문서 수정 필요 여부 판단, 데모/보고 기준 정리, 최종 daylog 작성
  - 참고 입력: 모든 lane execution 문서, `README.md`, `docs/current_status.md`, `docs/pwa_backend_status.md`, `docs/model_v2_status.md`, `docs/voice_stt_tts_status.md`, `product/decisions.md`
  - 수정 가능 파일/폴더: `daylog/2026-05-20.md`, 필요 시 `docs/current_status.md`, `docs/pwa_backend_status.md`, `docs/model_v2_status.md`, `docs/voice_stt_tts_status.md`
  - 검증: 실행하지 않은 항목 PASS 금지, fake/server/model/voice/field 근거 분리, `git diff --check`

## 순차 의존 항목
- [ ] 시작 전 작업트리 확인 → 검증: `daylog/2026-05-19.md` 수정과 5개 lane note 미추적 파일을 임의로 되돌리거나 덮어쓰지 않음.
- [ ] 공통 env/포트/장비 전략 확정 후 E2E 진행 → 검증: `5432/8000/3000/9001`, Android 접속 방식, 권한 상태 기록.
- [ ] DB 기동 → Alembic → schema 확인 → reports tests → HTTP smoke → cleanup 순서로 진행 → 검증: cleanup 전후 count와 upload 파일 상태 기록.
- [ ] `/detect/health ready` → 실제 `/detect` → PWA server mode → `source=server` 신고 저장 순서로 진행 → 검증: health ready만으로 server mode 완료 처리하지 않음.
- [ ] Android 접속 방식 → 권한 프롬프트 → fake mode 카메라/GPS/TTS/진동 → 신고/admin 순서로 진행 → 검증: 신고 ID 기준 추적.
- [ ] voice HTTP health/contract → CORS → 데스크톱 마이크 → 실폰 마이크 순서로 진행 → 검증: 서버 실패와 브라우저 실패를 분리.
- [ ] TTS 1차 생성 → 2차 cache hit → 실폰 청취 평가 순서로 진행 → 검증: HTTP header와 체감 평가를 모두 기록.
- [ ] 디스크 gate → v3 policy/audit → full sampling 실행 여부 판단 → 새 학습 보류 순서로 진행 → 검증: min-box/privacy audit 전 train 금지.
- [ ] lane별 execution 문서 → integration field report → daylog 순서로 진행 → 검증: daylog는 단일 통합자가 한 번만 작성.

## 리스크와 확인 필요
- 확인 필요: 2026-05-20 실행 세션이 Docker/PostGIS/local TCP/loopback을 허용하는 일반 개발 세션인지.
- 확인 필요: Android 실폰, USB 디버깅, ADB reverse, LAN, HTTPS 중 실제 접속 방식.
- 확인 필요: reports 테스트 row/upload를 삭제할지, disposable DB를 재생성할지, 보존할지.
- 확인 필요: `서울역으로 안내해줘`를 `set_destination`으로 확장할지 제품 판단.
- 확인 필요: browser ONNX Runtime Web latency 측정 기기와 브라우저 확보 여부.
- 리스크: LAN HTTP는 카메라, 마이크, 위치, service worker 권한이 secure context 문제로 막힐 수 있다.
- 리스크: fake detector는 UI/API/운영 흐름 근거일 뿐 정확도, 지연시간, 실제 보행 안전 근거가 아니다.
- 리스크: `source=server` headless/fixture smoke는 모델-백엔드-PWA 연결 근거이며 실폰 목걸이 field 성능 근거가 아니다.
- 리스크: v2 모델은 class `0` baseline이다. class `1..3` 한국 GT가 없으므로 4-class metric이나 정확도 90% 달성으로 보고하면 안 된다.
- 리스크: `/` 사용률 99%, 가용 16G 기록이 있어 full inference, 새 학습, 대형 다운로드는 디스크 gate 없이 시작하지 않는다.
- 리스크: STT 8/8 성공은 known-intent 로컬 샘플 기준이다. 보행 중 잡음, 바람, 복수 화자, 실폰 마이크 성능으로 확대 해석하지 않는다.
- 리스크: TTS direct-call cache와 HTTP `X-Voice-Cached` 근거를 분리해야 한다.
- 리스크: 원본 이미지, 위치 데이터, 음성 샘플, 생성 WAV, `.pt`, `.onnx`, `runs/`, `outputs/`, dataset images/labels는 GitHub 업로드 대상이 아니다.
- 리스크: 실폰 테스트는 안전한 실내/통제 smoke로만 표현하고 실제 시각장애인 대상 현장 검증으로 쓰지 않는다.
- 리스크: `/reports`, `/uploads`, status patch는 인증/권한 없이 열려 있다. 외부 공개 전 auth/rate-limit/storage 정책이 필요하다.

## 완료 기준
- [ ] 2026-05-20 lane별 실행 문서가 작성됨 → 검증: `docs/execution/2026-05-20_*`에서 PASS/FAIL/BLOCKED/PENDING이 분리됨.
- [ ] 공통 runtime gate가 기록됨 → 검증: DB/backend/web/voice/Android 접속 방식, env, 권한 상태가 남음.
- [ ] PostGIS reports tests와 HTTP smoke가 완료되거나 명확히 BLOCKED 처리됨 → 검증: pytest no-skip 결과, API 응답, row/upload cleanup 상태 또는 차단 사유 기록.
- [ ] Android fake mode 실폰/목걸이 smoke가 완료되거나 BLOCKED 처리됨 → 검증: 기기명, Android/Chrome, 접속 방식, 권한, 카메라/GPS/TTS/진동 기록.
- [ ] fake 신고와 `/admin` 운영 흐름이 확인되거나 PENDING 사유가 기록됨 → 검증: 신고 ID, 이미지, 위치 품질, source, duplicate 후보, 상태 변경 기록.
- [ ] server mode smoke가 재확인되거나 BLOCKED 처리됨 → 검증: `/detect/health`, 실제 `/detect`, bbox, `source=server`, cleanup 또는 실패 사유 기록.
- [ ] voice HTTP/PWA/마이크/TTS cache/fallback 검증이 분리 기록됨 → 검증: transcript, intent, confidence, UI action, `X-Voice-Cached: true`, 청취 또는 보류 사유 기록.
- [ ] model v3/v4 다음 작업이 실행 가능한 단위로 정리됨 → 검증: manifest row count, min-box/FP 정책, privacy/location audit, class `1..3` 데이터 계획 기록.
- [ ] 데모/보고 기준이 지켜짐 → 검증: fake, server, model metric, STT/TTS, 실폰 관찰 근거를 서로 섞지 않음.
- [ ] `daylog/2026-05-20.md`가 단일 통합으로 작성됨 → 검증: 변경 파일, 실제 검증, 미완료/막힘, 병렬 에이전트 통합 결과 포함.