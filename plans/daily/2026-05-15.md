# Hanium Dreamup / WalkSafe Assist 일일 계획 - 2026-05-15

## 기준
- 작성일: 2026-05-14 KST
- 대상일: 2026-05-15 KST
- 참고한 daylog/문서:
  - `plans/.work/2026-05-14/daily/pwa-accessibility-sensors.md`
  - `plans/.work/2026-05-14/daily/backend-postgis-api.md`
  - `plans/.work/2026-05-14/daily/vision-model-data-mlops.md`
  - `plans/.work/2026-05-14/daily/voice-stt-tts.md`
  - `plans/.work/2026-05-14/daily/integration-field-report.md`
  - `plans/daily/2026-05-15.md`: 현재 없음
  - `daylog/`: 현재 기록 파일 없음
  - `docs/execution/2026-05-14_summary.md`
  - `docs/execution/2026-05-14_frontend.md`
  - `docs/execution/2026-05-14_backend.md`
  - `docs/execution/2026-05-14_detect_adapter_implementation.md`
  - `docs/execution/2026-05-14_pwa_stt_implementation.md`
  - `docs/execution/2026-05-14_voice_contract_implementation.md`
  - `docs/execution/2026-05-14_external_validation_subset.md`
  - `docs/execution/2026-05-14_parallel_followup_summary.md`
  - `docs/project_3day_execution_plan.md`
  - `docs/frontend_3day_execution_plan.md`
  - `docs/backend_3day_execution_plan.md`
  - `docs/model_training_3day_execution_plan.md`
  - `docs/voice_stt_tts_3day_execution_plan.md`
  - `docs/current_status.md`
  - `docs/pwa_backend_status.md`
  - `docs/report_operations.md`
  - `docs/voice_stt_tts_status.md`
  - `docs/neck_worn_phone_test_checklist.md`

## 현재 상태 요약

- 저장소 루트의 별도 `AGENTS.md`와 기존 `plans/daily/2026-05-15.md`는 확인되지 않았다. 이번 계획은 사용자 제공 AGENTS 지침과 lane note, `docs/execution` 최신 로그를 기준으로 통합한다.
- PWA는 2026-05-14 기준 `npm run lint`, `npm run typecheck`, `/`, `/admin`, `/manifest.webmanifest`, `/sw.js` HTTP 확인이 통과했다. 다만 실폰 카메라, GPS, 방향 센서, TTS, 진동, PWA 설치/오프라인 shell은 아직 수동 검증 대기다.
- 백엔드는 PostGIS 컨테이너, Alembic `202605120001 (head)`, `backend/tests`, `/health`, `/detect/health`, `/reports?limit=5` smoke가 통과했다. `/detect`는 로컬 Ultralytics `.pt` adapter가 1차 구현됐지만, PWA는 아직 서버 `/detect`를 직접 호출하지 않고 fake detector 중심이다.
- 모델은 v2 `best.pt` 기준 test split과 VL2+VS2 tactile subset 외부 validation이 완료됐다. 외부 validation은 class `0 damaged_tactile_block` 전용이며, 4개 위험 클래스 전체 성능 근거는 아니다.
- 음성은 `voice/server.py` CORS, `/speech/stt` 업로드 검증, `get_current_location` intent, PWA `MediaRecorder` 녹음 UI와 intent handler가 구현됐다. 실제 브라우저/실폰 마이크 E2E와 TTS cache 생성은 아직 미완료다.
- 실폰 통합 검증은 2026-05-15의 핵심이다. fake 신고는 API/UI 통합 검증용이며 모델 정확도, 경보 지연, 실제 안전 판단 근거로 쓰지 않는다.

## 내일 핵심 목표

- Android 실폰 또는 동등한 HTTPS/ADB reverse 환경에서 PWA 카메라, GPS, 방향, TTS, 진동, 신고, 관리자 화면, PWA shell을 한 번에 검증한다.
- 신고 API/PostGIS smoke를 격리된 DB와 테스트 업로드 폴더에서 실행하고, 생성 row와 파일 정리 절차까지 확정한다.
- PWA 음성 명령 녹음 → `/speech/stt` → intent 처리 흐름을 실제 브라우저 마이크로 확인한다.
- v2 모델을 class `0` baseline으로 문서화하고, 외부 validation 결과 비교와 실패 프레임 샘플링 기준을 정리한다.
- 서버 `/detect` adapter는 API smoke로 확인하되, PWA가 아직 server detect를 쓰지 않는 gap을 명확히 기록한다.
- 실행 결과는 통과/실패/막힘을 분리해 `docs/execution` 형식으로 남길 수 있게 정리한다.

## 분야별 상세 체크리스트

### 1. PWA/Accessibility/Sensors

- [ ] 실폰 접속 경로 준비 → 검증: `adb reverse tcp:3000 tcp:3000`, `adb reverse tcp:8000 tcp:8000`, 필요 시 `adb reverse tcp:9001 tcp:9001` 설정 후 Android Chrome에서 `http://localhost:3000` 접속 여부 확인
- [ ] PWA 실행 환경 확인 → 검증: `NEXT_PUBLIC_API_BASE_URL`, `NEXT_PUBLIC_DETECTOR_MODE=fake`, `NEXT_PUBLIC_VOICE_API_BASE` 값을 기록하고 `/`, `/admin`, `/manifest.webmanifest`, `/sw.js`가 모바일에서 열리는지 확인
- [ ] 후면 카메라 권한과 프리뷰 확인 → 검증: Android Chrome에서 카메라 권한 허용 후 후면 카메라 프리뷰가 첫 화면에 표시되는지 기록
- [ ] 목걸이 착용 각도 점검 → 검증: `docs/neck_worn_phone_test_checklist.md` 기준으로 전방 1~3m와 바닥 일부가 함께 보이고 줄/옷깃이 렌즈를 가리지 않는지 기록
- [ ] fake detection UI 확인 → 검증: `데모 탐지 모드`, bbox, 4개 위험 클래스 순환, 신뢰도 표시, `source: "fake"` 전제가 사용자/운영자 화면에서 오해 없이 드러나는지 확인
- [ ] TTS 위험 안내 청취 → 검증: 점자블록 파손, 전방 장애물, 공사 장애물, 노면 파임 4개 문구가 짧고 행동 지시가 명확한지 실폰 스피커로 기록
- [ ] 진동 패턴 체감 확인 → 검증: 음성 켜짐/꺼짐 상태의 위험 알림, 신고 성공, 신고 실패, 유사 신고 안내 진동이 구분되는지 기록
- [ ] GPS 권한과 위치 품질 확인 → 검증: 위치 허용/거부 시 좌표, 정확도 m, `missing` 또는 low accuracy 상태가 화면과 신고 payload에 반영되는지 확인
- [ ] 방향 센서 확인 → 검증: 기기를 회전했을 때 방향 값 또는 `대기 중` 상태가 표시되고, 방향 미지원 시 신고 검토 플래그가 과장 없이 표시되는지 확인
- [ ] 실폰 fake 신고 E2E 실행 → 검증: fake 탐지 후 `현재 위험 신고`를 실행하고 성공/실패 TTS·진동·상태 문구, `/admin`의 이미지/위치/source/review flag를 확인
- [ ] 관리자 화면 운영 흐름 확인 → 검증: `/admin` 목록, 상세 이미지, 상태/유형/source/날짜/반경 필터, `new → reviewed → resolved` 상태 변경을 실제 신고 데이터로 확인
- [ ] PWA 설치와 오프라인 shell 확인 → 검증: 홈 화면 추가 또는 설치 프롬프트, standalone 재접속, 네트워크 차단 후 shell fallback, manifest/icon cache 동작을 기록
- [ ] 접근성 빠른 점검 → 검증: TalkBack 또는 DevTools로 위험 배너 `aria-live`, 음성 토글 `aria-pressed`, 신고 버튼 disabled 이유, 카메라/bbox 장식 요소의 `aria-hidden` 낭독 여부 확인
- [ ] 실폰 이슈 기록 → 검증: 기기명, Android/Chrome 버전, 접속 방식, 권한 상태, 실패 재현 단계, 스크린샷 또는 짧은 관찰 메모를 결과 로그에 남김

### 2. Backend/PostGIS/API

- [ ] smoke 환경 격리 → 검증: disposable PostGIS 또는 로컬 개발 DB 여부, 테스트 전용 `UPLOAD_DIR`, 실행 전 row/file 상태를 기록
- [ ] DB 재현성 확인 → 검증: `docker compose up -d db`, Alembic `upgrade head`, Alembic current `202605120001 (head)`, PostGIS healthy 상태 확인
- [ ] read-only API smoke → 검증: `GET /health`, `GET /detect/health`, `GET /reports?limit=1` 응답을 기록
- [ ] 신고 생성 smoke → 검증: 테스트 JPEG/PNG/WebP로 `POST /reports` 실행 후 HTTP `201`, `status: new`, `image_path`, `location_quality`, `review_flags`, `duplicate_report_ids` 확인
- [ ] 신고 조회 smoke → 검증: 생성 ID로 `GET /reports/{id}`를 호출해 저장된 class, source, gps, heading, image path가 request metadata와 일치하는지 확인
- [ ] 신고 목록/필터 smoke → 검증: `GET /reports`의 `limit/status/class_name/source/created_from/created_to` 필터가 예상 결과를 반환하는지 확인
- [ ] PostGIS radius query 확인 → 검증: 기준 좌표와 `radius_m` 조건으로 생성 신고가 포함되고, 일부 좌표 파라미터만 전달할 때 HTTP `400`이 반환되는지 확인
- [ ] duplicate check 확인 → 검증: 같은 `class_name`, 반경 25m, `captured_at` 전후 10분 조건에서 두 번째 신고와 `/reports/duplicate-check`가 첫 신고 ID를 후보로 반환하는지 확인
- [ ] 상태 변경 확인 → 검증: `PATCH /reports/{id}/status`로 `new → reviewed → resolved` 변경 후 단건/목록에서 상태가 반영되는지 확인
- [ ] negative upload/error smoke → 검증: `unsupported_image_type`, `image_extension_mismatch`, `image_content_mismatch`, `empty_image`, `upload_too_large` 중 최소 3개 오류 코드와 HTTP status를 확인
- [ ] 생성 row와 업로드 파일 정리 → 검증: 생성 ID 기준 DB 삭제 또는 disposable DB reset, 업로드 파일 제거, 정리 후 목록/파일 상태 확인
- [ ] 실제 `.pt` adapter API smoke → 검증: `MODEL_ARTIFACT_PATH`와 `MODEL_VERSION` 설정 후 `/detect/health`가 `ready`인지 확인하고, 샘플 이미지 `POST /detect` 결과가 `source: "server"` 또는 정상 빈 `detections: []`로 반환되는지 확인
- [ ] 문서 상태 충돌 정리 → 검증: placeholder 기준 문서와 2026-05-14 adapter 구현 로그의 차이를 업데이트 대상 목록으로 기록하고, 실제 수정 필요 여부를 표시

### 3. Vision Model/Data/MLOps

- [ ] 디스크 최신 상태 확인 → 검증: `df -h`와 주요 경로 용량을 기록하고, 추가 validation/export/failure sampling 가능 여부를 판단
- [ ] 보존 대상 재확인 → 검증: `best.pt`, `last.pt`, `results.csv`, `args.yaml`, `data.yaml`, `SHA256SUMS.txt`, VL2 subset metric log 존재 여부 확인
- [ ] 정리 승인 필요 항목 분리 → 검증: `datasets/walksafe_kr_v1`, `TS8/TS9.zip`, `runs/validation` 산출물 등 삭제/이동 후보를 사용자 승인 필요 항목으로 표시
- [ ] v2 지표 비교표 작성 → 검증: v2 validation, test split, VL2+VS2 external subset의 precision, recall, mAP50, mAP50-95를 한 표로 정리
- [ ] v2 판정 문구 고정 → 검증: “class 0 damaged tactile block baseline은 유지, 4-class 서비스 모델은 아님”을 결과 로그와 handoff 문구에 명시
- [ ] 실패 프레임 bucket 정의 → 검증: `missed_defect`, `false_positive_normal_tactile`, `low_light_or_blur`, `small_or_far`, `new_class_gap` 기준을 문서화
- [ ] 실패 후보 30장 이상 샘플링 → 검증: 각 항목에 source, expected class, failure type, action, 개인정보 비식별 필요 여부를 기록
- [ ] VL1+VS1 hard-negative 가능성 확인 → 검증: 공간 확보 후 streaming 또는 subset 방식으로 정상 점자블록 오탐 점검 가능 여부와 예상 산출물 크기를 기록
- [ ] 모델 handoff 값 확정 → 검증: `MODEL_ARTIFACT_PATH`, `MODEL_VERSION=walksafe-kr-tactile-v2-full-20260514-best-02a6be87`, class order, threshold 후보, class coverage 제한을 백엔드/PWA 공유 형식으로 정리
- [ ] ONNX export 준비 상태 확인 → 검증: `best.onnx` 부재, 필요 패키지, 출력 경로, 예상 용량, PT vs ONNX 동등성 검증 계획을 기록
- [ ] v3/v4 데이터 보강 후보 정리 → 검증: class 1~3 한국 데이터 부족과 class 0 실패 보정 데이터 우선순위를 분리해 다음 작업 후보로 남김

### 4. Voice STT/TTS

- [ ] voice server 실행과 계약 smoke → 검증: `voice/server.py` 기동 후 `/health`, 7개 이상 `/speech/intent`, `/speech/stt` validation error 계약을 `scripts/check_voice_contract.py` 또는 동등 절차로 확인
- [ ] PWA dev server와 voice base 연결 확인 → 검증: 브라우저에서 `NEXT_PUBLIC_VOICE_API_BASE`가 실제 voice server를 가리키고 CORS 오류가 없는지 확인
- [ ] 브라우저 마이크 E2E 확인 → 검증: `음성 꺼`, `음성 켜`, `다시 말해줘`, `신고해`, `목적지 서울역으로 설정해`, `길 안내 시작해`, `지금 어디야` 발화 후 transcript, intent, confidence, UI 상태를 기록
- [ ] 실폰 voice 접속 방식 확인 → 검증: Android에서 `127.0.0.1:9001` 직접 접근이 아닌 ADB reverse 또는 LAN IP 방식 중 하나를 고정하고 성공/실패를 기록
- [ ] 실제 사람 음성 CSV 재생성 → 검증: `samples/voice/stt/myvoice/지금어디야.m4a`가 `get_current_location`으로 분류되는지 확인하고 평균/p95 지연을 최신 CSV에 반영
- [ ] TTS cache 대상 문구 확정 → 검증: PWA `RISK_ALERTS`와 맞춘 4개 위험 문구, `신고 저장 완료`, `목적지를 다시 말씀해 주세요.`, `다시 말씀해 주세요.` 목록을 확정
- [ ] `/speech/tts` cache WAV 생성 → 검증: `use_cache=true` 첫 요청은 생성, 두 번째 요청은 `X-Voice-Cached: true`, `outputs/voice/cache/*.wav` 존재 확인
- [ ] 위험 안내 fallback 확인 → 검증: voice server 미실행 상태에서도 PWA 위험 경고가 브라우저 `speechSynthesis` 또는 진동으로 전달되는지 확인
- [ ] `speechEnabled=false` 회귀 확인 → 검증: 음성 꺼짐 후 위험 경고와 `repeat_last`가 의도치 않게 음성을 재생하지 않는지 확인
- [ ] TTS 청취 평가 → 검증: 문구별 명료도, 속도, 시작 지연, 휴대폰 스피커 음량, 주변 소음 구분 가능 여부를 평가표로 기록

### 5. Integration/Field Test/Report

- [ ] 통합 실행 환경 고정 → 검증: DB `5432`, backend `8000`, web `3000`, voice `9001` 기동 후 `/health`, `/detect/health`, voice `/health`, `/`, `/admin` 응답 확인
- [ ] 실폰 접속 방식 선택 → 검증: ADB reverse 또는 HTTPS/LAN IP 중 하나를 고정하고 카메라/위치/마이크 권한 프롬프트가 실제로 뜨는지 확인
- [ ] 목걸이 착용 통제 테스트 → 검증: 안전한 실내/통제 환경에서 테스트 진행자가 동행하고, 실제 시각장애인 현장 테스트로 오해되지 않게 기록
- [ ] fake 탐지 보행 화면 통합 smoke → 검증: 실폰에서 bbox, 위험 문구, TTS 6초 쿨다운, 진동 패턴, 화면 미주시 인지 가능성을 함께 확인
- [ ] 신고 버튼과 음성 신고 비교 → 검증: `현재 위험 신고` 버튼과 `create_report` intent가 같은 검증 조건과 API 흐름을 타는지 확인
- [ ] `/admin` 운영 흐름 통합 확인 → 검증: 실폰에서 만든 신고가 관리자 화면에 보이고 이미지, 위치 품질, `fake_source`, `duplicate_report_ids`, 상태 변경이 확인되는지 기록
- [ ] `/detect` adapter와 PWA fake 모드 분리 기록 → 검증: 서버 `/detect` API smoke 결과와 PWA fake 신고 결과를 별도 근거로 분리해 보고 메모에 표시
- [ ] 결과 로그 초안 작성 → 검증: 실행 명령, 기기/브라우저, 접속 방식, 통과/실패, 막힘, 다음 수정 필요 항목을 `docs/execution/2026-05-15_*.md` 형식으로 정리
- [ ] 기존 문서 최신화 후보 정리 → 검증: 5/13 기준 문서 중 “STT 미연결”, “detect placeholder”처럼 최신 로그와 충돌하는 문장 목록을 확인 필요 항목으로 남김

## 병렬 실행 가능 항목

- [ ] 모델 지표 비교표 작성과 백엔드 disposable smoke 준비는 병렬 가능 → 검증: 모델 작업은 문서/로컬 파일 확인 중심, 백엔드는 격리 DB/UPLOAD_DIR 준비가 완료됐는지 각각 기록
- [ ] PWA 접근성 빠른 점검과 voice contract smoke는 병렬 가능 → 검증: PWA는 브라우저/DevTools 기준, voice는 `9001` API 기준으로 독립 결과를 남김
- [ ] TTS cache 문구 확정과 backend negative upload smoke는 병렬 가능 → 검증: voice cache 파일과 backend upload 오류 응답이 서로 다른 산출물로 기록됨
- [ ] 모델 handoff 값 정리와 `/detect` adapter API smoke 준비는 병렬 가능 → 검증: handoff 문서값과 backend env 값이 마지막에 한 번 대조됨
- [ ] 실폰 접속 방식 준비와 관리자 화면 필터/상태 변경 확인은 병렬 가능 → 검증: 실폰 준비자는 권한/ADB 상태를, 관리자 확인자는 기존/테스트 신고 데이터 기반 UI 동작을 기록
- [ ] 결과 로그 초안 템플릿 작성은 각 lane 작업과 병렬 가능 → 검증: 실제 실행하지 않은 항목은 `대기` 또는 `확인 필요`로 남아 있고 통과로 표시되지 않음

## 순차 의존 항목

- [ ] DB와 backend 기동 → 신고 생성 → `/admin` 확인 순서로 진행 → 검증: `/health`와 `/reports?limit=1` 성공 전에는 PWA 신고 E2E를 완료로 표시하지 않음
- [ ] web/voice/backend 포트와 ADB reverse 확정 → 실폰 권한 확인 → 실폰 PWA/STT E2E 순서로 진행 → 검증: Android에서 각 포트가 실제 접근되는지 먼저 확인
- [ ] 카메라 프리뷰와 fake detection 확인 → 신고 버튼 또는 `create_report` intent 실행 순서로 진행 → 검증: 현재 위험 이벤트가 없을 때 신고가 실행되지 않는지 확인
- [ ] `/speech/stt` 계약 smoke → 브라우저 마이크 E2E → 실폰 마이크 E2E 순서로 진행 → 검증: 서버 계약 실패 시 브라우저/실폰 결과를 기능 실패가 아니라 서버 준비 실패로 기록
- [ ] 디스크 여유 확인 → 추가 validation/failure sampling/ONNX 준비 순서로 진행 → 검증: 최신 `df -h` 기록 없이 대형 산출물을 새로 만들지 않음
- [ ] `/detect` adapter API smoke → server detect 결과 기록 → PWA 연결 gap 문서화 순서로 진행 → 검증: PWA가 server detect를 직접 호출하지 않았다면 “모델-백엔드-PWA 완전 연결”이라고 쓰지 않음
- [ ] TTS cache 생성 → 재요청 cache hit 확인 → 청취 평가 순서로 진행 → 검증: `X-Voice-Cached: true` 확인 전 cache 완료로 표시하지 않음
- [ ] 실폰 통합 테스트 → 결과 로그 작성 → status/plan 문서 최신화 후보 정리 순서로 진행 → 검증: 실제 테스트 결과와 문서 상태가 서로 다르면 `확인 필요`로 표시

## 리스크와 확인 필요

- 확인 필요: 현재 세션은 읽기 전용이므로 `plans/daily/2026-05-15.md` 파일을 실제 저장하지 못했다. 이 본문을 계획안으로 사용하고, 쓰기 가능한 세션에서 파일 반영이 필요하다.
- 확인 필요: 실폰, USB 디버깅, ADB reverse, HTTPS 개발 URL 중 하나가 없으면 2026-05-15 핵심 실폰 항목은 완료가 아니라 `수동 검증 대기`로 남겨야 한다.
- 확인 필요: Android에서 `localhost`와 `127.0.0.1`은 접속 방식에 따라 PC가 아니라 휴대폰 자신을 가리킬 수 있다. `3000`, `8000`, `9001` 포트 전략을 테스트 전에 고정해야 한다.
- 확인 필요: 최신 디스크 여유 공간은 작업 시작 시 다시 확인해야 한다. VL2+VS2 subset validation 후 여유 공간이 약 6.5~6.6G 수준으로 낮다는 기록이 있다.
- 리스크: PWA는 아직 서버 `/detect` adapter를 직접 호출하지 않는다. 내일 목표는 server detect API smoke와 fake PWA 통합 smoke를 분리하는 것이다.
- 리스크: v2 모델은 실질적으로 `damaged_tactile_block` 중심이다. 킥보드/자전거, 공사물, 포트홀 성능을 같은 수준으로 주장하면 안 된다.
- 리스크: fake 신고는 모델 성능, 실제 안전 판단, 최종 정확도, 경보 지연 근거로 사용할 수 없다.
- 리스크: `/reports`, `/uploads`, status patch는 인증 없이 열려 있다. 외부 공개나 장시간 네트워크 노출 시 인증/네트워크 제한이 필요하다.
- 리스크: TTS 동적 생성은 평균 2초 이상 걸린 기록이 있어 보행 중 위험 안내 primary path로 두면 안 된다.
- 리스크: Vibration API, DeviceOrientation, PWA 설치, Web Speech API는 브라우저/OS별 차이가 크다. Android Chrome 기준 결과와 iOS/다른 브라우저 결과를 분리해야 한다.
- 리스크: 실제 원본 음성, 생성 WAV, 데이터셋 이미지/라벨, `.pt`, `.onnx`, `runs/`, `outputs/` 산출물은 GitHub 업로드 대상이 아니다.
- 확인 필요: 5/13 기준 일부 문서는 STT 미연결 또는 detect placeholder라고 쓰고 있으나, 5/14 최신 로그에서는 PWA STT와 `/detect` adapter가 1차 구현됐다. 최종 문서 갱신 시 최신 로그를 우선해야 한다.
- 확인 필요: 실폰 테스트는 안전 확보된 실내/통제 환경에서 수행해야 하며, 실제 시각장애인 대상 현장 검증으로 표현하면 안 된다.

## 완료 기준

- [ ] 2026-05-15 결과가 lane별로 통과/실패/대기/확인 필요로 나뉘어 기록됨 → 검증: 실행하지 않은 항목이 통과로 표시되지 않음
- [ ] Android 실폰 또는 명확한 대체 환경에서 PWA 카메라, GPS, 방향, TTS, 진동, 신고, `/admin`, PWA shell 결과가 기록됨 → 검증: 기기명, 브라우저, 접속 방식, 권한 상태 포함
- [ ] 백엔드 신고 API smoke가 격리 환경에서 실행되고 생성 row/upload cleanup까지 확인됨 → 검증: 정리 후 DB row와 업로드 파일 상태 확인
- [ ] PWA 브라우저 마이크 STT E2E가 최소 supported intent 일부로 확인됨 → 검증: transcript, intent, confidence, UI action이 함께 기록됨
- [ ] TTS cache/fallback 정책이 실제 cache hit 또는 명확한 보류 사유와 함께 정리됨 → 검증: `outputs/voice/cache` 상태와 `X-Voice-Cached` 확인 여부 기록
- [ ] v2 모델의 test/external validation 비교와 class coverage 한계가 문서화됨 → 검증: class `0` baseline과 4-class 서비스 모델 아님이 명시됨
- [ ] `/detect` adapter API smoke와 PWA fake mode가 별도 근거로 분리됨 → 검증: `source: "server"` API 결과와 `source: "fake"` PWA 신고 결과를 혼용하지 않음
- [ ] 2026-05-16 이후 작업이 PR 또는 lane 단위 후보로 분리됨 → 검증: 실폰 이슈, backend 보강, model handoff, voice cache, 문서 최신화 후보가 각각 남아 있음