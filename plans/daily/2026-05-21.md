# Hanium Dreamup / WalkSafe Assist 일일 계획 - 2026-05-21

## 기준
- 작성일: 2026-05-20 KST
- 대상일: 2026-05-21 KST
- 참고한 daylog/문서:
  - `plans/.work/2026-05-20/daily/*.md` 5개 lane note
  - 기존 `plans/daily/2026-05-21.md`를 읽고 보정 반영
  - `product/vision.md`, `product/roadmap.md`, `product/backlog.md`, `product/done-criteria.md`, `product/decisions.md`
  - `daylog/2026-05-19.md`, `daylog/2026-05-20.md`
  - `docs/current_status.md`, `docs/pwa_backend_status.md`, `docs/voice_stt_tts_status.md`, `docs/model_training_status.md`, `docs/model_v2_status.md`
  - `docs/execution/2026-05-19_*.md`, `docs/execution/2026-05-20_model*.md`
  - `data_sources/manifests/walksafe_kr_v3_*_2026-05-20.*`, `data_sources/manifests/walksafe_kr_v4_class123_data_plan_2026-05-20.md`
  - `README.md`, `docs/api_reference.md`, `docs/report_operations.md`, `docs/inference_contract.md`, `docs/neck_worn_phone_test_checklist.md`
  - `plans/features`, `plans/verify/ready`, `plans/verify/reports`: 디렉터리는 있으나 파일 없음
  - `plans/features/2026-05-20.md`, `plans/features/2026-05-20-audit-final.md`: 없음 확인
  - `PM/status`, `PM/reports/verify`, `PM/reports/product-audit`, automation metrics: `PM` 디렉터리 자체 없음
- 저장소 내부 `AGENTS.md`는 없음. 사용자 제공 AGENTS 지침 기준으로 적용한다.
- 이 통합 작성 자체에는 하위 에이전트를 쓰지 않았고, 병렬 shell 조회로 lane note와 문서를 확인했다.

## 현재 상태 요약
- 제품 운영 모드는 `feature-growth`다. 신규 기능 60%, 검증/안정화 25%, 유지보수/문서 15% 비율을 기본으로 둔다.
- PWA는 카메라/센서 재연결, GPS watch 재시작, 방향 센서 상태 표시, 신고 접근성 보강과 정적 회귀 PASS 근거가 있다. Android 실폰, 목걸이 착용, TalkBack, offline, 브라우저/실폰 마이크 E2E는 완료 근거가 없다.
- Backend는 no-DB detect/uploads 테스트 `13 passed`, `/detect/health` ASGI ready 근거가 있다. PostGIS/Alembic/reports no-skip, reports HTTP smoke, upload/duplicate/cleanup 최신 근거는 없다.
- Model은 5/20에 v3 review queue 196행, privacy pass 196행/126 unique, train staging unique 35장, holdout candidate 2,282쌍, validator PASS, v3 1 epoch smoke training까지 진행됐다. 단 v3 smoke metric은 공식 성능이 아니다.
- v2는 class `0 damaged_tactile_block` baseline이다. class `1..3` 한국 GT가 없어 4-class metric이나 정확도 90% 달성 보고는 금지한다.
- Voice는 direct handler/ASGI 기준 health/intent/CORS/TTS cache 계약 근거가 있다. 실제 HTTP, 브라우저/실폰 마이크, TTS HTTP cache header/fallback/청취는 미완료다.
- 5/20 모델 산출물 다수와 5/21 계획 파일은 현재 미추적 로컬 산출물이다. 다음 실행자는 `git status`로 덮어쓰기 위험을 먼저 확인해야 한다.
- product audit, verify scheduler, automation metrics 산출물은 현재 확인 가능한 파일이 없어 product 문서, lane note, daylog, execution 문서 기준으로 계획한다.

## 내일 핵심 목표
- 신규 feature slice를 먼저 전진시킨다: IMU 3~5초 ROI fixture PoC, GeoJSON/Heatmap Seed, v4 class `1..3` manifest, hands-free 신고, evidence registry.
- 기존 catch-up 반복보다 product M2/M3 방향을 밀되, Android/PostGIS/Voice runtime은 gate가 열릴 때만 PASS로 기록한다.
- fake, server, model metric, voice direct-call, HTTP, field 근거를 서로 섞지 않는다.
- 막힌 항목은 mock, fixture, dry-run, local adapter, schema checklist 같은 safe alternative를 남긴다.
- 실행 결과는 lane별 `docs/execution/2026-05-21_*.md`와 단일 `daylog/2026-05-21.md`에 PASS/FAIL/BLOCKED/PENDING으로 기록한다.

## 분야별 상세 체크리스트

### 1. PWA/Accessibility/Sensors
- [ ] 시작 gate 확인 → 검증: `git status --short --branch --untracked-files=all`, `NEXT_PUBLIC_API_BASE_URL`, `NEXT_PUBLIC_DETECTOR_MODE`, `NEXT_PUBLIC_VOICE_API_BASE` 기록.
- [ ] IMU 3~5초 ROI fixture PoC를 신규 slice로 시작 → 검증: heading, 추정 속도, horizon, bbox 중심점 fixture로 ROI 내/외 판정이 재현되고 `P1-001`과 연결됨.
- [ ] ROI helper는 순수 계산으로 최소 구현 → 검증: `apps/web` typecheck/build 통과, 실제 안전 성능이 아니라 “실험적 fixture”로 문서화.
- [ ] ROI 결과를 사용자 경고에 과대 연결하지 않음 → 검증: 실제 TTS/진동 primary 판단으로 쓰지 않고 debug/상태 또는 문서 근거로만 제한.
- [ ] PWA 정적 회귀 실행 → 검증: `cd apps/web && npm run lint && npm run typecheck && npm run build`, `node --check apps/web/public/sw.js`.
- [ ] 접근성 정적/수동 점검 → 검증: `aria-live`, `aria-pressed`, 신고 disabled reason, bbox `aria-hidden`, 48px 터치 타깃 확인. TalkBack 없으면 DevTools 근거로만 기록.
- [ ] Android 접속 gate 기록 → 검증: ADB reverse/LAN/HTTPS, 기기명, Android/Chrome, 카메라/위치/마이크 권한을 PASS/BLOCKED로 기록.
- [ ] fake mode 목걸이 smoke 가능 시 실행 → 검증: 후면 카메라, bbox, GPS accuracy, heading, 4개 위험 TTS/진동, 6초 쿨다운, `source=fake` 기록.
- [ ] PWA 설치/offline shell 가능 시 확인 → 검증: standalone 실행, 네트워크 차단 후 `/` fallback 확인. 오프라인 신고 큐로 표현하지 않음.
- [ ] 실행 문서 작성 → 검증: `docs/execution/2026-05-21_pwa_accessibility_sensors.md`에 실행한 항목만 PASS로 기록.

### 2. Backend/PostGIS/API
- [ ] backend runtime gate 기록 → 검증: `DATABASE_URL`, `UPLOAD_DIR`, `MODEL_ARTIFACT_PATH`, `MODEL_VERSION`, 포트 `5432/8000`, 실행 전 reports count와 upload 파일 수 기록.
- [ ] GeoJSON/Heatmap Seed를 신규 slice로 시작 → 검증: disposable DB row 또는 synthetic fixture 2~3건을 GeoJSON `FeatureCollection`으로 변환하고 feature 수, WGS84 좌표, `source`, `location_quality` 포함 확인.
- [ ] GeoJSON은 local-only thin slice로 제한 → 검증: 외부 공개/지자체 API/운영 DB 사용 없이 fixture serializer 또는 테스트 DB 기준으로만 기록.
- [ ] disposable DB/cleanup 기준 확정 → 검증: 운영/공유 DB가 아닌지 확인하고 row/upload cleanup 또는 DB 재생성 절차 문서화.
- [ ] PostGIS/Alembic 재검증 가능 시 실행 → 검증: DB healthy, Alembic head `202605120001`, PostGIS extension, SRID 4326 확인.
- [ ] reports no-skip 테스트 실행 → 검증: `python -m pytest backend/tests/test_reports.py -q -rs`가 skip/fail 없이 PASS하거나 BLOCKED 사유 기록.
- [ ] 신고 Evidence Trace HTTP smoke 실행 → 검증: `/health`, `POST /reports`, detail/list/filter, `PATCH /status`, `/uploads/{filename}`를 report ID 기준으로 기록.
- [ ] upload matrix 확인 → 검증: JPEG/PNG/WebP 성공과 `unsupported_image_type`, `image_extension_mismatch`, `empty_image`, `image_content_mismatch`, `upload_too_large` status/detail.code 기록.
- [ ] duplicate/radius advisory 확인 → 검증: 같은 class/25m/±10분 duplicate ID, `/reports/duplicate-check`, radius list filter, partial radius query HTTP 400 확인.
- [ ] cleanup 수행 → 검증: 생성 report row와 upload 파일 삭제 후 count/file 상태가 실행 전 기준으로 돌아왔는지 기록.

### 3. Vision Model/Data/MLOps
- [ ] 시작 gate 확인 → 검증: `df -h`, v2 `best.pt`/`best.onnx` hash, `git ls-files '*.pt' '*.onnx' 'runs/**' 'datasets/**/images/**' 'datasets/**/labels/**'` 기록.
- [ ] v4 class `1..3` manifest thin slice를 최우선 신규 기능으로 진행 → 검증: class별 positive/negative 목표, `source_type`, `privacy_status`, `location_status`, `split_key`, `exclude_reason` 필드 정의.
- [ ] 직접 촬영/AI Hub 159 후보를 manifest 수준으로만 정리 → 검증: `School`, `Building_area`, `Bridge` zip 보유 여부와 예상 용량만 기록. 실제 다운로드는 승인/디스크 gate 전 보류.
- [ ] manual review 70행 decision template 입력/반영 계획 고정 → 검증: false positive extra box 47행, missed defect 20행, small/far 3행의 미입력 수와 decision 후보 기록.
- [ ] hard-negative 55행과 bbox 12행 최신 정책 반영 → 검증: hard-negative 빈 라벨 후보, bbox 기존 GT label 수용, small/far 50행과 min-box 9행 v3.0 제외 상태가 충돌 없이 설명됨.
- [ ] near-duplicate/split gate 후속 표 작성 → 검증: dHash 후보 120행, filename group 미커버 106행을 same-group same-split 또는 exclude 후보로 표시.
- [ ] JPEG restore warning 영향 확인 → 검증: holdout eval 전후 hash/mtime 확인 방법 또는 copy-on-write eval 대안을 문서화. 확인 전에는 추정으로 표시.
- [ ] v3 validator 재실행 가능 시 수행 → 검증: `python3 data_sources/scripts/validate_yolo_dataset.py datasets/walksafe_kr_v3`, `datasets/walksafe_kr_v3_holdout_candidate` PASS/FAIL 기록.
- [ ] v3 smoke 결과 evidence 분리 → 검증: validator PASS, 1 epoch smoke PASS, holdout-candidate 참고 평가를 공식 성능과 분리.
- [ ] 새 full training/대형 sampling 금지 유지 → 검증: full 2,347장 sampling, VL1+VS1 전체 inference, 대형 다운로드, full training을 실행하지 않았음을 기록.

### 4. Voice STT/TTS
- [ ] voice/web env 확인 → 검증: `NEXT_PUBLIC_VOICE_API_BASE`, `VOICE_CORS_ORIGINS`, web `3000`, voice `9001`, Android 접속 방식 기록.
- [ ] hands-free 신고 slice를 신규 기능으로 닫기 → 검증: 마이크 `신고해` → `create_report` → 기존 신고 버튼과 같은 disabled 조건, duplicate check, `POST /reports` 경로인지 확인.
- [ ] voice 서버 실제 HTTP 기동 가능 시 확인 → 검증: `GET /health` 응답, `scripts/check_voice_contract.py --base-url http://127.0.0.1:9001 --timeout 5` PASS 또는 socket 차단 사유 기록.
- [ ] PWA CORS 확인 → 검증: 브라우저 Network 또는 OPTIONS/curl로 `/speech/stt` CORS 오류 없음 기록.
- [ ] 데스크톱 마이크 핵심 명령 E2E → 검증: `신고해`, `음성 켜`, `음성 꺼`, `다시 말해줘`, `지금 어디야`의 transcript/intent/confidence/UI action 기록.
- [ ] TTS 7문구 HTTP cache 확인 → 검증: 각 문구 2회 `POST /speech/tts`, 두 번째 `X-Voice-Cached: true`, WAV 크기와 시작 지연 기록.
- [ ] fallback 회귀 확인 → 검증: server down, timeout, 마이크 권한 거부, `unknown`, confidence `< 0.7`, `speechEnabled=false`, `repeat_last`에서 앱이 멈추지 않음.
- [ ] 자연어 목적지 표현 결정안 작성 → 검증: `서울역으로 안내해줘`를 채택하면 `set_destination` 상태 저장까지만, route/Kakao API 없이 테스트. 미채택 시 `unknown` fallback 유지.
- [ ] 실폰 마이크/스피커 smoke 가능 시 수행 → 검증: 기기명, Android/Chrome, 마이크 권한, 핵심 명령 4개 이상 UI 변화, 청취 평가 기록.
- [ ] 실행 문서 작성 → 검증: `docs/execution/2026-05-21_voice_stt_tts.md`에 HTTP/PWA/실폰/direct-call 근거를 분리.

### 5. Integration/Field Test/Report
- [ ] evidence registry thin slice 작성 → 검증: 5/15 server E2E, 5/19 blocked 결과, 5/20 model 결과를 fake/server/model/voice/field 등급과 한계로 분리.
- [ ] 공통 runtime gate 표 작성 → 검증: DB `5432`, backend `8000`, web `3000`, voice `9001`, Android 접속 방식, 주요 env를 PASS/BLOCKED로 기록.
- [ ] 5/20 model 결과를 보고 기준에 반영 → 검증: v3 smoke는 pipeline 근거, v2는 class `0` baseline, class `1..3` 미확보를 명시.
- [ ] reports `/admin` smoke 가능 시 실행 → 검증: `POST /reports`, 목록/상세/이미지/위치품질/review flags/status `new -> reviewed -> resolved`, cleanup 전후 count 기록.
- [ ] server mode E2E 가능 시 재확인 → 검증: `/detect/health ready`, 실제 `/detect`, bbox, `source=server`, `metadata.source=server`, report ID, cleanup 기록.
- [ ] Android 목걸이 smoke 가능 시 실행 → 검증: 기기명, Android/Chrome, ADB reverse, 권한, 카메라 각도, GPS/heading, TTS/진동, `source=fake` 기록.
- [ ] 음성 신고 통합 확인 → 검증: `신고해` transcript/intent/confidence/UI action이 버튼 신고와 같은 disabled 조건, duplicate check, `POST /reports` 경로를 쓰는지 기록.
- [ ] runtime 차단 시 safe alternative 남김 → 검증: fixture, dry-run, report ID trace template, BLOCKED 조건을 남기고 PASS 처리하지 않음.
- [ ] lane 결과 통합 → 검증: `docs/execution/2026-05-21_integration_field_report.md`와 `daylog/2026-05-21.md`에 PASS/FAIL/BLOCKED/PENDING 분리.

## 병렬 실행 가능 항목
- [ ] PWA IMU ROI fixture PoC와 Model v4 class `1..3` manifest는 병렬 가능 → 검증: PWA는 `apps/web/**`, model은 `data_sources/**`, `model/**`로 쓰기 범위 분리.
- [ ] Backend GeoJSON/HTTP smoke와 Voice HTTP/TTS cache는 병렬 가능 → 검증: backend는 `backend/**`, voice는 `voice/**`, `scripts/check_voice_contract.py` 중심으로 산출물 분리.
- [ ] PWA 정적 회귀와 Backend no-DB 테스트는 병렬 가능 → 검증: Node/npm과 pytest no-DB 작업은 포트/DB 의존 없음.
- [ ] Model split/near-duplicate 문서화와 Backend upload negative matrix는 병렬 가능 → 검증: model은 manifest/count, backend는 API error contract/runtime 결과를 따로 기록.
- [ ] Desktop mic E2E와 Android fake mode smoke는 장비가 분리되면 병렬 가능 → 검증: 같은 Android 실폰 1대를 쓰면 순차 전환.
- [ ] Evidence registry 초안은 lane 실행과 병렬 가능 → 검증: 최종 판정은 모든 lane 결과가 들어온 뒤 단일 integrator가 갱신.
- [ ] 문서 stale 표현 검색과 runtime smoke는 병렬 가능 → 검증: 문서 담당자는 실행하지 않은 항목을 PASS로 쓰지 않음.

## 병렬 에이전트 배정안

### Batch 1
- Agent PWA ROI/Accessibility
  - 책임 범위: IMU 3~5초 ROI fixture PoC, PWA 정적 회귀, 접근성 상태, Android fake smoke 준비
  - 참고 입력: `plans/.work/2026-05-20/daily/pwa-accessibility-sensors.md`, `product/backlog.md`, `docs/neck_worn_phone_test_checklist.md`, `docs/pwa_backend_status.md`
  - 수정 가능 파일/폴더: `apps/web/**`, `docs/execution/2026-05-21_pwa_accessibility_sensors.md`
  - 검증: ROI fixture 결과, `npm run lint/typecheck/build`, `node --check apps/web/public/sw.js`, Android 가능 시 수동 기록

- Agent Backend GIS/Evidence Trace
  - 책임 범위: GeoJSON/Heatmap Seed, reports HTTP smoke, upload matrix, duplicate/radius, cleanup
  - 참고 입력: `plans/.work/2026-05-20/daily/backend-postgis-api.md`, `docs/api_reference.md`, `docs/report_operations.md`, `backend/tests/**`
  - 수정 가능 파일/폴더: `backend/**`, `backend/tests/**`, `docs/execution/2026-05-21_backend_postgis_api.md`
  - 검증: Alembic head, reports no-skip, HTTP smoke, GeoJSON fixture/API count, row/upload cleanup

- Agent Model Data Gate
  - 책임 범위: v4 class `1..3` manifest, manual review 70행, near-duplicate/split, JPEG integrity, v3 evidence 분리
  - 참고 입력: `plans/.work/2026-05-20/daily/vision-model-data-mlops.md`, `docs/execution/2026-05-20_model_data_mlops.md`, `data_sources/manifests/walksafe_kr_v3_*`
  - 수정 가능 파일/폴더: `model/**`, `data_sources/manifests/**`, `docs/execution/2026-05-21_model_data_mlops.md`
  - 검증: CSV/JSON row count, leakage/near-duplicate count, validator, 새 full training 미실행 확인

- Agent Voice Hands-Free
  - 책임 범위: voice HTTP contract, CORS, TTS HTTP cache/fallback, mic intent, `create_report` 경로 확인
  - 참고 입력: `plans/.work/2026-05-20/daily/voice-stt-tts.md`, `docs/voice_stt_tts_status.md`, `voice/**`, `apps/web/lib/voice-api.ts`, `apps/web/app/page.tsx`
  - 수정 가능 파일/폴더: `voice/**`, `scripts/check_voice_contract.py`, `scripts/test_stt.py`, `scripts/test_tts.py`, `apps/web/**` 중 voice 관련 파일, `docs/execution/2026-05-21_voice_stt_tts.md`
  - 검증: `/health`, contract script, CORS header, transcript/intent/confidence, `X-Voice-Cached: true`

### Batch 2
- Agent Integration Runtime/Field
  - 책임 범위: Batch 1 결과 통합, evidence registry, Android 목걸이 smoke, server mode E2E, `/admin`, 음성 신고와 버튼 신고 비교
  - 참고 입력: Batch 1 산출 문서, `plans/.work/2026-05-20/daily/integration-field-report.md`, `scripts/check_pwa_server_e2e.py`, `docs/neck_worn_phone_test_checklist.md`
  - 수정 가능 파일/폴더: `scripts/check_pwa_server_e2e.py`, `docs/execution/2026-05-21_integration_field_report.md`
  - 검증: `/detect/health ready`, 실제 `/detect`, bbox, `POST /reports`, `source=server`, `/admin` 상태 변경, cleanup

- Agent Docs/Daylog Integrator
  - 책임 범위: lane 결과 통합, stale 문서 수정 필요 여부 판단, evidence registry 최종화, daylog 작성
  - 참고 입력: 모든 lane execution 문서, `README.md`, `docs/current_status.md`, `docs/pwa_backend_status.md`, `docs/model_training_status.md`, `docs/voice_stt_tts_status.md`, `product/decisions.md`
  - 수정 가능 파일/폴더: `daylog/2026-05-21.md`, 필요 시 `docs/current_status.md`, `docs/pwa_backend_status.md`, `docs/model_training_status.md`, `docs/voice_stt_tts_status.md`
  - 검증: 실행하지 않은 항목 PASS 금지, fake/server/model/voice/field 근거 분리, `git diff --check`

## 순차 의존 항목
- [ ] 작업트리 확인 후 시작 → 검증: 5/20 미추적 산출물과 기존 `plans/daily/2026-05-21.md`를 덮어쓰지 않음.
- [ ] 공통 env/포트/장비 전략 확정 후 runtime E2E 진행 → 검증: `5432/8000/3000/9001`, Android 접속 방식, 권한 상태 기록.
- [ ] DB 기동 → Alembic → schema 확인 → reports tests → HTTP smoke → cleanup 순서로 진행 → 검증: cleanup 전후 row/upload count 기록.
- [ ] `/detect/health ready` → 실제 `/detect` → PWA server mode → `source=server` 신고 저장 순서로 진행 → 검증: health ready만으로 server mode 완료 처리하지 않음.
- [ ] Android 접속 방식 → 권한 프롬프트 → fake mode 카메라/GPS/TTS/진동 → 신고/admin 순서로 진행 → 검증: 신고 ID 기준 추적.
- [ ] voice HTTP health/contract → CORS → desktop mic → phone mic → `create_report` 통합 순서로 진행 → 검증: 서버 실패와 브라우저 실패를 분리.
- [ ] ROI helper → fixture 판정 → UI/문서 연결 → PWA 정적 회귀 순서로 진행 → 검증: fixture 결과와 사용자-facing 문구가 과대해석되지 않음.
- [ ] privacy/label/split gate → validator → 후속 학습 승인 판단 순서로 진행 → 검증: 내일은 full training을 자동 실행하지 않음.
- [ ] lane별 execution 문서 → integration field report → daylog 순서로 진행 → 검증: daylog는 단일 통합자가 한 번만 작성.

## 리스크와 확인 필요
- 확인 필요: 2026-05-21 실행 세션이 Docker/PostGIS/local TCP/loopback을 허용하는 일반 개발 세션인지. Safe alternative: no-DB tests, migration/schema checklist, fixture GeoJSON serializer. 추천: blocked runtime은 PASS로 쓰지 않는다.
- 확인 필요: Android 실폰, USB 디버깅, ADB reverse, LAN, HTTPS 중 실제 접속 방식. Safe alternative: 데스크톱 fixture와 DevTools 접근성 점검. 추천: field 근거로 대체하지 않는다.
- 확인 필요: GeoJSON/Heatmap Seed를 API endpoint로 둘지 fixture/script로 둘지. Safe alternative: synthetic/disposable DB 기반 serializer. 추천: 위치정보 외부 공개 없이 local-only로 시작한다.
- 확인 필요: `서울역으로 안내해줘`를 `set_destination`으로 확장할지. Safe alternative: rule 변경 없이 dry-run matrix와 `unknown` fallback 문서화. 추천: 채택해도 route/Kakao API는 구현하지 않는다.
- 리스크: `/` 사용률 99%, 가용 16~17G 수준이라 full sampling, 새 full training, 대형 다운로드는 위험하다. Safe alternative: bounded CSV/manifest/validator 작업.
- 리스크: v3 smoke weight는 35장 unique staging + 1 epoch 결과다. 공식 성능이나 개선 근거로 쓰지 않는다.
- 리스크: holdout candidate 2,282장은 v2 외부검증/오류분석에 이미 사용되어 최종 blind test로 약하다. Safe alternative: 참고 평가로만 분리하고 새 blind 후보 계획을 둔다.
- 리스크: holdout eval의 `corrupt JPEG restored and saved` warning 157회는 원본 symlink target 수정 가능성이 있다. Safe alternative: hash/mtime 확인과 copy-on-write eval 정책.
- 리스크: class `1..3` 한국 GT가 없어 4-class metric을 주장할 수 없다. Safe alternative: 직접 촬영/AI Hub 후보 manifest만 작성.
- 리스크: `source=fake`는 UI/API/운영 흐름 근거이고, `source=server` headless/fixture는 모델-백엔드-PWA 연결 근거다. 둘 다 field 성능 근거가 아니다.
- 리스크: STT local sample 8/8과 TTS direct-call cache는 HTTP/PWA/실폰 검증과 다르다. Safe alternative: direct-call과 HTTP header를 별도 표로 기록.
- 리스크: `/reports`, `/uploads`, status patch는 인증/권한 없이 열려 있다. 외부 공개, 운영 DB, secret, AWS/S3, Kakao Map 실제 key, 지자체 API 연동은 실행 계획에 넣지 않는다.
- 리스크: 원본 이미지, 위치 데이터, 음성 샘플, 생성 WAV, `.pt`, `.onnx`, `runs/`, dataset images/labels는 GitHub 업로드 대상이 아니다.

## 완료 기준
- [ ] 신규 feature slice가 최소 3개 이상 전진됨 → 검증: IMU ROI fixture, GeoJSON/Heatmap Seed, v4 class `1..3` manifest, hands-free 신고, evidence registry 중 실행 결과가 문서화됨.
- [ ] 공통 runtime gate가 기록됨 → 검증: DB/backend/web/voice/Android 접속 방식, env, 권한 상태가 PASS/BLOCKED로 남음.
- [ ] PostGIS reports tests와 HTTP smoke가 완료되거나 명확히 BLOCKED 처리됨 → 검증: pytest no-skip 결과, API 응답, row/upload cleanup 상태 또는 차단 사유 기록.
- [ ] upload/duplicate/radius가 확인되거나 safe alternative가 남음 → 검증: runtime code/status 또는 fixture/schema checklist 기록.
- [ ] Android fake mode 실폰/목걸이 smoke가 완료되거나 BLOCKED 처리됨 → 검증: 기기명, Android/Chrome, 접속 방식, 권한, 카메라/GPS/TTS/진동 기록.
- [ ] server mode smoke가 재확인되거나 BLOCKED 처리됨 → 검증: `/detect/health`, 실제 `/detect`, bbox, `source=server`, cleanup 또는 실패 사유 기록.
- [ ] voice HTTP/PWA/마이크/TTS cache/fallback 검증이 분리 기록됨 → 검증: transcript, intent, confidence, UI action, `X-Voice-Cached`, 청취 또는 보류 사유 기록.
- [ ] model v3/v4 gate가 더 명확해짐 → 검증: review queue/manifest row count, manual review, split/near-duplicate, class `1..3` 계획, JPEG integrity 기록.
- [ ] 데모/보고 기준이 지켜짐 → 검증: fake, server, model metric, STT/TTS, 실폰 관찰 근거를 서로 섞지 않음.
- [ ] `docs/execution/2026-05-21_*.md`와 `daylog/2026-05-21.md`가 작성됨 → 검증: 변경 파일, 실제 검증, 미완료/막힘, 병렬 에이전트 통합 결과 포함.