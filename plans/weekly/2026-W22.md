# Hanium Dreamup / WalkSafe Assist 주간 계획 - 2026-W22

## 기준
- 작성일: 2026-05-25 KST
- 범위: 2026-05-25 월요일 ~ 2026-05-31 일요일
- lane note 위치: `plans/.work/2026-05-25/weekly`
- 기존 주간 계획 후보: `plans/weekly/2026-W22.md`는 현재 checkout에서 확인되지 않음
- 주요 근거:
  - `product/*.md`
  - `docs/current_status.md`
  - `docs/report_operations.md`
  - `docs/walksafe-v2/*`
  - `plans/weekly/2026-W21.md`
  - `plans/daily/2026-05-25.md`, `plans/daily/2026-05-26.md`
  - `daylog/2026-05-24.md`, `daylog/2026-05-25.md`
  - `/home/ddobagi/PM/status/2026-05-25-features.md`
  - `/home/ddobagi/PM/status/2026-05-25-verify.md`
  - `/home/ddobagi/PM/status/2026-05-25-product-audit.md`
  - `/home/ddobagi/PM/status/2026-05-25-dirty.md`
  - `/home/ddobagi/PM/status/2026-05-24-automation-metrics.md`
- 운영 모드: `feature-growth`
- 권장 비율: 신규 기능 60% / 검증·안정화 25% / 유지보수·문서 15%
- 통합 원칙:
  - fake, mock, fixture, headless, model metric, Device E2E, field 근거를 분리한다.
  - 실행하지 않은 검증은 PASS로 쓰지 않는다.
  - PM 5/25 feature batch commit `82ec532`은 `verify blocked`, `push_ready=false`이므로 완료/배포 근거로 쓰지 않는다.
  - 원본 checkout은 upstream보다 `behind 10`이고 dirty 상태다. `git add .`, reset, 강제 push, 대형 산출물 staging은 금지한다.
- 통합 작업 자체:
  - 별도 하위 에이전트는 사용하지 않았다.
  - lane note, product 문서, PM 산출물, daylog, git 상태는 병렬 shell 조회로 나눠 확인했고, 최종 판단은 이 문서에서 단일 통합했다.

## 이번 주 핵심 목표

1. W22 source-of-truth를 고정한다.
   - 현재 원본 checkout, clean worktree `0ee4be0`, PM feature worktree `82ec532`을 분리해 어떤 근거가 어느 worktree 기준인지 명시한다.

2. 5/25 PM feature batch를 검증 대기 상태에서 인수 가능/보류로 재분류한다.
   - 대상: GeoJSON export endpoint/admin URL, mock route navigation cue fixture, motion ROI fixture/helper, voice intent schema endpoint.
   - 검증 blocked 원인은 sandbox mount quota 오류이며, push-ready가 아니다.

3. 제품 목표를 앞으로 미는 신규 slice를 우선한다.
   - PWA 녹음 fallback 세분화
   - TTS phrase catalog/cache-status dry-run
   - Stage1 threshold FP/FN review pack
   - public agency manual export kit
   - product v2 canonical summary와 scheduler-safe 후보 표

4. `server-v2` Stage1 기준 evidence spine을 만든다.
   - `/detect/v2 -> /reports/v2 -> reports/{id} -> status patch -> CSV/JSON/GeoJSON export -> cleanup`을 같은 report ID로 연결한다.
   - PostGIS가 막히면 serializer fixture 또는 ASGI/direct handler trace만 PARTIAL로 기록한다.

5. Android 목걸이/실폰 검증은 가능할 때만 Device E2E로 처리한다.
   - 불가하면 정적 접근성, mock route, fixture, direct handler 대안을 남기고 Device E2E는 BLOCKED로 둔다.

6. 모델은 Stage1 YOLO26s 후보를 이번 주 기준선으로 삼는다.
   - Stage1 test all mAP50-95 `0.730`
   - `damaged_tactile_block` threshold `0.50` image-level F1 `0.922151`
   - 이 수치는 실폰 field accuracy나 경보 지연 1초 달성 근거가 아니다.

7. product 문서 품질을 scheduler가 읽기 쉬운 형태로 보강한다.
   - PM product audit 점수는 91/100.
   - 보강점: v2 canonical summary, legacy detector 용어 drift, `scheduler_safe`, `requires_env`, `safe_alternative`, `verification` 컬럼.

## 날짜별 계획

### 월요일 (2026-05-25)
- [ ] W22 주간 기준선 확정 → 검증: `plans/.work/2026-05-25/weekly/*.md` 5개 lane note와 `plans/weekly/2026-W22.md` 부재 기록
- [ ] PM 산출물 상태 반영 → 검증: 5/25 features는 `committed_waiting_verify`, verify는 `blocked`, `push_ready=false`, product audit 91점, 5/24 automation metrics `push_ready=0` 기록
- [ ] worktree/source-of-truth 분리 → 검증: 원본 checkout dirty/behind 10, clean worktree `0ee4be0`, PM feature commit `82ec532`을 별도 기준으로 표기
- [ ] 주간 우선순위 확정 → 검증: 신규 기능 60%, 검증·안정화 25%, 문서 15% 비율과 P0/P1/P2 분류 반영
- [ ] 금지 범위 고정 → 검증: 운영 DB, secret, 외부 배포, Cloud STT/TTS, 공공기관 자동 제출, 대형 다운로드/업로드, destructive cleanup 제외
- [ ] W22 계획 작성 → 검증: 날짜별 계획, 분야별 체크리스트, 병렬 에이전트 배정안, 리스크, 완료 기준 포함

### 화요일 (2026-05-26)
- [ ] 5/25 feature batch 재검증 gate → 검증: PM worktree `82ec532` 기준 marker, `git log -1`, `git status`, verify blocked 원인 재확인
- [ ] Admin GeoJSON export 검증 → 검증: `FeatureCollection`, Point `[lng, lat]`, `geometry: null`, 필터 보존, CSV/JSON 회귀 확인
- [ ] navigation cue fixture 재실행 → 검증: `prepare10`, `soon3`, `now`, 위험 TTS보다 낮은 priority 확인
- [ ] motion ROI fixture 재실행 → 검증: heading, speed, 3초/5초 horizon, bbox center inside/outside 확인
- [ ] `/speech/intents` schema 검증 → 검증: `py_compile`, intent schema/classifier smoke, 가능 시 HTTP contract smoke
- [ ] product v2 canonical summary 작성 → 검증: `fake-v2/server-v2`, `/detect/v2`, `/reports/v2`, `custom_tactile`, `damaged_tactile_block`, TMAP 기본 provider, fake 자동 fallback 금지 반영
- [ ] 정적 회귀 가능한 범위 실행 → 검증: `git diff --check`, backend/voice py_compile, PWA lint/typecheck/build 또는 환경 BLOCKED 기록

### 수요일 (2026-05-27)
- [ ] report ID evidence trace 작성 → 검증: Stage1 `damaged_tactile_block` payload 1건이 `/reports/v2` 저장, detail 조회, status 변경, CSV/JSON/GeoJSON export까지 같은 ID로 연결
- [ ] 저장 허용/거부 matrix 확인 → 검증: `custom_tactile/damaged_tactile_block` 저장, `tactile_damage_area`, `normal_tactile_block`, `coco_general`은 422 또는 저장 제외
- [ ] public agency manual export kit 초안 → 검증: reviewed 후보, 중복 후보, 오탐 검토, 위치 품질, review flags, image path, export 형식 포함. 자동 POST 없음
- [ ] PWA 음성 요청 신고 fixture → 검증: `create_report` intent가 `trigger=voice`, `auto_reported=false`, 자동 신고 cooldown 우회 정책으로 이어짐
- [ ] PWA 녹음 fallback 설계/구현 slice → 검증: permission denied, STT timeout, server down, empty recording, low confidence가 서로 다른 UI/TTS/진동 상태로 구분됨
- [ ] 접근성 정적 점검 → 검증: `aria-live`, `aria-pressed`, `aria-describedby`, 신고 disabled reason, bbox `aria-hidden`, 주요 버튼 터치 영역 기록

### 목요일 (2026-05-28)
- [ ] TTS phrase catalog 작성 → 검증: 위험 안내, 음성 신고 완료/실패, 위치 확인, 목적지 저장, 길안내 준비 문구가 catalog로 고정됨
- [ ] `/speech/tts/cache-status` dry-run slice → 검증: 모델 로드/WAV 생성 없이 `cached`, `cache_key`, phrase id, path suffix 반환
- [ ] risk/navigation TTS 중재 회귀 → 검증: `riskActive`이면 navigation prompt 억제, 정상 점자블록 follow 안내는 위험 없음 조건에서만 낮은 priority로 동작
- [ ] Stage1 threshold review pack 작성 → 검증: 기존 saved prediction/eval 기반으로 threshold `0.50/0.60/0.75` precision/recall/F1, FP/FN queue 기준 정리
- [ ] FP/FN review queue manifest 설계 → 검증: `image_path`, `gt_has_damage`, `pred_conf`, `threshold_bucket`, `review_reason`, `privacy_status`, `split_key` 포함
- [ ] external/blind validation manifest preflight → 검증: 새 다운로드 없이 `source_type`, `label_status`, `privacy_status`, `location_status`, `exclude_reason` 필드 정의

### 금요일 (2026-05-29)
- [ ] PostGIS runtime 가능 시 no-skip 재검증 → 검증: disposable DB에서 `backend/tests/test_reports.py`, `test_reports_v2.py`, `test_reports_export.py` skip 없이 PASS 또는 DB/TCP BLOCKED 기록
- [ ] upload/duplicate/radius matrix → 검증: JPEG/PNG/WebP 성공, MIME/extension/content mismatch, empty image, upload too large, 같은 class 25m/10분 duplicate, partial radius 400 기록
- [ ] cleanup 절차 검증 → 검증: 생성 report row와 upload 파일 삭제 또는 disposable DB reset 전후 count 기록
- [ ] Android 목걸이 smoke 가능 시 실행 → 검증: 기기명, Chrome, ADB reverse/LAN, 권한, 후면 카메라 각도, GPS accuracy, heading, TTS/진동, source 분리
- [ ] PWA install/offline/TalkBack 가능 시 확인 → 검증: standalone 실행, offline shell fallback, TalkBack 읽기 순서와 live region 기록
- [ ] Device E2E 불가 시 대체 검증 → 검증: 정적 접근성, mock route, voice fixture만 PARTIAL로 남기고 Device E2E는 BLOCKED

### 토요일 (2026-05-30)
- [ ] 실폰 없는 demo runbook 작성 → 검증: mock route guide, Stage1 damage payload, admin export, public agency manual export 흐름을 한 문서에서 재현 가능
- [ ] TMAP 길안내 evidence 분리 → 검증: 5/24 live smoke, offline timing fixture, 실폰 길안내 보정을 별도 근거로 기록. TMAP 원본 응답 장기 저장 없음
- [ ] model evidence registry 갱신 → 검증: fake-v2 demo, health-only, real ASGI image smoke, image-level metric, Android/browser latency 미검증을 별도 등급으로 표기
- [ ] voice evidence 정리 → 검증: direct handler, HTTP, browser mic, phone mic, TTS cache, navigation intent를 PASS/PARTIAL/BLOCKED로 분리
- [ ] product backlog scheduler-safe 표 보강 → 검증: `scheduler_safe`, `requires_env`, `requires_secret`, `requires_device`, `safe_alternative`, `verification` 컬럼 후보 정리
- [ ] 데모 리허설 → 검증: fake/demo와 server/model evidence가 섞이지 않고, report ID trace와 admin export가 설명 가능

### 일요일 (2026-05-31)
- [ ] 주간 evidence 통합 → 검증: 각 lane 결과에 Static, Unit, Integration, Headless E2E, Device E2E, Model Eval, GIS/Ops, Release 등급 부여
- [ ] PASS/PARTIAL/BLOCKED/PENDING 정리 → 검증: 실행한 명령만 PASS, 환경·장비·권한 미충족은 BLOCKED 또는 PENDING으로 기록
- [ ] 다음 주 이관 항목 분류 → 검증: `학습 가능`, `데이터 필요`, `환경 필요`, `제품 판단 필요`, `C 작업`으로 분류
- [ ] stale/legacy 문서 보정 후보 정리 → 검증: legacy detector, Kakao 우선 표현, v2 class 0 baseline, fake fallback 표현 drift 목록화
- [ ] daylog/실행 문서 통합 → 검증: 실제 수정·검증을 수행한 실행 에이전트가 `docs/execution`과 `daylog/2026-05-31.md` 또는 일자별 daylog에 기록
- [ ] W23 입력 준비 → 검증: Android, PostGIS, voice mic, TMAP, ROI, product audit 보강점의 다음 액션과 safe alternative 명시

## 분야별 체크리스트

### PWA/Accessibility/Sensors
- [ ] navigation cue fixture 인수 → 검증: `prepare10`, `soon3`, `now`, risk TTS priority
- [ ] motion ROI fixture 인수 → 검증: heading/speed/horizon/bbox center 내외 판정
- [ ] PWA recording fallback → 검증: permission denied, timeout, server down, empty, low confidence 상태 분리
- [ ] voice report action → 검증: `trigger=voice`, `auto_reported=false`, GPS 없음 저장 금지
- [ ] risk/navigation TTS 중재 → 검증: 위험 중 길안내 음성 억제, normal tactile follow는 낮은 priority
- [ ] 접근성 정적 점검 → 검증: live region, pressed state, disabled reason, bbox hidden, 버튼 터치 영역
- [ ] 정적 회귀 → 검증: `npm run lint`, `npm run typecheck`, `npm run build`, `node --check apps/web/public/sw.js`
- [ ] Device E2E → 검증: Android 목걸이, 카메라/GPS/heading/TTS/진동, TalkBack, install/offline. 미실행 시 BLOCKED

### Backend/PostGIS/API
- [ ] GeoJSON export contract → 검증: `FeatureCollection`, `[lng, lat]`, `geometry: null`, properties 필드
- [ ] Admin export URL → 검증: CSV/JSON/GeoJSON format, 필터 보존, `limit` 제외 여부
- [ ] reports v2 허용/거부 matrix → 검증: damaged tactile만 저장, 보조/정상/general 객체 거부
- [ ] report ID trace → 검증: detect/report/detail/status/export/cleanup이 같은 ID로 연결
- [ ] PostGIS no-skip → 검증: disposable DB, Alembic head, reports tests no-skip
- [ ] upload/duplicate/radius → 검증: 허용 MIME 3종, upload error 5종, duplicate 25m/10분, radius 400
- [ ] cleanup → 검증: row/file count 전후 기록
- [ ] C 작업 분리 → 검증: migration, status history, auth/rate-limit, 운영 DB, 기관 자동 제출은 설계/backlog만

### Vision Model/Data/MLOps
- [ ] Stage1 기준선 고정 → 검증: checkpoint, config, threshold `0.50`, metric 날짜 명시
- [ ] threshold review pack → 검증: `0.50/0.60/0.75` TP/FP/FN/TN, precision/recall/F1 재대조
- [ ] FP/FN queue manifest → 검증: review reason, privacy status, split key 포함
- [ ] Stage1 payload fixture → 검증: `custom_tactile`, `damaged_tactile_block`, threshold, bbox, GPS/heading 입력 후보
- [ ] `tactile_damage_area` 정책 → 검증: 자동/음성 신고 제외, 보조 display/debug와 데이터 개선 큐만
- [ ] external/blind validation preflight → 검증: 새 다운로드 없이 manifest schema와 제외 기준
- [ ] ONNX/export preflight → 검증: 디스크 gate, 산출물 local-only, 실제 export는 조건부
- [ ] 대형 작업 금지 → 검증: 새 full training, 대형 다운로드, 외부 업로드, weight/dataset staging 없음

### Voice STT/TTS
- [ ] `/speech/intents` schema 인수 → 검증: py_compile, classifier smoke, HTTP 가능 시 contract script
- [ ] TTS phrase catalog → 검증: 위험/신고/위치/목적지/길안내 문구 고정
- [ ] cache-status dry-run → 검증: 모델 로드 없이 cache 상태 반환
- [ ] 자연어 목적지 최소 rule → 검증: “서울역으로 안내해줘”, “서울역으로 가자”는 set_destination까지만, 자동 route start 없음
- [ ] hands-free report fixture → 검증: mock `create_report`가 PWA action과 `/reports/v2` 정책으로 연결
- [ ] evidence telemetry → 검증: raw audio 없이 transcript, normalized, intent, confidence, failure reason만
- [ ] HTTP/CORS/mic gate → 검증: voice server, CORS, browser/phone mic 가능 시만 PASS

### Integration/Field Test/Report
- [ ] runtime gate → 검증: worktree, env, 포트, DB, Android, TMAP, voice 상태 표
- [ ] PM feature verify 복구 → 검증: quota 해소 후 marker/명령 재실행, 실패 시 BLOCKED 유지
- [ ] report ID evidence spine → 검증: detect/report/admin/status/export/cleanup 연결
- [ ] demo runbook → 검증: 실폰 없는 시연과 실폰 field 근거 분리
- [ ] public agency export kit → 검증: reviewed 후보 manual export, 자동 제출 없음
- [ ] evidence registry → 검증: 근거 등급, 재사용 범위, 한계 표기
- [ ] weekly closeout → 검증: PASS/BLOCKED, safe alternative, C 작업 분리

## 검증 기준

| 등급 | 이번 주 PASS 조건 | PASS 금지 조건 |
|---|---|---|
| Static | lint, typecheck, py_compile, `git diff --check`, schema/fixture 정적 확인 | 정적 결과를 runtime/field 성공으로 표현 |
| Unit | helper, serializer, intent, policy test가 재현 가능하게 통과 | DB/브라우저/실폰 검증을 단위 테스트로 대체 |
| Integration | backend/voice/web/PostGIS가 실제 또는 ASGI/TestClient로 연결 | DB skip 상태를 runtime PASS로 표현 |
| Headless E2E | ASGI, fixture, headless browser로 사용자 흐름 일부 확인 | 실폰 카메라/마이크/스피커/센서 검증처럼 표현 |
| Device E2E | Android 기기명, 접속 방식, 권한, 카메라/GPS/TTS/진동/TalkBack 기록 | 장비 미사용 상태에서 PASS 처리 |
| Model Eval | saved prediction/eval, metric, threshold review, manifest 근거 | fake result, single smoke를 정확도/field 성능으로 표현 |
| GIS/Ops | GeoJSON, radius, duplicate, status, reviewed export 근거 | 공공기관 자동 제출 또는 운영 DB 성공으로 표현 |
| Release | 도메인, HTTPS, secret, storage, rollback, 배포 smoke 승인 | 이번 주 범위에서는 기본적으로 PASS 대상 아님 |

## 병렬 에이전트 배정안

### 순차 선행 작업
- Coordinator가 먼저 source-of-truth/runtime gate를 고정한다.
- 원본 checkout, clean worktree, PM feature worktree가 섞이면 모든 결과가 오염되므로, Batch 1 작업 전 기준 경로와 commit을 표로 확정한다.
- 같은 `apps/web/**` 파일을 수정할 가능성이 있는 PWA fixture 작업과 PWA voice fallback 작업은 구현 단계에서 순차 진행한다.
- daylog 최종 통합은 병렬 금지다.

### Batch 0: Runtime / Source Gate
- Agent Runtime Gate
  - 책임 범위: 원본 checkout, clean worktree, PM feature worktree, env, 포트, DB, Android/ADB, TMAP, voice 상태 분리
  - 참고 입력: `daylog/2026-05-25.md`, `/home/ddobagi/PM/status/2026-05-25-dirty.md`, `/home/ddobagi/PM/status/2026-05-25-verify.md`
  - 수정 가능 파일/폴더: `docs/execution/2026-05-26_runtime_gate.md` 또는 해당 일자 runtime gate 문서
  - 검증 방법: `git status`, ahead/behind, `git log -1`, env/port/device/DB PASS/BLOCKED 표, 금지 파일 staging 없음

### Batch 1: 독립 실행 가능
- Agent PWA Accessibility/Fixture
  - 책임 범위: navigation cue, motion ROI, risk/navigation TTS 중재, 접근성 정적 점검
  - 참고 입력: `plans/.work/2026-05-25/weekly/pwa-accessibility-sensors.md`, `docs/walksafe-v2/frontend_display_policy.md`, `docs/walksafe-v2/navigation_integration_policy.md`
  - 수정 가능 파일/폴더: `apps/web/**`, `docs/execution/*_pwa_accessibility_sensors.md`
  - 검증 방법: navigation/motion fixture, risk policy smoke, `npm run lint`, `npm run typecheck`, `npm run build`

- Agent Backend Export/Reports
  - 책임 범위: GeoJSON export, report serializer, public agency export kit, reports v2 trace, upload/duplicate/radius matrix
  - 참고 입력: `plans/.work/2026-05-25/weekly/backend-postgis-api.md`, `docs/report_operations.md`, `docs/walksafe-v2/backend_api_contract.md`
  - 수정 가능 파일/폴더: `backend/**`, `backend/tests/**`, `docs/execution/*_backend_postgis_api.md`
  - 검증 방법: serializer fixture, pytest, `/reports/v2` 허용/거부 matrix, CSV/JSON/GeoJSON export, cleanup 기록

- Agent Model Threshold/Evidence
  - 책임 범위: Stage1 threshold review, FP/FN manifest, payload fixture, external validation manifest, model evidence registry
  - 참고 입력: `plans/.work/2026-05-25/weekly/vision-model-data-mlops.md`, `docs/execution/2026-05-23_reviewed_yolo26s_final_selection.md`, `configs/walksafe_two_model_runtime_stage1_mvp_20260523.json`
  - 수정 가능 파일/폴더: `model/**`, `data_sources/manifests/**`, `configs/**`, `docs/execution/*_model_data_mlops.md`
  - 검증 방법: saved prediction/eval 재대조, manifest schema, no full training/download/upload

- Agent Voice TTS/Intent
  - 책임 범위: `/speech/intents` schema, TTS phrase catalog, cache-status dry-run, 자연어 목적지 rule, sample manifest schema
  - 참고 입력: `plans/.work/2026-05-25/weekly/voice-stt-tts.md`, `docs/walksafe-v2/voice_command_strategy.md`, PM feature execution output
  - 수정 가능 파일/폴더: `voice/**`, `scripts/check_voice_contract.py`, `tests/test_voice_intents.py`, `docs/execution/*_voice_stt_tts.md`
  - 검증 방법: py_compile, intent regression, contract smoke 또는 HTTP BLOCKED, 모델 로드/Cloud API 없음

- Agent Product Canonical
  - 책임 범위: v2 canonical summary, scheduler-safe 후보 표, legacy terminology drift, PM 산출물 경로 혼선 정리
  - 참고 입력: `product/*.md`, `/home/ddobagi/PM/reports/product-audit/2026-05-25/hanium-dreamup.md`, `docs/current_status.md`, `docs/walksafe-v2/*`
  - 수정 가능 파일/폴더: `product/**`, `docs/execution/*_product_pm_quality.md`
  - 검증 방법: product audit 보강점 반영, 완료 근거 과대해석 없음, safe alternative 표 포함

### Batch 2: Batch 1 이후
- Agent PWA Voice Fallback
  - 책임 범위: PWA 녹음 fallback 상태 세분화와 voice report fixture 연결
  - 참고 입력: Batch 1 PWA/Voice 결과, `docs/voice_stt_tts_status.md`, `docs/walksafe-v2/voice_command_strategy.md`
  - 수정 가능 파일/폴더: `apps/web/**`, 필요 시 `voice/**`, `docs/execution/*_pwa_voice_fallback.md`
  - 검증 방법: permission denied, timeout, server down, empty, low confidence fixture. phone mic 미검증 분리

- Agent Integration Trace
  - 책임 범위: report ID evidence trace, demo runbook, public agency manual export 흐름 통합
  - 참고 입력: 모든 lane execution 문서, `docs/neck_worn_phone_test_checklist.md`, `product/done-criteria.md`
  - 수정 가능 파일/폴더: `docs/execution/*_integration_field_report.md`, 필요 시 trace helper 1개
  - 검증 방법: detect/report/admin/status/export/cleanup trace, PASS/BLOCKED 분리, fake/mock/model/field 근거 분리

- Agent Device Field
  - 책임 범위: Android 목걸이, TalkBack, install/offline, 실폰 mic, TTS/진동 수동 smoke
  - 참고 입력: `docs/neck_worn_phone_test_checklist.md`, `docs/walksafe-v2/frontend_display_policy.md`
  - 수정 가능 파일/폴더: `docs/execution/*_device_field_smoke.md`
  - 검증 방법: 기기명, Chrome, 접속 방식, 권한, 카메라 각도, GPS accuracy, heading, TTS/진동, source 분리. 장비 없으면 BLOCKED

- Agent Daylog Integrator
  - 책임 범위: lane별 실행 결과를 단일 daylog로 통합
  - 참고 입력: Batch 0/1/2 산출물, 실제 실행 명령 결과
  - 수정 가능 파일/폴더: `daylog/YYYY-MM-DD.md`
  - 검증 방법: 실제 실행한 검증만 PASS, 미실행은 BLOCKED/PENDING, 변경 파일과 병렬 에이전트 결과 포함

## 우선순위

### P0: 이번 주 반드시 닫을 것
- W22 source-of-truth/runtime gate
- 5/25 PM feature batch의 검증 대기 상태 재분류
- GeoJSON export/admin URL 계약 확인
- navigation cue와 motion ROI fixture 확인
- `/speech/intents` schema 확인
- product v2 canonical summary와 scheduler-safe 후보 표
- report ID evidence trace 초안
- PASS/PARTIAL/BLOCKED/PENDING 기준 통일

### P1: gate가 열리면 완료할 것
- PWA recording fallback 세분화
- TTS phrase catalog/cache-status dry-run
- public agency manual export kit
- Stage1 threshold FP/FN review pack
- PostGIS no-skip/HTTP smoke
- Android 목걸이/TalkBack/install/offline smoke
- voice browser/phone mic E2E

### P2: 이번 주에는 조건부 또는 문서화만
- ONNX 실제 export
- full training 또는 대형 inference
- 목적지 검색/geocoding
- Kakao fallback 실제 구현
- 운영 DB migration/security/auth/rate-limit
- AWS/S3, 외부 배포, Cloud STT/TTS
- 지자체 민원 시스템 자동 제출
- 공공 데이터셋 공개

## 리스크와 확인 필요

- PM verify quota 오류
  - 상태: 5/25 verify는 sandbox mount quota 오류로 blocked.
  - 대안: marker와 local smoke를 검증 대기 근거로 남기고, push-ready로 쓰지 않는다.

- 원본 checkout dirty/behind 10
  - 상태: 자동화/사용자 변경이 섞여 있다.
  - 대안: PM feature worktree 또는 clean worktree 기준으로 검증하고, 파일 단위로만 반영한다.

- PostGIS/Docker/local TCP 접근
  - 상태: 최신 no-skip/runtime PASS는 환경 gate 필요.
  - 대안: serializer fixture, ASGI direct handler, disposable DB 가능한 세션 대기.

- Android/ADB/TalkBack/실폰 mic
  - 상태: Device E2E 미검증.
  - 대안: 정적 접근성, mock route, fixture, desktop/direct handler. headless 결과를 Device E2E로 쓰지 않는다.

- TMAP key/약관/장기 저장
  - 상태: 5/24 live smoke는 성공했지만 원본 응답 장기 저장은 확인 필요.
  - 대안: offline timing fixture와 mock route를 기본 검증으로 둔다.

- voice TTS 모델/cache 환경
  - 상태: Qwen 생성, Cloud STT/TTS, 유료 API는 C 작업.
  - 대안: phrase catalog와 cache-status dry-run으로 먼저 고정한다.

- motion ROI field 미검증
  - 상태: fixture/helper 수준.
  - 대안: JSON fixture와 pure helper만 유지하고 사용자 TTS/진동 primary 판단에 직접 연결하지 않는다.

- Stage1 evidence 과대해석 위험
  - 상태: ASGI/저장 이미지 smoke와 model metric은 field accuracy가 아니다.
  - 대안: threshold review pack과 FP/FN queue로 운영 부담을 정리한다.

- 공공기관 export 오해 위험
  - 상태: export kit는 manual submission 준비용이다.
  - 대안: 자동 POST 없음, 운영자 검수 후 제출 정책을 문서에 반복 명시한다.

## 주간 완료 기준

- [ ] `plans/weekly/2026-W22.md` 본문이 lane note와 PM/product/daylog 근거를 반영한 주간 계획으로 저장됨 → 검증: 기준, 날짜별 계획, 분야별 체크리스트, 병렬 배정안, 리스크, 완료 기준 포함
- [ ] source-of-truth/runtime gate가 기록됨 → 검증: 원본 checkout, clean worktree, PM feature worktree, env/포트/DB/장비 상태 분리
- [ ] 5/25 PM feature batch가 인수 가능/보류/BLOCKED로 재분류됨 → 검증: GeoJSON, navigation cue, motion ROI, voice intent schema 각각 결과 기록
- [ ] report ID evidence trace가 작성됨 → 검증: detect/report/detail/status/export/cleanup이 같은 ID 또는 fixture ID로 연결
- [ ] product v2 canonical summary와 scheduler-safe 보강안이 작성됨 → 검증: product audit 보강점 반영, legacy 용어 drift 목록화
- [ ] PWA/voice 신규 slice 최소 1개가 검증됨 → 검증: recording fallback 또는 TTS catalog/cache-status dry-run PASS/PARTIAL/BLOCKED 기록
- [ ] Stage1 threshold review pack 또는 FP/FN manifest 초안이 작성됨 → 검증: threshold별 tradeoff와 review queue 필드 포함
- [ ] PostGIS와 Android 항목은 실행 가능 여부가 명확히 분리됨 → 검증: 가능 시 PASS, 불가 시 safe alternative와 BLOCKED 사유 기록
- [ ] fake/mock/headless/model/device/field 근거가 섞이지 않음 → 검증: evidence registry 또는 주간 정리 표에 검증 등급 포함
- [ ] 주간 daylog 또는 일자별 daylog가 실제 실행 결과 기준으로 작성됨 → 검증: 변경 파일, 실행 명령, 미실행 사유, 병렬 에이전트 결과 통합 포함