# Hanium Dreamup / WalkSafe Assist catch-up 스케줄 - 2026-05-15

## 기준
- 작성일: 2026-05-15 KST
- 검토한 계획 날짜: 2026-05-14 KST
- catch-up 대상일: 2026-05-15 KST
- 참고한 계획표/daylog:
  - `plans/daily/2026-05-14.md`: 현재 저장소에 없음
  - `plans/catchup/2026-05-15.md`: 현재 저장소에 없음
  - `plans/daily/2026-05-15.md`
  - `plans/.work/2026-05-15/catchup/*.md` 5개 lane note
  - `plans/.work/2026-05-14/daily/*.md` 5개 lane note
  - `daylog/2026-05-14.md`, `daylog/2026-05-15.md`: 없음
  - `docs/execution/2026-05-14_*.md`
  - `docs/execution/2026-05-15*.md`: 없음

## 미완료 판단 요약
- 5/14 로컬 구현/스모크 완료 근거는 있음: PWA lint/typecheck와 라우트 smoke, 백엔드 PostGIS/Alembic/pytest/API health, `/detect` adapter 구현, voice 계약 smoke, VL2+VS2 tactile subset 외부 validation.
- 5/15 새벽 추가 완료 근거는 없음: daylog와 `docs/execution/2026-05-15*.md`가 확인되지 않음.
- 실폰/브라우저 권한 기반 검증은 미완료 후보: 카메라, GPS, 방향 센서, 마이크, TTS, 진동, PWA 설치/오프라인 shell, `/admin` 운영 E2E.
- PWA server detector는 코드 wiring까지 완료됐지만, 실제 브라우저/실폰에서 `/detect` 호출과 `source: "server"` 신고 저장은 미완료.
- 모델 v2는 class `0 damaged_tactile_block` 중심 baseline으로만 확정 가능하며, 4-class 서비스 모델 완료로 판단하면 안 됨.
- TTS cache는 `outputs/voice/cache/.gitkeep`만 있어 WAV 생성/cache hit 근거가 없음.
- 문서 상태 충돌이 있음: 일부 기존 문서는 STT 미연결 또는 `/detect` placeholder 기준이고, 최신 5/14 실행 문서는 구현 완료를 기록함.

## 오늘 처리할 catch-up 체크리스트
- [ ] 기준 문서 누락 상태 확정 → 검증: `plans/daily/2026-05-14.md`, `plans/catchup/2026-05-15.md`, daylog 부재를 결과 로그에 명시.
- [ ] 통합 실행 환경 기동 → 검증: DB `5432`, backend `8000`, web `3000`, voice `9001`의 `/health`, `/detect/health`, voice `/health`, `/`, `/admin` 응답 기록.
- [ ] 백엔드 신고 API 격리 smoke → 검증: 전용 DB/`UPLOAD_DIR`에서 신고 생성·조회·목록 필터·radius·duplicate·status patch·negative upload 최소 3개·row/file cleanup 확인.
- [ ] `/detect` 실서버 adapter smoke → 검증: `MODEL_ARTIFACT_PATH`, `MODEL_VERSION` 설정 후 `/detect/health ready`, 샘플 `POST /detect` HTTP 200, `source: "server"` 또는 정상 빈 결과 기록.
- [ ] 실폰 접속 방식 확정 → 검증: ADB reverse 또는 LAN/HTTPS 중 하나로 Android Chrome 접속, 기기명·Android/Chrome 버전·권한 프롬프트 기록.
- [ ] PWA 카메라/GPS/방향/TTS/진동 확인 → 검증: 후면 카메라 프리뷰, 위치 정확도/거부 상태, 방향 값 또는 대기 상태, 위험 TTS, 진동 패턴 기록.
- [ ] fake 탐지 신고 E2E와 `/admin` 확인 → 검증: `source: "fake"` 신고 생성 후 이미지, 위치 품질, review flag, duplicate 후보, `new → reviewed → resolved` 확인.
- [ ] server detector PWA E2E → 검증: `NEXT_PUBLIC_DETECTOR_MODE=server`에서 카메라 프레임 `/detect` 호출, bbox 표시, `source: "server"` payload 저장 확인.
- [ ] PWA 설치/오프라인/accessibility quick check → 검증: standalone 실행, offline shell fallback, `aria-live`, `aria-pressed`, disabled reason, 장식 요소 낭독 여부 기록.
- [ ] voice 브라우저/실폰 마이크 E2E → 검증: `신고해`, `음성 켜/꺼`, `다시 말해줘`, `지금 어디야` 등의 transcript, intent, confidence, UI action 기록.
- [ ] TTS cache/fallback/회귀 확인 → 검증: 대상 문구 확정, `/speech/tts use_cache=true` 2회 요청, `X-Voice-Cached: true`, cache WAV 존재, `speechEnabled=false` 회귀 확인.
- [ ] 모델 디스크/보존 게이트 확인 → 검증: `df -h`, 주요 경로 용량, `best.pt`, `last.pt`, `results.csv`, `args.yaml`, `data.yaml`, `SHA256SUMS.txt` 존재 기록.
- [ ] 모델 지표·handoff 정리 → 검증: v2 val/test/VL2+VS2 external metric 표, `MODEL_VERSION`, class order, threshold 후보, class coverage 제한 문서화.
- [ ] 실패 프레임/ONNX/backlog 정리 → 검증: failure bucket 정의, 후보 30장 이상 수집 여부, VL1+VS1 hard-negative 가능성, `best.onnx` 부재와 export 계획 기록.
- [ ] 5/15 실행 로그 작성 → 검증: 통과/실패/대기/확인 필요를 분리해 `docs/execution/2026-05-15_*.md` 형식으로 남김.

## 병렬 에이전트 배정안
### Batch 1
- Agent Backend/PostGIS/API
  - 책임 범위: 신고 API smoke, duplicate/radius/status/negative upload, cleanup, `/detect` 실서버 smoke.
  - 참고 입력: `plans/.work/2026-05-15/catchup/backend-postgis-api.md`, `docs/execution/2026-05-14_backend.md`, `2026-05-14_detect_adapter_implementation.md`.
  - 수정 가능 파일/폴더: `backend/**`, `docs/execution/2026-05-15_backend*.md`, 테스트 전용 `backend/uploads/test/**`.
  - 검증: pytest, HTTP smoke, DB row/file cleanup 전후 상태.

- Agent Voice STT/TTS
  - 책임 범위: voice server 계약 smoke, 브라우저/실폰 마이크 E2E, TTS cache 생성, 음성 꺼짐 회귀.
  - 참고 입력: `plans/.work/2026-05-15/catchup/voice-stt-tts.md`, `docs/execution/2026-05-14_voice_pwa_smoke.md`, `2026-05-14_next_step_parallel.md`.
  - 수정 가능 파일/폴더: `voice/**`, `scripts/check_voice_contract.py`, `outputs/voice/cache/**`, `docs/execution/2026-05-15_voice*.md`.
  - 검증: `scripts/check_voice_contract.py`, 실제 transcript/intent/UI 결과, `X-Voice-Cached: true`.

- Agent Model/Data/MLOps
  - 책임 범위: 디스크/보존 상태, metric 비교표, model handoff, failure sampling 기준, ONNX 준비, v3/v4 backlog.
  - 참고 입력: `plans/.work/2026-05-15/catchup/vision-model-data-mlops.md`, `docs/execution/2026-05-14_external_validation_subset.md`, `2026-05-14_model.md`.
  - 수정 가능 파일/폴더: `docs/execution/2026-05-15_model*.md`, 필요 시 `runs/validation/**` 산출물. 대용량 삭제는 승인 전 금지.
  - 검증: 파일 존재/hash, metric 표, class coverage 제한 명시, 디스크 여유 기록.

- Agent PWA/Accessibility/Sensors
  - 책임 범위: PWA 모바일 접속, 카메라/GPS/방향/TTS/진동, 설치/offline, accessibility quick check.
  - 참고 입력: `plans/.work/2026-05-15/catchup/pwa-accessibility-sensors.md`, `docs/execution/2026-05-14_frontend.md`, `2026-05-14_pwa_server_detector_wiring.md`.
  - 수정 가능 파일/폴더: `apps/web/**`, `docs/execution/2026-05-15_pwa*.md`.
  - 검증: Android Chrome 수동 결과, lint/typecheck, 권한/기기/접속 방식 기록.

### Batch 2
- Agent Integration/Field Report
  - 책임 범위: Batch 1 결과 통합, fake/server/voice/admin E2E 구분, 최종 field report와 문서 충돌 목록 작성.
  - 참고 입력: 모든 Batch 1 결과, `plans/.work/2026-05-15/catchup/integration-field-report.md`.
  - 수정 가능 파일/폴더: `docs/execution/2026-05-15_integration*.md`, `plans/catchup/2026-05-15.md`.
  - 검증: 실행하지 않은 항목이 완료로 표시되지 않고, 통과/실패/대기/확인 필요가 분리됨.

## 순차 실행 항목
- DB/backend 기동 → 신고 API smoke → PWA 신고 E2E → `/admin` 확인 순서로 진행.
- backend `/detect` ready 확인 → PWA `server` mode 실행 → bbox 표시와 `source: "server"` 저장 확인 순서로 진행.
- voice 계약 smoke → 브라우저 마이크 E2E → 실폰 마이크 E2E 순서로 진행.
- 실폰 접속 방식 확정 → 카메라/위치/마이크 권한 확인 → 통합 field smoke 순서로 진행.
- 디스크 여유 확인 → cleanup 승인 여부 결정 → 추가 validation/export/failure sampling 순서로 진행.
- TTS cache 생성 → cache hit 확인 → 실폰 청취 평가 순서로 진행.
- lane별 결과 로그 작성 → 문서 충돌 목록 작성 → catch-up 완료 판정 순서로 진행.

## 확인 필요 / 보류
- `plans/daily/2026-05-14.md`가 없어 원 계획표 체크박스 기준 완료 판정은 불완전함.
- 2026-05-14/2026-05-15 daylog와 2026-05-15 execution 문서가 없어 새벽 작업 완료 근거는 없음.
- 현재 워크트리에 `apps/web/app/page.tsx`, `apps/web/lib/detect-api.ts`, 일부 `docs/execution`, `plans/` 변경/미추적 상태가 있으므로 후속 에이전트는 덮어쓰지 말고 먼저 diff를 확인해야 함.
- 실폰, USB 디버깅, ADB reverse 또는 HTTPS/LAN 접속 환경이 없으면 핵심 실폰 항목은 완료가 아니라 `수동 검증 대기`로 남김.
- VL2+VS2 external validation은 class `0 damaged_tactile_block` 전용이다. 4-class 성능 근거로 확장 금지.
- fake detector 신고는 UI/API 통합 확인용이며 모델 정확도나 실제 안전 판단 근거로 사용 금지.
- 디스크 여유가 낮다는 기록이 있으므로 대형 validation/export 전 삭제·이동 승인이 필요함.
- TTS cache는 아직 생성 근거가 없으므로 `outputs/voice/cache/.gitkeep`만으로 완료 처리 금지.
- 실폰 테스트는 안전한 실내/통제 환경으로 기록하고, 실제 시각장애인 현장 검증으로 표현하지 않음.

## 완료 기준
- [ ] 5/15 결과 문서에 통과/실패/대기/확인 필요가 lane별로 분리되어 있음.
- [ ] Android 실폰 또는 명확한 대체 환경에서 카메라, GPS, 방향, TTS, 진동, 마이크, PWA 설치/offline 결과가 기록됨.
- [ ] 백엔드 신고 API smoke가 격리 환경에서 실행되고 생성 row/upload cleanup까지 확인됨.
- [ ] `/detect` adapter API smoke와 PWA server detector E2E가 각각 별도 근거로 기록됨.
- [ ] fake 신고와 server detect 결과가 혼용되지 않고 `source` 기준으로 분리됨.
- [ ] voice 마이크 E2E와 TTS cache hit/fallback/회귀 결과가 기록됨.
- [ ] v2 모델 metric, handoff 값, class coverage 제한이 문서화됨.
- [ ] 완료 근거가 없는 항목은 완료가 아니라 미완료 후보 또는 확인 필요로 남아 있음.

## 병렬 에이전트 활용 메모
- 이번 통합 검토에서는 하위/병렬 에이전트를 새로 사용하지 않음.
- 기존 `plans/.work/2026-05-15/catchup` lane note 5개가 이미 lane별 조사 결과 역할을 하므로, 이를 5/14 실행 문서와 대조해 통합함.
- 통합 결론: 로컬 구현/스모크는 상당수 완료됐지만, 오늘 catch-up의 핵심은 실폰·브라우저·통합 E2E·TTS cache·모델 handoff·문서 충돌 정리다.