NEXT_ROUND_A: yes

# Hanium Dreamup / WalkSafe Assist catch-up audit - 2026-05-18 r1

## 완료

- PWA 정적 검증: `node --check`, lint, typecheck, build, server-mode build PASS.
- Backend no-DB/ASGI 검증: `test_detect` 11 passed, `test_uploads` 2 passed, 전체 `13 passed, 17 skipped`; `.pt` ASGI `/detect` smoke는 `source=server` 확인.
- Model/Data r1 후속: v3 manifest 40행, hard-negative FP review 21행, summary JSON, contact sheet 3장 확인.
- Voice 로컬 검증: STT dry-run 8개, 실제 음성 8/8, TTS 7문구 direct-call cache 확인.
- r1 실행 보고와 `daylog/2026-05-18.md` 작성 완료.
- `git diff --check`: PASS.

## A 계속 가능

- [ ] 잔여 stale 문서 표현 정리 → 다음 라운드 지시/검증: `README.md`, `docs/current_status.md`에 남은 `모델 미연결 상태`, `모델 미구현` 표현을 2026-05-18 기준인 `.pt` server adapter 구현 완료 + fake detector는 데모/fallback 용도라는 표현으로 좁게 수정한다. 검증: `rg -n "모델 미연결|모델 미구현|어댑터 없음" README.md docs/current_status.md docs/model_* docs/frontend_handoff_without_model.md`, `git diff --check`.

## B 사용자 확인 필요

- Android 실폰 검증을 진행할 실제 접속 방식 선택 필요: ADB reverse, LAN IP, HTTPS 중 하나.
- 실폰/목걸이 field test에 사용할 기기, Android/Chrome 버전, 권한 프롬프트 기록 가능 여부 확인 필요.
- `서울역으로 안내해줘`를 `set_destination` intent로 확장할지 제품 요구사항 결정 필요.
- PostGIS runtime smoke 후 테스트 row/upload를 삭제해도 되는지, 또는 별도 disposable DB만 사용할지 확인 필요.

## C 위험/대형 작업

- PostGIS schema migration 후보: `status/source/class_name` check constraint, `updated_at` trigger, status history.
- VL1+VS1 전체 1,038장 hard-negative inference.
- full failure sampling 재실행과 새 학습.
- browser/ONNX Runtime Web latency 측정 자동화.
- class `1..3` 한국 GT 수집/라벨링과 4-class metric 산출.
- 인증/권한, CORS 운영 정책, rate limit, 외부/persistent storage, 배포 환경 구성.

## 진전 없음/중단

- PostGIS/Alembic/reports no-skip PASS, reports HTTP smoke, duplicate/radius, row/upload cleanup: Docker socket 권한과 DB/TCP 제한으로 5/17 대비 실질 진전 없음.
- server-mode headless E2E: `127.0.0.1:8000/health` 접근이 `Operation not permitted`로 다시 실패.
- Android 실폰/목걸이 카메라, GPS/heading, TTS/진동, 마이크, PWA 설치/offline, TalkBack: `adb` 없음/장비 접근 부재로 중단.
- fake 신고 `/admin` 운영 흐름과 server mode `source=server` 신고 저장: backend/PostGIS/실폰 runtime 미확보로 중단.
- Voice HTTP `/health`, contract, PWA CORS, `/speech/tts` HTTP cache header, 브라우저/실폰 마이크 E2E: sandbox loopback/브라우저·실폰 환경 부재로 중단.

## git/daylog 상태

- 브랜치: `main...origin/main`.
- 작업트리 dirty: tracked 수정 18개, untracked 실행 문서/daylog/manifest/plan 다수.
- `git diff --stat`: tracked 기준 18 files, 313 insertions, 194 deletions.
- `daylog/2026-05-18.md` 존재.
- commit/push 없음.
- 이번 audit는 읽기 전용 조사라 별도 daylog는 작성하지 않음.

## 다음 판단

A 항목은 문서 표현 보정 1건뿐이라 다음 라운드 자동 수행 가능하다. 환경/장비/DB/TCP 제한으로 막힌 항목은 다음 라운드 A에 넣지 않는다.