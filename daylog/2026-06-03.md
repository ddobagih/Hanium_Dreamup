# 2026-06-03

## AIHub 수동 다운로드 재확인 및 라벨 후보 스캔

- 사용자가 AIHub 대상 데이터를 수동 다운로드 완료했다고 알려온 뒤 `~/Downloads` 기준으로 목표 zip 존재/0바이트/부분다운로드 여부를 재확인했다.
- 614, 557 CASE2, 189 probe, 71604 업사이클링 라벨 zip은 모두 zip 구조가 열렸고 부분다운로드/0바이트 zip은 발견되지 않았다.
- 71604 원본 `TL_2D_실환경.zip`/`VL_2D_실환경.zip`은 없지만, 사용자가 선택한 업사이클링 라벨의 관계데이터 JSON에는 `objects`, `bbox`, `label`, `relationships`가 있어 우선 스캔 대상으로 인정했다.
- 189 `Polygon_1_new.zip`은 이전 0바이트 상태에서 약 10GB 정상 zip으로 교체된 것을 확인했다.
- 수동 다운로드 라벨/프로브 스캔 결과를 `manual_download_inspection_20260603.json`에 저장했다.

변경 파일:

- `data_sources/manifests/aihub_filetrees_20260602/manual_download_inspection_20260603.json`
- `daylog/2026-06-03.md`

검증:

- 614: `TL1.zip`, `TL2.zip`, `TL3.zip`, `TL4.zip`, `VL1.zip` 모두 zip open OK.
- 557 CASE2: `TL06`, `TL07`, `TL08`, `VL03`, `VL04` 모두 zip open OK.
- 189: `Bbox_1_new.zip`, `Surface_1.zip`, `Polygon_1_new.zip` 모두 zip open OK. 각 zip에 이미지와 XML 라벨이 함께 들어 있음을 확인했다.
- 71604 업사이클링: 관계데이터 14개 zip, QA 14개 zip 확인. 관계데이터 zip은 JSON bbox 객체 라벨 구조를 포함한다.
- 관련 다운로드 루트에서 `.crdownload`/`.part` 등 부분다운로드 파일 0개, 0바이트 zip 0개 확인.

## AIHub 라벨 기반 원천 다운로드 후보 선정

- 다운로드된 라벨 zip을 기준으로 원천 데이터 후보를 역산했다.
- 189는 `Bbox_1_new`, `Surface_1`, `Polygon_1_new`가 이미지+XML을 이미 포함하므로 추가 원천 다운로드 없이 smoke 변환 대상으로 선정했다.
- 557 CASE2는 `경계석` 라벨 수와 source 용량을 기준으로 `VS03` 2GB를 1순위, `TS06` 17GB와 `VS04` 15GB를 후순위로 정했다.
- 186은 실외 validation source `35421` 3GB, `35419` 5GB를 1차 후보로 정했다.
- 71604 업사이클링은 라벨 image_path의 outdoor clip 목록만 추려 원천데이터 부분 다운로드 후보로 정했다.
- 614는 `PM_code`/`area_code` 의미 매핑과 수동 obstruction relabeling이 필요하므로 39GB `VS1`도 확인 전 보류로 정했다.
- 현재 디스크 여유 약 114GB라서 92~101GB급 source zip은 1차 다운로드에서 제외했다.

변경 파일:

- `data_sources/manifests/aihub_filetrees_20260602/source_download_candidates_20260603.json`
- `docs/aihub_source_download_selection_20260603.md`
- `daylog/2026-06-03.md`

검증:

- 557 CASE2 label zip별 category/image count 산출.
- 186 label zip별 target hit와 source filekey 후보 매핑 산출.
- 71604 업사이클링 outdoor `image_path` clip 분포 산출.
- 614 label zip별 `PM_code`/`area_code` 분포 산출.
- 189 package별 이미지/XML 포함 여부와 XML label name 분포 확인.
- `git diff --check` 통과.
