# 2026-08-12 WalkSafe 작업 로그

## 목표와 적대 검수

- 저장소 현대화 결과를 다시 적대적으로 검수하고, 실제 결함만 최소 보완한 뒤 독립 회귀·GitHub `current` 비강제 게시까지 완료하는 것을 목표로 삼았다.
- 두 독립 검수에서 Goal graph 신규 27건, 관리자 앱 CI 산출물 누락, 팀 기능표의 구버전·부분 import와 비동기 덮어쓰기, 활성 문서 사각지대와 ambient Node 안내를 재현했다.

## 보완 내용

- FP046 과거 증거는 byte-exact로 보존하고 `apps/android-gateway/test/privacy-rights.test.ts` 한 경로의 봉인값→현재값 forward amendment와 활성 회귀를 추가했다.
- 독립 검수의 승계 근거 부족 P1을 반영해 add-only `NOT_GOAL_EVENT_NO_COMPLETION_CREDIT` compatibility binding을 추가했다. checker는 기록의 exact path·byte·SHA·field, source commit `1e976419dc98a2ee336c02e1d08ccbd62e4625c0`의 실제 blob, 봉인·현재 tuple과 live file을 함께 검증한다.
- 팀 기능표의 JSON·localStorage를 schema·날짜·전체 119개 기능 지문과 exact field/ID set에 결속하고, 검증 뒤 원자 적용·동시 import generation·편집 revision·초기/처리 중 busy UI를 추가했다.
- quality workflow가 사용자·관리자 앱의 release lint report와 unsigned APK를 각각 확인한 뒤 두 앱의 reports/APK를 업로드하도록 보완했다.
- 최종 독립 검수의 P2를 반영해 실패 실행의 부분 artifact 업로드를 막고, 기존 사용자 artifact 내부 경로는 유지하면서 관리자 artifact를 별도 이름으로 분리했다. 회귀 테스트는 upload step의 action·성공 조건·이름·경로를 exact하게 고정한다.
- 현재 출시 상태 검사는 HTML 주석과 과거 문단을 제외하고 문서별 지정 현재 상태 줄 자체가 checkpoint 값과 일치하도록 강화하고, 상태 code span의 순서·개수도 exact 비교해 같은 줄의 모순 값도 거부한다.
- 첫 게시 commit의 GitHub Actions는 shallow checkout에 FP046 compatibility binding의 source commit 객체가 없어 active-session-control 3건이 실패했다. 검증을 제거하지 않고 workflow가 고정 commit 하나만 depth 1로 가져와 commit object를 확인한 뒤 control tests를 실행하도록 보완했다.
- 활성 문서 검사에 수정 가능한 Android README와 checkpoint-derived 현재 상태 검사를 추가하고, reporter actor·navigation·exact Node 개발 안내와 이전 현대화 CI 종료 기록을 교정했다.
- 전체 회귀에서 report-retention SIGTERM 테스트가 종료된 손자 프로세스의 외부 reaping 시점에 의존하는 간헐 실패를 발견했다. 강제 child-subreaper에서 `SIGKILL(9)`·`Z` 상태를 확인하고, 제품 runner 변경 없이 PID start time과 absent·재사용·`X/Z`를 bounded wait로 확인하도록 테스트를 고쳤다.

## 변경 파일

- CI·검사: `.github/workflows/quality.yml`, `scripts/check_walksafe_active_docs.py`, `scripts/check_walksafe_goal_graph_v2_4.py`
- 협업·가이드: `docs/planning/walksafe_feature_implementation_catalog.html`, `docs/guides/development-environment-guide.md`, Android source-near README 3개, `tests/README.md`
- 회귀·기록: `tests/test_report_retention_scheduler.py`, `tests/test_repository_catalogs.py`, `tests/test_walksafe_active_docs.py`, `tests/test_walksafe_project_continuation_v2_4.py`, `tests/test_web_build_manifest.py`, FP046 compatibility binding, 현대화 phase log와 이 daylog
- 최종 결속: repository catalog 재생성과 checkpoint managed snapshot content hash 2필드만 공식 계산값으로 조정한다.

## 검증과 경계

- Goal graph `--skip-continuation`은 기존 64줄/63건/SHA `bfab9a20b8ab621f94b45dc27d153a03d0c9062c398fb9ac51a96e13ee098417`과 exact 일치했다.
- 활성 문서 검사는 52문서·449 links·82 script refs·7 selectors로 PASS했다. 문서·catalog·workflow 회귀 101건과 strict continuation을 포함한 active-session 45건도 PASS했다.
- 후속 최종 회귀에서는 retention 전체 59건과 강제 child-subreaper 두 경로가 PASS했고, active-session singleton은 신규 회귀를 포함해 46건 PASS했다.
- 격리 PostGIS와 잠긴 CPython·Node·Java 환경의 최종 `all`은 unit Python 748 PASS·8 SKIP, 기능 Python 742 PASS, 백업 무결성 54 PASS, 통합 Python 73 PASS, Gateway 88 PASS, Web production build, Android 사용자·관리자 debug/release build·lint를 모두 통과했다. 사용자·관리자 release lint XML과 unsigned APK 네 파일도 모두 non-empty였다.
- Chromium에서 119개 정상 round-trip, 잘못된 지문·행 집합·schema·field 거부, localStorage fail-closed, 동시 import 최신값 유지, 후행 편집 유지, 초기 busy 상태와 console error 0을 확인했다. 현재 CI 회귀는 정적 계약까지이며 이 독립 Chromium 시나리오의 상시 브라우저 자동화는 후속 P2다.
- checkpoint managed 818경로의 content hash 두 필드만 최종 공식 계산값 `7419a29ef7d1b4e1347c9111dde2b39e7abcdf3e7efb05ad28ee1f39df30242e`로 조정했고 strict continuation은 PASS했다.
- 별도 적대 검수에서 checkpoint의 과거 base `a3ad7eead6b5d834d3e0675422475a9aad351e3d`가 standalone 저장소에 없고 handoff HEAD도 오래돼 공식 gate repository-state capture가 종료 코드 2임을 확인했다. 이번 비제품 현대화 게시에서는 seq58·base/head를 임의 변경하지 않으며, 다음 제품 Goal start/resume·completion credit 전에 승인된 zero-credit standalone snapshot re-anchor가 필요하다.
- 정식 시험 279건과 실제 기기·현장·배포·외부 검토, 출시 gate 5개는 계속 `NOT_RUN`이며 출시는 `NOT_ELIGIBLE`이다. 저장소 자동 검증을 실제 출시 증거로 승격하지 않는다.
