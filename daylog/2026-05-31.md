# 2026-05-31

## Android ARCore depth architecture worker

### 한 일
- 현재 checkout에서 Android Gradle 프로젝트와 `android_depth_estimation_design.md` 존재 여부를 확인했다.
- Android 프로젝트가 없어 build graph에는 연결하지 않고, ARCore depth 추정 reference 설계를 문서/순수 Kotlin 파일로 추가했다.
- `ArCoreFrameProvider`, `DepthSampler`, `RobustDepthStats`, `CoordinateMapper`, `TrackedObjectDepth`, `MessagePolicy` 책임을 분리했다.

### 변경 파일
- `docs/android/arcore_depth_estimation_architecture.md`
- `docs/android/kotlin/ArCoreDepthArchitecture.kt`
- `daylog/2026-05-31.md`

### 검증
- `find`로 Android Gradle/Manifest 및 `android_depth_estimation_design.md` 미존재 확인.
- `grep`으로 요청된 Kotlin symbol 존재 확인.
- `python3` delimiter/required symbol sanity check 통과.
- `kotlinc`는 로컬에 없어 Kotlin compile은 실행하지 못했다.

### 남은 리스크
- 실제 ARCore depth image 좌표계와 detector preview 좌표계는 Android 실기기에서 overlay fixture로 검증해야 한다.
- 현재 Kotlin 파일은 build에 포함되지 않는 reference 산출물이다. Android 앱 생성 후 package/path와 ARCore adapter 구현을 확정해야 한다.

## Android ARCore depth 앱 스캐폴딩 및 웹 depth policy 통합

### 한 일
- 첨부 설계 기준으로 Android native 우선 경로를 반영해 `apps/android` Gradle/Android 앱 스캐폴딩을 추가했다.
- ARCore `DepthMode.AUTOMATIC`, Raw Depth + confidence, Full Depth fallback을 담당하는 `ArCoreFrameProvider`를 추가했다.
- 객체 polygon 내부 depth robust sampling, outlier 제거, `median/p20/IQR/MAD` 통계를 위한 `DepthSampler`를 추가했다.
- model/image/depth 좌표 변환용 `CoordinateMapper`, depth 결과/신뢰도/보폭 안내 데이터 모델, metric source 전용 `MessagePolicy`를 추가했다.
- 웹 경로는 첨부 정책에 맞춰 `model_estimate`/polygon trend에서 “약 N보 앞” 거리 안내를 금지하고, metric source만 보폭 안내에 쓰도록 보정했다.
- Android 스캐폴딩 정적 검증 스크립트를 추가했다.

### 변경 파일
- `apps/android/**`
- `scripts/check_android_depth_scaffold_20260531.py`
- `apps/web/app/_walksafe/risk-guidance.ts`
- `apps/web/app/_walksafe/hooks/useRiskFeedback.ts`
- `apps/web/app/page.tsx`
- `apps/web/app/_walksafe/components/AssistPanel.tsx`
- `apps/web/app/_walksafe/components/CameraSurface.tsx`
- `apps/web/app/_walksafe/depth-estimator.ts`
- `apps/web/app/_walksafe/hooks/useDepthSensor.ts`
- `apps/web/tests/risk-evaluator-policy.test.ts`
- `backend/app/schemas.py`
- `backend/app/services/detect_v2.py`
- `backend/tests/test_detect_v2.py`

### 검증
- `bash scripts/check_frontend_risk_evaluator_policy_20260523.sh` → PASS
- `bash scripts/check_frontend_step_length_policy_20260525.sh` → PASS
- `cd apps/web && npm run typecheck` → PASS
- `cd apps/web && npm run lint` → PASS
- `cd apps/web && npm run build` → PASS
- `python3 scripts/check_frontend_accessibility_static.py` → PASS
- `PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_detect_v2.py -q` → 12 passed
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS

### 남은 리스크
- 로컬에 Gradle wrapper/Gradle 명령이 없어 `apps/android`의 실제 `assembleDebug`는 실행하지 못했다.
- ARCore depth 품질, Raw/Full depth 좌표계, 카메라 preview 충돌 여부는 Android 실기기에서 검증해야 한다.
- 현재 Android 앱은 depth core 스캐폴딩 단계이며 실제 모델 inference/AR render loop 연결은 다음 단계에서 이어야 한다.

## Android Tactile/Message Worker 정책 보강

### 한 일
- `MessagePolicy`에서 정상 점자블록을 장애물 경고가 아닌 `PATH_GUIDANCE` 안내로 분리했다.
- 손상 점자블록은 사용자 경고 없이 `REPORT_ONLY`로 남기고, `tactile_damage_area`는 사용자 안내에서 제외했다.
- metric source별 confidence threshold, per-purpose rate limit, distance/confidence hysteresis 골격을 추가했다.
- MessagePolicy JVM 테스트를 추가해 path guidance, report-only, source threshold downgrade, rate limit, hysteresis 동작을 검증 대상으로 만들었다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/MessagePolicy.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/MessagePolicyTest.kt`
- `daylog/2026-05-31.md`

### 검증
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS
- MessagePolicy path guidance/threshold/rate-limit/hysteresis 정적 확인 스크립트 → PASS
- `apps/android` Gradle wrapper/로컬 `gradle` 명령이 없어 JVM 테스트 실행은 스킵했다.

## Android Tracker/Risk Worker

### 한 일
- `depth` 패키지에 순수 Kotlin 객체 추적/접근 위험 로직을 신규 파일로 추가했다.
- `TrackState`, `ObjectTracker`, `DistanceObservation`, `ApproachSpeed`, `TTC` 계산, `IdSwitchSuspicionPolicy`, TTC 기반 risk policy를 구현했다.
- metric depth 기반 접근 속도/TTC, bbox scale fallback TTC, stable frame/gap reset, ID switch 의심, pseudo distance의 metric 경고 차단 테스트를 추가했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/ObjectTracker.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/TrackerRiskPolicy.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/ObjectTrackerTest.kt`
- `daylog/2026-05-31.md`

### 검증
- `grep`으로 신규 tracker 파일에 ARCore/Android import 없음 확인 → PASS
- trailing whitespace check → PASS
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS
- Gradle wrapper/로컬 `gradle` 명령이 없어 `apps/android` JVM 테스트 실행은 스킵했다.

## Android depth 설계서 기준 P0/P1 보강 통합

### 한 일
- 첨부 `android_depth_estimation_design_1.md` 기준으로 Android depth core를 보강했다.
- ARCore depth `Image`를 `rowStride`/`pixelStride` 기준 immutable snapshot으로 변환하는 `ArCoreImageConverters`와 `DepthFrameSnapshot`을 추가했다.
- Raw Depth + confidence 우선, Full Depth fallback, sensor depth 실패 시 pseudo trend fallback을 `ObjectDepthEstimator`로 연결했다.
- polygon 우선 geometry 추출, bottom contact, tactile centerline skeleton, polygon erosion 기반 robust sampling, confidence median 산출을 추가했다.
- track ID 안정성, ID switch suspicion, metric source에서만 approach/TTC 계산하는 tracker/risk policy skeleton을 추가했다.
- `MessagePolicy`에서 `trustedForStepGuidance`를 강제해 monocular/model/pseudo source는 사용자 보폭 안내에 쓰지 않게 했다.
- Web UI 문구는 Android native ARCore와 혼동되지 않도록 WebXR depth로 명확히 바꿨다.
- Android architecture 문서를 현재 `apps/android` 구현 기준으로 갱신했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/*.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/*.kt`
- `apps/web/app/page.tsx`
- `apps/web/app/_walksafe/components/AssistPanel.tsx`
- `docs/android/arcore_depth_estimation_architecture.md`
- `scripts/check_android_depth_scaffold_20260531.py`
- `daylog/2026-05-31.md`

### 검증
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS
- Android depth policy consistency static check → PASS
- `bash scripts/check_frontend_risk_evaluator_policy_20260523.sh` → PASS
- `cd apps/web && npm run typecheck` → PASS
- `cd apps/web && npm run lint` → PASS
- `cd apps/web && npm run build` → PASS
- `bash scripts/check_frontend_step_length_policy_20260525.sh` → PASS
- `python3 scripts/check_frontend_accessibility_static.py` → PASS
- `PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_detect_v2.py -q` → 12 passed
- `git diff --check` on touched Android/Web/backend policy files → PASS

### 남은 리스크
- 로컬에 Gradle wrapper/`gradle` 명령이 없어 Android JVM 테스트와 `assembleDebug`는 실행하지 못했다.
- Android 앱은 아직 실제 ARCore session/render loop, detector inference, TTS/haptic 실행부에 연결되지 않은 depth core 단계다.
- ARCore depth/image/display 좌표계, Raw/Full depth 품질, 카메라 권한/lifecycle은 Android 실기기에서 검증해야 한다.

## Android depth JVM/static test 보강

### 한 일
- `CoordinateMapper`의 rotation/mirror 좌표 변환 기대값과 역변환 round-trip JVM 테스트를 추가했다.
- `MessagePolicy`에서 `trustedForStepGuidance=false`인 metric source가 정상 점자블록 path guidance에 보폭 수를 노출하지 않는 테스트를 추가했다.
- Android depth 정적 스캐폴드 스크립트가 신규 CoordinateMapper 테스트와 trustedForStepGuidance 테스트를 확인하도록 보강했다.

### 변경 파일
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/CoordinateMapperTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/MessagePolicyTest.kt`
- `scripts/check_android_depth_scaffold_20260531.py`
- `daylog/2026-05-31.md`

### 검증
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS
- changed file trailing whitespace check → PASS
- Gradle wrapper/로컬 `gradle` 명령이 없어 Android JVM 테스트 실행은 스킵했다.

## Android ARCore 런타임 smoke 연결 및 Gradle 검증

### 한 일
- `MainActivity`를 TextView scaffold에서 최소 ARCore lifecycle/permission/render loop smoke 앱으로 확장했다.
- `GLSurfaceView.Renderer`에서 camera external texture를 만들고 `Session.setCameraTextureNames(...)` 후 `Session.update()`를 호출하도록 연결했다.
- `DepthSmokePipeline`을 추가해 중앙 ROI에서 Raw Depth + confidence를 우선 sample하고 부족하면 Full Depth로 fallback하는 smoke 결과를 UI에 표시하게 했다.
- ARCore depth/camera image acquire 예외 처리를 `NotYetAvailableException` 외 `NotTrackingException`, `DeadlineExceededException`, `ResourceExhaustedException`, `IllegalStateException`까지 보수적으로 확장했다.
- Gradle wrapper `9.3.1`을 추가하고 AGP 9 내장 Kotlin 정책에 맞춰 `org.jetbrains.kotlin.android` 플러그인을 제거했다.
- Android build artifact가 들어가지 않도록 `apps/android/.gitignore`를 추가하고 README 실행 명령을 wrapper 기준으로 갱신했다.

### 변경 파일
- `apps/android/gradlew`
- `apps/android/gradlew.bat`
- `apps/android/gradle/wrapper/gradle-wrapper.jar`
- `apps/android/gradle/wrapper/gradle-wrapper.properties`
- `apps/android/.gitignore`
- `apps/android/build.gradle.kts`
- `apps/android/app/build.gradle.kts`
- `apps/android/README.md`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/ArCoreFrameProvider.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/DepthSmokePipeline.kt`
- `scripts/check_android_depth_scaffold_20260531.py`
- `daylog/2026-05-31.md`

### 검증
- `cd apps/android && ./gradlew test` → BUILD SUCCESSFUL
- `cd apps/android && ./gradlew assembleDebug` → BUILD SUCCESSFUL
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS
- `bash scripts/check_frontend_risk_evaluator_policy_20260523.sh` → PASS
- `bash scripts/check_frontend_step_length_policy_20260525.sh` → PASS
- `PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_detect_v2.py -q` → 12 passed
- `git diff --check` on touched Android/Web/backend/daylog files → PASS

### 남은 리스크
- Android 실기기에서 Google Play Services for AR 설치/권한/카메라 lifecycle/DepthMode 지원 여부를 확인해야 한다.
- 현재 smoke UI는 중앙 ROI depth 확인용이며, 실제 detector inference 결과와 `ObjectDepthEstimator`의 객체별 depth 연결은 다음 단계다.
- 카메라 preview 렌더링은 depth smoke용 texture만 설정한 상태라 실제 카메라 영상을 화면에 그리는 background renderer는 아직 없다.

## Android 객체별 ARCore depth 런타임 연결 보강

### 한 일
- `CameraBackgroundRenderer`를 추가해 ARCore camera external texture를 실제 화면 배경으로 렌더링하게 했다.
- `MainActivity` 루프에서 같은 ARCore `Frame` 기준으로 camera image, Raw Depth, Full Depth snapshot을 수신하고 detector 결과를 객체별 depth pipeline에 전달하도록 연결했다.
- 실제 `.tflite` detector asset 연결 전에는 기본 detector를 비워 가짜 중앙 사람 detection/경고가 나가지 않게 했다.
- detection 누락 frame에서 이전 geometry로 stale warning이 계속 나가지 않도록 runtime pipeline이 빈 결과를 반환하게 했다.
- `DepthSampler`가 outlier 제거 후 최소 sample 수를 다시 검증하고, 비정상 `maxSamples` 값을 안전하게 보정하도록 했다.
- `ObjectTracker`가 동일한 detection 2개를 `Set` 변환으로 합치지 않도록 list 기반 matching으로 바꿨다.
- `MessagePolicy`에서 `tactile_block` 같은 넓은 점자블록 class도 장애물 경고가 아니라 path guidance로 처리하게 했다.
- Android TFLite export dry-run과 모델 asset 현황을 확인했다. 실제 `.tflite` export는 현재 Python 3.14 환경에서 TensorFlow wheel이 없어 보류했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/CameraBackgroundRenderer.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/ObjectDepthRuntimePipeline.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/DepthSampler.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/ObjectTracker.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/MessagePolicy.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/ObjectDepthRuntimePipelineTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/DepthSamplerTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/ObjectTrackerTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/MessagePolicyTest.kt`
- `apps/android/README.md`
- `scripts/check_android_depth_scaffold_20260531.py`
- `daylog/2026-05-31.md`

### 검증
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS
- `cd apps/android && ./gradlew test` → BUILD SUCCESSFUL
- `cd apps/android && ./gradlew assembleDebug` → BUILD SUCCESSFUL
- `python3 scripts/export_android_tflite_models_20260531.py --dry-run` → PASS
- `bash scripts/check_frontend_risk_evaluator_policy_20260523.sh` → PASS
- `bash scripts/check_frontend_step_length_policy_20260525.sh` → PASS
- `PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_detect_v2.py -q` → 12 passed
- `git diff --check` → PASS
- Android/script text file trailing whitespace check → PASS

### 남은 리스크
- 실제 detector는 아직 `NoopAndroidFrameDetector`라 Android 앱에서 실제 사람/점자블록 detection은 나오지 않는다.
- `apps/android/app/src/main/assets/models/*.tflite`가 아직 없다. 현재 weight 파일은 존재하지만 Python 3.14 환경에서 TensorFlow 설치가 불가능해 TFLite export가 막혀 있다.
- runtime 좌표 변환은 아직 depth snapshot 기준 identity mapper라, 실제 detector 연결 시 ARCore camera image/model/depth 좌표 mapping을 실기기에서 별도 검증해야 한다.
- groundDistance/rayDistance/pose 기반 corridor 계산은 core helper만 있고 runtime에는 아직 연결되지 않았다.

## Android TFLite 실제 모델 export 및 detector 연결

### 한 일
- repo 밖 `~/.local/share/hanium-dreamup`에 Python 3.12 standalone/venv를 만들고 TFLite export 전용 환경을 구성했다.
- CPU PyTorch wheel을 먼저 설치해 CUDA 대용량 의존성 설치를 피했고, TensorFlow 2.19.1/Ultralytics 8.4.48/ONNX/ONNX Runtime import를 확인했다.
- custom tactile YOLO26s와 COCO YOLO26n을 Android용 float32 TFLite로 export했다.
- export script가 `_float32` 대상에 `_float16.tflite`를 잘못 복사할 수 있던 선택 로직을 고쳐 output name에 맞는 `.tflite`를 우선 선택하게 했다.
- Android asset에 실제 `.tflite` 2개를 배치하고 APK에서 압축되지 않도록 `noCompress += "tflite"`를 설정했다.
- TensorFlow Lite interpreter 기반 `TfliteAndroidFrameDetector`를 추가해 ARCore `cameraImage`를 custom/COCO 두 모델 inference에 연결했다.
- YUV camera image를 RGB float tensor로 변환하고 letterbox inverse로 model bbox를 image-normalized bbox로 복원하는 `YuvImagePreprocessor`를 추가했다.
- 같은 camera frame에서 두 모델을 돌릴 때 YUV decode를 한 번만 수행하도록 전처리 경로를 보정했다.
- YOLO end-to-end output `[1, 300, 6]` 전용 parser와 COCO class map을 추가했다.
- `MainActivity`에서 TFLite asset이 있으면 실제 detector를 사용하고, asset이 없으면 no-op detector로 안전하게 fallback하도록 연결했다.
- letterbox 역변환 단위 테스트와 Android depth scaffold 정적 검증을 보강했다.
- Python TFLite smoke로 COCO `bus.jpg` 입력에서 output shape `[1, 300, 6]`와 normalized xyxy/class 형식을 확인했다.

### 변경 파일
- `.gitignore`
- `apps/android/app/build.gradle.kts`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelClassMap.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/YoloEndToEndOutputParser.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/YuvImagePreprocessor.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TfliteAndroidFrameDetector.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/YoloEndToEndOutputParserTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/YuvImagePreprocessorTest.kt`
- `scripts/check_android_depth_scaffold_20260531.py`
- `scripts/export_android_tflite_models_20260531.py`
- `daylog/2026-05-31.md`

### 생성/배치한 로컬 산출물
- `apps/android/app/src/main/assets/models/custom_tactile_yolo26s_float32.tflite` — 38,460,640 bytes
- `apps/android/app/src/main/assets/models/coco_yolo26n_float32.tflite` — 10,343,256 bytes
- `~/.local/share/hanium-dreamup/python-3.12.13+20260510`
- `~/.local/share/hanium-dreamup/venv-tflite-py312`

### 검증
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS
- `cd apps/android && ./gradlew test` → BUILD SUCCESSFUL
- `cd apps/android && ./gradlew assembleDebug` → BUILD SUCCESSFUL
- `unzip -v apps/android/app/build/outputs/apk/debug/app-debug.apk | grep -E 'custom_tactile|coco_yolo'` → 두 TFLite asset 모두 `Stored` 0% 확인
- `~/.local/share/hanium-dreamup/venv-tflite-py312/bin/python scripts/export_android_tflite_models_20260531.py --dry-run` → PASS
- Python TFLite interpreter shape 확인 → custom input `[1, 960, 960, 3]`, output `[1, 300, 6]`; COCO input `[1, 640, 640, 3]`, output `[1, 300, 6]`
- Python TFLite smoke on Ultralytics `bus.jpg` → normalized xyxy + score + class id rows 확인
- `bash scripts/check_frontend_risk_evaluator_policy_20260523.sh` → PASS
- `bash scripts/check_frontend_step_length_policy_20260525.sh` → PASS
- `PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_detect_v2.py -q` → 12 passed

### 남은 리스크
- Android 실기기에서 ARCore camera image/depth 좌표 정합성과 bbox overlay 위치를 확인해야 한다.
- TFLite 모델은 로컬 asset에 존재하지만 root `.gitignore`의 `*.tflite` 정책상 기본 git diff에는 나타나지 않는다. 공유/재현 시 export script 실행 또는 별도 artifact 전달이 필요하다.
- 현재 Android detector는 CPU TFLite interpreter 2개를 순차 실행한다. 실기기 FPS/발열을 보고 inference 주기 조절, NNAPI/GPU delegate, 모델 경량화를 검토해야 한다.
- ground distance/corridor/ARCore pose 기반 보정은 아직 runtime에 완전히 연결되지 않았고, 현재 객체 depth는 image/depth normalized sampling 중심이다.

## Cloudflare 휴대폰 실기기 웹 테스트/로그 수집 세팅

### 한 일
- 휴대폰에서 외부 URL로 카메라 테스트가 가능하도록 Next.js 웹 앱을 `server-v2` 모드로 실행하고 Cloudflare quick tunnel을 열었다.
- phone test 분석을 위해 `TestCapturePanel`을 추가했다. 사용자가 `맞음 저장`, `틀림 저장`, `스냅샷만`을 누르면 현재 카메라 프레임, 기대 대상/거리/메모, 탐지 결과, 위험도/깊이/GPS/heading 상태를 로컬 JSON+JPG로 저장한다.
- Cloudflare 단일 URL에서 backend v2 detector를 호출할 수 있도록 Next.js API proxy를 추가했다.
- 테스트 로그 수집 API `/api/walksafe-test-log`를 추가했다.
- FastAPI backend를 real two-model YOLO 모드로 실행하고, Next dev 서버와 Cloudflare tunnel을 장시간 테스트용으로 실행했다.

### 변경 파일
- `apps/web/app/api/detect/v2/route.ts`
- `apps/web/app/api/detect/v2/health/route.ts`
- `apps/web/app/api/walksafe-test-log/route.ts`
- `apps/web/app/_walksafe/components/TestCapturePanel.tsx`
- `apps/web/app/page.tsx`
- `apps/web/app/globals.css`
- `daylog/2026-05-31.md`

### 실행 중인 로컬 프로세스
- FastAPI backend: `http://127.0.0.1:8000`
- Next.js web: `http://127.0.0.1:3000`
- Cloudflare quick tunnel: `https://dpi-nerve-opening-slideshow.trycloudflare.com`

### 검증
- `npm run typecheck` → PASS
- `npm run lint` → PASS
- `NEXT_PUBLIC_DETECTOR_MODE=server-v2 NEXT_PUBLIC_API_BASE_URL=/api npm run build` → PASS, 단 Turbopack NFT tracing warning 1건 있음
- `curl http://127.0.0.1:8000/detect/v2/health` → real/ready
- `curl http://127.0.0.1:3000/api/detect/v2/health` → real/ready
- `curl https://dpi-nerve-opening-slideshow.trycloudflare.com/api/detect/v2/health` → real/ready
- 로컬 `bus.jpg`로 `/api/detect/v2` smoke 호출 → bus/person detection 반환 확인
- `/api/walksafe-test-log` smoke 호출 → JSON/JPG 저장 확인 후 smoke 산출물 삭제
- `bash scripts/check_frontend_risk_evaluator_policy_20260523.sh` → PASS
- `bash scripts/check_frontend_step_length_policy_20260525.sh` → PASS
- `python3 scripts/check_frontend_accessibility_static.py` → PASS
- `git diff --check` → PASS

### 남은 리스크
- Cloudflare quick tunnel은 임시 URL이며 uptime 보장이 없다.
- 외부 공개 URL이므로 테스트 중 민감한 얼굴/번호판/개인 공간 촬영을 피해야 한다.
- 웹 테스트는 Web/PWA 경로 검증이며 Android native ARCore depth/TFLite APK의 좌표 정합성과는 별도다.
- Next build에서 `/api/walksafe-test-log` route 관련 Turbopack NFT tracing warning이 1건 남아 있으나 빌드는 성공했다.

## Cloudflare 카메라 권한 진단 표시 보강

### 한 일
- 휴대폰 Chrome에서 카메라 권한 팝업이 뜨지 않는 원인을 구분할 수 있도록 `useCamera`에 `navigator.permissions.query({ name: "camera" })` 기반 권한 상태 조회를 추가했다.
- 카메라 fallback 화면에 `granted/prompt/denied/unsupported/unknown` 권한 상태와, `denied`일 때 Chrome 사이트 설정에서 카메라 허용 후 새로고침해야 한다는 안내를 표시하도록 했다.
- 권한 재요청 버튼을 누를 때 권한 상태를 다시 갱신하도록 연결했다.

### 변경 파일
- `apps/web/app/_walksafe/hooks/useCamera.ts`
- `apps/web/app/_walksafe/components/CameraSurface.tsx`
- `apps/web/app/page.tsx`
- `daylog/2026-05-31.md`

### 검증
- `npm run typecheck` → PASS
- `npm run lint` → PASS
- `curl -I -L https://dpi-nerve-opening-slideshow.trycloudflare.com` → HTTP 200
- `git diff --check` → PASS

### 남은 리스크
- 브라우저 권한 상태가 `denied`이면 웹 코드가 권한 팝업을 강제로 다시 띄울 수 없고, 사용자가 Chrome 사이트 설정/Android 앱 권한에서 직접 허용해야 한다.

## Cloudflare 카메라 권한 요청 흐름 수정

### 한 일
- 모바일 Chrome에서 카메라 권한 팝업이 뜨지 않는 문제를 코드 이슈로 재검토했다.
- `useCamera`가 페이지 로드 직후 자동으로 `getUserMedia()`를 호출하던 흐름을 제거하고, 사용자가 `카메라 권한 요청` 버튼을 누를 때만 `getUserMedia()`가 실행되도록 수정했다.
- 카메라 오류 메시지에 `DOMException.name`을 포함해 `NotAllowedError`, `SecurityError`, `NotReadableError` 등 원인을 화면에서 구분할 수 있게 했다.
- Cloudflare URL에서 Next dev HMR cross-origin 차단이 발생해 `allowedDevOrigins`에 현재 trycloudflare host를 추가하고 Next dev server를 재시작했다.
- service worker version을 갱신하고 `/api` 경로를 네트워크 우선 API prefix에 추가했다.

### 변경 파일
- `apps/web/app/_walksafe/hooks/useCamera.ts`
- `apps/web/app/_walksafe/components/CameraSurface.tsx`
- `apps/web/next.config.mjs`
- `apps/web/public/sw.js`
- `daylog/2026-05-31.md`

### 검증
- `npm run typecheck` → PASS
- `npm run lint` → PASS
- Next dev server 재시작 완료
- `curl -I -L https://dpi-nerve-opening-slideshow.trycloudflare.com` → HTTP 200
- `curl https://dpi-nerve-opening-slideshow.trycloudflare.com/api/detect/v2/health` → real/ready
- `git diff --check` → PASS

### 남은 리스크
- 기존 휴대폰 탭이 이전 JS bundle을 잡고 있을 수 있어 탭을 닫고 URL을 새로 열어야 최신 권한 요청 흐름이 적용된다.

## WebXR depth 버튼 비활성화 및 서버 탐지 박스 유지 보강

### 한 일
- 실기기 Cloudflare 서버 인식 테스트에서 `WebXR 깊이 시작` 버튼이 브라우저 AR 세션으로 전환되어 검은 화면처럼 보일 수 있어, server-v2/camera-only 모드에서는 WebXR depth 시작 버튼을 숨기고 비활성화 안내를 표시하도록 수정했다.
- 서버 탐지 요청을 시작할 때 기존 박스를 즉시 지우지 않고 “새 프레임 분석 중 · 이전 결과 유지” 상태로 둬서, 걷거나 손떨림이 있을 때 박스가 매 요청마다 깜빡이며 사라지는 현상을 줄였다.
- 서버 v2 탐지 결과 stale timeout을 0.9초에서 1.8초로 늘려 모바일 네트워크/추론 지연 중에도 정상 detection overlay가 너무 빨리 사라지지 않도록 했다.

### 변경 파일
- `apps/web/app/_walksafe/components/AssistPanel.tsx`
- `apps/web/app/_walksafe/hooks/useDetectionV2.ts`
- `apps/web/app/page.tsx`
- `daylog/2026-05-31.md`

### 검증
- `cd apps/web && npm run typecheck` → PASS
- `cd apps/web && npm run lint` → PASS
- `bash scripts/check_frontend_risk_evaluator_policy_20260523.sh` → PASS
- `python3 scripts/check_frontend_accessibility_static.py` → PASS
- `git diff --check` → PASS
- `curl -I -L --max-time 20 https://dpi-nerve-opening-slideshow.trycloudflare.com` → HTTP 200
- `curl --max-time 20 https://dpi-nerve-opening-slideshow.trycloudflare.com/api/detect/v2/health` → real/ready

### 남은 리스크
- 실제 휴대폰 화면에서 박스가 안 뜨는 최종 원인이 “서버 결과 0개”인지 “오버레이 갱신/표시 문제”인지는 사용자가 같은 URL을 새로 열어 추가 스냅샷 로그를 저장해야 판별 가능하다.

## 실기기 테스트 분석/프록시/권한 경로 통합

### 한 일
- 병렬 작업 결과를 통합해 two-model 탐지 요청 audit를 UI와 실기기 테스트 스냅샷 metadata에 연결했다.
- 테스트 저장 metadata에 detector mode, API base, camera/video geometry, viewport, 최신 detect v2 audit를 포함해 휴대폰 테스트 결과를 나중에 분석할 수 있게 했다.
- Cloudflare URL에서 `NEXT_PUBLIC_API_BASE_URL=/api`를 쓸 때 관리자/자동신고가 끊기지 않도록 reports, reports/v2, status, export, duplicate-check, summary, uploads 프록시 route를 추가했다.
- 기존 `/api/detect/v2` 프록시도 공통 backend proxy helper를 통해 `x-request-id`와 content headers를 전달하도록 정리했다.
- 실기기 테스트 로그 디렉터리 `apps/web/walksafe-test-logs/`를 gitignore에 추가했다.
- PostGIS 컨테이너를 기동하고 Alembic head 적용 후 Cloudflare URL의 reports/summary 프록시까지 확인했다.

### 변경 파일
- `.gitignore`
- `apps/web/app/page.tsx`
- `apps/web/app/_walksafe/components/TestCapturePanel.tsx`
- `apps/web/app/api/_backend.ts`
- `apps/web/app/api/detect/v2/route.ts`
- `apps/web/app/api/detect/v2/health/route.ts`
- `apps/web/app/api/reports/route.ts`
- `apps/web/app/api/reports/v2/route.ts`
- `apps/web/app/api/reports/summary/route.ts`
- `apps/web/app/api/reports/export/route.ts`
- `apps/web/app/api/reports/duplicate-check/route.ts`
- `apps/web/app/api/reports/[reportId]/status/route.ts`
- `apps/web/app/api/uploads/[...path]/route.ts`

### 검증
- `cd apps/web && npm run typecheck -- --pretty false` → PASS
- `cd apps/web && npm run lint` → PASS
- frontend policy scripts 전체 + accessibility static check → PASS
- `NEXT_PUBLIC_DETECTOR_MODE=server-v2 NEXT_PUBLIC_API_BASE_URL=/api npm run build` → PASS, 단 `walksafe-test-log` route의 local fs fallback 관련 Turbopack trace warning 1건 있음
- `.venv/bin/python -m pytest backend/tests -q` → 89 passed
- `python3 scripts/check_android_depth_scaffold_20260531.py` → PASS
- `cd apps/android && ./gradlew testDebugUnitTest assembleDebug` → BUILD SUCCESSFUL
- `git diff --check` → PASS
- `docker compose up -d db` 후 `docker compose ps db` → `walksafe-postgis` healthy
- `curl https://dpi-nerve-opening-slideshow.trycloudflare.com/api/detect/v2/health` → 200 ready
- `curl https://dpi-nerve-opening-slideshow.trycloudflare.com/api/reports?limit=1` → 200
- `curl https://dpi-nerve-opening-slideshow.trycloudflare.com/api/reports/summary?...` → 200
- `curl -F metadata=... https://dpi-nerve-opening-slideshow.trycloudflare.com/api/walksafe-test-log` → 200, smoke log 저장 확인

### 남은 리스크
- Web 경로의 depth는 Android native ARCore가 아니라 브라우저/WebXR/폴리곤 추세 경로다. 실제 metric depth는 Android 앱 실기기에서 별도 검증해야 한다.
- Cloudflare/Next/backend/PostGIS는 로컬 장시간 프로세스로 떠 있는 상태다. 종료하려면 tunnel/dev/backend/db를 별도로 중지해야 한다.
- production build 경고 1건은 실패는 아니지만, 테스트 로그 route의 로컬 파일 저장 fallback을 production 배포 전 `/tmp` 또는 명시 env로 분리하면 더 깔끔하다.

## 실기기 보행 중 사람 박스 반응성 완화

### 한 일
- 사용자가 걸으면서 사람을 비출 때 박스가 늦거나 안 뜬다는 피드백에 맞춰, 실시간 서버 탐지 경로의 보수성을 완화했다.
- 프론트 서버 탐지 주기를 기본 450ms로 낮추고, env override 하한을 250ms까지 허용했다.
- 모바일 업로드/추론 지연을 줄이기 위해 카메라 캡처 이미지를 최대 edge 960px, JPEG quality 0.78로 축소했다.
- COCO/person 후보가 motion blur에서 낮은 confidence로도 백엔드까지 들어오도록 COCO prefilter confidence를 0.10으로 낮췄다.
- 실제 runtime config의 person threshold를 0.30에서 0.20으로 낮췄다.
- 백엔드 서버를 새 설정으로 재시작했다.

### 변경 파일
- `apps/web/app/_walksafe/config.ts`
- `apps/web/app/_walksafe/hooks/useCamera.ts`
- `backend/app/services/detect_v2.py`
- `backend/tests/test_detect_v2.py`
- `configs/walksafe_two_model_runtime_stage1_mvp_20260523.json`
- `daylog/2026-05-31.md`

### 검증
- `cd apps/web && npm run typecheck -- --pretty false` → PASS
- `cd apps/web && npm run lint` → PASS
- `bash scripts/check_frontend_risk_evaluator_policy_20260523.sh` → PASS
- `bash scripts/check_frontend_step_length_policy_20260525.sh` → PASS
- `python3 scripts/check_frontend_accessibility_static.py` → PASS
- `.venv/bin/python -m pytest backend/tests/test_detect_v2.py -q` → 12 passed
- `NEXT_PUBLIC_DETECTOR_MODE=server-v2 NEXT_PUBLIC_API_BASE_URL=/api npm run build` → PASS, 기존 test-log route trace warning 1건 있음
- `git diff --check` → PASS
- `curl https://dpi-nerve-opening-slideshow.trycloudflare.com/api/detect/v2/health` → 200 ready
- 로컬 sanity: `COCO_GENERAL_INFERENCE_CONF=0.1`, `person_threshold=0.2` 확인

### 남은 리스크
- threshold를 낮춘 만큼 보행자 false positive가 늘 수 있다. 휴대폰 테스트 패널에서 `틀림 저장`을 몇 개 남기면 person threshold를 0.20~0.25 사이로 재조정해야 한다.
- 서버 탐지 주기를 450ms로 낮춰 배터리/네트워크/CPU 사용량이 늘었다. 발열이나 버벅임이 생기면 600~700ms로 되돌리는 게 안전하다.

## Android ARCore 실기기 APK 안정화 재검증

### 한 일
- STT 확인은 별도 요청 범위였음을 분리하고, Android native ARCore APK 작업으로 초점을 되돌렸다.
- `MainActivity`에 ARCore `checkAvailability()` 분기를 추가해 미지원/확인 실패 기기에서 시작 흐름을 명확히 막거나 재시도하게 했다.
- `Session.setDisplayGeometry(rotation, width, height)`를 surface 변경 시점과 session 생성 직후 호출해 ARCore preview/depth 좌표 정합 준비를 보강했다.
- ARCore 설치 요청 후 앱 복귀 시 session 시작을 재시도하는 pending 플래그를 추가했다.
- 카메라 권한 거부/영구 차단 시 재요청 또는 앱 설정 이동 버튼을 표시하도록 했다.
- camera unavailable/시작 실패 경로에서 `Session.close()`까지 수행하도록 리소스 해제를 보강했다.
- GL render loop에서 camera preview를 먼저 그리고, TFLite detector는 300ms 간격으로 throttle해 실기기 preview frame drop 위험을 줄였다.
- Android README와 architecture 문서를 현재 APK 산출/설치 경로와 TFLite asset 포함 상태 기준으로 갱신했다.
- Android static scaffold check가 ARCore availability/display geometry/permission settings/throttle 보강을 확인하도록 갱신했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/README.md`
- `docs/android/arcore_depth_estimation_architecture.md`
- `scripts/check_android_depth_scaffold_20260531.py`
- `daylog/2026-05-31.md`

### 검증
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS
- `cd apps/android && ./gradlew test assembleDebug --no-daemon` → BUILD SUCCESSFUL
- `apksigner verify --print-certs apps/android/app/build/outputs/apk/debug/app-debug.apk` → debug signer 검증 PASS
- `aapt dump badging apps/android/app/build/outputs/apk/debug/app-debug.apk` → package `kr.co.hanium.dreamup.walksafe`, minSdk 26, targetSdk 36, CAMERA permission, launcher `MainActivity`, AR/depth feature optional 확인
- `unzip -v apps/android/app/build/outputs/apk/debug/app-debug.apk | grep -E 'custom_tactile|coco_yolo|two_model_runtime'` → 두 TFLite asset `Stored` 0% 및 config 포함 확인
- APK SHA-256: `f5129c1e3c2aa685f371b72c3a9cb69770a02fc4d129245e9593ba7fdcbc2f67`
- APK 경로/크기: `apps/android/app/build/outputs/apk/debug/app-debug.apk`, 약 66M
- `adb devices -l` → 연결된 실기기 없음. 설치 실행은 미수행.

### 남은 리스크
- 실기기 연결 후 `adb install -r apps/android/app/build/outputs/apk/debug/app-debug.apk`와 실제 ARCore camera/depth/TFLite 좌표 정합 확인이 필요하다.
- 현재 APK는 debug 서명이다. release 서명 APK가 필요하면 keystore/비밀번호를 환경변수나 CI secret로 받아 별도 signingConfig를 추가해야 하며, 비밀값은 repo에 넣지 않는다.
- TFLite asset은 root `.gitignore`의 `*.tflite` 정책 때문에 기본 Git 추적 대상이 아니므로 공유/재현 시 export script 또는 별도 artifact 전달이 필요하다.

## Android ARCore 실기기 프레임 저하 완화

### 한 일
- 실기기에서 프레임이 너무 느리다는 피드백을 반영해 Android native APK의 render/inference 병목을 완화했다.
- `MainActivity`에서 TFLite 두 모델 추론을 GL render thread 직접 실행에서 단일 백그라운드 스레드(`walksafe-tflite-detector`) 실행으로 분리했다.
- preview는 매 frame 먼저 렌더링하고, detector 결과는 최신 완료 결과를 재사용하도록 바꿨다.
- detector 동시 실행을 막기 위해 `AtomicBoolean` in-flight guard를 추가했다.
- detector 주기를 1초로 늦춰 CPU saturation과 preview frame drop 위험을 줄였다.
- session stop/restart 시 이전 detector 결과가 섞이지 않도록 generation guard를 추가했다.
- Android static scaffold check가 백그라운드 detector/throttle 구조를 확인하도록 갱신했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `scripts/check_android_depth_scaffold_20260531.py`
- `daylog/2026-05-31.md`

### 검증
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS
- `cd apps/android && ./gradlew test assembleDebug --no-daemon` → BUILD SUCCESSFUL
- `apksigner verify --print-certs /home/ddobagi/Code/hanium-dreamup/apps/android/app/build/outputs/apk/debug/app-debug.apk` → debug signer 검증 PASS
- APK SHA-256: `7973b907c9939b80aa0c50dcf479c034d772b54b2b2cce1c749499f9af1b2d47`
- APK 경로/크기: `/home/ddobagi/Code/hanium-dreamup/apps/android/app/build/outputs/apk/debug/app-debug.apk`, 약 66M
- `git diff --check` on touched Android/script/daylog files → PASS

### 남은 리스크
- 이 패치는 preview 부드러움을 우선해 detector 갱신 주기를 1초로 제한한다. 박스 반응이 너무 느리면 700~800ms로 재조정하거나 모델 경량화/NNAPI delegate를 검토해야 한다.
- CPU TFLite 두 모델 자체는 여전히 무겁다. 실기기에서 발열이나 UI 지연이 남으면 custom/COCO 교차 실행, input size 축소 모델, delegate 적용이 필요하다.

## Android native 정적 데이터셋/계약 검증 진행

### 한 일
- 사용자가 실기기에서 프레임 드랍 해소와 person track/`1보` 표시를 확인한 뒤, 기능 추가 전 검증 우선순위에 맞춰 Android native 쪽 debug 관찰성을 보강했다.
- `MainActivity` UI detail에 detector 결과 수, detector age, top class/score, bbox center/size, depth sample count/ratio/median을 표시하도록 추가했다. 박스 overlay 없이도 탐지/좌표/depth sampling 상태를 볼 수 있게 하는 목적이다.
- Android TFLite asset/config/Kotlin class map/threshold 계약을 정적으로 확인하는 스크립트를 추가했다.
- 현재 로컬 정적 이미지 dataset readiness를 확인하는 스크립트를 추가했다. Reviewed tactile3 dataset은 로컬에 없어 full static metric rerun은 blocked로 표시하고, 남아있는 prediction/report/model artifact를 분리했다.
- 기존 Stage1 per-image presence CSV와 prediction labels만으로 FP/FN 후보 CSV를 재가공했다.
- 기존 v2 external-validation `predictions.json`과 holdout manifest만으로 image-level presence threshold sweep을 재계산했다.
- 실행 결과와 제한사항을 execution 문서로 정리했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `scripts/check_android_tflite_contract_20260531.py`
- `scripts/check_static_dataset_readiness_20260531.py`
- `scripts/summarize_presence_error_candidates_20260531.py`
- `scripts/evaluate_predictions_manifest_presence_20260531.py`
- `docs/execution/2026-05-31_android_static_dataset_contract_progress.md`
- `runs/reports/stage1_yolo26s_reviewed_test_presence_error_candidates_20260531/`
- `runs/reports/predictions_manifest_presence_20260531/`
- `daylog/2026-05-31.md`

### 검증
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, Android 내부 계약 일치. 단 backend stage1 threshold와 Android threshold 차이는 warning으로 확인.
- `python scripts/check_static_dataset_readiness_20260531.py` → completed, reviewed tactile3 dataset 부재로 full static metric rerun blocked 확인.
- `python scripts/summarize_presence_error_candidates_20260531.py` → completed, Stage1 threshold별 FP/FN 후보 CSV 생성.
- `python scripts/evaluate_predictions_manifest_presence_20260531.py` → completed, v2 external predictions+manifest image-level sweep 생성.
- `python -m py_compile scripts/check_android_tflite_contract_20260531.py scripts/check_static_dataset_readiness_20260531.py scripts/summarize_presence_error_candidates_20260531.py scripts/evaluate_predictions_manifest_presence_20260531.py` → PASS.
- `python data_sources/scripts/validate_yolo_dataset.py datasets/walksafe_kr_v1/data.yaml` → PASS, 단 이미지/라벨 0개.
- `python data_sources/scripts/validate_yolo_dataset.py datasets/walksafe_v1/data.yaml` → PASS, 단 이미지/라벨 0개.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `cd apps/android && ./gradlew test --no-daemon` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew assembleDebug --no-daemon` → BUILD SUCCESSFUL.

### 산출물
- 새 debug APK: `apps/android/app/build/outputs/apk/debug/app-debug.apk`
- APK sha256: `dbc75e61eb44c68b7162b5658938e55935aa2f2c52361945b0b0c6747e606897`

### 남은 리스크
- Reviewed tactile3 원본 image/GT dataset이 현재 로컬에 없어 Stage1 full metric rerun은 아직 불가하다.
- Android `model-config/two_model_runtime.json`은 현재 런타임 source of truth가 아니라 Kotlin 상수와 일치하는 mirror 상태다.
- `1보`/ARCore metric depth 정확도와 bbox-depth identity mapper 가정은 RGB-D + 실측 거리 dataset 또는 실기기 캡처로 별도 검증해야 한다.

## Android JSON runtime source-of-truth 및 bbox overlay 보강

### 한 일
- Android TFLite detector가 `model-config/two_model_runtime.json`을 실제 런타임 설정으로 읽도록 변경했다.
- detector asset path, input size, custom class order, COCO class order, allowlist, threshold를 JSON에서 가져오도록 연결했다.
- COCO 80-class order를 Android JSON config에 추가해 class id mapping도 설정 파일에 포함했다.
- `TwoModelRuntimeConfig` parser와 JVM test를 추가했다.
- TFLite export helper가 JSON의 `input_size`와 `asset` 파일명을 사용하도록 수정했다.
- camera preview 위에 developer bbox overlay를 추가했다. detection bbox와 best depth output bbox를 화면에 그리고 class/confidence/source/distance/sample 정보를 표시한다.
- Android static scaffold/contract check를 JSON runtime source-of-truth 기준으로 갱신했다.

### 변경 파일
- `apps/android/app/build.gradle.kts`
- `apps/android/app/src/main/assets/model-config/two_model_runtime.json`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/DebugBboxOverlayView.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TfliteAndroidFrameDetector.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelRuntimeConfig.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelRuntimeConfigTest.kt`
- `scripts/check_android_depth_scaffold_20260531.py`
- `scripts/check_android_tflite_contract_20260531.py`
- `scripts/export_android_tflite_models_20260531.py`
- `docs/execution/2026-05-31_android_static_dataset_contract_progress.md`
- `daylog/2026-05-31.md`

### 검증
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/export_android_tflite_models_20260531.py --dry-run` → PASS, JSON-derived imgsz/output asset name 사용 확인.
- `python -m py_compile scripts/check_android_tflite_contract_20260531.py scripts/export_android_tflite_models_20260531.py scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `cd apps/android && ./gradlew test --no-daemon` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew assembleDebug --no-daemon` → BUILD SUCCESSFUL.
- touched file trailing whitespace check → PASS.

### 산출물
- 새 debug APK: `apps/android/app/build/outputs/apk/debug/app-debug.apk`
- APK sha256: `2b49f488b332e52943e3e949b615aa00366742aba656ea561194cdd022aeb283`

### 남은 리스크
- overlay는 단순 normalized bbox-to-view mapping이다. 실기기에서 박스가 밀리거나 회전되어 보이면 ARCore image/display/depth coordinate transform을 별도로 구현해야 한다.
- Android threshold는 아직 backend `/detect/v2` threshold와 다르며, 이 차이는 의도인지 별도 결정이 필요하다.

## Android native 다음 스텝 계획서 작성

### 한 일
- Android native ARCore APK 기준 다음 작업 계획서를 작성했다.
- overlay 기반 좌표 정합, ARCore coordinate mapping, metadata-only capture log, threshold/config 정리, RGB-D 검증 dataset 설계, 제품 기능 연결 전 gate를 우선순위로 분리했다.
- Codex가 바로 할 수 있는 작업과 사용자/실기기 없이는 완료할 수 없는 작업을 분리했다.

### 변경 파일
- `plans/features/2026-05-31_android_native_next_step_plan.md`
- `daylog/2026-05-31.md`

### 검증
- `python` sanity check로 계획서 제목과 `[단계]`/`검증:` 형식 포함 확인 → PASS.

### 남은 리스크
- overlay 좌표 정합, 실제 `N보` 정확도, 목걸이 착용 field 확인은 실기기 관찰 전까지 PASS로 표기할 수 없다.

## 문서 최신화: Android native 주경로 반영

### 한 일
- 기존 canonical 문서와 제품 문서를 훑고, 2026-05-31 기준으로 주 사용자 앱이 Web/PWA가 아니라 Android native ARCore/TFLite APK임을 반영했다.
- Android APK 경로와 SHA-256, JSON runtime source of truth, developer bbox overlay, bbox/depth 좌표 정합 gate를 README/current status/Android 문서에 정리했다.
- PWA, STT/TTS, TMAP/navigation은 demo·prototype·후순위 lane으로 재분류하고, backend/admin/report/export는 Android gate 이후 재사용할 운영 레이어로 정리했다.
- product vision/roadmap/backlog/done criteria/decisions를 Android native validation 중심으로 갱신했다.
- evidence 문서에 Android Device PASS 템플릿, privacy checklist, final report index, feature matrix addendum을 추가했다.
- `docs/walksafe-v2/*` 정책 문서에는 2026-05-31 우선순위 보정 note를 추가해 backend/Web/PWA/voice 문서가 Android Device evidence를 대체하지 않도록 했다.

### 변경 파일
- `README.md`
- `docs/README.md`
- `docs/current_status.md`
- `apps/android/README.md`
- `docs/android/arcore_depth_estimation_architecture.md`
- `docs/android/kotlin/ArCoreDepthArchitecture.kt`
- `docs/report_operations.md`
- `docs/pwa_backend_status.md`
- `docs/voice_stt_tts_status.md`
- `docs/walksafe-v2/README.md`
- `docs/walksafe-v2/two_model_runtime_plan.md`
- `docs/walksafe-v2/backend_api_contract.md`
- `docs/walksafe-v2/auto_report_policy.md`
- `docs/walksafe-v2/frontend_display_policy.md`
- `docs/walksafe-v2/navigation_integration_policy.md`
- `docs/walksafe-v2/voice_command_strategy.md`
- `docs/walksafe-v2/backend_model_integration_notes.md`
- `docs/walksafe-v2/reviewed_model_rollout_checklist.md`
- `docs/walksafe-v2/coco_inference_policy.md`
- `docs/walksafe-v2/public_agency_submission_policy.md`
- `docs/walksafe-v2/concept_decision_questions_20260523.md`
- `docs/evidence/feature_matrix.md`
- `docs/evidence/final_report_index.md`
- `docs/evidence/privacy_checklist.md`
- `docs/evidence/templates.md`
- `docs/execution/2026-05-31_android_static_dataset_contract_progress.md`
- `product/vision.md`
- `product/roadmap.md`
- `product/backlog.md`
- `product/done-criteria.md`
- `product/decisions.md`
- `daylog/2026-05-31.md`

### 검증
- stale 표현 grep: PWA 주경로/old 기준일/APK 미빌드/fake-only `/detect/v2` 등 핵심 stale 패턴 없음 → PASS.
- key path check: APK, Android JSON config, TFLite assets, Android scripts, Android plan/execution 문서 존재 확인 → PASS.
- touched docs trailing whitespace sanity check → PASS.
- `git diff --check -- <touched docs>` → PASS.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning으로 유지.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.

### 남은 리스크
- 날짜별 `docs/execution/` 전체를 모두 rewrite하지는 않았다. 과거 실행 기록은 history로 두고, 현재 source-of-truth 문서와 2026-05-31 execution 문서에 최신 해석을 반영했다.
- Android overlay 좌표 정합, ARCore depth 실측, `N보` 정확도는 실기기/RGB-D evidence 전까지 PASS가 아니다.

## Android native 계획 실행: worktree 정리, stale guard, metadata-only log

### 한 일
- `/home/ddobagi/Code`의 push용 worktree 2개를 clean 상태로 확인한 뒤 제거했다.
  - `hanium-dreamup-push-20260525`
  - `hanium-dreamup-pushprep`
- 팀 lane으로 Android coordinate mapping 계획 문서와 Android/backend threshold decision note를 작성했다.
- Android runtime에 `MetadataCaptureLog`를 추가했다. RGB/YUV 이미지나 depth 파일을 저장하지 않고 frame timestamp, detector age, bbox, best depth median/sample count만 메모리 ring buffer에 남긴다.
- session stop 시 metadata capture log를 clear하도록 연결했다.
- detector 결과가 `MAX_DETECTION_AGE_MS=2500`을 넘으면 depth pipeline 입력에서 제외하고 UI/debug detail에 stale로 표시하도록 guard를 추가했다.
- Android scaffold static check가 metadata capture log와 stale guard를 확인하도록 갱신했다.
- 계획서/현재 상태/Android README/evidence 문서에 이번 진행 상태와 최신 APK SHA를 반영했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MetadataCaptureLog.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/MetadataCaptureLogTest.kt`
- `scripts/check_android_depth_scaffold_20260531.py`
- `docs/android/arcore_coordinate_mapping_plan.md`
- `docs/android/android_backend_threshold_decision_20260531.md`
- `apps/android/README.md`
- `docs/current_status.md`
- `docs/README.md`
- `docs/android/arcore_depth_estimation_architecture.md`
- `docs/evidence/final_report_index.md`
- `docs/evidence/feature_matrix.md`
- `plans/features/2026-05-31_android_native_next_step_plan.md`
- `docs/execution/2026-05-31_android_static_dataset_contract_progress.md`
- `product/roadmap.md`
- `daylog/2026-05-31.md`

### 검증
- `git worktree remove /home/ddobagi/Code/hanium-dreamup-push-20260525` → removed
- `git worktree remove /home/ddobagi/Code/hanium-dreamup-pushprep` → removed
- `/home/ddobagi/Code` 내 `hanium-dreamup*` 폴더 확인 → `hanium-dreamup`만 남음
- `python -m py_compile scripts/check_android_depth_scaffold_20260531.py scripts/check_android_tflite_contract_20260531.py scripts/export_android_tflite_models_20260531.py` → PASS
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning 유지
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS
- `python scripts/export_android_tflite_models_20260531.py --dry-run` → PASS
- `cd apps/android && ./gradlew test assembleDebug --no-daemon` → BUILD SUCCESSFUL
- touched file sanity/trailing whitespace check → PASS
- `git diff --check -- <touched files>` → PASS

### 산출물
- 최신 debug APK: `apps/android/app/build/outputs/apk/debug/app-debug.apk`
- APK sha256: `55ecfda2205a91d317c879e353119798299ace25bff7f6c7501381761e013b56`

### 남은 리스크
- metadata-only capture log는 실기기 좌표 정합을 쉽게 보기 위한 보조 장치다. overlay/preview/depth coordinate Device PASS를 대체하지 않는다.
- 실제 ARCore RGB-D 거리, `N보` 정확도, 목걸이 착용 field는 아직 미검증이다.
