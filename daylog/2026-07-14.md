# 2026-07-14

## Reproducible release trust chain

- release input·receipt·artifact closure와 검증 후 publication 경계를 재감사해 `a89d138`에 고정했다.
- source/toolchain/receipt substitution, stale closure, ignored evidence, inference worker readiness 경계를 fail-closed로 보강했다.

## Exact submission Python boundary

- canonical 제출 builder·validator·visual audit·promotion 7개만 허용하는 `-I -S -B` runner를 추가했다.
- private Git 환경에서 exact HEAD/index/tracked blob과 generated-root 외 untracked/ignored path를 검증하고, repo-local filter를 실행할 수 있는 `git status`를 권위 경로에서 제거했다.
- exact 8-distribution site closure, RECORD hash/size·outside-site claim·중복 소유·bytecode와 per-distribution bundle을 import 전후에 검증한다.
- runner/repo-root 동일 checkout, process-local source marker, promotion 자식 runner 재진입을 강제했다.
- cleanroom runbook과 관련 README, manifest trust-source input, test-layer 분류를 동기화했다.
- 변경을 `9cfd547`에 고정했다.

## Changed Files

- `scripts/run_walksafe_submission_python_20260714.py`
- canonical 제출 entrypoint 7개와 `scripts/submission_manifest_policy.py`
- `configs/submission_toolchain_lock_20260713.json`
- `tests/test_submission_isolated_python.py`, manifest/promotion 회귀 테스트
- submission cleanroom/runbook·README·issue ledger

## Verification

- submission/release 집중 테스트 277개 통과, test-layer configuration 통과.
- exact-8 환경 probe: 8 distributions, 1,659 locked files, site closure 1,892 entries 통과.
- `--no-local` fresh clone에서 오염된 `GIT_*`/`PYTHONPATH`를 주입해 canonical marker 성공 경로를 통과했다.
- fresh clone에서 cross-checkout과 generated-path command가 각각 fail-closed임을 확인했다.
- 독립 보안·격리·계약 감사 3개 모두 P0/P1/P2 0.
- 저장소와 test 환경에 `.pyc`, `.pyo`, `__pycache__`, `.pytest_cache`를 남기지 않았다.

## Remaining

- 제품 범위 선택 후 selected Web/Android 동작을 구현하고 전체 cleanroom suite·최종 RC·제출 산출물을 다시 생성해야 한다.
- 사람 privacy receipt, Android signing key/certificate와 production endpoint/credential/field evidence는 외부 입력으로 남아 있다.
