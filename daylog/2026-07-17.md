# 2026-07-17

## Web 실폰 기본 기능 확인

- 연결된 SM-G981N의 실제 Android Chrome에서 localhost production PWA를 실행했다. 실제 후면 카메라 스트림은 720×1280·30fps, 실제 GPS callback은 정확도 10m 이내, service worker는 controlled 상태였다.
- 정확한 위치 좌표는 증거에 남기지 않았다. 서버 frame 처리, field telemetry, 신고 저장에는 동의하지 않았으므로 이 시험에서 해당 데이터와 JSONL은 만들지 않았다.
- 개인정보 제한 결과는 `artifacts/web-phone-smoke/20260716-235722_R3CN50F4APH/evidence.json`에 기록했다. localhost·dirty build smoke이므로 HTTPS PWA 설치/업데이트, live TMAP, TTS·진동·TalkBack·보행 안전 근거가 아니다.
- 시험 중 바꾼 Chrome 사이트 위치 권한은 차단으로, 사이트 카메라 권한은 제거 상태로 되돌렸다. 임시로 허용한 Chrome 앱 카메라 권한도 `granted=false`로 복구했고 adb forward/reverse와 local production server를 종료했다.

## Android ARCore 미지원 경계 보완

- 동기 ARCore availability가 `UNKNOWN_*`이면 `checkAvailabilityAsync`로 한 번 재확인하고 request/lifecycle generation, foreground, finishing/destroyed 상태로 늦은 callback을 차단했다. 확정 `UNSUPPORTED_DEVICE_NOT_CAPABLE`만 CameraX 제한 모드로 보내며 Session 생성의 `UnavailableDeviceNotCompatibleException`도 별도 확정 원인으로 기록한다.
- debug/release source-set twin으로 지원폰 기능시험용 CameraX 강제 진입을 추가했다. debug extra는 실제 READY 상태에서 한 번만 소비하고 release 구현은 extra 문자열이나 API를 읽지 않으며 항상 false를 반환한다.
- `camera_non_metric_session_started`에 `reason`과 관찰한 Availability `state`를 기록한다. 실제 `frameDetector.detect()` 성공 뒤 generation당 한 번 `camera_non_metric_frame_analyzed`를 기록하며 loaded model, fallback 여부, `metric=false`, `reports_allowed=false`, `state=detector_succeeded`를 보존한다.
- Android field strict는 실제 analyzed frame을 필수로 요구한다. 새 `--require-arcore-unsupported`는 availability unsupported + `UNSUPPORTED_DEVICE_NOT_CAPABLE` 또는 supported-state의 Session incompatibility만 인정하고 debug 강제, Depth만 미지원, UNKNOWN, 원인 누락과 혼합 세션은 거부한다. 요약은 `evidence_scope`와 `arcore_unsupported_verified`를 항상 표시한다.

## 실기기 기능 증거와 검증

- 현재 dirty worktree 전체를 별도 임시 검증 저장소에 반영한 commit `2a0f57095e94f60689ec29e416d50dcb84a1b289`로 debug APK를 만들었다. 검증 APK SHA-256은 `65d4987e1a0d5b7e296ccd0b9ee2553f55980fb5ea7120c86a4c4a7faacbf64c`이고 세 model asset 검사가 통과했다.
- SM-G981N에서 debug 강제 제한 모드가 실제 후면 카메라를 열고 unified model로 실제 CameraX frame 분석을 완료했다. strict 결과는 `FORCED_SUPPORTED_FUNCTIONAL`, `arcore_unsupported_verified=false`, 실패 없음이다. 같은 세션에 `--require-arcore-unsupported`를 적용하면 `arcore_unsupported_not_verified`로 exit 1이 나와 기능시험과 실제 미지원 근거가 분리됨을 확인했다.
- 증거는 `artifacts/android-field-sessions/20260717-002523_R3CN50F4APH/`에 있다. `validation_source.json`과 재적용 가능한 source patch도 함께 두었고 patch replay의 content manifest SHA-256 일치를 확인했다.
- 시험 전 폰에 설치돼 있던 APK를 먼저 백업했다. 시험 후 byte-identical 원본 SHA-256 `cb581990e1501aedb8fa43896e0bce591c0b0ddb5d903c65c23e0312c470c1a2`로 복구했고 카메라·정확한 위치·신체 활동 권한이 기존 granted 상태인 것과 active camera client가 없는 것을 확인했다.

## 자동 검증

- Android 관련 Python/scaffold/Full-RC 회귀: `148 passed`.
- Android debug JVM 전체 강제 재실행: `313/313`, failure/error/skip 0.
- Android release Kotlin compile: PASS. release source에는 debug force extra가 없다.
- Android field 요약 집중 회귀: `42 passed`; 두 Python script 문법 검사와 `git diff --check`: PASS.
- 기존 deprecated `announceForAccessibility` 경고만 남았다.

## Web PWA 실제 릴리스 전환 보완

- 기존 browser lifecycle의 `?e2e_update=<timestamp>` 재등록은 동일한 worker 본문도 업데이트처럼 보이게 할 수 있어 실제 A→B 증거가 아님을 확인했다. 기존 결과 필드는 `synthetic_same_build_query_trigger_ui_wiring_only`로 명확히 강등했다.
- `/sw-version.js` 동적 route가 runtime `WALKSAFE_SOURCE_COMMIT`과 실제 `BUILD_ID` 일치를 검증한 뒤에만 JavaScript release identity를 `no-store`로 반환하도록 했다. `/sw.js`는 이 identity를 import하고 source commit을 worker version/cache name으로 사용한다. 불일치·누락·안전하지 않은 custom dist directory에서는 503/worker install 실패로 닫힌다.
- 앱 등록은 `updateViaCache: "none"`으로 imported script까지 재검사하며, 사용자 승인 `skipWaiting()` promise는 message event의 `waitUntil`에 결합했다.
- `check_pwa_release_update_20260717.py`를 추가했다. 서로 다른 두 production BUILD_ID를 같은 loopback origin과 단일 Chromium profile에서 순차 실행해 query 없는 `/sw.js`, A controller 유지, B waiting worker, 사용자 버튼 적용 전 preemption 없음, 적용 후 B controller/cache, A cache 제거, B cached root와 동일한 offline fetch 및 hydrated reload, 안전 API offline unavailable을 검증한다.
- 실제 두-build local headless 결과는 `artifacts/pwa-release-update/20260717-local/evidence.json`에 기록했다. 이는 HTTPS 도메인, 실폰 설치·standalone·업데이트 UI 증거가 아니다.

## PWA 검증

- Web policy suite `npm test`: PASS, TypeScript `npm run typecheck`: PASS.
- PWA release checker와 Full-RC tooling 회귀: `109 passed`; checker 집중 회귀: `5 passed`.
- 기존 single-build lifecycle도 새 source-bound worker로 PASS했으며, 해당 query trigger는 실제 release 전환 증거가 아니라는 claim limit가 JSON에 남는다.
- 두-build browser 전환: baseline active/controller → candidate waiting → 사용자 apply → candidate active/controller/cache → offline hydrated reload 모두 PASS.

## Web PWA 실제 HTTPS 실폰 검증

- SM-G981N(Android 13, Chrome 150)의 실제 Chrome에서 임시 HTTPS 동일 origin을 유지한 채 baseline `441bac76481f3282727f344fb0d71ebaa3e646bb`에서 candidate `1c978c6606387c02bc3bd0fa6b67e150f0545e03`로 전환했다. baseline controller를 유지한 candidate waiting 상태와 실제 UI 버튼 적용 후 candidate active/controller, 기존 cache 제거를 확인했다.
- 브라우저 범위 offline에서 candidate cached root와 fetch root의 SHA-256이 `76fd3b095fac76ba65dbd87a2c5c458b4ff30138f3c5822bc95bbe60b84612ee`로 일치했고 안전 API 요청은 실패했다. 확인 직후 online 상태를 복원했다.
- Chrome native 설치 창까지는 정상적으로 표시됐지만 Google Play가 `User needs to (re)enter credentials`로 WebAPK 생성을 거부했다. fallback 홈 화면 추가 후에도 새 WebAPK package와 pinned Chrome shortcut이 없고 `standalone=false`여서 설치·독립 실행은 PASS로 기록하지 않았다.
- 구조화 증거와 화면·UI XML·sanitized logcat은 `artifacts/pwa-android-https/20260717-R3CN50F4APH/`에 남겼다. 실험용 server/tunnel, ADB forward, 임시 폰 파일, 두 build directory와 비밀값이 있던 runtime directory를 종료·삭제했다.

## 변경 파일

- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/ArCoreAvailabilityPolicy.kt`
- `apps/android/app/src/debug|release/java/kr/co/hanium/dreamup/walksafe/CameraFallbackTestOverride.kt`
- Android field log allowlist·README와 관련 JVM tests
- `scripts/pull_android_field_sessions_20260710.py`, `scripts/summarize_android_field_sessions_20260710.py`, 관련 Python tests·체크리스트

## 남은 항목

- 실제 ARCore 미지원 실기기의 CameraX/IMU 현장 세션은 아직 필요하다.
- live TMAP 경로와 실제 장애물 advisory, 사람이 듣고 느끼는 TTS·진동·TalkBack, 실외 보행·모델 일반화는 미검증이다.
- 실제 HTTPS 실폰 Chrome의 A→B 업데이트와 offline shell은 확인했다. PWA 설치·standalone 실행은 Google Play 계정 재인증 뒤 다시 검증해야 하며, 안정된 production HTTPS origin 검증도 남아 있다.
- 새 local commit, push, PR, 배포, 서명은 수행하지 않았다.

## 남은 정책 결함 감사와 보완

- 전체 목표를 다시 감사해 완료 상태가 아님을 확인했다. 특히 Web·Android의 LOW 비계량 카메라 advisory가 STOP 전용 진동 정책을 어기고 있었고, Android 비계량 경로에는 선택 3.A의 bounded sequential 전달·실패 rollback이 빠져 있었다.
- Web LOW advisory에서 vibration 권한과 queue 전달을 제거했다. production Chromium lifecycle은 advisory 후보를 진동 baseline 뒤에만 만들고 실제 `navigator.vibrate` 호출 delta가 0인지 검사한다.
- PWA는 prompt 수락과 `appinstalled`를 `install_requested`로만 표시한다. 실제 `display-mode: standalone` 실행이 확인될 때만 설치 실행 완료로 표시해 WebAPK 생성 실패를 설치 성공으로 오판하지 않는다.
- Android CameraX 비계량 경로는 TTS/TalkBack 전용으로 제한하고 capacity 3 FIFO, reserve/claim/confirm/reject를 적용했다. 실제 음성 전달 확인 뒤에만 global/key cooldown을 확정하며, 전달 실패와 gate·후보·TTL 소실은 cooldown 없이 폐기한다. actuator 호출 전 claim과 실제 TTS 시작 validator로 stale 전달 경쟁도 차단했다.
- 새 `tests/test_pwa_release_update_checker.py`를 canonical Unit 계층에 등록해 새 테스트가 계층 밖에서 누락되지 않게 했다.

## 통합 회귀

- Canonical Unit: Python `704 passed`, Web policy suite PASS, Android debug JVM PASS.
- 격리 tmpfs PostGIS `walksafe_goal_test`의 Canonical Functional: Python/PostGIS `366 passed`, Web lint/typecheck, Android debug lint PASS.
- Canonical Integration: Python `165 passed`, Next 16.2.6 production build, runtime trace `60`/`1,317`, Chromium PWA lifecycle, Android debug·AndroidTest·release build와 세 model APK asset 검사 PASS.
- Chromium 비계량 advisory는 서로 다른 frame 3개/902ms, UI·field log 계약, report request 0, `navigator_vibrate 3 -> 3`(`delta=0`)으로 통과했다. 이는 합성 browser-local 근거이며 실폰·live TMAP·사람의 TTS/TalkBack 관찰을 대체하지 않는다.
- 첫 Unit/Integration 시도의 실패는 각각 필수 `WEB_QUALITY_PYTHON`, `WALKSAFE_SOURCE_COMMIT` 미지정과 즉시 재실행 중 3100 포트 TIME_WAIT였다. 요구된 고정 환경을 명시하고 포트 해제 뒤 전체 계층을 다시 실행해 PASS를 확인했으며 제품 코드 실패로 승격하지 않았다.
- `git diff --check`는 PASS다. 이 tree는 exact clean candidate 검증을 통과한 경우에만 승인된 local source-freeze commit으로 승격하고 push·PR·배포·서명은 하지 않는다.

## 계속 OPEN

- 실제 ARCore 미지원 Android의 CameraX/IMU/TMAP/TTS/TalkBack 현장 세션, production HTTPS PWA 설치·standalone, 실외 live TMAP/마이크/보행·모델 일반화는 아직 필요하다.
- 운영 RBAC·secret·공유 storage, 실제 backup/restore, 서명 APK, 독립 reviewer·사람 개인정보 검수·기관 receipt도 아직 없다.

## 제출 재현 gate 보완

- 첫 clean two-clone 재현의 form 검증이 추적표의 exact `TMAP-only` marker 누락을 잡아 `328/329`에서 중단됐다. 코드·산출물 재현 실패가 아니라 제출 범위 표시 누락이었다.
- REQ-012 추적 행의 전역 경로 권한을 TMAP-only(`TMAP_ONLY`)로 명시해 사람이 읽는 표현과 machine tier 값을 둘 다 보존했다. 이 수정을 포함한 새 clean candidate에서 제출 재현·품질 gate를 다시 확인한다.

## Android clean-home 의존성 검증 보완

- exact candidate의 Android 품질 gate를 빈 Gradle home에서 실행하자 `guava-parent:33.3.1-jre` POM과 `junit-bom:5.10.2`, `5.11.0-M2` module metadata의 SHA-256 승인값이 없어 unit-test 앞에서 fail-closed했다. 기존 캐시를 쓰는 개발 검증은 이 누락을 드러내지 못했다.
- Gradle 9.3.1의 `--write-verification-metadata sha256`가 생성한 세 값을 Maven Central의 `repo.maven.apache.org`와 `repo1.maven.org` 두 HTTPS endpoint에서 받은 byte와 각각 대조해 일치를 확인한 뒤 `verification-metadata.xml`에 추가했다. 새 clean Gradle home에서 Android 품질 gate를 다시 실행한다.
- 다음 fail-closed에서 드러난 `kotlinx-coroutines-bom:1.8.0` POM과, Android 품질의 unit·lint·release 전체 명령을 메타데이터 생성 모드로 완주해 추가로 확인한 `junit-bom:5.9.2` module도 같은 두 Maven Central endpoint byte 대조를 통과해 승인값에 추가했다. 전체 probe는 79 tasks를 빈 Gradle home에서 완주했다.

## Web quality runtime source identity 보완

- Web release artifact lifecycle은 통과했지만 별도 fresh quality build의 browser lifecycle은 `service_worker_registration_missing`을 반복 재현했다. client static inventory는 byte-identical이었고, quality runtime의 `/sw-version.js`가 `WALKSAFE_SOURCE_COMMIT` 미지정 때문에 503과 fail-closed JavaScript를 반환해 `sw.js` 평가·등록이 중단된 것을 확인했다.
- 품질 정책의 `browser-lifecycle` fixed environment에 candidate `{source_commit}`을 결속했다. 영수증 runner 회귀는 build 단계뿐 아니라 browser lifecycle 명령에도 exact source commit이 주입되는지 검사한다.
- 변경된 품질 정책 SHA-256을 isolated Python wrapper, 품질 runner, Full-RC builder와 독립 validator의 네 고정 pin에 동일하게 반영해 정책·실행·패키지 경계가 어괋나지 않게 했다.

## exact 후보 제품 품질과 Full-RC 멀티DEX 보완

- exact 후보 `2178f30f21dcabdbf1e77d4a726d4f4b9e1be98c`에서 제출 two-clone 329/329와 생성 파일 37개 byte 동일성, canonical Unit/Functional/Integration을 통과했다. clean-room 제품 품질은 Web 8/8, Android 4/4, Backend 5/5(403 tests), Voice 2/2(155 tests)였다.
- 이 후보의 최종 Full-RC builder가 APK provenance를 canonical `classes.dex`에서만 찾다가 fail-closed했다. 실제 APK를 `apkanalyzer`/`dexdump`로 확인하니 유일한 WalkSafe `BuildConfig`와 정확한 commit/release marker는 정상 `classes2.dex`에 있었다. AGP 9.1·minSdk 26의 native multidex는 `multiDexKeepProguard`를 주 DEX 배치에 사용하지 않아 해당 실험 설정은 최종 source에 남기지 않았다.
- `scripts/walksafe_android_dex_binding.py`를 추가했다. 연속 canonical DEX 이름, v038 header/file/data bounds, SHA-1/Adler-32, string data section·MUTF-8/UTF-16 길이, class definition/static fields를 파싱해 유일한 WalkSafe `BuildConfig`의 `DEBUG=false`, `BUILD_TYPE=release`, exact source commit과 `walksafe-release-v1`을 확인한다. builder와 독립 validator는 helper SHA-256 `c1a6adec0837d950f3ba8138e5e72acf9e7eec62b7834a3df37842d8c9e55496`을 각각 고정하고 release provenance input에 포함한다.
- secondary DEX 정상 허용, stale BuildConfig+asset/current string, 같은 DEX 끝 문자열 추가, 중복 BuildConfig, 미검토 DEX 036/041, 잘못된 `utf16_size`, 과대 DEX index, helper source/bytecode 변조 거부 회귀를 추가했다. 실제 2-DEX release APK의 builder payload 검사가 통과했고 Full-RC/isolated bootstrap 관련 회귀는 `158 passed`, `git diff --check`는 PASS다.
- 변경 파일: `scripts/walksafe_android_dex_binding.py`, Full-RC builder/validator, `tests/test_walksafe_full_rc_tooling.py`, `tests/test_walksafe_isolated_python_bootstrap.py`, Android·release·scripts README.
- 이 source 수정 뒤 exact candidate와 네 제품 영수증, Full-RC bundle/독립 validation은 새 commit에 맞춰 모두 다시 생성해야 한다. push·PR·배포·서명은 수행하지 않는다.

## Full-RC Web archive 정렬 계약 보완

- exact 후보 `4930d3232080f8b498322666abc4fbc783a789e7`의 Full-RC builder는 통과했지만 독립 validator가 Web deployment archive 순서를 거부해 승격을 중단했다.
- archive의 파일 집합·hash·BUILD_ID·gzip header·mtime·uid/gid·type은 모두 정상이었다. 실제 1,690개 정규화 member는 GNU tar `--sort=name`의 디렉터리별 정렬·깊이우선 순서와 정확히 일치했지만 validator가 전체 경로 문자열의 평면 정렬을 요구한 것이 원인이었다.
- `scripts/validate_walksafe_full_rc_20260713.py`가 `PurePosixPath.parts` 기준 component-wise canonical order를 확인하도록 수정했다. 이 순서도 member set마다 하나로 고정되며 기존 unsafe path·중복·충돌·type·mode·metadata·hash 검사는 유지된다.
- GNU tar형 중첩 순서를 허용하고 평면 문자열 순서로 재배열한 archive를 거부하는 회귀 2개를 추가했다. 집중 회귀 `2 passed`, Full-RC·isolated bootstrap 관련 전체 `160 passed`, 기존 4930 실제 Web archive 재검증과 `git diff --check`가 통과했다.
- 이 수정도 source commit에 결속되므로 새 exact candidate에서 전체 계층, 네 제품 품질 영수증, Full-RC 생성·독립 검증을 다시 완료한 뒤에만 local source-freeze commit으로 승격한다.

## 최종 source-freeze 실기기 debug 업로드 실패 보완

- local source-freeze commit `0511b34f0ff54eb9c6e7eeb7c4d503c41f968259`의 debug APK를 SM-G981N에 설치했다. 설치본과 로컬 APK SHA-256은 `c17c9add6bf5649a0376318f5102b49a615412ad0015aa1cb85f121053ac51c5`로 일치했고 cold launch와 unified/legacy TFLite instrumentation `2/2`가 통과했다.
- 합성 로그인 ID와 metadata-only 현장 로그로 ARCore 정지 smoke를 시작하자 ARCore session·camera·unified model은 시작됐지만, 닫힌 `127.0.0.1:8000` debug endpoint의 `ConnectException`이 `walksafe-debug-log-uploader`에서 처리되지 않아 앱 process가 종료됐다. crash 직전 strict provenance/ARCore 기록은 `artifacts/android-field-sessions/20260717-052332_R3CN50F4APH/`에 보존했다.
- 원인은 두 debug uploader가 `RuntimeException`만 잡아 `IOException` 계열 네트워크 실패를 누락한 것이었다. Android 공식 지침도 변동하는 네트워크 예외를 정상 운영 조건으로 처리하도록 안내한다. metadata uploader와 명시적 frame-capture uploader 모두 `IOException`을 best-effort drop으로 처리하도록 최소 수정했다.
- 동기 executor와 실제 local connection refusal로 두 경로를 재현하는 JVM 회귀 2개를 추가했다. 수정 전에는 각각 `ConnectException`으로 실패했고 수정 후 집중 회귀가 통과했다.
- Android debug JVM 전체 `321/321`, failure/error/skip 0, debug lint, debug APK와 AndroidTest APK assemble, 세 model asset 검사가 통과했다. `git diff --check`도 PASS다.
- 이 보완 뒤 `0511...` 산출물은 새 source의 최종 산출물이 아니다. 새 local commit에 exact source를 결속해 실기기 재현과 전체 source-freeze/제품 품질/Full-RC gate를 다시 통과해야 하며 push·PR·배포·release signing은 하지 않는다.

## 기존 목표 재감사와 남은 안전 계약 보완

- 기존 목표를 완료로 처리하지 않고 다시 감사했다. Android field APK와 release APK의 SHA·DEX provenance를 분리하고, field APK는 실제 debug `BuildConfig`와 package/version/minSdk/targetSdk/`debuggable=true`·단일 v2/v3 signer를 고정 SDK 도구로 확인하게 했다. release는 같은 공통 검증 경로에서 `debuggable=false`를 강제한다.
- CameraX 제한 모드의 실제 frame 분석·최대 1 Hz 두 stream·60개 이상·15분 이상·1~30초 간격 계약과 release의 Noop logger/field UI 비노출 경계를 문서·요약·정적/JVM/Python 검증에 맞췄다. Android 독립 재검토는 문자열-only DEX 위조 거부, 실제 SDK 검사, field/release/summary SHA 분리, sampling 표현, release 경계를 모두 PASS로 판정했다.
- 보고서 보존 apply는 실행 전 backup/restore·DB/upload/lock identity와 fail-closed manifest를 강화했다. scheduler runner는 signal launch gap을 회수하고, `renameat2(RENAME_NOREPLACE)` 단일 전진 발행과 역방향 rollback으로 다른 audit inode를 삭제하지 않게 했다.
- 그룹 리더가 먼저 종료돼도 같은 process group에서 TERM을 무시하는 하위 프로세스를 grace 종료 뒤 `KILLPG`하고 리더를 reap하도록 보완했다. 직접 신호·그룹 신호 회귀를 저장소에 추가했고, 독립 probe도 launch gap·중첩 signal·pending/final 교체·고집 하위 프로세스에 잔존 process와 audit 손실이 없음을 확인했다.

## 현재 작업 트리 회귀

- 보고서 보존 scheduler·운영·backend 집중 회귀 `125 passed`, Full-RC tooling `130 passed`, runner `bash -n`, `git diff --check`: PASS.
- Canonical Functional은 전용 PostGIS에서 Python `449 passed`, Web lint/typecheck, Android debug lint가 통과했다.
- Canonical Integration은 Python `257 passed`, runtime model/TFLite 계약, backend 768 smoke, Next 16.2.6 production build와 `60` traces/`1,317` unique files, Android debug·AndroidTest·release build와 세 model APK asset 검사가 통과했다.
- 앞선 Canonical Unit은 Python `812 passed`, Web policy, Android debug JVM이 통과했고 이후 변경 영향 범위인 retention/Full-RC 회귀를 별도로 재실행했다. 최종 승격 전 exact clean candidate에서 전체 Unit/Functional/Integration과 제품 품질·Full-RC를 다시 실행한다.

## 계속 OPEN인 외부 게이트

- 실제 ARCore 미지원 Android의 CameraX/IMU/TMAP/TTS/TalkBack 현장 세션, production HTTPS PWA 설치·standalone, 실외 live TMAP·마이크·보행·모델 일반화는 자동 로컬 PASS로 대체하지 않는다.
- 운영 RBAC·secret·공유 storage, 실제 encrypted backup/restore, 서명 APK, 독립 사람 개인정보 검수·기관 receipt도 미완료다. 최종 로컬 gate 뒤 source-freeze commit만 만들며 push·PR·배포·서명은 하지 않는다.

## 고정 field APK 현장 절차 정합화

- 독립 최종 diff 감사가 두 Android 체크리스트가 고정 artifact 계약을 설명하면서도 mutable Gradle `app-debug.apk`를 설치·재해시하는 불일치를 발견했다.
- 두 절차 모두 clean commit 빌드 직후 `artifacts/android-field-apks/<source_commit>/app-debug.apk`를 새 private directory에 mode `0400`으로 복사하고, 설치·출발 gate·CameraX gate·귀가 후 회수까지 기록한 동일 `FIELD_APK`만 사용하도록 수정했다. 후속 build가 설치 증거 파일을 덮어쓰지 못하는 정적 회귀도 추가했다.

## PWA A→B 루트 산출물 결속 수정

- 최종 실제 A→B 검증이 Next 16의 루트 HTML에 `/_next/static/<commit>/` URL이 있을 것이라는 검사기 가정을 거부했다. 실제 HTML은 `/_next/static/chunks/...`를 사용하며, 캐시·온라인 루트 본문은 서로 정확히 같았으므로 앱 전환이 아니라 검사 조건의 결함이었다.
- v4 manifest로 재검증한 `.next/server/app/index.html`의 SHA-256을 `ReleaseArtifact`에 보존하고, candidate cached·online·applied·offline 루트 응답 바이트가 모두 이 artifact SHA와 정확히 같을 때만 통과하도록 수정했다. 문자열 decode/re-encode가 아닌 `Response.arrayBuffer()` 바이트를 직접 해시한다.
- marker 없는 Next root fixture, artifact SHA drift, cached/online drift, evidence 직렬화와 최종 재검증 회귀를 보강했다. checker 집중 회귀 `41 passed`, `git diff --check`가 통과했다.
- 기존 exact A `09a9a4500087028438cb51d3defbf8337326a513`와 B `f9f19234af537223c629ae98f0089e40310199cf` 산출물로 실제 단일 Chromium profile 전환을 다시 실행했다. waiting→사용자 apply→B controller/cache→offline hydrated reload가 통과했고 artifact·cached·online 루트 SHA-256은 모두 `5548476ef66a8a5c2e3c58c23932255dabda39958e21e6b7f8ea2633f4dc6fdb`로 일치했다.
- 이 검사기 수정은 source commit에 결속되므로 새 exact candidate에서 전체 계층·제품 품질·Full-RC를 다시 생성하고 검증한다. HTTPS production 설치·실기기 현장 검증을 이 로컬 PASS로 대체하지 않는다.

## 서명 Release APK 실기기 smoke gate 보완

- 기존 `android_arcore_unsupported_camera_field`는 같은 commit의 debug field APK를 15분 검증하면서도 서명 release APK 자체의 실행 증거가 아님을 명시한다. 완료 감사에서 `full` profile이 정확한 서명 release APK의 ARCore 미지원 실기기 실행을 별도 구조화 계약으로 강제하지 않는 공백을 확인했다.
- `android_signed_release_physical_smoke`를 `full` 전용 필수 check로 추가했다. release artifact SHA-256과 설치 APK SHA-256의 exact 일치, 실제 `UNSUPPORTED_DEVICE_NOT_CAPABLE`, CameraX fallback·detector·활성 authoritative TMAP·low 비계량 방향 안내·TTS/TalkBack·무진동·gate loss 뒤 `TMAP_ONLY`를 요구하며 거리·걸음·경로 변경·신고 권한은 모두 false로 닫는다. `web-release`와 `android-research`에는 이 서명 release check를 추가하지 않는다.
- 운영 예시와 testing/script 문서를 같은 profile 경계로 맞추고 정상·변조 회귀를 추가했다. 보존 v2 patch와 정식 적용 diff가 byte 단위로 일치했으며 `git diff --check`, 예시 JSON parsing, profile probe와 release-evidence 전체 `180 passed`가 통과했다.
- 이 변경은 새 clean source commit이 있어야 제품 품질 runner를 실행할 수 있다. 로컬 commit 뒤 Web artifact, 네 제품 품질 receipt, unsigned APK, Full-RC·SBOM·독립 validation을 모두 새 commit에 결속해 재생성한다. operator 서명, 실제 ARCore 하드웨어 미지원 실폰, 외부 acceptance와 10개 production blocker는 여전히 별도 외부 gate다. push는 하지 않는다.
