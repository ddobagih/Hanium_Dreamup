# 2026-08-11 WalkSafe 작업 로그

## 최종 목표와 보존

- 현재 Android 제품·지원 코드·산출물·가이드·테스트 도구를 하나의 재현 가능한 `current` 기준으로 정리하고, 정확한 commit의 GitHub CI까지 확인하는 것을 최종 목표로 고정했다.
- 현대화 전 `current` commit `f0093863e82bfc80d9f11915cef33a51d44b8730`을 원격 archive branch·annotated tag·권한 제한 bundle·`--no-hardlinks` 검증 clone으로 보존했다.
- 기존 `main` `c38b2591270b4650ec3376d11ef2c42bb9a1ae01`은 변경 금지 기준으로 고정했다.

## 가이드·구조·레거시

- 프로젝트, 저장소, 코드, 개발 환경, 산출물, 테스트, 스크립트, 데이터·AI, 보안·개인정보, 릴리스·운영, 팀 작업 가이드를 `docs/guides/`에 정리했다.
- 루트·모듈 README, `CONTRIBUTING.md`, `SECURITY.md`, CODEOWNERS, PR template을 현재 Android 사용자·관리자 제품 경계와 연결했다.
- Gateway의 local·Backend·혼합 책임을 source에 맞춰 분리하고, hash 결속된 과거 Gateway·배포 README는 byte-exact로 유지한 채 현행 차이를 중앙 가이드에 기록했다.
- 참조·경로·hash 감사를 통과한 과거 AI handoff 2개만 `legacy/ai_tasks/`로 byte-exact 이동했다. 나머지 결속·증거 자료는 원래 경로에서 역사 자료로 구분했다.

## 카탈로그·팀 분담 산출물

- 저장소 전체, 스크립트, 테스트를 결정론적으로 분류하는 생성기·JSON/Markdown 카탈로그·회귀 테스트를 추가했다. 미분류·중복·결속 자료 오분류는 fail-closed한다.
- `docs/planning/walksafe_feature_implementation_catalog.html`에 9개 분야·119개 세부 기능, 우선순위·상태·남은 일·담당·완료 표시를 정리했다. 최종 정책 추적 감사에서 빠진 최소정보 기관 제출본 생성·내보내기 작업을 `AD-08`로 추가했다.
- 실제 브라우저에서 119개 로드, `백업` 검색 4개, 담당자 localStorage 저장·재로드, JSON import/export를 확인했고 console error는 0건이었다.

## 테스트·CI 도구

- hash 결속된 날짜형 실행기는 HEAD byte·mode exact로 보존하고 `scripts/run_walksafe_test_layers_current.sh`를 현행 실행기로 추가했다.
- Python 테스트 180개를 Unit 35, Functional 29, Integration 5, Backup 1, Model audit 2, Historical 107, Active session 1로 중복 없이 분류했다. 최종 실행에서 과거 HEAD·source byte에 결속된 42개가 active로 오분류된 것을 발견해 assertion 완화·skip 없이 역사 분류로 교정했다. 원격 CI에서 추가로 확인한 Web 포함 2026-07-13 Full-RC 테스트 3개와 분리 보존한 host-lock 테스트도 대상 script lifecycle과 맞춰 역사 분류로 교정했다.
- 일반 품질 환경은 CPython 3.12.13, backup integrity는 별도 CPython 3.14.6·pytest 8.4.2·hash lock으로 분리했다. 두 환경 혼용과 fallback은 실행기·CI·테스트에서 거부한다.
- GitHub quality workflow에 정확한 도구 버전, hash lock, current secret scan, catalog·활성 문서·OpenAPI·continuation·checkpoint control, model audit, commit-stable `all`을 연결했다.
- Next 16 production NFT trace는 53개 trace·1,295개 unique file로 검사했고 exact 예외 2개만 추가했다. Android debug·AndroidTest·사용자·관리자 unsigned release build와 model asset 검사도 PASS했다.
- 최종 현행 계층 단일 실행에서 Unit Python 743 PASS·8 SKIP, Functional Python 741 PASS, Gateway 88 PASS, Backup 54 PASS, Integration Python 73 PASS를 확인했다. Web production build·53 trace/1,295 unique file 검사, Android JVM·lint·debug·AndroidTest·사용자·관리자 unsigned release와 model asset 검사도 PASS했다.
- 최종 독립 재검수에서 README의 과거 history adapter 설명과 `active-session-control`의 strict continuation 전제 불일치를 발견했다. 문서를 현행 분류와 맞추고 runner가 strict checker를 먼저 실행하도록 교정했으며, 관련 회귀를 추가했다.
- 사용자-facing 사실 감사에서 Android source-near README의 Backend 직결 설명을 실제 Gateway `/api/navigation/*`·`/api/reports/v2` 경로로 교정하고, 결속된 과거 Android README 대신 중앙 현행 코드 지도로 연결했다. 기능 분담표의 인쇄/PDF에서도 담당·완료 입력을 보존하도록 CSS를 교정했다.

## 독립 검수와 현재 경계

- 단계별 구현 전 계획 검토와 구현 후 독립 검수를 수행했다. 가이드·콘텐츠·레거시와 runner·CI 구조의 동결 전 판정은 P0 0·P1 0이다.
- Gateway OpenAPI는 9 paths·13 operations, Android model은 unified img768·13 classes, registry의 PT 1개·TFLite 3개와 hash가 일치한다.
- 정식 시험 279개는 전부 `NOT_RUN`, release gate 5개는 전부 `NOT_RUN`·미면제이며 출시는 `NOT_ELIGIBLE`이다. 저장소 자동 검사 결과를 실기기·현장·배포·외부 수락 증거로 승격하지 않았다.

## 변경 파일 그룹

- 진입·가이드: `README.md`, `AGENTS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `.github/`, 루트·모듈 README, `docs/guides/`
- 카탈로그·계획: `docs/catalogs/`, `docs/planning/repository-modernization-20260811/`, `docs/planning/walksafe_feature_implementation_catalog.html`
- 레거시: `legacy/`, `legacy/archive-manifest.json`, `ai_tasks/README.md`
- 테스트·CI: `.github/workflows/quality.yml`, `scripts/run_walksafe_test_layers_current.sh`, `scripts/check_walksafe_active_docs.py`, `scripts/generate_repository_catalogs.py`, `tests/`
- 보안·모드: `configs/walksafe_gitleaks_reviewed_findings.json`, `scripts/check_walksafe_gitleaks_report.py`, `scripts/restore_walksafe_private_evidence_modes.py`, `.gitignore`
- 최종 결속: `docs/control/walksafe-project-continuation-checkpoint.json`의 managed snapshot content hash 2필드만 공식 계산값으로 조정

## GitHub 게시와 CI 교정

- 현대화 commit `3506e3afd113d1350ed74e82e0791b53161870a3`을 원격 `current`에 게시했다. `main`, 사전 보존 archive branch·tag는 변경하지 않았다.
- 첫 exact-SHA quality run `31495903939`에서 setup-node toolcache와 공식 Node archive의 root closure 차이를 확인했다. 테스트 실패가 아니라 commit-stable 계층 진입 전 도구 잠금 실패였다.
- quality workflow가 공식 Node 22.23.1 archive의 고정 SHA-256을 검증하고 exact 권한으로 추출한 뒤 toolchain checker를 통과한 root만 npm·후속 단계에 사용하도록 수정했다.
- `GITHUB_PATH`의 다음-step 적용과 restrictive umask를 고려해 같은 step의 PATH 고정, tar `--same-permissions`, 관련 workflow 회귀 assertion을 추가했다.
- 독립 재현에서 공식 archive의 5,866-entry root closure, Node 22.23.1, npm 10.9.8이 lock과 exact 일치함을 확인했다. 수정 commit의 exact-SHA CI 재실행 감시를 남은 종료 조건으로 두었다.

## 두 번째 CI의 계층 경계 교정

- exact-SHA run `31497090277`에서 Node 잠금 설치, Web·Gateway, general·backup 환경 설치와 모든 정적 gate가 통과한 뒤 Python unit 28건이 실패했다.
- 12건과 15건은 각각 catalog상 `HISTORICAL`인 Web 포함 2026-07-13 product-quality·operator/full-RC 스크립트 테스트가 현행 Unit에 섞인 문제였다. 추가 교차검사에서 같은 역사 release checker를 직접 읽는 integration 테스트 1개도 확인해 세 파일을 역사 inventory로 이동했다.
- 현재 제출 정책 테스트 중 실제 과거 LibreOffice host bytes를 읽는 1건은 별도 history 파일로 보존하고, Unit에는 lock의 exact 경로·크기·SHA·버전 레코드를 호스트 비의존으로 검증하는 회귀를 유지했다.
- setup-python base에 직접 general lock을 설치하던 CI를 정확한 CPython 3.12.13 전용 venv 생성·hash-lock 설치·절대 interpreter 전달 방식으로 교정했다. backup CPython 3.14.6 환경과의 분리는 그대로 유지했다.

## 최종 로컬 수용 검증

- 교정 뒤 `scripts/run_walksafe_test_layers_current.sh all`을 처음부터 다시 단일 실행해 종료 코드 0을 확인했다.
- 180개 Python test 파일은 Unit 35, Functional 29, Integration 5, Backup 1, Model audit 2, Historical 107, Active session 1로 exact partition되며 중복·누락이 없다.
- YAML·shell 문법, catalog 재생성 일치, 활성 문서 링크·script selector, workflow·runner meta 회귀, continuation·active checkpoint, model audit와 최종 staged tree 보안 검사를 동결 전 다시 확인한다.
- 다음 종료 조건은 독립 P0/P1 0, checkpoint content hash 일치, 최종 commit tree와 검사한 index tree 일치, 비강제 `current` push 및 그 exact SHA의 GitHub quality 성공이다.
- 최종 환경 독립 검수의 비차단 P2 1건도 반영했다. pinned setup-python 3.12.13의 공식 `python-path`를 처음부터 저장해 초기 mode 복원·secret policy·Node lock·venv 생성까지 절대경로로 실행하며, general·backup 환경 분리 뒤에도 ambient `python`을 사용하지 않는다.

## 세 번째 exact-SHA CI의 fresh Gradle 검증 교정

- commit `feb68a9de8abe62704ec61846126948c7402732c`의 run `31500636819`는 secret·도구 lock·두 Python 환경·OpenAPI·continuation·catalog·문서·inventory·model audit를 모두 통과했다.
- 전체 계층의 Android fresh build에서 `com.google.guava:guava-parent:33.3.1-android` POM 하나가 dependency verification metadata에 없어 중단됐다. 로컬 warm cache가 가린 재현성 누락이며 제품 테스트 실패가 아니다.
- Maven Central의 20,632-byte POM, 게시 SHA-1, 로컬 Gradle cache를 독립 대조해 SHA-256 `6e11986ea7250b51f847157e2dc937f32a306804dfce0007a5e81ddb9b95c579`를 확인했다.
- verification metadata에는 해당 component·artifact·SHA 항목 하나만 추가하고 exact 회귀를 추가한다. 별도 빈 Gradle user home의 실패 task와 새 exact-SHA CI 성공을 종료 조건으로 유지한다.
- 빈 Gradle user home에서 `:app:processDebugNavigationResources --rerun-tasks`가 PASS했고 임시 646MB cache는 안전 삭제했다. current Unit도 Python 744 PASS·8 SKIP, Gateway 88 PASS, Web 정책 suite, Android 사용자·관리자 JVM 테스트가 PASS했다.
- 독립 검수는 component/artifact/SHA가 각각 1개만 증가하고 최종 767개 checksum의 `Generated by Gradle` 패턴, 정렬·유일성, lock/build/settings 무변경을 확인해 P0/P1/P2 0으로 마감했다.

## 봉인된 runtime binding의 1회 승계

- Gradle 검증 메타데이터의 현재 SHA가 `eaa662a434257a71c595ab510889657579e5e5a107041813338a257b56674d17`로 바뀌면서, 과거 FP008·FP046 시작 gate가 봉인한 이전 SHA와 현재 파일을 직접 비교하던 continuation 검사가 정확히 3건 실패했다.
- 과거 receipt·repository-state log·seq47·48·53 event와 이후 hash chain은 당시 SHA를 올바르게 봉인하므로 수정하지 않았다.
- continuation checker에는 실제 과거 event 3개에만 해당 경로의 이전 SHA `0f2fc21ad52bd81b877f4a2cecdf587841f4dcac2c87e0e3139a0f94374c0084`와 승인된 현재 SHA 한 쌍을 허용하는 exact amendment를 추가했다. FP008·FP046 공통 검사는 event ID·경로·순서·필드·봉인 SHA·현재 SHA·비-symlink를 모두 fail-closed로 확인한다.
- 세 활성 역사 receipt의 정상 조합과 receipt SHA·순서·다른 경로·현재 live SHA 변조 거부 회귀를 추가했다. 전체 continuation은 마지막 managed snapshot content hash 재계산 전 예상된 content-set 불일치 1건만 남음을 확인했다.
- 독립 회귀에서 경로 전역 amendment가 새 gate의 정상적인 현재 SHA 봉인을 거부하는 범위 오류를 발견했다. 즉시 과거 FP008 시작·재개와 FP046 시작 event ID 3개로 범위를 좁히고, 비대상 새 gate가 기존 receipt==live 규칙으로 PASS하는 회귀까지 추가했다.
- 최종 CPython 3.12.13 회귀는 관련 5개 파일 116 PASS, active-session-control은 strict continuation 뒤 42 PASS였다. catalog·runner validate·활성 문서 41개와 로컬 링크 447개·script 참조 75개·selector 7개도 PASS했다.
- verification metadata는 200,224 bytes, component 443개·artifact/checksum 각 767개이며 모든 checksum origin은 기존과 같은 `Generated by Gradle`이다. event-scoped 구현 독립 재검수는 과거 receipt·event·anchor 변경 0, P0/P1/P2 0으로 마감했다.
