# 2026-08-10 WalkSafe 작업 로그

## 중단 작업 복원과 통제 문서

- 중단 지점의 기록·코드·검증 자료를 다시 대조하고 FP046 source manifest를
  exact 103경로, backend 실행 source closure를 exact 30경로로 닫았다.
- hardlink 20개를 내용 변화 없이 해제하고 continuation checkpoint seq54~57을
  기록했다. 다음 Goal은 단일 관리자 복구 `GAP-008`이며 상태는
  `READY_NOT_STARTED`다.
- NPC materializer가 canonical binding의 core projection을 비교하도록 교정하고
  회귀 검증을 추가했다.
- continuation checker와 exact257 successor 검증은 PASS했다.

## 현재 프로젝트 통합과 GitHub 게시

- `/home/ddobagi/Code/hanium-dreamup-walksafe-rc2-20260715`를 현재 코드와
  산출물의 단일 독립 Git 저장소로 만들었다.
- `current` 브랜치를 root commit으로 게시하고 GitHub 기본 브랜치를
  `current`로 전환했다. 최종 공개본은 3,429개 추적 파일이다.
- 기존 GitHub `main`의 commit
  `c38b2591270b4650ec3376d11ef2c42bb9a1ae01`은 변경하지 않았다.
- 캐시, 빌드 결과, 키 저장소, 구형 mirror, private exact 자료와 raw AI 검수
  이미지·라벨 256개는 현재 저장소에서 제외했다.
- 최종 공개 감사에서 누락된 TypeScript 빌드 캐시 1개와 개인정보·정밀 좌표·얼굴·
  현장 이미지가 포함된 기존 Office 원본·제출 후보 3개를 추가로 발견했다.
  생성 manifest와 함께 로컬 legacy로 옮기고 정확한 경로를 `.gitignore`에 막았다.
- 민감 Office blob이 남지 않도록 새로 만든 `current`의 초기 root 이력만 다시
  작성해 `--force-with-lease`로 교체했다. 기존 GitHub `main`은 건드리지 않았다.
- 후속 Office 컨테이너 감사에서 남은 빈 개발보고서 양식의 Google Docs 원본
  식별 메타데이터를 발견했다. 파일을 수정하지 않고 legacy로 추가 격리했으며,
  공개 저장소는 제출 시점에 현행 공식 양식을 외부에서 새로 받도록 정리했다.

## 레거시와 복구 자료

- 기존 linked worktree, 로컬 snapshot, stale FP046 작업본, 현재 저장소 제외 자료를
  `/home/ddobagi/Code/legacy/hanium-dreamup-pre-standalone-20260810` 아래에
  종류별로 보관했다.
- 구 이력 recovery bundle은 430,057,800 bytes이며 SHA-256은
  `2c966e841f5be2a195c1690d11c29f21104c284f75ec5276a36ecc38abb7bf9c`다.
  `git bundle verify`를 통과했다.
- 과거 worktree 네 곳은 `git worktree repair` 후 접근 가능함을 확인했고,
  누락된 `/tmp` record는 prune하지 않고 보존했다.
- 공개 제외 Office 원본·후보와 manifest는
  `excluded-from-current/publication-sensitive-office-20260810/`에, 빌드 캐시는
  `excluded-from-current/.hanium-dreamup-stage-excluded-20260810/ignored-leftovers/`에
  삭제 없이 보관했다.
- 공개 제외 Office 보관 디렉터리는 `0700`, 파일은 `0600`으로 제한했다.

## 팀 분담용 기능 목록

- `docs/planning/walksafe_feature_implementation_catalog.html`을 작성했다.
- 사용자 앱, 보행 세션, AI·AR/센서, 길안내·음성, 신고·관리자,
  개인정보·보안, 서버·Gateway·저장소, AI 데이터·모델 운영,
  품질·출시·산출물의 9개 분야에 118개 세부 기능을 쉬운 문장으로 정리했다.
- 각 항목에 우선순위, 현재 상태, 남은 일, 담당자, 완료 체크를 두고 검색·필터,
  로컬 저장, 10개 필드 분담 JSON 내보내기·불러오기, 인쇄/PDF 기능을 넣었다.
- Web/PWA, legacy 1·2·3, 기존 한이음 제출 후보와 분리된 음성 prototype은
  구현 대상에서 제외했다.

## 검증 결과와 남은 경계

- HTML/JavaScript parse, 118개 기능 ID 중복, 검색·상태 필터, 담당자 저장·재로딩,
  저장소 `null`·쓰기 실패, JSON 왕복, 320·390px 모바일 표시와 브라우저 console을
  검증했다. 독립 재검수 결과는 blocking 0·major 0·minor 0이다.
- gitleaks baseline scan은 발견 0건, symlink·special file·hardlink·100MiB 초과
  blob은 모두 0건이다.
- 새 독립 저장소의 continuation checker는 PASS했다.
- 공개 범위 설명 변경은 runbook이 허용하는 snapshot reconcile로 처리했다.
  transition history·Goal 상태·canonical binding·gate receipt는 변경하지 않았다.
- 깨끗한 이력에는 privacy/legacy 자료가 포함된 구 commit
  `a3ad7eead6b5d834d3e0675422475a9aad351e3d`을 의도적으로 넣지 않았다.
  이 때문에 해당 commit을 직접 조회하는 기존 Goal graph checker는 63건으로
  FAIL하지만, archive의 구 이력 기준 live 35경로와 관련 249경로는 byte-identical이며
  Git lookup만 대체하면 오류가 0건이다. 새 이력 독립 실행 지원은 기능 목록의
  `QA-09` 후속 작업으로 기록했다.
- backend 집중 시험은 331 PASS·12 SKIP·1 FAIL이며, 실패 1건은 새 privacy HMAC DB
  초기화를 반영하지 않은 report storage reconciliation lifespan mock이다(`QA-08`).
- formal 279개, 실제 기기·배포·외부 기관·release gate는 아직 `NOT_RUN`이고
  release는 `NOT_ELIGIBLE`이다. 내부 구현 통과를 출시 완료로 계산하지 않았다.

## 변경 파일

- `.gitignore`
- `apps/web/tsconfig.tsbuildinfo` 제거 및 legacy 보관
- `docs/README.md`
- `templates/README.md`
- `templates/SUBMISSION_BUILD_MANIFEST.json`과 provenance 포함 Office 4개 제거 및
  legacy 보관
- `docs/control/walksafe-project-continuation-checkpoint.json`의 snapshot reconcile 2필드
- `docs/planning/walksafe_feature_implementation_catalog.html`
- `daylog/2026-08-10.md`
- FP046·NPC·continuation 관련 기존 control 문서와 검사기 변경은 위 독립 root
  commit에 포함했다.
