# 2026-07-15

## WalkSafe RC1 production-boundary hardening

- Backend readiness를 dependency별 daemon single-flight와 callback 없는 bounded polling으로 고정하고, TMAP live probe single-flight·failure cooldown을 추가했다.
- remote PostgreSQL은 URL-explicit `sslmode=verify-full`·`gssencmode=disable`만 허용하고 libpq service/ambient transport 우회를 거부하도록 보강했다.
- `/reports/summary`가 actor·purpose를 확인하고 durable read audit 실패 시 응답을 보류하도록 수정했다.
- Web Voice STT BFF에 pre-body global/actor/IP rate와 단일 upload admission, bounded multipart read·abort cleanup을 추가하고 uncooperative stream cancel에도 413과 slot release가 끝나도록 수정했다.
- production Chromium privacy 검증을 실제 built page의 consent·camera/GPS·detect/report·철회·reload 동작으로 전환하고 모든 CDP call에 phase/global deadline을 적용했다.
- Android `MainActivity`의 same-frame tactile supplier와 실제 pause/destroy/destination lifecycle transport cancellation을 production entry에서 검증하고 stale completion race를 닫았다.
- canonical 2026-07-15 사실과 2026-07-13 과거 pre-source-freeze 검증 snapshot을 분리하고 PNG·Office·상세 설계 builder/validator가 status·실행일·시점 경계를 표시하도록 수정했다.
- 변경을 `89ba802`, `ae39625`, `9b02adb` 세 로컬 커밋으로 분리했다. push·PR·배포·서명은 수행하지 않았다.

## Changed Files

- `backend/`, `apps/web/`, `contracts/walksafe.openapi.json`, 배포 예시와 관련 테스트
- `apps/android/` tactile/navigation/cancellable call production composition과 JVM 테스트
- `scripts/check_pwa_browser_lifecycle_20260711.py`, Full-RC·submission builder/validator와 tooling 테스트
- `docs/submission/`, `docs/status/walksafe_rc1_issue_ledger_20260713.md`

## Verification

- Backend 전체: 354 passed, 실패·오류·skip 0.
- Web: `npm test`, `npm run typecheck`, `npm run lint --max-warnings=0` 통과.
- Android: JVM 244 passed, 실패·오류·skip 0; `lintDebug` 통과.
- Submission/Full-RC tooling: 55 passed; canonical OpenAPI/route fixture `--check` 통과.
- fresh production Chromium privacy/PWA lifecycle: detect 10, report 2, external fetch 0으로 통과.
- Backend/Web, Android, submission 시점 일관성 독립 재감사에서 잔여 P0/P1 없음.
- `git diff --check` 통과. 전용 worktree는 커밋 후 clean 상태다.
- 원본 dirty worktree는 HEAD `052cbe22de498eaeb1c21eaeb07f30f262340f15`, branch `codex/walksafe-v3-relabel-task-package`, status SHA-256 `dd68189009e7b2b3be11f97e3928ccbe86870cc348bab2f8ba284ce6c460bb58`로 불변임을 재확인했다.

## Remaining

- Web을 TMAP 중심으로 둘지 WebXR-capable local tactile을 별도 제품 범위로 둘지 사용자 선택이 필요하다.
- Android ARCore 미지원 기기를 명시적 TMAP-only 제한 모드로 둘지 설치 단계에서 제외할지 사용자 선택이 필요하다.
- 선택 반영 뒤 source freeze, 최신 계층 수치 교체, PNG→Office→manifest 재생성, 두 clean clone Full-RC 검증이 남아 있다.
- 사람 privacy receipt, Android signing key/certificate, production endpoint/credential과 실제 device/field 증거는 외부 입력이다.

## WalkSafe RC1 hardening continuation

- 현재 source-freeze device evidence를 `NOT_RUN_CURRENT_SOURCE_FREEZE`와 null 실행값으로 고정하고, 2026-07-13 SM-G981N 2/2 계측은 범위·날짜가 있는 historical evidence로 분리했다.
- Android tactile production 조립을 실제 `MainActivity.publishDetectionSnapshot`과 snapshot 단위 처리 경계에서 컴파일 검증하도록 수정했다. evidence drop, identity self-attestation, matcher fallback, captured/live route gate 상수화 변이는 각각 exact JVM 테스트를 실패시킨다.
- raw source regex scaffold 검증은 production call-site 존재를 확인하는 보조 검사로 축소하고, 동작 계약은 JVM 테스트가 담당하도록 했다.
- test-layer 분류를 `8c4560c`, 최초 scaffold 교체를 `0f98023`, device evidence 분리를 `c49baee`, 독립 false-pass 감사 뒤 production composition 보정을 `61b9ecc`로 로컬 커밋했다. `0f98023`의 regex/helper 한계는 후속 커밋이 대체했다. push·PR·배포·서명은 수행하지 않았다.

### Changed Files

- Android `MainActivity`, tactile composition, JVM 테스트와 scaffold checker/Python baseline
- submission canonical facts, shared builder/validator, asset/design/form/promoter/final validator
- `docs/status/walksafe_rc1_issue_ledger_20260713.md`

### Verification

- Android scaffold PASS, Python baseline 1 passed, focused JVM 14+7 passed.
- Android 전체 JVM 247 passed, failure/error/skip 0; `lintDebug`, `assembleDebug` 통과.
- 별도 `/tmp` 독립 재감사에서 네 위험 변이는 모두 exact JVM 테스트를 실패시켰고 무해한 guard 순서 변경은 focused 22/22를 유지했다. 의미 리뷰와 mutation 리뷰 모두 잔여 P0/P1 0이다.
- submission 집중 검증 47 passed. partial/null/current-PASS, 3/3, 다른 기기·날짜, 추가 key, legacy flat count, JSON float 변이가 모두 거부됐고 독립 layout/semantic 리뷰의 잔여 P0/P1은 0이다.
- submission 광역 bundle은 93 passed, 4 failed였다. 실패는 shared env의 executable Pillow `.pyc`와 exact-8 env가 확인한 LibreOffice 26.2.4/lock 26.2.3 불일치로 fail-closed 됐으며 artifact 생성은 진행하지 않았다.
- `git diff --check` 통과. 격리 worktree의 코드 변경은 커밋됐고 문서 기록만 남았다.
- 원본 dirty worktree의 HEAD `052cbe22de498eaeb1c21eaeb07f30f262340f15`와 branch `codex/walksafe-v3-relabel-task-package`는 유지됐다. read-only 재확인 시 `-uall` status SHA-256은 `74a35f38a41c08746d07875a16a5c06bf31bdde367ae96c553473fed0b3a1f8c`로 이전 기록과 달랐으며, 이번 continuation에서는 원본 worktree에 write 명령을 실행하지 않았다.

### Remaining

- 추천 제품 범위는 Web 길안내를 TMAP-only로 명시하고, Android ARCore/Depth 미지원 기기는 설치 가능한 TMAP-only 제한 모드로 두는 것이다. 사용자 확정 전에는 이 범위 변경을 구현하지 않는다.
- source freeze 전 private clean exact-8 환경과 갱신된 LibreOffice toolchain lock 검토가 필요하다.
- 제품 선택과 toolchain gate 해결 뒤 source freeze, 계층 전체 재검증, PNG→Office→manifest→Full-RC 생성, 두 clean clone과 최종 독립 audit가 남아 있다.

## WalkSafe RC1 submission toolchain gate

- 기존 환경을 변경하지 않고 0700 권한의 private exact-8 Python 환경을 새로 만들었다. 8개 distribution, RECORD 기준 locked file 1,659개, site entry 1,892개, `.pyc`/`.pyo` 0개를 확인했다.
- submission toolchain lock을 v2로 올려 native tool의 version·호출 파일·immediate runtime path/bytes/SHA를 결합했다. LibreOffice는 인접한 `soffice.bin`을 요구하고 실행 전과 `finally`에서 런처와 runtime을 다시 검증한다.
- 전체 UNO/dynamic-library closure는 잠그지 않으며 clean-room 문서에 same-host P2 trust boundary로 명시했다.
- 도구체인 변경은 로컬 커밋 `4f616ef`로 기록했다. push·PR·artifact 생성은 수행하지 않았다.

### Changed Files

- `configs/submission_toolchain_lock_20260713.json`
- `scripts/submission_manifest_policy.py`, `scripts/run_walksafe_submission_python_20260714.py`
- `tests/test_submission_manifest_policy.py`
- `docs/submission/CLEAN_ROOM_REPRODUCTION_20260713.md`
- `docs/testing/test_layers_20260711.md`, `docs/status/walksafe_rc1_issue_ledger_20260713.md`

### Verification

- private exact-8 환경에서 실제 v2 library/tool/font attestation 통과.
- 정책/runner bundle 53 passed, 2 environment-specific cases는 같은 exact-8 실제 attestation으로 별도 확인했다. 집중 회귀 9 passed.
- invocation/runtime의 실행 전·후 변이와 decoy runtime 관계 제거 변이가 모두 fail-closed했다.
- clean clone runner가 새 lock·정책·source 경계를 통과해 validator까지 도달했고, 아직 재생성하지 않은 과거 artifact만 예상대로 거부했다.
- `git diff --check`, `dpkg --verify` 통과. 현재 artifact는 source freeze 전 상태이므로 생성·승격하지 않았다.

### Remaining

- 제품 범위 확정 뒤 현재 source freeze에서 계층 전체를 재실행하고 canonical 수치를 교체한다.
- 새 PNG→Office→manifest→Full-RC를 생성한 뒤 두 clean clone과 최종 독립 audit A/B를 수행한다.
