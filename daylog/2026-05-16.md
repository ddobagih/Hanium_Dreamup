# 2026-05-16

## 확인한 근거
- `plans/catchup/2026-05-16.md`
- `plans/.work/2026-05-16/execute-r1/pwa-accessibility-sensors.md`
- `plans/.work/2026-05-16/execute-r1/backend-postgis-api.md`
- `plans/.work/2026-05-16/execute-r1/vision-model-data-mlops.md`
- `plans/.work/2026-05-16/execute-r1/voice-stt-tts.md`
- `plans/.work/2026-05-16/execute-r1/integration-field-report.md`
- `docs/execution/2026-05-16_backend_postgis_api.md`
- `docs/execution/2026-05-16_model_data_mlops.md`
- `docs/execution/2026-05-16_integration_field_report.md`
- `git status --short --untracked-files=all`, `git diff --stat`, 파일별 `git diff`, `git diff --check`
- 실행 라운드는 r1이라 이전 audit의 `A 계속 가능` 항목 제한 검사는 적용 대상이 아니다.
- 이번 통합 daylog 작성에는 새 하위/병렬 에이전트를 사용하지 않았다.

## 오늘 한 일
- 2026-05-16 r1 실행 note 5개와 현재 작업트리 diff를 대조해 수행/변경/검증 결과를 통합했다.
- PWA/Accessibility/Sensors:
  - 음성 안내 꺼짐 상태에서 `repeat_last`가 마지막 상태를 음성으로 재생하지 않도록 수정했다.
  - 관리자 신고 row와 상태 버튼에 선택 상태 `aria-pressed`를 추가했다.
  - service worker가 navigation 요청에만 `/` shell fallback을 쓰고, static asset은 runtime cache하며, API/외부 GET 실패에는 shell HTML을 섞지 않도록 보강했다.
- Backend/PostGIS/API:
  - `backend/tests/test_reports.py`의 report 생성 helper를 이미지 파일명/bytes/content-type 지정 가능하게 확장했다.
  - `/reports?limit=1`, `created_to`, JPEG/PNG/WebP 업로드, 동일 신고 `new -> reviewed -> resolved`, `empty_image` negative 테스트를 추가했다.
  - backend 관련 문서 충돌 목록과 DB migration backlog를 실행 문서로 정리했다.
- Vision Model/Data/MLOps:
  - v2 모델 handoff 표, artifact hash, class order, threshold/env 후보, ONNX 제한 문구를 정리했다.
  - `walksafe_kr_v2` test subset 기반 failure 후보 CSV/summary를 생성했다.
  - VL1+VS1 hard-negative dry-run 결과와 v3/v4 backlog를 정리했다.
- Voice STT/TTS:
  - `scripts/test_stt.py` dry-run 명령 목록에 `지금 어디야`를 추가했다.
  - 실제 사람 음성 샘플 8개를 재평가했고 `지금어디야.m4a`가 `get_current_location`으로 분류됨을 확인한 산출물을 남겼다.
  - TTS 전체 문구 cache 생성은 완료 근거가 없어 미완료로 남겼다.
- Integration/Field Test/Report:
  - 5/15 실행 근거를 fake/server/STT/TTS/실폰 field test 기준으로 재분류했다.
  - 문서 충돌 목록, 데모/보고 기준, 5/17 후속 후보를 `docs/execution/2026-05-16_integration_field_report.md`에 정리했다.
- git commit/push는 수행하지 않았다.

## 변경 파일
- `apps/web/app/page.tsx`: `speechEnabled=false`일 때 `repeat_last`가 음성을 재생하지 않도록 조건 수정.
- `apps/web/app/admin/page.tsx`: 신고 row와 상태 버튼의 선택 상태를 `aria-pressed`로 노출.
- `apps/web/public/sw.js`: 즉시 활성화, client claim, navigation fallback/static runtime cache/API fallback 분리.
- `backend/tests/test_reports.py`: reports smoke matrix와 helper 보강.
- `scripts/test_stt.py`: dry-run intent 명령에 `지금 어디야` 추가.
- `docs/execution/2026-05-16_backend_postgis_api.md`: backend 실행 결과, 검증 제한, 문서 충돌, DB backlog 기록.
- `docs/execution/2026-05-16_model_data_mlops.md`: model handoff, failure sampling, hard-negative 계획, v3/v4 backlog 기록.
- `docs/execution/2026-05-16_integration_field_report.md`: field/integration 근거 재분류, 문서 충돌, 보고 기준 기록.
- `daylog/2026-05-16.md`: r1 실행 결과 통합 기록.
- 실행 note/계획 산출물:
  - `plans/catchup/2026-05-16.md`
  - `plans/.work/2026-05-16/catchup/*.md`
  - `plans/.work/2026-05-16/execute-r1/*.md`
- Git 추적 제외 로컬 산출물:
  - `outputs/voice/stt_intent_dry_run_2026-05-16.csv`
  - `outputs/voice/stt_myvoice_results_2026-05-16.csv`
  - `runs/failure_sampling/walksafe_kr_v2_test_subset_20260516/failure_candidates.csv`
  - `runs/failure_sampling/walksafe_kr_v2_test_subset_20260516/summary.json`

## 검증
- PWA:
  - `node --check apps/web/public/sw.js`: PASS.
  - `cd apps/web && npm run lint`: PASS.
  - `cd apps/web && npm run typecheck`: PASS.
  - `cd apps/web && npm run build`: PASS.
  - `git diff --check -- apps/web/app/page.tsx apps/web/app/admin/page.tsx apps/web/public/sw.js`: PASS.
- Backend:
  - `.venv/bin/python -m py_compile backend/tests/test_reports.py`: PASS.
  - `timeout 120s .venv/bin/python -m pytest backend/tests/test_reports.py -q -rs`: `17 skipped`, PostGIS DB 접속 불가.
  - `docker compose ps db`: Docker socket 권한 없음.
  - 전체 `backend/tests`: 현재 sandbox에서 `FastAPI TestClient` timeout으로 완료 확인 못함.
- Model/Data:
  - `df -h`: `/` 가용 `18G`, 사용률 `98%`.
  - `sha256sum`: `best.pt`, `best.onnx` hash 확인.
  - `.venv` 모델 패키지 확인: `ultralytics 8.4.48`, `torch 2.11.0+cu130`, `onnxruntime 1.26.0`.
  - full test split failure sampling: exit code `137`로 중단.
  - seed 고정 subset sampling: sample `360`장, GT box `594`, candidate `264`행, review CSV `40`행 생성.
  - VL1+VS1 dry-run: tactile filenames `1,038`, positive `0`, negative `1,038`, boxes `0`.
  - CSV/JSON 무결성: summary `40`행과 CSV `40`행 일치.
- Voice:
  - `cd apps/web && npm run lint && npm run typecheck`: PASS.
  - `.venv-voice` 기준 Python compile: PASS.
  - `scripts/test_stt.py --dry-run-intents`: 8개 intent PASS.
  - 실제 사람 음성 STT 재평가: samples `8`, success `8`, avg `2.219s`, p95 `2.481s`, `지금어디야.m4a -> get_current_location`.
  - 직접 함수 호출 기준 `/health`, 7개 `/speech/intent`, STT error shape 확인: PASS.
  - `curl` localhost smoke: sandbox `Operation not permitted`.
  - `/speech/tts` batch cache 생성: 완료 근거 없음.
- Integration:
  - `.venv/bin/python -m py_compile scripts/check_pwa_server_e2e.py`: PASS.
  - `cd apps/web && npm run lint`: PASS.
  - `cd apps/web && npm run typecheck`: PASS.
  - `git diff --check`: PASS.
  - `adb devices`: `adb: command not found`.
  - `docker compose ps`: Docker socket permission denied.
  - `ss -ltnp`: netlink socket permission denied.
  - `.venv/bin/python -m pytest backend/tests/test_detect.py -q`: 3분 이상 출력 없이 반환되지 않아 완료 근거에서 제외.

## 리스크/확인 필요
- Android 실폰 카메라/GPS/방향/진동/TTS/마이크, 목걸이 착용 통제 테스트, PWA 설치/offline/TalkBack 수동 점검은 미실행이다.
- fake 신고 생성 후 `/admin`에서 이미지/source/duplicate/status 변경 확인은 미실행이다.
- server mode는 5/15 headless fixture E2E 근거가 있으나 실제 실폰 카메라 field 근거는 아니다.
- PostGIS 접근 가능한 세션에서 `backend/tests/test_reports.py`와 전체 `backend/tests` 재실행이 필요하다.
- ONNX full metric equivalence, browser/ONNX Runtime Web latency, 4-class metric은 미실행이다.
- class `1..3` 한국 GT가 없어 `new_class_gap` 실제 샘플 CSV는 생성하지 못했다.
- 생성된 failure CSV는 자동 후보라 수동 시각 검수 및 개인정보/위치정보 비식별 검토가 필요하다.
- 브라우저/실폰 마이크 E2E, PWA dev server와 `NEXT_PUBLIC_VOICE_API_BASE` CORS 연결 확인은 미실행이다.
- TTS 대상 7개 문구 cache 생성과 `X-Voice-Cached: true` 확인은 미완료다.
- `서울역으로 안내해줘`는 여전히 `unknown`이다. `set_destination` 확장 여부는 제품 판단이 필요하다.
- 현재 작업트리에는 5/15 문서/daylog 변경, `scripts/check_pwa_server_e2e.py`, ignored `.next`/voice/model 산출물이 함께 남아 있다. 이번 통합에서는 기존 5/15 기록을 수정하지 않았다.
