# 2026-07-01

## Changes
- Read pasted objective file: `/home/ddobagi/.codex/attachments/3cbaa99d-7cbe-4648-969b-ba83d1d09fc1/pasted-text-1.txt` and requirements docs.
- Updated `backend/app/schemas.py` to enforce BBox overflow validation (`x + width <= 1`, `y + height <= 1`).
- Added bbox overflow rejection test in `backend/tests/test_reports_v2.py`.
- Expanded `REPORT_V2_METADATA_ALLOWLIST` in `backend/app/api/reports.py` with required Android/depth metadata keys.
- Added allowlist retention assertions to report tests.
- Verification: not executed (not requested).
- Implemented `/reports/v2` coordinate-gate policy in backend: pending/non-pass `coordinate_gate_status` now marks `performance_excluded=true` and appends exclusion reason.
- Added `ReportV2Metadata.coordinate_gate_status` field and coverage tests in `backend/tests/test_reports_v2.py`.
- Implemented web TestCapturePanel default-off and consent gating: added `NEXT_PUBLIC_WALKSAFE_TEST_CAPTURE_PANEL` + localStorage consent flow in `apps/web/app/page.tsx`; panel renders only after explicit consent.
- Added `x-test-capture-consent` header and metadata/image size pre-validation to `apps/web/app/_walksafe/components/TestCapturePanel.tsx`.
- Hardened `apps/web/app/api/walksafe-test-log/route.ts`:
  - env gate `WALKSAFE_TEST_LOG_ENABLED`
  - consent/auth checks
  - metadata JSON/object/type and size validation
  - retention cleanup for aged date folders
  - DELETE endpoint for id-based deletion
  - added error response codes.
- Added env defaults to `apps/web/.env.example` for test capture/test-log controls.
- Continued Android 작업: `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
  - 누락 컴파일 심볼 및 상수를 보강: `CAMERA_PERMISSION_REQUEST`, `NAVIGATION_PERMISSION_REQUEST`, `PREF_STEP_LENGTH_KEY`, `REPORT_UPLOAD_COOLDOWN_MS`, 단계 보정/검색 페이지 상수.
  - `MessageLevel.priorityScore()` 확장 추가 및 후보 선택 정렬 우선순위 적용.
  - 리포트 업로드 파이프라인 상태 객체 `ReportUploadState` 및 `reportCooldownKey()` 추가, 업로드 실행 중/쿨다운 상태 문자열 반영.
  - `ReportUploadHttpException` catch를 위한 import 추가.
  - 경로 관련 권한 헬퍼 정리(미사용 함수 제거), `haversineMeters` import 추가, 네비게이션 권한 요청 분기 정합.
  - 검증: 실행 안 함(요청 없음).
- 추가 Android 수정: `ensureNavigationPermissionForReport()`가 report 후보 생성 시 위치 권한만 요청하도록 분리해, 경로/스텝용 권한 없이도 report 후보 경로에서 GPS 권한만 먼저 취득하도록 변경. 파일 `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`.
- Android UX 보강: `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
  - 목적지 검색에 "검색 취소" 버튼 추가 (`destinationCancelButton`) 및 검색 in-flight 취소 토큰/플래그 적용.
  - 목적지 검색 in-flight 중 응답 무효화 로직(생성 ID) 추가로 취소 후 레거시 응답 UI 반영 방지.
  - 경로 버튼 상태에 `경로 취소` 상태 추가 (`routeRequestInFlight` 기준).
  - 경로 요청 중(HTTP 비동기) 취소 동작 추가 (`cancelActiveRouteRequest`) 및 요청 ID 토큰으로 레거시 응답 무효화.
  - routeExecutor 실행 거부(RejectedExecutionException) 대비 상태 복구 라인 추가.
- Android 리포트 업로드/메타 연동 정합성 마무리:
  - `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`에 보고서 업로드용 JPEG 스냅샷 상수(`REPORT_FRAME_CAPTURE_MAX_DIMENSION`, `REPORT_FRAME_CAPTURE_JPEG_QUALITY`)를 추가.
  - `DetectionSnapshot`에 캡처 이미지 전달 경로(`reportImage`)가 유지되도록 상태 구조 반영 점검 및 패치.
  - 요구사항 추적표 `docs/walksafe_requirement_traceability_matrix_20260701.md`의 다음 항목 상태를 최신 구현 기준으로 갱신: `WS-REQ-008/021/024/025/027/028/031/032/033/042/047`, 미완료 P0/P1 목록 정리.
- Verification: 실행 안 함(요청 없음).

## YOLO26n 잘못된 640 resume 학습 중지 및 strict 768 재학습 준비

- 현재 실행 중이던 `walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_20260630_b8w2` run이 resume 이후 `imgsz=640`으로 진행 중임을 확인했다.
- 대상 process group `3134583`을 종료했다. SIGTERM 후 잔여 프로세스가 있어 SIGKILL로 정리했다.
- 검증: `pgrep '[y]olo detect train'` 결과 학습 프로세스 없음, `nvidia-smi` 기준 GPU 사용률 0%, 메모리 약 216MiB 확인.
- 640으로 섞인 기존 run은 중단 상태로 보존하고 삭제하지 않았다.
- 새 strict 768 학습 시작 스크립트 생성:
  - `logs/run_walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_strict768_20260701_b8w2_start.sh`
- strict 768 resume 스크립트 생성:
  - `logs/run_walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_strict768_20260701_b8w2_resume_strict768.sh`
- 새 시작 스크립트는 `yolo26n.pt`에서 `epochs=300`, `imgsz=768`, `batch=8`, `workers=2`, `save_period=10`, `patience=200`으로 실행하도록 준비했다.
- resume 스크립트는 checkpoint 경로를 인자로 받고, resume 시에도 `imgsz=768`, `batch=8`, `workers=2`를 명시하도록 준비했다.
- 두 스크립트는 `bash -n`으로 문법 검사를 통과했다.

## YOLO26n strict 768 새 학습 시작

- 사용자가 새 strict 768 학습 진행을 요청함.
- 시작 전 `yolo detect train` 프로세스가 없고, strict run 디렉터리가 아직 없음을 확인했다.
- 새 run을 백그라운드로 시작했다.
- run: `runs/detect/walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_strict768_20260701_b8w2`
- 시작 PID: `4170256`
- 로그: `logs/walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_strict768_20260701_b8w2_20260701_120341.log`
- 시작 스크립트: `logs/run_walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_strict768_20260701_b8w2_start.sh`
- 검증:
  - 실행 프로세스 명령에 `imgsz=768`, `epochs=300`, `batch=8`, `workers=2`가 포함됨을 확인했다.
  - `args.yaml`에서 `imgsz: 768`, `resume: false` 확인했다.
  - 로그에서 `Image sizes 768 train, 768 val`, `Starting training for 300 epochs...`, `1/300 ... 768` 진행을 확인했다.

## WalkSafe 문서 정합성 감사 및 보강

- 코드/설정/스크립트 기준으로 WalkSafe 핵심 문서의 오래된 상태를 갱신했다.
- Android report 업로드 경로, `/reports/v2 source=android`, Android metadata allowlist, 현재 APK hash, Android 640 runtime config와 strict 768 학습 후보의 차이를 문서에 반영했다.
- 새 debug APK SHA-256은 `ce9f08aa5a2f13f481811db4688db8b45a7860f937f638e17c5f9f3279256db9`이다.
- 새 문서:
  - `docs/walksafe_documentation_audit_20260701.md`: 현재 상태, 근거 경로, 수정한 불일치, 검증 방법, 남은 리스크 정리.
  - `docs/archive_candidates_20260701.md`: 삭제/이동 없이 구식·중복 가능 문서 후보와 대체 문서 정리.
- 갱신 문서:
  - `README.md`
  - `docs/README.md`
  - `docs/current_status.md`
  - `docs/walksafe-v2/README.md`
  - `docs/walksafe-v2/backend_api_contract.md`
  - `docs/walksafe-v2/android_report_source_metadata_decision_20260601.md`
  - `docs/report_operations.md`
  - `docs/api_reference.md`
  - `apps/android/README.md`
  - `docs/android/arcore_depth_estimation_architecture.md`
  - `docs/android/android_device_overlay_depth_checklist_20260601.md`
  - `docs/android/android_server_debug_log_runbook_20260601.md`
  - `docs/android/android_native_mvp_evidence_20260630.md`
  - `docs/evidence/feature_matrix.md`
  - `docs/evidence/final_report_index.md`
  - `docs/walksafe_requirement_traceability_matrix_20260701.md`
  - `model/README.md`
  - `product/roadmap.md`
  - `product/vision.md`
- 작은 리팩터링:
  - `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`: report 후보 입력 인자명을 `apkSha256`로 정리.
  - `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/report/AndroidReportCandidatePolicy.kt`: 필드명을 `apkSha256`로 정리하고 JSON 키 `apk_sha256`은 유지. `captured_at` 생성 시 timestamp 확장 함수 호출 대상을 수정했다.
  - `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`: 잘못 이스케이프된 Kotlin 문자열 보간을 지역 변수로 풀고, 권한 헬퍼 호출명을 기존 함수와 맞췄다.
  - `apps/android/app/build.gradle.kts`: `BuildConfig.VERSION_NAME` 참조를 위해 buildConfig 생성을 켰다.
  - `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/navigation/WalkingRouteModels.kt`: 목적지 검색 응답 모델을 추가했다.
  - `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/report/AndroidReportCandidatePolicyTest.kt`: 최신 report candidate 필수 메타데이터 필드를 테스트 fixture에 반영했다.
  - `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/navigation/BackendWalkingRouteClientTest.kt`: `WalkingRouteTransport.get()` 추가에 맞춰 테스트 더블을 보강했다.
- 검증:
  - `git diff --check`: PASS.
  - 오래된 문구 검색: `Upload is not connected`, `source=server 유지` 등 canonical 문서 잔존 표현 없음. 이전 APK hash는 감사 문서의 이력 설명에만 남김.
  - `PYTHONDONTWRITEBYTECODE=1 python3 scripts/check_android_tflite_contract_20260531.py`: `ok=true`, 경고는 unified TFLite asset 누락과 threshold drift.
  - `PYTHONDONTWRITEBYTECODE=1 python3 scripts/check_android_depth_scaffold_20260531.py`: PASS.
  - `bash -n scripts/run_walksafe_unified_aihub183_png_yolo26n_768_20260627.sh`: PASS.
  - `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_report_policy.py backend/tests/test_android_debug_logs.py -q`: 7 passed.
  - `cd apps/android && ./gradlew testDebugUnitTest --no-daemon`: BUILD SUCCESSFUL.
  - `cd apps/android && ./gradlew assembleDebug --no-daemon`: BUILD SUCCESSFUL.
  - `sha256sum apps/android/app/build/outputs/apk/debug/app-debug.apk`: `ce9f08aa5a2f13f481811db4688db8b45a7860f937f638e17c5f9f3279256db9`.
- 남은 리스크:
  - Device PASS, PostGIS no-skip E2E, 실제 unified TFLite asset/export, strict 768 학습 최종 metric, truncated-image warning 감사가 남아 있다.
  - 문서는 삭제하거나 이동하지 않았고, archive candidate 목록만 작성했다.

## YOLO26n strict 768 학습 중단 확인 및 재개

- 사용자가 strict 768 학습이 멈춘 것 같다고 보고하고 재개를 요청함.
- 확인 결과 실제 `yolo detect train` 프로세스가 없고, `results.csv`는 13 epoch 완료까지 기록되어 있었다.
- 기존 로그는 14/300 epoch 진행 중에서 끝나 있었고, `last.pt`는 13 epoch 완료 상태로 확인했다.
- strict 768 resume 스크립트로 `last.pt`에서 재개했다.
- resume checkpoint: `runs/detect/walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_strict768_20260701_b8w2/weights/last.pt`
- resume 로그: `logs/walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_strict768_20260701_b8w2_resume_last_20260701_200405.log`
- 검증:
  - `Resuming training ... from epoch 14 to 300 total epochs` 확인.
  - `Image sizes 768 train, 768 val` 확인.
  - 실행 명령과 `args.yaml`에서 `imgsz=768`, `batch=8`, `workers=2` 확인.
  - GPU 사용 및 학습 프로세스 실행 확인.

## YOLO26n strict 768 학습 재중단 확인 및 재개

- 사용자가 학습이 다시 멈췄다고 보고하고 재개를 요청함.
- 확인 결과 `yolo detect train` 프로세스가 없고, `results.csv`는 13 epoch 완료까지만 기록되어 있었다.
- 최신 로그는 14/300 train loop 100% 지점에서 끝나 있었으나 validation/result 기록은 없어, `last.pt` 기준으로는 13 epoch 완료 상태였다.
- strict 768 resume 스크립트로 다시 `last.pt`에서 재개했다.
- resume 로그: `logs/walksafe_unified_aihub183_png_yolo26n_img768_e300_open150_strict768_20260701_b8w2_resume_last_20260701_203828.log`
- 검증:
  - 실행 명령에서 `resume=.../last.pt imgsz=768 batch=8 workers=2` 확인.
  - 로그에서 `Resuming training ... from epoch 14 to 300 total epochs` 확인.
  - 로그에서 `Image sizes 768 train, 768 val` 확인.
  - GPU 사용 및 학습 프로세스 실행 확인.

## WalkSafe 구현 범위 감사, 정지 실기기 smoke, AIHub189 offline validation 확인

- 사용자 요청에 따라 10개 병렬 에이전트로 Android 권한/report/navigation/feedback/model runtime, backend reports, web/admin, TestCapturePanel, AIHub189 evaluator, evidence 문서를 읽기 전용 감사했다.
- 확인 결과 전체 구현 완료로 판정하면 안 되며, 코드가 있는 항목과 evidence/test gap이 남은 항목을 분리했다.
- 정지 실기기 smoke:
  - 연결 기기: `R3CN50F4APH`, 모델 `SM-G981N`.
  - `apps/android/app/build/outputs/apk/debug/app-debug.apk` 설치 성공.
  - `adb shell am start -W -n kr.co.hanium.dreamup.walksafe/.MainActivity` cold launch 성공.
  - `pidof kr.co.hanium.dreamup.walksafe`가 `7308` 반환.
  - `dumpsys activity activities`에서 `kr.co.hanium.dreamup.walksafe/.MainActivity`가 `RESUMED`, visible true.
  - `AndroidRuntime:E` 필터 logcat 출력 없음.
- 검증:
  - `./gradlew test assembleDebug`: BUILD SUCCESSFUL.
  - `sha256sum apps/android/app/build/outputs/apk/debug/app-debug.apk`: `ce9f08aa5a2f13f481811db4688db8b45a7860f937f638e17c5f9f3279256db9`.
  - `python3 scripts/check_android_tflite_contract_20260531.py`: ok=true, unified asset missing 및 Android/backend threshold drift warning.
  - `python3 scripts/check_android_depth_scaffold_20260531.py`: PASS.
  - `npm run typecheck`: PASS.
  - `npm run lint`: FAIL, `apps/web/app/page.tsx`의 `react-hooks/set-state-in-effect`.
  - `python3 -m pytest`: system Python에는 pytest 없음.
  - `.venv/bin/python -m pytest tests/test_aihub189_depthprediction_offline.py backend/tests/test_report_policy.py backend/tests/test_android_debug_logs.py`: AIHub189 test collection 실패. Python 3.14에서 동적 import 모듈이 `sys.modules`에 등록되지 않아 dataclass 처리 중 실패.
- 주요 gap:
  - Android `/reports/v2`는 multipart 전송 코드가 있으나 `captured_at` 값이 wall-clock UTC가 아니라 ARCore frame timestamp 기반이다.
  - report 후보 권한 요청이 damaged tactile prefilter 전에 위치 권한을 요구할 수 있다.
  - heading은 수집되지만 Android report metadata JSON에 `heading`을 넣지 않는다.
  - unified TFLite asset은 없고 legacy fallback 상태다.
  - fallback reason/model fallbackUsed는 report/debug metadata에 충분히 남지 않는다.
  - damaged tactile block report-only는 구현됐지만, 단일 `bestOutput` 기반이라 STOP/WARNING 객체가 선택되면 report-only 후보가 누락될 수 있다.
  - Web/admin Android source는 대부분 반영됐지만 cluster UI source counts와 전용 회귀 테스트가 부족하다.
  - TestCapturePanel/test-log는 기본 off와 consent/env gate가 있으나 auth optional, image 삭제 누락, metadata schema 검증 부족이 남아 있다.
  - AIHub189 evaluator는 대부분 구현됐지만 confidence disabled/unknown 상태 명시와 테스트 실행성, 다중 conf/inner zip 회귀 테스트가 부족하다.
- 제외 1~4는 완료로 올리지 않고 `BLOCKED`/`DEFERRED`/`OUT_OF_SCOPE`로 유지해야 함을 확인했다.

## WalkSafe 제외 1~4 외 필수 구현 보강

- 10개 병렬 에이전트 감사 결과를 바탕으로 Android report/runtime/evidence, backend reports, web/admin/test capture, AIHub189 offline evaluator의 gap을 보강했다.
- Android:
  - hazard-only 경로에서 report/location pre-request를 줄이고, route cancel 시 navigator/currentDestination을 clear해 hazard-only 상태로 복귀하도록 정리했다.
  - `/reports/v2` multipart upload 경로가 실제 후보 이미지 없이는 placeholder를 올리지 않도록 막고, HTTP error/cooldown/duplicate 상태를 UX status에 반영했다.
  - `captured_at`을 wall-clock UTC ISO-8601로 보내도록 `DetectionSnapshot.capturedAtMs` 기반으로 수정했다.
  - report/debug metadata에 heading, `fallback_used`, `loaded_model_key`, `model_load_reason`, runtime config `source_model` trace를 추가했다.
  - `AndroidRiskSelectionPolicy`를 추가해 STOP/WARNING 우선순위와 damaged tactile block report-only 후보 선택을 분리했다.
  - `AndroidFeedbackActuator`가 utterance 완료/오류/중지 시 audio focus를 release하도록 보강했다.
- Backend/web/admin:
  - `/reports/v2` metadata allowlist에 Android fallback/model load fields를 추가하고 raw debug payload는 계속 제외했다.
  - coordinate gate missing/pending/non-pass를 performance exclusion으로 처리했다.
  - backend bbox overflow 검증과 x/y overflow 회귀 테스트를 추가했다.
  - admin Android source summary/cluster counts와 reviewed/redacted damaged tactile agency export URL 정책 테스트를 보강했다.
  - `/api/walksafe-test-log`와 `TestCapturePanel`을 default-off, env/auth/consent, metadata/JPEG validation, delete path 기준으로 보강했다.
- AIHub189:
  - offline evaluator의 confidence policy를 `disabled_unknown_direction`으로 명시하고, inner zip cache가 전체 추출 없이 선택 읽기만 하도록 보강했다.
  - frame별 `.conf` converter cache, ZED depth conversion, bbox robust sampling, ceil step policy 테스트를 보강했다.
- Evidence:
  - `docs/walksafe_requirement_traceability_matrix_20260701.md`, `docs/current_status.md`, `docs/evidence/final_report_index.md`, `apps/android/README.md`, `docs/android/android_native_mvp_evidence_20260630.md`를 새 APK hash와 정지 smoke 범위로 갱신했다.
  - 새 APK SHA-256: `bcfb377e0a07c9b8dc0499a4fac3de0018985e51bdb651ee96f598cd1e53b6be`.
- 검증:
  - `cd apps/android && ./gradlew test assembleDebug --no-daemon`: BUILD SUCCESSFUL.
  - `python3 scripts/check_android_tflite_contract_20260531.py`: ok=true, `check_scope=static_contract_only`, `unified_asset_present=false`, `expected_runtime_model=legacy_two_model`, `expected_fallback=true`.
  - `python3 scripts/check_android_depth_scaffold_20260531.py`: PASS.
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py backend/tests/test_android_debug_logs.py backend/tests/test_reports_v2.py -q`: 8 passed, 32 skipped(PostGIS 미기동).
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/test_aihub189_depthprediction_offline.py -q`: 3 passed.
  - `bash scripts/check_frontend_admin_report_summary_policy_20260525.sh`: PASS.
  - `bash scripts/check_frontend_walksafe_test_log_policy_20260701.sh`: PASS.
  - `cd apps/web && npm run typecheck`: PASS.
  - `cd apps/web && npm run lint`: PASS.
  - `sha256sum apps/android/app/build/outputs/apk/debug/app-debug.apk`: `bcfb377e0a07c9b8dc0499a4fac3de0018985e51bdb651ee96f598cd1e53b6be`.
  - 정지 실기기 smoke(`R3CN50F4APH`, `SM-G981N`, Android 13): install Success, launch Status ok, PID 11927, `MainActivity` RESUMED/visible/reportedDrawn, AndroidRuntime:E empty.
  - `git diff --check`: PASS.
- 제외/남은 범위:
  - 실외 이동/현장 보행, route PASS, N보 실측 정확도, TTS/진동 체감은 제외.
  - unified TFLite 생성/학습 완료/final asset 투입은 제외.
  - 운영 승인/릴리즈 승인/운영 보안 완성은 제외.
  - PostGIS disposable DB 기동과 `skipped=0` no-skip 검증은 제외.

## WalkSafe 잔여 RTM 보강: accessibility, debug frame, PWA opt-in

- Android TalkBack/live accessibility 보강:
  - `MainActivity` status live region을 기본 `POLITE`로 두고 위험 안내 시 `ASSERTIVE`, 길안내 시 `POLITE`로 유지하도록 static test를 추가했다.
  - screen reader가 활성화된 경우 app TTS 중복 발화를 막고, 위험 상황의 haptic만 유지하도록 `AndroidFeedbackActuator.vibrateRiskOnly()`를 추가했다.
  - release build에서 raw frame capture 버튼이 보이지 않도록 `BuildConfig.DEBUG` gate를 추가했다.
  - route guide metadata에 `bearing_deg`, `turn_type`, `point_type`, `facility_type` 파싱을 추가하고 Android route client test로 고정했다.
- PWA root/demo 격리 보강:
  - `NEXT_PUBLIC_WALKSAFE_PWA_ENABLED=false`를 기본값으로 추가했다.
  - `usePwaStatus()`의 service worker registration을 `NEXT_PUBLIC_WALKSAFE_PWA_ENABLED=true` opt-in으로 제한했다.
  - PWA policy test에 service worker default-off/explicit opt-in 검증을 추가했다.
- RTM/evidence 갱신:
  - `WS-REQ-001`, `WS-REQ-010`, `WS-REQ-011`, `WS-REQ-012`, `WS-REQ-021`, `WS-REQ-022`, `WS-REQ-046` 상태를 최신 구현 증거에 맞춰 정정했다.
  - `WS-REQ-014`, `WS-REQ-016`은 final unified TFLite asset 이후 가능한 항목이므로 `BLOCKED`로 명확화했다.
  - 새 APK SHA-256: `39d79e8fc1254fa9a03bccf8c2a54f2208872e308bfa5a46678f037eedf07b31`.
- 검증:
  - `cd apps/android && ./gradlew test assembleDebug --no-daemon`: BUILD SUCCESSFUL.
  - `python3 scripts/check_android_tflite_contract_20260531.py`: ok=true, static contract only, unified asset missing/threshold drift warnings.
  - `python3 scripts/check_android_depth_scaffold_20260531.py`: PASS.
  - `adb -s R3CN50F4APH install -r apps/android/app/build/outputs/apk/debug/app-debug.apk`: Success.
  - `adb -s R3CN50F4APH shell am start -W -n kr.co.hanium.dreamup.walksafe/.MainActivity`: Status ok, cold launch, TotalTime 488, WaitTime 492.
  - `adb -s R3CN50F4APH shell pidof kr.co.hanium.dreamup.walksafe`: 14247.
  - `adb -s R3CN50F4APH shell dumpsys activity activities`: `MainActivity` RESUMED/visible/reportedDrawn.
  - `adb -s R3CN50F4APH logcat -d AndroidRuntime:E *:S`: no output.
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py backend/tests/test_android_debug_logs.py backend/tests/test_reports_v2.py -q`: 8 passed, 32 skipped(PostGIS 미기동).
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/test_aihub189_depthprediction_offline.py -q`: 3 passed, 6 warnings.
  - `cd apps/web && npm run typecheck`: PASS.
  - `cd apps/web && npm run lint`: PASS.
  - `./node_modules/.bin/tsc --ignoreConfig --ignoreDeprecations 6.0 --module CommonJS --target ES2022 --moduleResolution Node --jsx react-jsx --esModuleInterop --skipLibCheck --outDir /tmp/hanium_frontend_settings_policy_20260701 tests/settings-privacy.test.ts app/_walksafe/hooks/useWalkSafeSettings.ts`: PASS.
  - `NODE_PATH=/home/ddobagi/Code/hanium-dreamup/apps/web/node_modules node /tmp/hanium_frontend_settings_policy_20260701/tests/settings-privacy.test.js`: `settings privacy checks passed`.
  - `bash scripts/check_frontend_admin_report_summary_policy_20260525.sh`: PASS.
  - `bash scripts/check_frontend_walksafe_test_log_policy_20260701.sh`: PASS.
  - `bash scripts/check_frontend_pwa_policy_20260526.sh`: PASS.
  - `git diff --check`: PASS before daylog append.

## WalkSafe goal continuation: RTM gap closure follow-up

- 10개 읽기 전용 병렬 에이전트 감사 결과를 반영해 남은 code/test gap을 추가로 닫았다.
- Android:
  - `RouteNavigator`에 accuracy-overlap off-route candidate gate와 consecutive off-route sample confirmation을 추가했다.
  - `MessagePolicy`가 `DepthEstimator`의 `timeToCollisionMs`를 받아 접근 TTC 기반 WARNING/STOP 보조 경고를 내도록 연결했다.
  - `MainActivity.onPause()`에서 `AndroidFeedbackActuator.close()`를 호출해 background 전환 시 TTS/audio focus를 즉시 release하도록 보강했다.
  - `AndroidReportUploaderTest`, `MainActivityReportUploadStaticTest`를 추가해 multipart metadata/image part와 upload UX state token을 고정했다.
  - `AndroidReportCandidatePolicyTest`에 `source_model`과 `threshold_used` 직접 assert를 추가했다.
- Backend/web:
  - TMAP route normalizer가 guide point `bearing_deg`를 route polyline next segment에서 계산하도록 추가했다.
  - `WalkingRouteGuidePoint` schema와 backend/API docs에 `bearing_deg`를 추가했다.
  - Android debug frame capture endpoint의 default-disabled, metadata size limit, enabled storage/recent path 테스트를 추가했다.
  - Admin model filter에 `unified_walksafe`를 노출하고 admin policy script로 회귀 확인을 추가했다.
  - `/api/walksafe-test-log` env config를 request time에 읽도록 정리하고, default disabled/token-missing/metadata oversize/JPEG marker/image oversize/retention 테스트를 추가했다.
- Evidence/docs:
  - canonical APK SHA-256을 `2f6136c18e7f354e14164f189328db372645c794a73ddfb22219aa39137d275b`로 갱신했다.
  - `docs/walksafe_requirement_traceability_matrix_20260701.md`에서 제외 1~4 항목을 `BLOCKED`/`DEFERRED_DEVICE`/`OUT_OF_SCOPE`로 정리하고, `WS-REQ-026`, `WS-REQ-028`, `WS-REQ-044`, `WS-REQ-054` 등 완료 가능한 항목은 최신 증거로 정정했다.
  - `docs/walksafe-v2/android_report_source_metadata_decision_20260601.md` allowlist 문서에 `fallback_used`, `loaded_model_key`, `model_load_reason`을 추가했다.
- 검증:
  - `cd apps/android && ./gradlew test assembleDebug --no-daemon`: BUILD SUCCESSFUL.
  - `sha256sum apps/android/app/build/outputs/apk/debug/app-debug.apk`: `2f6136c18e7f354e14164f189328db372645c794a73ddfb22219aa39137d275b`.
  - `python3 scripts/check_android_tflite_contract_20260531.py`: ok=true, static contract only, unified asset missing/threshold drift warnings.
  - `python3 scripts/check_android_depth_scaffold_20260531.py`: PASS.
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py backend/tests/test_android_debug_logs.py backend/tests/test_reports_v2.py backend/tests/test_navigation_routes.py -q`: 24 passed, 32 skipped(PostGIS 미기동).
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/test_aihub189_depthprediction_offline.py -q`: 3 passed, 6 warnings.
  - `cd apps/web && npm run typecheck`: PASS.
  - `cd apps/web && npm run lint`: PASS.
  - `bash scripts/check_frontend_admin_report_summary_policy_20260525.sh`: PASS.
  - `bash scripts/check_frontend_walksafe_test_log_policy_20260701.sh`: PASS.
  - 정지 실기기 smoke(`R3CN50F4APH`, `SM-G981N`, Android 13): install Success, `am start -W` Status ok/COLD/TotalTime 478/WaitTime 482, PID 15524, `MainActivity` RESUMED/visible/reportedDrawn, AndroidRuntime:E empty.
- 제외/남은 범위:
  - 실외 이동/현장 보행, outdoor route PASS, N보 실측 정확도, TTS/진동 체감 PASS는 제외 1.
  - final unified TFLite 생성/학습 완료/final asset Android 투입 완료 판정은 제외 2.
  - 운영 승인/릴리즈 승인/비밀값/외부 제출/auth/RBAC/rate-limit/CSRF/signed URL은 제외 3.
  - PostGIS disposable DB와 `skipped=0` no-skip 검증은 제외 4.

## WalkSafe policy decision board

- 사용자 정책 결정을 돕기 위해 `/home/ddobagi/Downloads/abc.html`을 작성했다.
- Android, Backend/Admin, Web/PWA/TestCapture, Model/AIHub189, Privacy/Ops 축으로 현재 임시 정책값, 결정 선택지, 영향, 근거 문서를 정리했다.
- 5개 읽기 전용 하위 에이전트 조사 결과를 반영해 Android cooldown/duplicate, coordinate gate PASS 표현 주의, TestCapture retention/consent, agency export 품질 조건, unified asset/fallback, AIHub189 reference 한계를 보강했다.
- 변경 파일:
  - `/home/ddobagi/Downloads/abc.html`
  - `daylog/2026-07-01.md`
- 검증:
  - `ls -lh /home/ddobagi/Downloads/abc.html`: 52K HTML 생성 확인.
  - `wc -c /home/ddobagi/Downloads/abc.html`: 52368 bytes.
  - `rg -n "WalkSafe 정책 결정 보드|Android 정책|Backend/Admin 정책|Web/PWA/TestCapture 정책|Model Runtime / AIHub189 정책|Privacy / Ops 정책|duplicate-check|performance_excluded=false|POST_NOTIFICATIONS|표현 주의" /home/ddobagi/Downloads/abc.html`: 핵심 섹션/보강 문구 확인.
  - `python3 -c "from html.parser import HTMLParser; HTMLParser().feed(open('/home/ddobagi/Downloads/abc.html', encoding='utf-8').read()); print('html_parse_ok')"`: `html_parse_ok`.

## WalkSafe goal continuation: route-bearing risk context and refreshed smoke

- Android route/risk 보강:
  - `MotionContext`에 `routeAlignmentQuality`와 route bearing/heading alignment quality 계산을 추가했다.
  - `ObjectDepthRuntimePipeline.process()`가 motion context를 받아 `ObjectDepthEstimator` confidence path로 전달하도록 연결했다.
  - `MainActivity`가 active route의 `RouteNavigator.currentBearingDeg()`와 최신 heading을 기반으로 depth motion context를 구성하도록 연결했다.
  - `RouteNavigator`가 현재 guide bearing을 노출하도록 추가했다.
- 테스트:
  - `MotionContextTest`를 추가해 unknown heading/bearing neutral 처리, wrap-around, route alignment gate를 고정했다.
  - `ObjectDepthRuntimePipelineTest`에 motion context가 depth confidence로 전달되는지 검증을 추가했다.
  - `RouteNavigatorTest`에 current route bearing 노출 테스트를 추가했다.
- Evidence/docs:
  - canonical APK SHA-256을 `1f5b2020053d755e6f52054453c82bd2eb45d3bd6ade3740a8d9bfa2f35967c9`로 갱신했다.
  - `docs/walksafe_requirement_traceability_matrix_20260701.md`의 `WS-REQ-018`을 route-bearing motion context 보강 근거로 `DONE` 정정했다. field validation은 제외 1로 유지했다.
  - `docs/current_status.md`, `docs/evidence/feature_matrix.md`, Android evidence 문서에 route-bearing motion context와 새 smoke 결과를 반영했다.
- 검증:
  - `cd apps/android && ./gradlew testDebugUnitTest --no-daemon`: BUILD SUCCESSFUL.
  - `cd apps/android && ./gradlew test assembleDebug --no-daemon`: BUILD SUCCESSFUL.
  - `sha256sum apps/android/app/build/outputs/apk/debug/app-debug.apk`: `1f5b2020053d755e6f52054453c82bd2eb45d3bd6ade3740a8d9bfa2f35967c9`.
  - `python3 scripts/check_android_tflite_contract_20260531.py`: ok=true, static contract only, unified asset missing/threshold drift warnings.
  - `python3 scripts/check_android_depth_scaffold_20260531.py`: PASS.
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py backend/tests/test_android_debug_logs.py backend/tests/test_reports_v2.py backend/tests/test_navigation_routes.py -q`: 24 passed, 32 skipped(PostGIS 미기동).
  - `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -p no:cacheprovider tests/test_aihub189_depthprediction_offline.py -q`: 3 passed, 6 warnings.
  - `cd apps/web && npm run typecheck`: PASS.
  - `cd apps/web && npm run lint`: PASS.
  - `bash scripts/check_frontend_admin_report_summary_policy_20260525.sh`: PASS.
  - `bash scripts/check_frontend_walksafe_test_log_policy_20260701.sh`: PASS.
  - `bash scripts/check_frontend_pwa_policy_20260526.sh`: PASS.
  - 정지 실기기 smoke(`R3CN50F4APH`, `SM-G981N`, Android 13): install Success, `am start -W` Status ok/COLD/TotalTime 480/WaitTime 484, PID 16571, `MainActivity` RESUMED/visible/reportedDrawn, AndroidRuntime:E empty.
- 제외/남은 범위:
  - 실외 이동/현장 보행, outdoor route PASS, N보 실측 정확도, TTS/진동 체감 PASS는 제외 1.
  - final unified TFLite 생성/학습 완료/final asset Android 투입 완료 판정은 제외 2.
  - 운영 승인/릴리즈 승인/비밀값/외부 제출/auth/RBAC/rate-limit/CSRF/signed URL은 제외 3.
  - PostGIS disposable DB와 `skipped=0` no-skip 검증은 제외 4.
