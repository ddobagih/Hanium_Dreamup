# 2026-07-25 작업 로그

## FP-006 staged suffix 중단 복구 준비

- 터미널 중단 뒤 v2.3 checkpoint와 FP-006 완료 증거를 다시 감사했다. sequence 1~8은 확정된 이력으로 byte-exact 보존하며, FP-006 완료와 FP-010 준비를 담은 sequence 9~12는 외부 commit·snapshot으로 고정되지 않은 staged suffix임을 확인했다.
- sequence 12 후보를 만든 뒤 현재 상태 회귀 두 파일이 보완되어 기존 FP-006 working snapshot과 완료 증거가 stale해진 원인을 확인했다. 제품 구현 결함이 아니라 제어 회귀 증거의 최종 byte 변경이며, 테스트를 되돌리거나 검사를 우회하지 않는다.
- 사용자의 명시 승인에 따라 기존 후보를 `/tmp` forensic bundle로 보존하고, 오늘자 daylog와 최종 제어 파일을 포함한 새 증거를 실제 시각으로 다시 생성한 뒤 sequence 9~12만 원자 재봉인한다. 실패 시 live 파일 전체를 원래 byte로 복구한다.
- 이 시점에는 FP-010 시작 Gate나 제품 구현을 실행하지 않았다. FP-006 재봉인과 전체 Goal graph·continuation 회귀가 통과한 뒤에만 다음 동적 Goal을 시작한다.
- 정식 시험 279개, 실제 기기·대상 사용자·현장 시험, 5개 출시 Gate는 계속 `NOT_RUN`이며 출시는 `NOT_ELIGIBLE`이다.

## FP-006 복구와 FP-010 시작

- FP-006 완료 증거와 r013 Gap·Backlog, FP-010 Goal, checkpoint를 sequence 9~12로 원자 재봉인하고 Goal graph·continuation 검사를 통과시켰다.
- 첫 FP-010 시작 Gate는 FP-006 장착 정적 시험이 active-session test layer에 배정되지 않아 검사 15에서 실패했다. 실패 receipt와 원출력은 보존했다.
- `scripts/run_walksafe_test_layers_20260711.sh`에 누락 시험을 배정한 뒤 새 Gate `...FP010-20260725-002`의 19개 검사를 모두 통과시키고 sequence 13 `GOAL_STARTED`를 설치했다.

## FP-010 첫 실행 가입 내부 구현

- `FirstRunOnboardingPolicy`에 목적·안전, 연령 구간, 통합 동의, 불투명 가입 제출, SMS 확인, 14~17세 보호자 확인, 활성화, 확인 로그인, JIT 권한 관찰, 기기점검, FP-004 훈련 순서의 불변 상태기계를 추가했다.
- epoch·revision·request·attempt 결속, 중복·늦음·역순·취소·재시작 거부, 검증된 receipt prefix만 복원하는 fail-closed 계약을 추가했다.
- 운영 검증기는 미설정 상태에서 항상 거부한다. 원 비밀번호·OTP·전화번호·전화 hash·정확 생년월일·장애유형은 모델링하거나 저장하지 않는다.
- `MainActivity`에서 첫 실행 완료 전 보행 시작·재개·목적지·경로·신고와 카메라·마이크·위치·출력을 차단했다. DEVICE_CHECK 사전점검은 별도 lease로 격리하고 공식 환경·장착 증거를 오염시키지 않게 했다.
- 레거시 reporter ID를 복원하지 않고 저장값을 폐기했으며, 확인된 actor만 FP-004 훈련과 기존 정책에 연결했다. FP-004 재교육 전환은 로그인 generation·세션·출력을 함께 폐기한다.
- TalkBack POLITE 상태, heading, 논리적 탐색 순서, 48dp 조작 영역을 추가하고 화면읽기 사용 시 비긴급 TTS 중복을 막았다.
- 독립 코드·개인정보·비동기 관문 검토 결과의 식별자 형식, 불변 map, visibility, DEVICE_CHECK 격리, gateway stale-auth 경합을 모두 보완했다. 최종 독립 판정은 차단·중대·중간 발견 0건 `PASS`다.

## 검증과 제어 전이

- 집중 Android 회귀를 실행해 `BUILD SUCCESSFUL`을 확인했다.
- Android 전체 단위시험 563개, debug APK 조립, lint를 실행해 모두 통과했다. APK SHA-256은 `4704574cc466609ebe4d8f5320cd3daa496c76539282e7e43db1b6f04c1f8249`다.
- FP-010 trace builder와 v2.3 sequence 14~17 후보·변조 회귀 20개를 실행해 모두 통과했다.
- 11개 FP-010 결과·r014·overlay 산출물을 생성하고 `GAP-019`만 `MISSING → PARTIAL`로 재평가했다. 집계는 `MISSING 15 / PARTIAL 26 / IMPLEMENTED 0`이다.
- sequence 14 `CANONICAL_BINDINGS_UPDATED`, 15 FP-010 `GOAL_COMPLETED`, 16 FP-011 `GOAL_MATERIALIZED`, 17 FP-011 `GOAL_READY`를 원자 설치하고 전체 Goal graph·continuation 검사를 통과시켰다.
- 다음 Goal은 `WS-GOAL-EPIC-02-FP-011-R001`, 정책·Gap은 `FP-011 / GAP-020`이다.

## 변경·증거 경로

- Android: `MainActivity.kt`, `FirstRunOnboardingPolicy.kt`, `WalkSessionReadiness.kt`와 대응 단위·정적 회귀.
- 제어 자동화: FP-010 trace builder·테스트, v2.3 Goal graph·continuation 회귀와 test-layer registry.
- 증거: `docs/control/execution/goal-results/WS-GOAL-EPIC-02-FP-010-R001/`, r014 Gap·Backlog, FP-010 active overlay, FP-011 Goal, continuation checkpoint.
- 외부 signup·SMS·guardian 공급자, verified-prefix 운영 저장소, 실제 사용자 TalkBack, 실제 기기, `TC-FP-010-01`~`04`, 정식 279개와 5개 Gate는 `NOT_RUN`이다. 출시는 `NOT_ELIGIBLE`이며 `GAP-019`는 최대 `PARTIAL`이다.

## v2.4 Goal successor 후보 준비

- v2.3 checkpoint `7f62b099...67b6`, manifest `dfa61568...329c`, 17개 이력과 tail `bc71126a...01d6`을 byte-exact 동결 경계로 보존했다.
- v2.4 builder·continuation checker·Goal checker와 현재/동결 이력 테스트를 추가하고, 실행 계약을 quick 2개·full 19개로 고정했다. 승인 파일은 정확한 manifest·`PACKAGE_PREPARED` seq1 해시에 결속하며, 실제 승인 SHA 자리표시는 활성화 직전까지 미확정으로 유지한다.
- v2.2 trace 이력 어댑터가 v2.4 현재 checkpoint를 v2.3 규칙으로 해석하던 오류를 수정했다. SHA가 고정된 v2.3 archive를 사용하고, 이 후속 제어 파일 변경만 경로·SHA가 일치할 때 허용하도록 좁게 결속했다.
- 변경 범위: `scripts/build_walksafe_goal_graph_v2_4.py`, `scripts/check_walksafe_project_continuation_v2_4.py`, `scripts/check_walksafe_goal_graph_v2_4.py`, v2.4 및 v2.2/v2.3 history 테스트, `scripts/run_walksafe_test_layers_20260711.sh`, control/runbook·README 문서, v2.4 Goal package 6개 파일, continuation checkpoint.

## v2.4 합성 전이와 실제 seq1 설치 검증

- 격리 합성 환경에서 seq1 → quick 2/2 → seq2 `PACKAGE_ACTIVATED` → full 19/19 → seq3 `GOAL_STARTED(FP011)` 전이를 완료했다. full 영수증 SHA-256은 `82d6c2a2b94e552728efe3011dc906ee65691fa654b6e8ede75eaf70b3f06533`, seq3 event SHA-256은 `159a7d5b74a13960acda662d0c1426caba54cc0f6d6b15c29bac54dcdc76df3d`다.
- seq2 활성화 이벤트와 seq3 시작 이벤트에 임의 필드를 넣고 해시 체인을 다시 계산한 후보가 각각 exact-field 검사에서 거부되는 것을 확인했다.
- `/tmp` 쿼터를 확보하기 위해 과거 WalkSafe 임시 검증 복제본 약 3.7GB를 삭제하지 않고 `/home/ddobagi/.cache/walksafe-v24-prior-tmp-archive.AuN8tB`로 이동했다.
- 실제 저장소에는 비활성 seq1 후보만 설치했다. manifest SHA-256은 `7325de1f413423dff7c19390b85b489c981f46511464ca81969e226ca8908b07`, seq1 event SHA-256은 `58c2b31637b5355609db14bf9e0779d4fd33d99f97a692866718a51fa64875d9`다.
- 실제 설치본 검증: builder/checker 3종 PASS, Goal 제어 `490 passed, 3 skipped`, control·trace `150 passed, 6 deselected`, test-layer registry validate PASS, Python 9개 파일 구문 검사와 shell 구문 검사 PASS.
- 실제 상태는 `READY_NOT_ACTIVATED / PREPARED_NOT_ACTIVATED`, focus `WS-GOAL-EPIC-02-FP-011-R001` `READY`다. 실제 v2.4 활성화 증거와 seq2·seq3 전이는 생성하지 않았으며, 위 manifest·seq1에 byte-exact 결속된 사용자 승인 문구를 기다린다.
