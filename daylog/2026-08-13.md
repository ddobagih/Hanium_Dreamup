# 2026-08-13 WalkSafe 작업 로그

## 재개와 목표

- 강제 종료된 Codex 세션, local-memory, 정본 checkpoint와 미커밋 작업 트리를 대조해 `WS-GOAL-EPIC-03-NPC-SINGLE-ADMIN-RECOVERY-R001`의 R003 완료 통제 보강 직전으로 재개했다.
- 기존 방식대로 기능 구현·내부 검증·산출물 동기화·별도 적대 검수·완료 전환을 순서대로 수행하고, 정식 시험·실기기·외부 검토·배포·출시 credit는 부여하지 않는다.

## NPC 완료 통제 보완

- 중단 직전 9개 연계 회귀를 동일 명령으로 재실행해 318 PASS·25 FAIL을 복원했다. 실패를 완료 전환 전 정본 불일치, R027의 과거 제품 실행 closure 재실행 테스트 결함, Goal graph synthetic fixture 결함으로 분류했다.
- `backend/tests/test_admin_runtime_acl_hardening.py`를 현재 Python unit test layer에 등록하고 layer registry 검사를 통과시켰다.
- R027 live 회귀는 R002 이후 바뀐 completion-control 파일 때문에 과거 제품 producer를 재실행하지 않고, R002에 동결된 네 R027 산출물의 실제 bytes/SHA를 직접 대조하도록 교정했다.
- R003 증거 reader를 단일 `openat(O_NOFOLLOW)` descriptor의 `fstat → read → fstat` 경계로 바꾸고 owner·gid·link count·ctime·inode와 경로 entry를 함께 검증한다.
- R003 게시기는 checkpoint lock을 최종 authority 재구성과 add-only 게시 전체에 유지한다. Goal graph 완료 검사와 managed closure도 R003 최종 출력·필수 이력을 같은 authority reader로 다시 검증한다.
- 정상 seq61/62 synthetic fixture가 새 managed-closure 전제만 명시적으로 격리하도록 테스트를 고쳤다.

## 현재 검증

- test layer registry `validate`: PASS.
- R003 strict review, R027, NPC Goal graph successor 대상 회귀: 80 PASS.
- descriptor 안정성·hardlink·게시 lock 대상 회귀와 관련 Goal graph 테스트: 43 PASS.
- `git diff --check`: PASS.
- 정식 시험, 실제 기기·현장 recovery drill, 외부 법률·보안·접근성 검토, production deployment와 release gate는 계속 `NOT_RUN`; release는 `NOT_ELIGIBLE`이다.

## R003 독립 검수와 완료 영수증

- R003 assignment를 최종 동결된 138개 evidence, 27개 control, 20개 immutable history, R002 원본 4개에 결속해 add-only로 발행했다.
- 별도 reviewer가 R001 disposition 9건과 추가 적대 감사 10건을 독립 확인했다. blocking·major·minor open finding 없이 `APPROVED`했으며, 기존 R001/R002 산출물은 수정하지 않았다.
- locked Python 3.12.13 환경에서 R003 직접 범위 197 PASS·2 deselected, 연계 trace/artifact/R016 49 PASS를 확인했다.
- strict gate의 `--check-review-result`, `--write-post-review`, `--check-post-review`가 모두 PASS했다. R003 independent review와 `completion-receipt-v3.json`을 add-only로 발행했으며 영수증 상태는 `ACCEPTED/PASS`이다.

## Seq61/62 전환 후속 통제

- 첫 projected seq62 검사에서 과거 FP047 종료 digest와 FP046 시작 digest 사이의 정식 start-snapshot overlay를 일반적인 불연속 edge로 오인하는 fail-closed 회귀를 발견했다. 봉인된 FP047 start snapshot이 FP046 최종 digest로 정확히 연결되는 경우만 허용하고, 임의 endpoint·무관 edge·predecessor 불일치는 계속 거부하도록 Goal graph와 회귀 테스트를 보완했다.
- R004는 새 테스트의 layer 미등록, R005는 mutable R003 context 재계산, R006은 R003 이후 control digest 합성 누락, R007은 final-managed-closure omission 테스트 fixture의 후속 권한 누락을 각각 검수 전에 발견했다. 네 assignment는 add-only 이력으로 보존하고 reviewer result/independent review는 발행하지 않은 채 `SUPERSEDED_BEFORE_REVIEW`로 다음 round에 결속했다.
- 완료 소비자는 R003 당시 증거를 exact frozen context로 재구성하고, 후속 round의 최신 control cohort로 겹치는 digest를 대체한다. final-managed closure에는 대체된 assignment들과 최종 후속 review 3개 산출물을 모두 포함하며, 하나라도 누락된 self-consistent snapshot도 거부한다.
- R008 최종 제어 범위에 Goal graph·seq61/62 적용기·layer registry·R004~R008 validator와 테스트를 포함했다. locked Python 3.12.13/pytest 8.4.2에서 관련 57 PASS, layer registry `validate` PASS, `py_compile`과 `git diff --check` PASS를 확인했다.
- R008 assignment를 최종 bytes에 add-only로 고정하고 별도 reviewer가 15개 control cohort와 projected seq62를 재검수했다. 5개 finding을 모두 닫아 `APPROVED` result/independent review를 발행했으나, 실제 catalog refresh가 R004 후속 검수 스크립트의 lifecycle 미분류를 발견해 checkpoint 전환은 수행하지 않았다.
- R008 승인 결과는 exact predecessor로 동결했다. Repository catalog allowlist에 R004~R009 review builder를 `CURRENT / REPOSITORY_WRITE`로 명시하고 저장 catalog 검증을 추가했으며, R009가 R008의 assignment/result/independent bytes와 R004~R007 대체 이력을 재생하도록 구성했다.
- R009 전 제어 회귀는 관련 60 PASS, 여섯 review builder의 직접 lifecycle/side-effect 검사 PASS, layer registry `validate` PASS, `py_compile`과 `git diff --check` PASS다. 다음 단계는 R009 독립 검수 후 catalog refresh와 preflight를 다시 수행하고, 모두 통과한 경우에만 seq61/62를 원자 게시하는 것이다.
- R009 독립 검수는 `APPROVED`, catalog refresh와 별도 preflight는 PASS했다. Live checkpoint가 seq60인 동안 projected seq62 catalog를 읽는 catalog 단위 테스트 2건은 원자 전환 중간창의 예상 불일치로 판정했고, production 검사는 완화하지 않았다.
- 두 차례 `--write`는 checkpoint transport가 같은 디렉터리에 만든 staging 파일을 catalog source universe가 새 Git-visible 파일로 감지해 commit guard에서 안전하게 중단됐다. 두 시도 모두 checkpoint 원본 hash와 seq60 상태를 보존했다.
- Commit guard 안에서만 정확한 transport staging 이름·현재 사용자 소유·0600 mode·단일 link·source/projected exact bytes를 검증해 source universe와 managed path 집합에서 제외하도록 최소 보완했다. 변조 bytes나 stage 2개 이상은 계속 fail-closed다.
- R010은 R008과 R009 승인 산출물 6개를 exact predecessor로 재생하고 이 transport 보완을 최종 검수한다. 현재 관련 회귀 65 PASS, R004~R010 lifecycle/side-effect 직접 검사 PASS, layer registry `validate`, `py_compile`, `git diff --check` PASS다.
