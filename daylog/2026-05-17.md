# 2026-05-17

## 확인한 근거
- `daylog/2026-05-17.md`: 기존 미추적 daylog 본문 확인 후 갱신 기준으로 사용.
- `README.md`, `plans/daily/2026-05-17.md`, `plans/catchup/2026-05-17.md`, `plans/catchup/2026-05-17-execution-r1.md`
- `plans/.work/2026-05-17/execute-r1/*.md`
- `docs/execution/2026-05-17_integration_field_report.md`
- `docs/execution/2026-05-17_model_data_mlops.md`
- `daylog/2026-05-16.md`, `git diff -- daylog/2026-05-16.md`
- `find .. -name AGENTS.md -print`: 저장소 내부 `AGENTS.md` 없음. 사용자 제공 AGENTS 지침 적용.
- `git status --short --branch --untracked-files=all`: `main...origin/main`, `HEAD=36d9e2b`, 수정/미추적 파일 존재.
- `git diff --stat`, `git diff --name-status`, 파일별 `git diff`, `git diff --check`
- 이번 daylog 통합에는 새 하위/병렬 에이전트를 사용하지 않았다.

## 오늘 한 일
- 2026-05-17 r1 실행 note 5개와 현재 git status/diff를 대조해 실제 수행/변경/검증 결과를 통합했다.
- Backend/PostGIS/API:
  - Python 3.14/AnyIO 환경에서 `FastAPI TestClient`, sync endpoint, `StaticFiles`, threadpool 경로가 hang 되는 문제를 backend 범위에서 우회/수정했다.
  - DB dependency와 read/update endpoint를 async 경로로 조정하고, `/detect` 모델 추론 호출에서 AnyIO threadpool 사용을 제거했다.
  - `/uploads/{filename}`을 `StaticFiles` mount 대신 직접 응답 라우트로 교체했다.
  - backend 테스트를 `httpx.ASGITransport` 기반 `ASGITestClient`로 전환하고 업로드 라우트 회귀 테스트를 추가했다.
- PWA/Accessibility/Sensors:
  - `apps/web` 코드 변경은 없었다.
  - PWA 정적 검증, server-mode build, `scripts/check_pwa_server_e2e.py` 문법 검증이 실행된 것으로 확인했다.
  - Android 실폰, 목걸이 착용, PWA install/offline, TalkBack, `/admin` 수동 흐름은 환경 제약으로 미완료다.
- Vision Model/Data/MLOps:
  - `best.pt`와 `best.onnx` hash, 대형 산출물 Git 미추적 상태를 재확인했다.
  - ONNX full metric equivalence를 test split 전체 `2,347`장으로 실행했고 계획 기준 내 통과로 판정했다.
  - test split 120장 PT/ONNX latency를 측정했다. ONNX CPU p95는 서버 후보로 가능하지만 PT보다 빠르지는 않아 backend ready artifact는 `.pt` 유지로 판단했다.
  - VL1+VS1 hard-negative 200장 subset을 생성해 inference를 실행하고 FP 지표를 산출했다.
  - 기존 failure 후보 40행을 contact sheet로 시각 검수해 v3 curation action을 붙였다.
- Voice STT/TTS:
  - 기본 Qwen3 TTS model id가 네트워크 없는 환경에서 Hugging Face metadata 조회로 실패하던 문제를 수정했다.
  - 로컬 Hugging Face cache snapshot이 있으면 모델 로드 경로만 snapshot으로 대체하고, cache key와 응답 model id는 기존 기본값 기준을 유지하도록 했다.
  - PWA 기준 TTS 7문구 cache를 생성/확인했다.
  - STT intent dry-run과 실제 사람 음성 샘플 8개를 2026-05-17 산출물로 재실행했다.
- Integration/Field Test/Report:
  - 통합 실행 환경을 확인했다. `NEXT_PUBLIC_*`, `MODEL_*` env는 미설정, Docker socket 권한 없음, `adb` 미설치, 포트 조회 권한 제한 상태였다.
  - headless server-mode E2E 재시도는 sandbox loopback 연결 제한으로 `/health` 접근 단계에서 실패했다.
  - stale 상태 문서에 2026-05-17 최신 보정을 추가해 fake/server/model/field 근거를 분리했다.
- git commit/push는 수행하지 않았다.

## 변경 파일
- Backend:
  - `backend/app/database.py`
  - `backend/app/detector.py`
  - `backend/app/main.py`
  - `backend/tests/asgi_client.py`
  - `backend/tests/test_detect.py`
  - `backend/tests/test_reports.py`
  - `backend/tests/test_uploads.py`
- Voice:
  - `voice/tts.py`
- 문서:
  - `README.md`
  - `docs/current_status.md`
  - `docs/model_integration_plan.md`
  - `docs/neck_worn_phone_test_checklist.md`
  - `docs/pwa_backend_status.md`
  - `docs/execution/2026-05-17_integration_field_report.md`
  - `docs/execution/2026-05-17_model_data_mlops.md`
  - `daylog/2026-05-17.md`
- 계획/실행 note:
  - `plans/catchup/2026-05-17.md`
  - `plans/catchup/2026-05-17-execution-r1.md`
  - `plans/daily/2026-05-17.md`
  - `plans/.work/2026-05-16/daily/*.md`
  - `plans/.work/2026-05-17/catchup/*.md`
  - `plans/.work/2026-05-17/execute-r1/*.md`
- 현재 작업트리에는 `daylog/2026-05-16.md` 수정도 남아 있다. 이번 통합에서는 기존 변경으로 확인하고 되돌리지 않았다.
- Git 추적 제외 로컬 산출물 확인:
  - `outputs/voice/stt_intent_dry_run_2026-05-17.csv`
  - `outputs/voice/stt_myvoice_results_2026-05-17.csv`
  - `outputs/voice/cache/qwen3_tts_*.wav`
  - `runs/benchmark/walksafe_kr_v2_test_latency_20260517/summary.json`
  - `runs/validation/aihub513_vl1_vs1_tactile_hard_negative_200_20260517/**`
  - `runs/validation/aihub513_vl1_vs1_tactile_hard_negative_200_20260517_inference/**`
  - `runs/failure_sampling/walksafe_kr_v2_test_subset_20260516/manual_review_20260517*`
  - `runs/failure_sampling/walksafe_kr_v2_test_subset_20260516/review_sheets_20260517/*.jpg`

## 검증
- 이번 daylog 갱신 중 직접 확인:
  - `git status --short --branch --untracked-files=all`: 수정/미추적 파일 존재 확인.
  - `git diff --stat`, `git diff --name-status`, 파일별 `git diff`: 변경 범위 확인.
  - `git diff --check`: PASS.
- r1 실행 note 기준 확인된 검증:
  - Backend: `py_compile` PASS, `pytest backend/tests/test_detect.py -q -rs` `11 passed`, `pytest backend/tests/test_uploads.py -q -rs` `2 passed`, `pytest backend/tests -q -rs` `13 passed, 17 skipped`. skip 사유는 PostGIS test DB 미접속.
  - Backend ASGI smoke: `/detect/health` unavailable/ready, `best.pt` resized image `/detect` `200`, detection `3`, first `source=server`, upload negative 5종 확인.
  - PWA: `node --check apps/web/public/sw.js`, `npm run lint`, `npm run typecheck`, `npm run build`, server-mode build, `py_compile scripts/check_pwa_server_e2e.py` PASS.
  - Model/Data: PT/ONNX test split 전체 metric equivalence 기준 통과. PT `mAP50-95 0.481`, ONNX `mAP50-95 0.482`.
  - Model/Data latency: PT p95 `12.6493ms`, ONNX CPU p95 `58.1289ms`.
  - Hard-negative: VL1+VS1 200장 기준 conf `0.25` FP image `32/200`, conf `0.35` FP image `21/200`.
  - Voice: Python compile PASS, STT dry-run 8개 success, 실제 사람 음성 8개 success, avg `1.807s`, max `2.048s`, TTS 7문구 두 번째 요청 cache hit 확인.
  - Integration: `scripts/check_pwa_server_e2e.py --web-mode dev ...`는 FAIL. `127.0.0.1:8000/health` 연결이 sandbox에서 `Operation not permitted`로 차단됨.

## 리스크/확인 필요
- Docker socket 권한 없음과 PostGIS 접근 불가로 reports 성공 생성, duplicate/radius, Alembic runtime, row/upload cleanup은 미확인이다.
- sandbox local TCP 제한으로 curl 기반 실제 HTTP smoke와 server-mode headless E2E는 통과 처리하지 않았다.
- Android 실폰 카메라/GPS/방향/진동/TTS/마이크, 목걸이 착용, PWA 설치/offline shell, TalkBack 점검은 수동 검증 대기다.
- fake 신고 생성 후 `/admin`에서 이미지/source/duplicate/status 변경 확인은 미실행이다.
- 브라우저/실폰 마이크 STT E2E, PWA CORS, TTS 청취 평가, 휴대폰 스피커 명료도/시작 지연 평가는 미실행이다.
- VL1+VS1 전체 `1,038`장 hard-negative inference는 보류했다. 200장에서 명확한 FP 후보가 나왔고 전체 확장은 약 5GB 이상 추가 복사가 필요하다.
- full test split failure sampling 재시도는 미실행이다. 기존 `exit code 137` 회피용 streaming/저장량 제한 방식이 필요하다.
- browser/ONNX Runtime Web latency와 class `1..3` 한국 GT 기반 4-class metric은 미실행이다.
- contact sheet 검수는 빠른 triage이며 정식 개인정보/위치정보 비식별 검수는 아니다.
- v2 모델은 class `0 damaged_tactile_block` baseline이다. 4-class 서비스 성능 근거로 쓰지 않는다.
- `서울역으로 안내해줘`는 여전히 `unknown`이다. `set_destination` 확장 여부는 제품 판단이 필요하다.

## 병렬 에이전트 활용 메모
- 이번 통합 daylog 작성에는 새 하위/병렬 에이전트를 사용하지 않았다.
- r1 lane별 실행 note가 이미 수행/변경/검증/미완료를 분리해 담고 있어 직접 대조했다.