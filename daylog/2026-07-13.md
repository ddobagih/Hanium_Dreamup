# 2026-07-13

## WalkSafe RC1 product and release hardening

- 기준 commit `f72304e`에서 전용 worktree/branch를 만들고, 기존 dirty worktree는 수정하지 않았다.
- Web의 음성 fallback, server-v2 개인정보 gate, single-instance gateway/session·login limiter 경계를 보강했다.
- Backend의 OpenAPI auth 계약, remote DB TLS, readiness 비차단 처리, 민감 조회 audit, report image crash reconciliation, shared rate-limit 정리를 구현했다.
- Voice의 인증·업로드 자원 제한·disconnect 취소·global admission과 immutable model snapshot 검증을 보강했다.
- Android의 cancellable gateway, report consent, capture-frame 결합 tactile route supplier를 production 경로에 연결했다.
- Web/Android/Backend/Voice/migration/config/SBOM을 source-bound하게 묶는 deterministic Full-RC builder·validator, structured quality receipt, operator attestation gate를 추가했다.
- 제출 PNG 의미값을 canonical facts에 결합하고, runtime 의미·원자 promotion·privacy receipt 경계를 보강했다.
- 작업 범위는 `1c96d1d`부터 `1bb9a62`까지의 커밋으로 분리했다.

## Changed Files

- `apps/web/` gateway, privacy, speech, release evidence 코드와 테스트
- `apps/android/` network, report, tactile projection, build provenance 코드와 JVM 테스트
- `backend/` API, readiness, audit, persistence, limiter 코드·migration·테스트
- `voice/` server, model integrity, resource/rate policy와 테스트
- `scripts/` Full-RC, quality receipt, attestation, submission build/promotion 도구
- `tests/`, `docs/status/`, `docs/submission/`, `configs/` 관련 계약·근거·lock

## Verification

- 변경별 Web policy/typecheck/lint/build, Android JVM/Gradle, Backend·Voice·submission Python 회귀 테스트를 실행했다.
- 최종 release/submission 집중 테스트 131개가 통과했다.
- release hardening 독립 감사 두 회에서 P0/P1/P2가 모두 0이었다.
- 원본 worktree의 HEAD `052cbe2`, branch와 `--untracked-files=all` status hash를 기준값으로 보존했다.

## Remaining

- Web tactile 범위와 Android ARCore 미지원 기기 정책은 사용자 제품 선택이 필요하다.
- 최종 source freeze 뒤 full cleanroom suite, 두 clone 제출 재생성, 사람 visual receipt와 외부 signing/production 입력 검증이 남아 있다.
