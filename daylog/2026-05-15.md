# 2026-05-15

## 확인한 근거
- `daylog/2026-05-15.md`: 기존 파일 없음 확인.
- `find daylog -maxdepth 2 -type f`: 기존 daylog 파일 없음.
- `find . -name AGENTS.md -print`: 저장소 내부 `AGENTS.md` 없음. 사용자 메시지의 AGENTS 지침을 기준으로 적용.
- `README.md`: 프로젝트 현황, 실행/검증 명령, 문서 위치 확인.
- `git status --short --untracked-files=all`: 현재 작업트리 변경 파일 확인.
- `git diff -- apps/web/app/page.tsx`, `apps/web/lib/detect-api.ts`: PWA server detector wiring 코드 확인.
- `docs/execution/2026-05-14_next_step_parallel.md`
- `docs/execution/2026-05-14_pwa_server_detector_wiring.md`
- `docs/execution/2026-05-14_summary.md`
- `docs/execution/2026-05-14_detect_adapter_implementation.md`
- `docs/execution/2026-05-14_voice_pwa_smoke.md`
- `plans/daily/2026-05-15.md`
- `plans/catchup/2026-05-15.md`
- `plans/.work/2026-05-15/catchup/*.md`
- 하위/병렬 에이전트는 새로 사용하지 않음. 현재 갱신은 파일/문서 확인 범위로 처리함.

## 오늘 한 일
- 기존 daylog 부재, 2026-05-15 실행 문서 부재, 저장소 내부 `AGENTS.md` 부재를 확인했다.
- 현재 작업트리 기준으로 PWA server detector 연결 변경을 확인했다.
  - `NEXT_PUBLIC_DETECTOR_MODE=server`일 때 `/detect/health` 확인 후 카메라 프레임을 `/detect`로 전송하는 흐름이 추가됨.
  - 서버 응답 detections를 `DetectionEvent`로 매핑하고 `source: "server"`를 유지하는 client가 추가됨.
  - fake detector 흐름은 유지됨.
- 2026-05-15 계획/보강 계획 문서를 확인했다.
  - `plans/daily/2026-05-15.md`
  - `plans/catchup/2026-05-15.md`
  - lane별 catch-up note 5개
- 최신 근거 기준으로 완료/대기 상태를 정리했다.
  - 완료 근거 있음: PWA lint/typecheck 문서 기록, voice 계약 smoke 문서 기록, backend `/detect` adapter 테스트/스모크 문서 기록, v2 `best.pt` 기반 backend `/detect` TestClient smoke 문서 기록.
  - 대기 상태: 실폰 카메라/GPS/방향/TTS/진동, 브라우저/실폰 마이크 E2E, PWA server detector 실제 브라우저 호출, `source: "server"` 신고 저장, PWA 설치/offline, TTS cache hit, 통합 field smoke.

## 변경 파일
- `apps/web/app/page.tsx`: 수정됨. 서버 탐지 모드 상태, 주기적 `/detect` 호출, UI 상태 메시지 연결 확인.
- `apps/web/lib/detect-api.ts`: 신규 미추적 파일. `/detect/health`, `/detect` client와 응답 매핑 로직 확인.
- `docs/execution/2026-05-14_next_step_parallel.md`: 신규 미추적 문서.
- `docs/execution/2026-05-14_pwa_server_detector_wiring.md`: 신규 미추적 문서.
- `plans/daily/2026-05-15.md`: 신규 미추적 문서.
- `plans/catchup/2026-05-15.md`: 신규 미추적 문서.
- `plans/.work/2026-05-14/daily/*.md`: 신규 미추적 lane note 5개.
- `plans/.work/2026-05-15/catchup/*.md`: 신규 미추적 lane note 5개.
- `daylog/2026-05-15.md`: 이 본문으로 갱신 대상.

## 검증
- 직접 확인:
  - `git status --short --untracked-files=all`: 위 변경 파일 목록 확인.
  - `git diff --stat`: tracked diff 기준 `apps/web/app/page.tsx` 105 insertions, 3 deletions 확인.
  - `git diff -- apps/web/app/page.tsx`: server detector polling, health check, UI status 연결 확인.
  - `sed -n`으로 `apps/web/lib/detect-api.ts`와 관련 실행/계획 문서 내용을 확인.
  - `find docs/execution -maxdepth 1 -type f -name '2026-05-15*.md'`: 2026-05-15 실행 문서 없음 확인.
- 오늘 이 갱신 과정에서 `npm`, `pytest`, 브라우저/실폰 검증은 새로 실행하지 않았다.
- 문서상 확인된 기존 검증:
  - `cd apps/web && npm run lint`: 통과 기록.
  - `cd apps/web && npm run typecheck`: 통과 기록.
  - `git diff --check`: 통과 기록.
  - `scripts/check_voice_contract.py`: 통과 기록.
  - backend `/detect/health`: ready 기록.
  - backend `/detect`: v2 `best.pt` 샘플에서 detection 4개, 첫 detection `source: server` 기록.

## 리스크/확인 필요
- `plans/daily/2026-05-14.md`, 기존 daylog, `docs/execution/2026-05-15*.md`가 없어 오늘 새 실행 완료 근거는 제한적이다.
- PWA server detector wiring은 코드와 문서 근거는 있으나, 실제 브라우저/실폰에서 `/detect` 호출, bbox 표시, `source: "server"` 신고 저장은 아직 확인 필요다.
- 일부 기존 문서는 STT 미연결 또는 `/detect` placeholder 기준일 수 있다. 최신 판단은 2026-05-14 후속 실행 문서를 우선해야 한다.
- VL2+VS2 external validation은 class `0 damaged_tactile_block` 중심 근거다. 4개 위험 클래스 전체 성능 근거로 쓰면 안 된다.
- TTS cache는 cache hit와 WAV 생성 근거가 확인되지 않았다.
- 현재 변경 파일이 미커밋/미추적 상태이므로 후속 작업자는 덮어쓰기 전에 `git status`와 diff를 먼저 확인해야 한다.
---

## 병렬 검증 추가 기록 - 10:58 KST

### 사용한 병렬 에이전트
- PWA/Accessibility validation lane
- Backend/PostGIS/API validation lane
- Voice STT/TTS validation lane
- Model/Data/MLOps validation lane

### 한 일
- PWA 정적 검증을 병렬 실행했다.
  - `cd apps/web && npm run lint && npm run typecheck && npm run build`: 통과.
  - `adb devices`: `adb`는 사용 가능하지만 연결된 Android 기기 없음.
  - `next build`가 생성한 `apps/web/next-env.d.ts` 변경은 PWA lane에서 원복됨.
- Backend 검증을 병렬 실행했다.
  - `python -m pytest backend/tests`: `11 passed, 11 skipped`.
  - reports tests 11개는 PostGIS DB 미기동으로 skip됨.
  - TestClient 기준 `/health`, `/detect/health`, `/detect` model unavailable 계약 확인.
- Voice 검증을 병렬 실행했다.
  - 임시 `uvicorn voice.server:app --host 127.0.0.1 --port 9001` 기동 후 `scripts/check_voice_contract.py`: 통과.
  - `/health`에서 CUDA/GPU/STT/TTS 설정 확인.
  - `outputs/voice/cache` WAV는 0개라 TTS cache hit는 미확인.
  - 임시 voice 서버 종료 후 9001 리스너 없음 확인.
- Model/Data 검증을 병렬 실행했다.
  - `/` 파티션 가용 5.9G, 사용률 100% 확인.
  - v2 freeze artifact `sha256sum -c` 통과.
  - Git이 `.pt`, `.onnx`, `runs/**`, `datasets/walksafe_kr_v2/**` 대형 산출물을 추적 중이라는 근거 없음.
- 부모 에이전트가 병렬 결과를 통합해 `docs/execution/2026-05-15_parallel_validation_summary.md`를 작성했다.

### 변경 파일
- `docs/execution/2026-05-15_pwa_validation.md`
- `docs/execution/2026-05-15_backend_validation.md`
- `docs/execution/2026-05-15_voice_validation.md`
- `docs/execution/2026-05-15_model_validation.md`
- `docs/execution/2026-05-15_parallel_validation_summary.md`
- `daylog/2026-05-15.md`

### 검증 결과 요약
- 통과: PWA lint/typecheck/build, backend detect tests 및 unavailable 계약, voice API contract, model v2 artifact hash/Git 대형 산출물 미추적 확인.
- 추가 확인: `git diff --check` 통과.
- 부분 통과/대기: backend reports tests는 PostGIS 미기동으로 skip.
- 미실행: Android 실폰, PWA runtime E2E, PWA server detector `source: "server"` 신고 저장, 실제 STT 마이크 E2E, TTS cache hit, ONNX export/latency/failure sampling.

### 실폰 테스트 메모
- 사용자 확인: 실폰 테스트는 이전에 수행한 적이 있다.
- 이번 병렬 검증에서는 Android 기기 연결이 없어 실폰 테스트를 재실행하지 않았다.
- 실폰 재검증은 후속 일정에서 별도로 진행한다.

### 리스크/다음 확인 필요
- 디스크가 `/` 100% 사용 중이므로 cleanup 승인 전 대형 validation/export를 진행하지 않는다.
- PostGIS를 기동한 뒤 backend reports tests를 재실행해야 한다.
- 실폰 연결 후 카메라/GPS/방향/TTS/진동/PWA 설치/offline/accessibility를 수동 검증해야 한다.

---

## 디스크 정리 후보 병렬 조사 - KST

### 한 일
- 사용자 요청에 따라 디스크 정리 후보를 4개 병렬 lane으로 읽기 전용 조사했다.
  - repo-local data/artifacts
  - Downloads/raw AI Hub archives
  - development caches/environments
  - Docker/system-level reclaim
- 삭제, 이동, 압축, prune은 실행하지 않았다.
- 통합 결과를 `docs/execution/2026-05-15_disk_cleanup_candidates.md`에 기록했다.

### 확인한 주요 후보
- 낮은 위험 후보:
  - `~/.local/share/Trash`: `2.4G`
  - `~/.cache/pip`: `3.2G`
  - `~/.npm`: `1.1G`
  - `/tmp`: `603M` 중 `/tmp/vello-jdk21` `543M`
  - Docker reclaimable image: 약 `1.017G`
- 재생성 가능하지만 작업 영향 있는 후보:
  - `.venv`: `5.4G`
  - `.venv-voice`: `5.4G`
  - `apps/web/node_modules`: `625M`
  - `~/.cache/huggingface`: `6.1G`
- 고위험/승인 필요 후보:
  - `/home/ddobagi/Downloads` AI Hub 원본 zip: 약 `400G`
  - `datasets/walksafe_kr_v2/images`: `199G`
  - `datasets/walksafe_kr_v1/images`: `49G`
  - `runs/validation/aihub513_vl2_vs2_tactile_subset`: `18G`

### 변경 파일
- `docs/execution/2026-05-15_disk_cleanup_candidates.md`
- `daylog/2026-05-15.md`

### 리스크/확인 필요
- AI Hub 원본 zip과 `datasets/**/images`는 재현성에 직접 영향이 있어 사용자 승인 없이 삭제하지 않는다.
- `LOCAL_DATASETS/raw_downloads/*.zip`는 symlink라 삭제해도 공간 확보 효과가 거의 없다.

---

## 낮은 위험 디스크 정리 실행 및 프로그램 후보 조사 - KST

### 한 일
- 사용자 승인 범위에 따라 낮은 위험 정리 후보를 삭제했다.
  - 휴지통
  - `~/.cache/pip`
  - `~/.npm`
  - `/tmp/vello-jdk21`, `/tmp/runapp-remote-check`, `/tmp/node-compile-cache`
  - unused Docker images `eclipse-temurin:21-jdk`, `nvidia/cuda:12.0.1-base-ubuntu22.04`, `hello-world:latest`
  - stopped container `stoic_robinson`
- 삭제 전후 용량을 기록했다.
  - 정리 전 `/`: `5.9G` available, `100%` used
  - 정리 후 `/`: `14G` available, `99%` used
- 사용자가 별도로 설치한 것으로 보이는 프로그램 중 최근 7일 사용 근거가 명확하지 않은 후보를 조사했다.
- 조사 결과를 문서화했다.

### 변경 파일
- `docs/execution/2026-05-15_low_risk_cleanup_result.md`
- `docs/execution/2026-05-15_installed_program_usage_candidates.md`
- `daylog/2026-05-15.md`

### 검증
- `df -h /`로 정리 전후 가용 공간 확인.
- `du -sh`로 Trash, `/tmp` 정리 결과 확인.
- `docker system df`로 Docker reclaimable image `0B` 확인.
- `apt-get -s autoremove` 결과 자동 제거 후보 없음 확인.

### 리스크/확인 필요
- 프로그램 사용 여부는 Linux에서 완전한 실행 이력을 제공하지 않으므로 후보로만 정리했다.
- Wine, Unity, VS Code, OBS, CUDA toolkit 등은 사용자 승인 없이 삭제하지 않았다.

---

## OBS 삭제 시도 및 설명 보강 - KST

### 한 일
- OBS Studio 삭제 요청에 따라 `apt-get -s remove obs-studio`로 제거 범위를 먼저 확인했다.
- `sudo -n apt-get remove -y obs-studio`를 실행했으나 현재 세션에서 sudo 대화형 인증이 필요해 실패했다.
- OBS Studio는 여전히 설치 상태임을 확인했다.
- 결과를 `docs/execution/2026-05-15_obs_removal_attempt.md`에 기록했다.

### 변경 파일
- `docs/execution/2026-05-15_obs_removal_attempt.md`
- `daylog/2026-05-15.md`

### 검증/결과
- `dpkg-query` 기준 `obs-studio install ok installed`.
- sudo 실패 메시지: `sudo: interactive authentication is required`.

### 리스크/확인 필요
- `apt autoremove` 후보에는 VLC/Qt/OBS plugin 관련 패키지가 포함되므로, 실제 자동 제거는 사용자 확인 후 진행해야 한다.

---

## Wine/OBS/Snap 정리 시도 - KST

### 한 일
- 사용자 승인에 따라 Wine 사용자 prefix `~/.wine`을 삭제했다.
- 삭제 전 `~/.wine`: `1.8G`.
- 삭제 후 `/` 가용 공간은 `16G`로 증가했다.
- OBS Studio와 Wine apt package 제거를 `sudo -n apt-get purge`로 시도했으나 sudo 대화형 인증 필요로 실패했다.
- Snap disabled revisions 제거도 `sudo -n snap remove`로 시도했으나 동일하게 실패했다.
- 결과와 사용자가 로컬 터미널에서 실행할 명령을 `docs/execution/2026-05-15_wine_obs_snap_cleanup_attempt.md`에 기록했다.

### 변경 파일
- `docs/execution/2026-05-15_wine_obs_snap_cleanup_attempt.md`
- `daylog/2026-05-15.md`

### 검증/결과
- `~/.wine removed` 확인.
- `df -h /`: 가용 `16G`, 사용률 `99%`.
- `sudo` 실패 메시지: `sudo: interactive authentication is required`.

### 남은 일
- 사용자가 로컬 터미널에서 sudo 비밀번호를 입력해 OBS/Wine package와 Snap old revisions를 제거해야 한다.

---

## Wine/OBS/Snap 정리 완료 - KST

### 한 일
- 사용자 제공 sudo 인증으로 승인된 root-level 정리를 수행했다.
- OBS/Wine apt package를 삭제했다.
  - `obs-studio`
  - `winehq-stable`
  - `wine-stable`
  - `wine-stable-amd64`
  - `wine-stable-i386:i386`
- Snap old disabled revisions 14개를 제거했다.
- 비밀번호 값은 문서에 기록하지 않았다.

### 변경 파일
- `docs/execution/2026-05-15_wine_obs_snap_cleanup_result.md`
- `daylog/2026-05-15.md`

### 검증/결과
- `dpkg-query`에서 OBS/Wine 대상 패키지 조회 결과 없음.
- `snap list --all | awk 'NR==1 || /사용 불가|disabled/'` 결과 header만 출력되어 disabled revision 없음.
- `/opt/wine-stable missing`, `~/.wine missing` 확인.
- `df -h /`: 가용 `18G`, 사용률 `99%`.

### 실행하지 않은 것
- `apt autoremove --purge`는 실행하지 않았다.
- dry-run 결과 자동 제거 후보가 243개이며 `vlc`, Qt, OBS plugin, Wine i386 dependency 등이 포함되어 사용자 확인 필요.

---

## PWA server-mode static follow-up - KST

### 한 일
- PWA server detector env build 후속 검증을 실행했다.
- 초기 `git status --short`와 `apps/web/next-env.d.ts` diff 상태를 기록했다.
- `apps/web`에서 lint/typecheck와 server detector env build를 실행했다.
- `next build`가 생성한 `apps/web/next-env.d.ts` churn만 원복했다.
- 결과를 `docs/execution/2026-05-15_pwa_server_mode_followup.md`에 기록했다.

### 변경 파일
- `docs/execution/2026-05-15_pwa_server_mode_followup.md`
- `daylog/2026-05-15.md`

### 검증/결과
- `cd apps/web && npm run lint && npm run typecheck`: 통과.
- `cd apps/web && NEXT_PUBLIC_DETECTOR_MODE=server NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000 npm run build`: 통과.
- `git diff -- apps/web/next-env.d.ts`: 원복 후 출력 없음.

### 실행하지 않은 것
- Android/실폰, 브라우저 runtime, 수동 PWA/accessibility 검증은 실행하지 않았다.

---

## Backend/PostGIS follow-up - KST

### 한 일
- 시작 시 `git status --short --untracked-files=all`로 기존 미커밋/미추적 변경을 기록했다.
- `docker compose up -d db`로 PostGIS를 기동하고 `walksafe-postgis` healthy 상태를 확인했다.
- `.venv/bin/python`으로 Alembic `upgrade head`와 `current`를 실행해 `202605120001 (head)`를 확인했다.
- `.venv/bin/python -m pytest backend/tests`를 실행했다.
- FastAPI `TestClient`로 `GET /health`, `GET /detect/health` smoke를 실행했다.
- 결과를 `docs/execution/2026-05-15_backend_postgis_followup.md`에 기록했다.
- 백엔드 소스는 수정하지 않았고, 모델 학습/export는 실행하지 않았다.

### 변경 파일
- `docs/execution/2026-05-15_backend_postgis_followup.md`
- `daylog/2026-05-15.md`

### 검증/결과
- `docker compose ps db`: `walksafe-postgis` healthy.
- `.venv/bin/python --version`: `Python 3.14.4`.
- `.venv/bin/python -m alembic -c backend/alembic.ini upgrade head`: 통과.
- `.venv/bin/python -m alembic -c backend/alembic.ini current`: `202605120001 (head)`.
- `.venv/bin/python -m pytest backend/tests`: `22 passed in 0.40s`.
- `GET /health`: 200, `{"status": "ok"}`.
- `GET /detect/health`: 200, `model_status=unavailable`, `reason=model_not_configured`.

### DB/uploads 메모
- Pytest 전 `reports` row: 6개.
- Pytest 후 `reports` row: 12개. 테스트가 6개 row를 추가했고 삭제하지 않는다.
- `backend/uploads/test/`에 테스트 이미지 6개, 총 120 bytes가 생성되었다.
- `backend/uploads/test/`는 `.gitignore`에 의해 ignored 상태다.
- 사용자 지시에 따라 persistent DB row와 테스트 업로드 파일은 삭제하지 않았다.

---

## Model ONNX follow-up - KST

### 한 일
- v2 `best.pt` 경로, 크기, sha256 및 freeze manifest를 확인했다.
- `.venv`의 `ultralytics`, `onnx`, `onnxruntime` export 의존성을 확인했다.
- 추가 install/download 없이 v2 `best.pt`를 ONNX로 export했다.
- ONNX checker, raw tensor PT-vs-ONNX smoke, 30장 CPU latency smoke를 실행했다.
- 결과를 `docs/execution/2026-05-15_model_onnx_followup.md`에 기록했다.

### 변경 파일/산출물
- `docs/execution/2026-05-15_model_onnx_followup.md`
- `daylog/2026-05-15.md`
- ignored artifact: `runs/detect/walksafe_kr_tactile_v2_full/weights/best.onnx`

### 검증/결과
- `best.pt` sha256: `02a6be87626e9ba00bb72715d45d8c27d06103d851882a08f404260e453d8e94`.
- `best.onnx` sha256: `c21f47013ad340761a2743bc20ba36da8aa4110c40a16bb0ddf7b6913858efe7`, size `10,567,352 bytes`.
- ONNX raw equivalence smoke: output shape `(1, 8, 8400)`, max abs diff `0.0010375977`, allclose `rtol=1e-3`, `atol=1e-3` 통과.
- Latency smoke: PT p95 `13.70ms`, ONNX Runtime CPU p95 `68.67ms`, ONNX/PT p95 ratio `5.014`.

### 실행하지 않은 것
- 학습은 실행하지 않았다.
- full test-split metric equivalence와 browser/ONNX Runtime Web latency는 실행하지 않았다.

---

## Voice cache follow-up - KST

### 한 일
- `.venv-voice`로 `127.0.0.1:9001` 임시 voice server를 기동했다.
- `scripts/check_voice_contract.py`를 실행해 `/health`, `/speech/intent`, `/speech/stt` validation contract를 확인했다.
- `outputs/voice/cache`, Hugging Face cache, GPU memory 상태를 확인했다.
- `/speech/tts` cache 확인을 짧은 문구로 시도했으나 첫 요청 HTTP 500으로 중단했다.
- 임시 voice server를 종료하고 포트 listener가 없는 것을 확인했다.

### 변경 파일
- `docs/execution/2026-05-15_voice_cache_followup.md`
- `daylog/2026-05-15.md`

### 검증/결과
- `scripts/check_voice_contract.py --base-url http://127.0.0.1:9001 --timeout 3`: PASS.
- `outputs/voice/cache`: `.gitkeep`만 존재, WAV 0개, `4.0K`.
- Hugging Face cache: `~/.cache/huggingface` `6.1G`, Qwen CustomVoice cache `2.4G`.
- GPU: RTX 5070 Ti, total `16303` MiB, used `631` MiB, free `15207` MiB 관측.
- `/speech/tts`: offline download guard 상태에서 첫 요청 HTTP 500. cache hit 미확인.

---

## 다음 스텝 병렬 실행 통합 기록 - KST

### 사용한 병렬 lane
- Backend/PostGIS follow-up
- PWA server-mode/static follow-up
- Model ONNX/handoff follow-up
- Voice STT/TTS cache follow-up

### 한 일
- Backend/PostGIS:
  - `docker compose up -d db`로 PostGIS를 기동했다.
  - Alembic current `202605120001 (head)` 확인.
  - `python -m pytest backend/tests`: `22 passed in 0.40s`.
  - TestClient `/health`, `/detect/health` smoke 확인.
- PWA:
  - `npm run lint`, `npm run typecheck` 통과.
  - `NEXT_PUBLIC_DETECTOR_MODE=server NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000 npm run build` 통과.
  - `next-env.d.ts` generated churn은 원복됨.
- Model:
  - v2 `best.pt` hash 확인.
  - ONNX export 성공: `runs/detect/walksafe_kr_tactile_v2_full/weights/best.onnx`.
  - PT/ONNX smoke equivalence 통과.
  - CPU latency smoke 기록.
- Voice:
  - voice contract smoke 통과.
  - `/speech/tts` cache 시도는 HTTP 500으로 실패.
  - blocker: `sox` 미설치, Hugging Face offline mode metadata 조회 실패.
- 병렬 결과를 `docs/execution/2026-05-15_next_step_parallel_followup.md`에 통합했다.

### 변경 파일/산출물
- `docs/execution/2026-05-15_backend_postgis_followup.md`
- `docs/execution/2026-05-15_pwa_server_mode_followup.md`
- `docs/execution/2026-05-15_model_onnx_followup.md`
- `docs/execution/2026-05-15_voice_cache_followup.md`
- `docs/execution/2026-05-15_next_step_parallel_followup.md`
- `daylog/2026-05-15.md`
- ignored 산출물: `runs/detect/walksafe_kr_tactile_v2_full/weights/best.onnx`

### 부작용/상태
- PostGIS `reports` row가 테스트로 `6 -> 12`가 됐다.
- `backend/uploads/test/`에 ignored 테스트 이미지 6개, 총 `120 bytes`가 생겼다.
- PostGIS 컨테이너는 기동 상태로 유지했다.
- Voice server는 종료됐고 `9001` listener 없음.

### 다음 확인 필요
- backend `/detect` ready smoke와 PWA runtime server mode E2E.
- Voice TTS를 위해 `sox` 설치/로컬 모델 loading 방식 결정.
- PostGIS test row cleanup/reset 정책 결정.

---

## Runtime follow-up after reset - KST

### 한 일
- 사용자 확인 사항으로 STT/TTS는 외부 API가 아니라 로컬 모델/로컬 voice server 기준임을 기록했다.
- PostGIS 테스트 row reset을 수행했다.
- backend `/detect`를 실제 v2 `best.pt`로 ready 상태까지 연결해 smoke 확인했다.
- PWA server mode를 headless Chromium fake media로 runtime smoke 확인했다.
- `sox`, `libsox-fmt-all` 설치 후 로컬 TTS cache hit을 확인했다.

### 변경 파일/산출물
- `docs/execution/2026-05-15_runtime_followup_after_reset.md`
- `daylog/2026-05-15.md`
- 로컬 TTS cache 산출물: `outputs/voice/cache/qwen3_tts_cd250a528361467f9b26.wav`

### 검증/결과
- PostGIS `reports` count: `12 -> 0`; `backend/uploads/test` 파일 6개 삭제 후 파일 없음.
- `/detect/health`: HTTP `200`, `model_status=ready`.
- known-positive image `/detect`: HTTP `200`, `detections_count=4`, first detection `damaged_tactile_block`, confidence `0.9443610310554504`, `source=server`.
- PWA runtime: browser에서 `GET /detect/health` 200, `POST /detect` 200 관측; DOM에 `서버 탐지 모드`, `서버 연결됨 · 위험 없음` 표시 확인.
- Voice contract: PASS.
- TTS cache: 1차 `x-voice-cached:false`, 2차 `x-voice-cached:true`, cache WAV `122924 bytes` 생성.

### 상태/미실행
- backend/PWA/voice/Chromium 임시 프로세스는 종료했다.
- PostGIS 컨테이너는 healthy 상태로 유지했다.
- fake camera에서는 detection이 없어 PWA report 저장 payload의 `source:"server"`까지는 확인하지 못했다.
- 실폰 재검증은 사용자 메모대로 나중에 한다.

---

## PWA server detection E2E - KST

### 한 일
- 병렬 에이전트로 PWA server detector/report 흐름과 브라우저 E2E 자동화 방식을 조사했다.
- `scripts/check_pwa_server_e2e.py`를 추가했다.
- headless Chromium CDP에서 `getUserMedia`를 known-positive 이미지 기반 `canvas.captureStream()`으로 주입해 PWA server mode E2E를 실행했다.
- E2E 검증 report를 DB/API로 확인한 뒤 test row reset 정책에 맞춰 다시 정리했다.

### 변경 파일
- `scripts/check_pwa_server_e2e.py`
- `docs/execution/2026-05-15_pwa_server_detection_e2e.md`
- `daylog/2026-05-15.md`

### 검증/결과
- `.venv/bin/python -m py_compile scripts/check_pwa_server_e2e.py`: PASS.
- `.venv/bin/python scripts/check_pwa_server_e2e.py`: PASS.
- PWA browser video: `1118x1987`, `readyState=4`, `srcObject=true`.
- 저장 report: `source=server`, `metadata.source=server`, `class_name=damaged_tactile_block`, confidence `0.9445814490318298`.
- E2E 후 PostGIS `reports` count를 `0`으로 reset했고 `backend/uploads`는 `.gitkeep`만 남겼다.

### 상태/미실행
- backend/PWA/Chromium 임시 프로세스는 종료했다.
- PostGIS 컨테이너는 계속 기동 중이다.
- 실폰 재검증은 사용자 메모대로 나중에 한다.

---

## PWA production server E2E - KST

### 한 일
- 병렬 에이전트로 Next production build/start E2E 주의점과 스크립트 확장 지점을 조사했다.
- `scripts/check_pwa_server_e2e.py`에 `--web-mode dev|start`와 `--skip-web-build` 옵션을 추가했다.
- `--web-mode start`로 `npm run build` 후 `npm run start` 기준 production E2E를 실행했다.
- 검증 report를 DB에서 확인한 뒤 reset했다.

### 변경 파일
- `scripts/check_pwa_server_e2e.py`
- `docs/execution/2026-05-15_pwa_production_server_e2e.md`
- `daylog/2026-05-15.md`

### 검증/결과
- `.venv/bin/python scripts/check_pwa_server_e2e.py --web-mode start`: PASS.
- `npm run build`: PASS.
- `npm run start -- --hostname 127.0.0.1 --port 3000`: ready.
- backend 로그에서 `GET /detect/health` 200, `POST /detect` 200, `POST /reports` 201 확인.
- 저장 report: `source=server`, `metadata.source=server`, `class_name=damaged_tactile_block`, confidence `0.9445814490318298`.
- `next build`로 생긴 `apps/web/next-env.d.ts` generated churn은 원복했다.
- E2E 후 PostGIS `reports` count를 `0`으로 reset했고 `backend/uploads`는 `.gitkeep`만 남겼다.

### 상태/미실행
- backend/PWA/Chromium 임시 프로세스는 종료했다.
- PostGIS 컨테이너는 계속 기동 중이다.
- 실폰 재검증과 실제 배포 환경 검증은 하지 않았다.
