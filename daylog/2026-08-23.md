# 2026-08-23 WalkSafe 작업 로그

## 종료 시점

- 사용자의 컴퓨터 종료 요청으로 2026-08-23 11:23 KST에 진행 중인 에이전트를 모두 중단했다.
- 브랜치는 `recovery/fp046-r008-wip-20260822`, HEAD는 `8a146bbf9c5a7612377abb939fc4d94570d45cca`이다.
- 커밋·push·제품 코드 수정은 하지 않았다. 현재 작업 트리는 보존해야 하는 미커밋 recovery WIP이며 reset/clean 대상이 아니다.

## 완료한 복구·검증

- seq72~76 continuation/Goal 검증과 R011/R004 권한 경계를 복구했다.
- 현재 checkpoint는 exact seq76 READY에서 그대로 유지된다.
  - SHA-256: `bf0f44f0ed2a777c2a3d62b81fa995d26e3b725e398a37034b7d70fa1d3a1a36`
  - 2,217,805 bytes, mode `0600`, nlink `1`
  - tail: `WS-GOAL-GRAPH-V2-4-GOAL-READY-FP046-R002-20260815-001`
- seq77/gate/seq78 통제 코드에서 다음 결함을 수정했다.
  - 잘못된 exact pytest selector
  - 정상 seq79+ suffix에서 review authority가 깨지는 문제
  - review보다 과거인 seq77 시각
  - seq78 managed cohort 게시 경합
  - JSON `0`/`false`/`0.0` 타입 혼동
  - 게시 후 interrupt cleanup의 uncertainty 오분류
- R003 구현 완료 시점 검증은 combined 180 PASS, Goal graph 전체 160 PASS, builder 73 PASS, seq77+gate 60 PASS, seq78 31 PASS였다.

## 보존된 review 기록

- R001 assignment만 존재하며 SHA-256 `e1b3807d7de0d3473d271e8aa36cd73d0b2e5bde3ecc00f0c8652642efc376dd`, 22,114 bytes이다.
- R002 assignment만 존재하며 SHA-256 `1fbb07eb4fe16e943a5ca13f9d78ad10f51952d01dbbc1ca6d698b91ed0c5885`, 22,838 bytes이다.
- R003 assignment만 존재하며 SHA-256 `fb3bf40e349c4edf6cabdd7a8afde5c491802f3d943f8b2b4beda5b84d54658c`, 24,009 bytes이다.
- 세 round 모두 기존 bytes를 수정하지 않는다. R001/R002/R003 result와 independent-review는 생성되지 않았다.
- R002는 6개 control finding으로, R003는 review writer 입력 cohort ABA 경합으로 `REJECT`됐다.

## 중단된 R004 WIP

- R003의 신규 High finding은 `write_assignment`와 `write_independent`가 최초 입력 파일 descriptor를 게시 완료까지 유지하지 않아 A→B→A 순간 변경 뒤 self-invalid evidence를 성공 게시할 수 있다는 것이다.
- R004 목표는 assignment/independent 전체 입력 cohort를 retained descriptor로 고정하고 게시 직전·직후 검증하는 것이다.
- 중단 시점에 builder는 아직 active R003이다.
  - `REVIEW_DIR = review-rounds/R003`
  - `PRESERVED_REVIEW_PATHS = (R001, R002 assignment)`
  - retained input cohort 구현은 미완료다.
- 반면 consumer/gate/test 일부는 이미 fail-first R004 기대값으로 바뀌었다.
  - catalog generator/test는 R001/R002/R003 assignment + active R004 triad, 총 6 review 경로를 기대한다.
  - continuation/Goal 테스트 일부는 active R004와 R003 exact pin을 기대한다.
  - gate source guard는 active triad 외 preserved assignment도 포함하도록 변경 중이다.
  - seq77/seq78/gate 테스트 이름과 assertion 일부가 R004로 전환됐다.
- 따라서 현재 트리는 통합 전 중간 상태이며 전체 테스트 PASS를 기대하면 안 된다. 종료 직전 `git diff --check`는 PASS였다.
- 에이전트가 계산한 provisional seq77 managed path 기대값은 963 paths, path-set SHA-256 `ea6f65a9a38ae2b6bc5b3e320c9b0ad7c1b26c3a1085dc49df0a4535fab64352`이나 builder R004 완성 뒤 반드시 다시 계산한다.

## 다음 시작 순서

1. HEAD, seq76 checkpoint, R001/R002/R003 assignment 해시와 result/independent 부재를 다시 확인한다.
2. review builder/test를 active R004로 완성한다.
   - R003 supersession reason: `R003_REVIEW_INPUT_COHORT_ABA_RETAINED_DESCRIPTOR_REQUIRED`
   - confirmed finding: `REVIEW_INPUT_COHORT_ABA_CAN_PUBLISH_SELF_INVALID_EVIDENCE`
   - R001/R002/R003 assignment를 ordered exact pin하고 세 round output 부재를 검증한다.
   - assignment/independent 입력 전체를 retained descriptor cohort로 유지하고 ABA fail-first 회귀를 통과시킨다.
3. 이미 부분 전환된 continuation, Goal, catalog generator/test, gate와 seq77/seq78 테스트를 builder R004 상수에 맞춘다.
4. 전용 회귀, exact contract selector, Goal graph 전체, continuation과 test-layer validate를 실행한다.
5. 코드가 안정된 뒤에만 add-only R004 assignment를 만들고 executor/reviewer 분리 검토를 진행한다.
6. R004 triad가 완성된 뒤 catalog JSON 3개를 한 번만 재생성한다.
7. seq77 → private five-check gate → seq78 → 실제 seq78 digest를 고정하는 zero-credit seq79 successor 순서로 진행한다.
8. 이 통제가 끝난 뒤에만 제품 수정을 시작한다.

## 아직 하지 않은 제품 수정

- DB privacy HTTP 503 원인은 runtime `rate_group='privacy'`가 PostgreSQL check constraint에 없어 첫 INSERT가 SQLSTATE 23514로 실패하는 것이다. 새 Alembic migration과 expected-head/tests/docs 갱신이 필요하다.
- 계정 삭제 404 원인은 Gateway/OpenAPI/client에는 3개 route가 있지만 Nginx allowlist가 catch-all 404로 막는 것이다. Nginx exact location/regex 3개, product boundary 5→8, 관련 tests/docs 갱신이 필요하다.
- 위 두 제품 영역의 파일은 이번 세션에서 수정하지 않았다.

## 주요 변경 영역

- control: continuation/Goal checker와 테스트, repository catalog generator/test, current test-layer runner.
- 신규 WIP: seq77 reanchor, private gate, seq78 start, separated review builder와 각 테스트, authorization/contract, R001/R002/R003 assignment.
- `docs/catalogs/*.json`은 R004 review triad 완성 전의 파생 중간 상태이므로 지금 재생성하지 않는다.

## 재개 주의

- checkpoint를 seq77로 쓰거나 R004 evidence를 생성하지 않은 상태다.
- 현재 중간 상태를 정본·승인으로 표현하지 않는다.
- `git clean`, reset, 기존 review assignment 수정, seq78 전 commit, push를 하지 않는다.
