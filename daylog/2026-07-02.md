# 2026-07-02

## WalkSafe 사용자 정책 결정 반영

- 사용자 정책 결정을 `docs/walksafe-v2/policy_decisions_20260702.md`에 P-01~P-17 기준으로 정리했다.
- `P-06`을 추가해 report image 서버 저장, 얼굴/차량번호 무모자이크, EXIF 제거 유지, report row+image 180일 삭제 기준을 명시했다.
- `docs/walksafe-v2/threshold_tuning_playbook_20260702.md`를 P-04 threshold 조정 판단 문서로 유지하고 README 색인에 추가했다.
- `docs/report_operations.md`, `docs/walksafe-v2/public_agency_submission_policy.md`, `docs/current_status.md`, `docs/evidence/privacy_checklist.md`에서 기관 제출 기능을 제품 범위 밖으로 정리하고 운영자 내부 export 기준으로 표현을 맞췄다.
- `docs/data_retention_policy.md`와 `scripts/check_report_retention_dry_run.py`를 실제 report row/image 180일 보존 기준에 맞췄다.
- `/home/ddobagi/Downloads/abc.html` 정책 결정 보드를 2026-07-02 사용자 결정 기준으로 갱신했다.

## Verification

- `rg`로 옛 충돌 문구(`기관 제출 후보`, `agency candidate`, `row와 image를 모두 삭제할까`, `오탐 민원`) 잔존 없음 확인.
- `python3 -m py_compile scripts/check_report_retention_dry_run.py` 통과.
- `candidate_for_row()` 직접 호출로 `resolved` report가 180일 retention 후보가 되는지 확인: `retention_180_ok`.
- `html.parser.HTMLParser`로 `/home/ddobagi/Downloads/abc.html` 파싱 통과: `html_parse_ok`.

## Earlier Remaining Decisions

- 아래 항목들은 같은 날 후속 사용자 결정으로 확정되어 다음 섹션에 반영했다.
- 중복 기준, voice 중복 처리, 30초 TTC bucket, 진행음 기본값, 신고자 식별자, 180일 삭제 enable 방식, 빨간 신호등 STOP 후속 여부.

## WalkSafe 추가 정책 결정 및 duplicate backend 보강

- 사용자 추가 결정 반영:
  - 중복 신고 기준은 같은 class, 10m, captured_at 전후 1분.
  - 중복 후보여도 저장하고 `duplicate_candidate` tag와 후보 ID를 남김.
  - 사용자가 명시적으로 신고 요청을 했고 중복 후보이면 "이미 신고가 된 상태입니다" TTS 안내.
  - TTC 30초 AWARE bucket은 내부 상태로만 사용.
  - 진행음은 기본 on, 볼륨 20%, 설정 off 가능.
  - 주요 기능은 로그인 필수, 신고자는 로그인 user id 기준.
  - 180일 삭제 job은 dry-run/backup/audit 확인 후 운영에서만 enable.
  - 빨간 신호등 STOP은 후속. 현재 모델은 `traffic light` 객체 클래스만 있고 색상 구분 없음.
- `backend/app/services/duplicates.py` 기본값을 10m/1분으로 변경했다.
- `backend/app/api/reports.py`에서 중복 후보가 있으면 저장 payload에 `review_flags=["duplicate_candidate"]`와 `duplicate_report_ids`를 남기도록 보강했다.
- `backend/app/services/report_serialization.py`에서 metadata `review_flags`가 응답 review flags에 반영되도록 했다.
- v1/v2 duplicate 테스트 fixture를 10m/1분 기준과 duplicate tag 기대값으로 갱신했다.
- `docs/walksafe-v2/policy_decisions_20260702.md`, `docs/walksafe-v2/threshold_tuning_playbook_20260702.md`, `docs/report_operations.md`, `docs/walksafe-v2/auto_report_policy.md`, `docs/api_reference.md`, `docs/frontend_handoff_without_model.md`, `/home/ddobagi/Downloads/abc.html`을 새 정책에 맞췄다.
- AIHub189 관련 문구를 명확히 했다: 현재는 synthetic nested zip fixture unit PASS이며 실제 AIHub 원본 `Depth_001~005.zip` full run evidence나 Android ARCore PASS가 아니다.

## Verification

- `python3 -m py_compile backend/app/services/duplicates.py backend/app/services/report_serialization.py backend/app/api/reports.py scripts/check_report_retention_dry_run.py` 통과.
- `.venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_reports.py::test_duplicate_candidate_support backend/tests/test_reports_v2.py::test_create_report_v2_reuses_duplicate_policy tests/test_aihub189_depthprediction_offline.py -q`: 3 passed, 2 skipped(PostGIS 미기동), 6 warnings.
- `python3 -c ... HTMLParser ... /home/ddobagi/Downloads/abc.html`: `html_parse_ok`.
- `rg`로 구식 duplicate/auth/기관 제출 충돌 문구 잔존 없음 확인.

## WalkSafe 정책 적용 후속 + AIHub189 원본 full validation

- `scripts/evaluate_aihub189_depthprediction_offline.py`를 원본 AIHub189 nested zip full run에 맞게 보강했다.
  - `--detections-csv`가 없으면 실제 frame manifest 기반 `generated_probe_bbox` CSV를 생성한다.
  - `full_original_zip_run`, `detector_input_kind`, `disp16_scale`, `confidence_policy`를 summary에 기록한다.
  - 대형 inner zip은 전체 메모리 로딩 대신 output temp 디렉터리의 임시 seekable zip으로 한 개씩 처리한다.
  - 실제 `.conf`의 section 형식(`[LEFT_CAM_FHD] fx=...`)도 파싱한다.
  - subsampling valid ratio 분모를 실제 샘플링 시도 수로 고쳐 원본 1920x1080 평가가 과소 판정되지 않게 했다.
- AIHub189 실제 원본 `/home/ddobagi/Downloads/한이음 드림업 데이터셋/인도보행 영상/뎁스프리딕션/Depth_001~005.zip` full offline validation을 실행했다.
  - input sha256: `f02358a0e1e784671d8de01aea94048b457a948bedf6b45596c01b24f03dea03`
  - output: `artifacts/aihub189_depthprediction_original_full_20260702/` (git ignored)
  - result: frames/evaluated 2461, buckets stop 883 / warning 652 / ignore 926, `source_kind=offline_zed_reference`, `reference_not_ground_truth=true`, `arcore_pass=false`, `detector_input_kind=generated_probe_bbox`, `scale_status=unverified`.
- AIHub evidence 문서 `docs/evidence/aihub189_depthprediction_original_full_20260702.md`를 추가하고, `docs/current_status.md`, `docs/evidence/final_report_index.md`, `docs/android/android_native_mvp_evidence_20260630.md`, `docs/android/aihub189_depthprediction_offline_validation_20260701.md`, RTM, 정책 문서를 갱신했다.
- Backend report export에 `duplicate_report_ids` 컬럼을 추가하고, DB 없는 unit test로 `duplicate_candidate` flag와 후보 ID export를 검증했다.
- Admin UI에서 `기관 제출 후보` 표현을 제거하고 `검토 완료 손상 점자블록(redacted)` 내부 export 표현으로 바꿨다.
- Android native에 사용자 확정 정책 일부를 반영했다.
  - TTC bucket: `<=3s STOP`, `3~10s WARNING`, `10~30s AWARE` internal-only.
  - `AWARE`는 기본 TTS message를 만들지 않는다.
  - vibration은 STOP에서만 pattern을 반환한다.
  - route progress beep는 기본 on, 별도 볼륨 20%, UI on/off 및 볼륨 순환, 위험/길안내 audio gate 뒤에서 3초 주기로 재생하도록 연결했다.
- 원본 zip, inner zip, 추출 이미지/depth, 생성 CSV/JSON artifact는 커밋하지 않도록 `.gitignore`에 `artifacts/`를 추가했다.

## Verification

- `.venv/bin/python -m pytest -p no:cacheprovider tests/test_aihub189_depthprediction_offline.py -q`: 5 passed, 8 warnings.
- `.venv/bin/python scripts/evaluate_aihub189_depthprediction_offline.py --outer-zip '/home/ddobagi/Downloads/한이음 드림업 데이터셋/인도보행 영상/뎁스프리딕션/Depth_001~005.zip' --output-dir artifacts/aihub189_depthprediction_original_full_20260702 --full-original-zip-run`: frames/evaluated 2461, stop 883 / warning 652 / ignore 926.
- `sha256sum '/home/ddobagi/Downloads/한이음 드림업 데이터셋/인도보행 영상/뎁스프리딕션/Depth_001~005.zip'`: `f02358a0e1e784671d8de01aea94048b457a948bedf6b45596c01b24f03dea03`.
- `.venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py tests/test_aihub189_depthprediction_offline.py -q`: 9 passed, 8 warnings.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --tests 'kr.co.hanium.dreamup.walksafe.depth.MessagePolicyTest' --tests 'kr.co.hanium.dreamup.walksafe.depth.ObjectTrackerTest' --tests 'kr.co.hanium.dreamup.walksafe.feedback.WalkSafeFeedbackPolicyTest'`: BUILD SUCCESSFUL.
- `cd apps/web && npm run typecheck`: PASS.
- `cd apps/web && npm run lint`: PASS.
- `rg` stale 문구 검색: `기관 제출 후보`, `25m/10분`, `로그인 전까지는 익명`, `AIHub 원본으로 Android ARCore PASS`, `신호등 색상 STOP 구현됨`, `full 19GB dataset run 아님`, `3 passed, 6 warnings` 매치 없음.
- `git check-ignore -v artifacts/aihub189_depthprediction_original_full_20260702/{summary.json,depth_eval.csv,frame_manifest.csv}`: `.gitignore:54:artifacts/`로 ignore 확인.

## Remaining

- 운영 auth/RBAC/rate-limit/upload 접근 제어는 출시 lane `BLOCKED`로 남긴다.
- Android native STT voice input 자체는 후속 구현 필요. 현재 명시 신고는 버튼 경로로 `trigger=voice` 서버 계약과 TTS 정책을 검증한다.
- 신호등 red/green/yellow 색상 STOP은 사용자 지시에 따라 `DEFERRED` 유지.
- AIHub189 결과는 offline ZED reference이며 Android ARCore field PASS가 아니다.

## WalkSafe reporter/explicit report 후속 구현

- Backend `/reports/v2` metadata schema/allowlist/export에 `reporter_user_id`를 추가했다.
- Android `AndroidReportCandidatePolicy`가 login user id를 `reporter_user_id`로 보낸다.
- Android `MainActivity`에 로컬 login user id 입력/저장 gate를 추가했다.
  - user id가 없으면 depth start, destination search, route start/select, explicit report를 막는다.
  - 이 gate는 운영 auth가 아니라 local runtime policy guard다.
- Android explicit report 버튼 경로를 추가했다.
  - GPS가 없으면 저장하지 않고 "위치 정보가 필요합니다."를 TTS로 안내한다.
  - 서버 응답의 `duplicate_report_ids`를 파싱하고, 중복 후보이면 "이미 신고가 된 상태입니다."를 TTS로 안내한다.
  - 성공 시 explicit request는 "신고를 접수했습니다."를 안내한다.
  - 자동 신고는 기존처럼 silent policy를 유지한다.
- `/home/ddobagi/Downloads/abc.html`, `docs/walksafe-v2/policy_decisions_20260702.md`, `docs/report_operations.md`, `docs/walksafe-v2/threshold_tuning_playbook_20260702.md`를 reporter 구현 상태와 운영 auth blocker 구분에 맞게 갱신했다.

## Verification

- `python3 -m py_compile backend/app/schemas.py backend/app/api/reports.py backend/tests/test_report_policy.py`: PASS.
- `.venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py -q`: 4 passed.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --tests 'kr.co.hanium.dreamup.walksafe.report.AndroidReportCandidatePolicyTest' --tests 'kr.co.hanium.dreamup.walksafe.report.AndroidReportUploaderTest' --tests 'kr.co.hanium.dreamup.walksafe.report.MainActivityReportUploadStaticTest'`: BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest`: BUILD SUCCESSFUL.
- `.venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py tests/test_aihub189_depthprediction_offline.py -q`: 9 passed, 8 warnings.
- `python3 -c "from html.parser import HTMLParser; HTMLParser().feed(open('/home/ddobagi/Downloads/abc.html', encoding='utf-8').read()); print('html_parse_ok')"`: `html_parse_ok`.
- `rg` stale 문구 검색: `기관 제출 후보`, `25m/10분`, `로그인 전까지는 익명`, `AIHub 원본으로 Android ARCore PASS`, `신호등 색상 STOP 구현됨`, `full 19GB dataset run 아님`, `3 passed, 6 warnings`, `reporter 전용 필드`, `schema/API/admin/export 구현` 매치 없음.

## WalkSafe Android 음성 신고 버튼 연결

- Android native `MainActivity`에 `SpeechRecognizer` 기반 음성 신고 버튼을 추가했다.
  - `RECORD_AUDIO` 권한은 음성 신고 진입 시점에만 요청한다.
  - 인식 문구에 "신고"와 "해줘/해주세요/해/접수" 계열이 있으면 기존 explicit report route를 호출한다.
  - GPS가 없으면 저장하지 않고 "위치 정보가 필요합니다."를 안내한다.
  - duplicate 응답이면 "이미 신고가 된 상태입니다." 안내를 유지한다.
  - raw audio/transcript 서버 저장은 하지 않는다.
- Android 정적 테스트에 음성 신고 권한, `SpeechRecognizer`, `RecognizerIntent.ACTION_RECOGNIZE_SPEECH`, explicit report 연결 확인을 추가했다.
- `docs/report_operations.md`, `docs/current_status.md`, `docs/walksafe-v2/policy_decisions_20260702.md`, `docs/voice_stt_tts_status.md`, `docs/stt_tts_current_status.md`, `docs/walksafe-v2/voice_command_strategy.md`, `/home/ddobagi/Downloads/abc.html`을 음성 신고 구현 상태에 맞게 갱신했다.
  - 구현 완료: 버튼 기반 Android native 음성 신고 경로와 upload/TTS 정책 연결.
  - 후속 유지: 실폰 mic/TTS 청취, TalkBack 충돌, 보행 중 인식률 Device evidence.

## Verification

- `cd apps/android && ./gradlew :app:testDebugUnitTest`: BUILD SUCCESSFUL.
- `.venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py tests/test_aihub189_depthprediction_offline.py -q`: 9 passed, 8 warnings.
- `python3 -m py_compile backend/app/schemas.py backend/app/api/reports.py backend/tests/test_report_policy.py tests/test_aihub189_depthprediction_offline.py`: PASS.
- `python3 -c "from html.parser import HTMLParser; from pathlib import Path; path=Path('/home/ddobagi/Downloads/abc.html'); HTMLParser().feed(path.read_text(encoding='utf-8')); print('html_parse_ok')"`: `html_parse_ok`.
- `rg` stale 문구 검색: `기관 제출 후보`, `25m/10분`, `로그인 전까지는 익명`, `AIHub 원본으로 Android ARCore PASS`, `신호등 색상 STOP 구현됨`, `voice 연결/실기기 UX는 후속`, `native voice 신고 안내 UX는 후속`, `Android native STT/mic은 후속` 매치 없음.

## WalkSafe 감사 후속 보강

- 병렬 읽기 전용 감사에서 나온 stale/gap을 반영했다.
- Backend `report_to_response()`가 저장된 metadata의 `duplicate_report_ids`를 목록/상세/status 응답 top-level로 복원하도록 보강했다.
- Admin detail에서 `duplicate_candidate` 라벨, 중복 후보 ID 요약, `reporter_user_id`/`duplicate_report_ids`/coordinate gate/threshold metadata를 볼 수 있게 했다.
- RTM과 evidence index, threshold tuning playbook, policy decisions, `/home/ddobagi/Downloads/abc.html`을 갱신했다.
  - AIHub189 원본 full run은 완료 evidence로 표기하되 `arcore_pass=false`를 유지했다.
  - 기관 제출 후보성 문구를 내부 export preset으로 정리했다.
  - 신호등 색상 STOP은 `DEFERRED`로 표기했다.
  - voice/GPS/duplicate/progress beep 관련 이미 구현된 항목을 "후속 구현 필요"로 남기지 않게 정리했다.

## Verification

- `.venv/bin/python -m pytest -p no:cacheprovider backend/tests/test_report_policy.py tests/test_aihub189_depthprediction_offline.py -q`: 10 passed, 8 warnings.
- `cd apps/android && ./gradlew :app:testDebugUnitTest`: BUILD SUCCESSFUL.
- `cd apps/web && npm run typecheck`: PASS.
- `cd apps/web && npm run lint`: PASS.
- `bash scripts/check_frontend_admin_report_summary_policy_20260525.sh`: `admin report summary policy checks passed`.
- `python3 -m py_compile backend/app/services/report_serialization.py backend/tests/test_report_policy.py`: PASS.
- `python3 -c "from html.parser import HTMLParser; from pathlib import Path; path=Path('/home/ddobagi/Downloads/abc.html'); HTMLParser().feed(path.read_text(encoding='utf-8')); print('html_parse_ok')"`: `html_parse_ok`.
- `rg` stale 문구 검색: `기관 제출 후보`, `제출 후보성`, `agency candidate`, `실제 AIHub 원본 .*전체 실행 완료`, `full run evidence가 생겨도`, `Android mic 연결은 별도`, `voice 연결/실기기 UX는 후속`, `native voice 신고 안내 UX는 후속`, `Android native STT/mic은 후속`, `후속 구현이 필요`, `route progress beep와 별도 volume 설정은 아직`, `신호등 색상 STOP 구현됨`, `AIHub 원본으로 Android ARCore PASS`, `25m/10분`, `로그인 전까지는 익명` 매치 없음.
