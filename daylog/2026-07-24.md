# 2026-07-24 작업 로그

## Goal graph-v2 중단 복구와 v2.1 후속 패키지

- 2026-07-23 봉인 직후 원문을 세션 로그에서 byte 단위로 복구해 `walksafe-completion-graph-v2` 19개 파일을 원래 manifest·path-set·content-set과 일치하는 미활성 감사본으로 되돌렸다.
- 중단 당시의 계약 보강 변경은 `walksafe-completion-graph-v2-1` 후속 패키지로 보존했다. v2.0 manifest와 최초 `PACKAGE_PREPARED` event를 신뢰 anchor로 고정하고, v2.1의 supersession record와 archived event를 추가했다.
- Workstream 본문의 선형 “다음 Goal 자동 시작” 문구를 dependency-ready frontier 계약으로 교정하고 EPIC-02 상태 의미, EPIC-12 선행·완료 의존성과 다섯 정식 증거 역할을 정합화했다.
- 257개 산출물을 현재 Register SHA에 결속해 정확히 한 번 분할하는 `artifact_work_queue`와 저장소 내부 완료·외부 완료를 분리하는 `completion_boundary` 계약을 검사기에 추가했다.
- `ARTIFACT_WORK`는 한 `DLV-*`만 소유하고 중복 live ownership·실행 가능 대상 미생성·완료 Goal과 비종결 Register 불일치를 거부한다. 현재 분할은 종결 104, 비활성 1, 적용성 대기 87, 실행 트리거 대기 65이며 첫 구조 재평가 후보는 `DLV-DES-21`이다.
- Work Item 완료 증거는 유형별 required role과 `WORK_ITEM_COMPLETION::<goal_id>`의 정확한 집합만 허용한다. EPIC-12도 정식 실행 1개·Gate 5개·출시판정 1개와 정확한 다섯 증거 역할을 요구한다.
- 과거 EPIC-02 Phase A는 불변 구현 기록·overlay·출력으로 보존하고 이후 FP-018 구현 bytes를 과거 live SHA와 비교해 막지 않도록 continuation/start gate 계약을 교정했다.

## 현재 경계

- v2.1은 이미 봉인된 준비 event를 가진 미활성 predecessor였으므로 byte-exact 감사본으로 복원했다. 보강된 실행 정본 후보는 v2.2이며 아직 `READY_NOT_ACTIVATED`이고 제품 코드는 변경하지 않았다.
- 정식 시험 279개, 실제 기기 시험, 5개 출시 Gate는 계속 `NOT_RUN`·미면제이고 출시는 `NOT_ELIGIBLE`이다.
- v2.2 `PACKAGE_PREPARED` 재봉인, checkpoint 1.13.0 전환, 회귀·전체 시작 Gate 검증을 통과한 뒤 결속된 사용자 activation 권한으로 패키지를 활성화하고 FP-018부터 실행한다.

## 독립 NO-GO 검수와 v2.2 승계 결정

- v2.1의 scheduler·완료 경계·외부 요청·action packet·증거 hash-cycle·bootstrap coverage를 adversarial fixture로 재검수했다. 다중 due 선택, 임의 trigger, 내부 미완료 상태의 외부완료 오판, assessment 정지, 중간 event replay 변조, request·packet identity와 완료증거 우회, FP-017/GAP-026 종결 교착을 찾아 checker와 회귀를 보강했다.
- 전체 Git-visible 변경을 시작 Gate에 결속하기 위해 continuation checker에 canonical repository-state capture를 추가했다. HEAD·branch, porcelain-v2 raw 상태, worktree·index·rename·delete·untracked, checkpoint controlled snapshot을 묶고 checkpoint 및 현재 event gate 경로만 제외한다. TOCTOU, 특수파일, hidden index flag, Git 환경 우회와 정상 commit 뒤 base HEAD ancestry를 회귀로 검증했다.
- 기존 v2.1은 manifest `d8eccbe651c89895b9d1830d7af63b3d18f9bcf23683a5b0f61cc54eb5b01649`, 최초 event `b2a1b08556ee11713854dcafd4df9107860eeab563b1e3cfe1a191384b1bb425`로 이미 준비 봉인됐음을 확인했다.
- 세션 patch 이력을 역적용해 `/tmp/walksafe-v21-original-20260723T170830Z`에 v2.1 원본을 복구했고, protected 20/20, managed 21경로, path-set `28b034519b85127019410487df6b61cf1ee6010ed626659c4d0a0380d38bd1b3`, content-set `1462f0366b7fa6ae2a2f9499c3b1d1a4860abf6e17ee5e2e38ff626d5e1ce2d0`을 독립 검산했다.
- `initial_event_is_immutable` 계약을 지키기 위해 v2.1을 덮어쓰지 않고 byte-exact 감사본으로 복원하며, 독립 검수에서 발견한 보강은 `walksafe-completion-graph-v2-2` v2.2.0 successor로 승계하기로 결정했다. Root·Workstream·FP-018 Goal ID는 유지하고 package·manifest·경로와 새 `PACKAGE_PREPARED` 계보만 버전한다.

## v2.2 설치와 활성화 준비

- 검수 완료 후보를 `docs/control/goals/walksafe-completion-graph-v2-2/`의 정확한 20개 경로로 설치했다. manifest SHA-256은 `c761ed6d9ac83fb98a2dded6eb99b639610e67a7bf13bdd306f8be38e996577b`, package path-set은 `0c2b597f79fd0bb5131e3e623bcb5b31387b25a20c51e4e2ef9975f957846c0b`, content-set은 `ab2f3c67d81679b4ebbc9dcd2e2a42e77afe4074991d15fd1c9be383b4c799ef`다.
- 저장소의 v2.1에서 중단 후 수정됐던 Master·README·manifest·template 2개를 복구본으로 교체하고 전체 디렉터리 `diff`가 없음을 확인했다. 복구 후 manifest는 `d8eccbe651c89895b9d1830d7af63b3d18f9bcf23683a5b0f61cc54eb5b01649`다.
- Goal checker의 scheduler·typed request·action packet·evidence·bootstrap·repository-state 보강 회귀는 새 논리 실패 없이 통과했다. repository-state는 전체 Git-visible 상태의 17개 요약 필드를 receipt·event와 3-way exact 비교하고, `intent-to-add`·unmerged는 허용하면서 불가능한 상태 조합과 임의 JSON 타입은 fail-closed로 거부한다.
- 사용자의 Goal 연속 진행 요청 원문은 줄바꿈 없는 UTF-8 SHA-256 `51ec01e446fc9534f4b623d8cd5fd583475213827988d7266840ac9d5d091502`로 확인했다. v2.2 activation authorization을 event 전용 add-only 경로에 만들고 파일 SHA-256 `d3b4d27c86bec6c6f2c53b6ebc5c71a653483754ce467a12584781fde21ed01c`로 checker trust anchor에 결속하도록 준비했다.

## FP-004 완료와 FP-005 실행 포인터 전환

- 중단된 `WS-GOAL-EPIC-02-FP-004-R001` 세션을 snapshot reconcile과 `WORK_SESSION_RESUMED` 사건으로 복구했다.
- 전맹·저시력 우선 사용자 온보딩, 순서가 고정된 위험안내·일시정지·재개·안전정지 연습, 계정 범위 교육 상태, 연령·보호자 확인, 필수 음성·진동 조건을 Android 보행 시작 fail-closed에 연결했다.
- 변경한 주요 구현·시험 경로는 `MainActivity.kt`, `AndroidFeedbackActuator.kt`, `PriorityUserOnboardingPolicy.kt`, `WalkSessionReadiness.kt`와 관련 JVM·정적 테스트, Android README·사용자 설명서다.
- focused 시험, 사용자 앱 JVM `480/480`, debug assemble, lint를 통과했다. FP-004 builder `--check`와 10개 추적 회귀, 독립검토는 blocker·major 0으로 통과했다.
- append-only Gap·Backlog r011을 생성해 `GAP-013=MISSING→PARTIAL`로 재평가했다. 전체 판정은 `BLOCKED 5 / CONFLICTING 18 / EVIDENCE_MISSING 4 / MISSING 18 / PARTIAL 23 / IMPLEMENTED 0`이며 정식 시험·우선 사용자·실제 기기·보호자 확인 공급자는 계속 `NOT_RUN`, 출시는 `NOT_ELIGIBLE`이다.
- FP-004를 `COMPLETE_AT_TARGET`으로 전이하고 FP-005 Goal 문서를 추가해 `WS-GOAL-EPIC-02-FP-005-R001`을 `READY`·focus로 정했다. 다음 행동은 밝고 건조한 일반 도심 보도, 측정 가능한 환경 품질, 확인 불가 조건의 보수적 제한과 횡단보도 참고 고지를 Android 사전점검·안전정지에 연결하는 것이다.
- `walksafe-project-resumption-runbook.md`, continuation checker·회귀, checkpoint의 r011 snapshot·handoff·controlled-path manifest를 FP-005 기준으로 동기화했다. Goal graph와 continuation 전체 검사, `git diff --check`로 최종 검증했다.

## v2.3 활성 predecessor 승계 준비

- 활성 v2.2의 20개 사건과 FP-005 `READY` tail을 `superseded-v2.2.0-active-checkpoint.json`에 byte-exact 보존했다. archive SHA-256은 `c15d677c9d8f9b68227cb0b2ffaac3aef4794e2bdb031b8cc4ca4a4790fc6024`, tail은 `ec531638c3b3ce68b611e198dba06f6703d837aa712d6aae403f212f8272030d`다.
- v2.3은 v2.2 Goal 문서 17개를 경로·hash 그대로 import하고, README·manifest·active supersession record·v2.2 checkpoint archive·template 2개만 native로 관리한다. native 6, protected 5, imported 17, managed 23 계약을 versioned builder·Goal checker·continuation checker·회귀에 결속했다.
- 패키지는 `WS-GOAL-PACKAGE-WALKSAFE-COMPLETION-GRAPH-V2-3`, manifest SHA-256은 `dfa615686b0223826497fae424c1f3c41538b271102879a9497a33b81f66329c`, 새 이력 seq1 `PACKAGE_PREPARED` hash는 `3e2a585a8224527c5c732c3ac39cfc13fb35ed85613a1f97a0295339c71170ef`다.
- 동결 v2.2 Goal 원본 225개 회귀는 archive adapter로, FP-018·NPC·FP-004 추적 원본 23개 회귀는 별도 history adapter로 실행한다. 현재 통제 계약 hash는 quick `1608a4d72bfb775313abab3b2e414f14994b573e7f8c631f8500ecd4af7a639c`, full `a8a7fc6e0bd88585a73684b46ca3e9ca1ddabadfc2ae7bd293f28d5839bcbd4c`다.
- 주요 변경은 `build_walksafe_goal_graph_v2_3.py`, v2.3 Goal·continuation checker와 회귀, 두 history adapter, v2.3 native 패키지, checkpoint, 통제 README·Goal README·재개 runbook·scripts README·test-layer registry다. 이 준비 과정에서는 제품 코드를 변경하지 않았다.
- builder 생성·`--check`, v2.3 continuation·Goal 검사, test-layer validate, `git diff --check`를 통과했다. GOAL_CONTROL은 `553 passed`, CONTROL_AND_TRACE는 `120 passed, 6 deselected`, FP-018·NPC·FP-004 history adapter는 `23 passed`였고 독립 감사도 `Blocker 0 / Major 0 / Minor 0`으로 통과했다.
- 상태는 `PREPARED_NOT_ACTIVATED / READY_NOT_ACTIVATED`, focus는 FP-005다. `PACKAGE_ACTIVATED`와 `GOAL_STARTED`는 만들지 않았으며, v2.3 package ID·manifest·seq1에 결속된 사용자 명시 승인 뒤에만 quick gate와 전체 시작 gate를 통과해 FP-005를 시작한다.
- 정식 시험 279개, 실제 기기 시험, 5개 출시 Gate는 계속 `NOT_RUN`이고 출시는 `NOT_ELIGIBLE`이다.

## v2.3 활성화와 FP-005 완료·FP-006 전환 준비

- 사용자의 명시 승인에 따라 v2.3을 활성화하고 전체 시작 Gate receipt `f023723484fa21fff80a60be98537ae2f790508ff88af594a5dd0d7b2fc5ebb0`에 결속해 FP-005 `GOAL_STARTED`를 기록했다.
- `OfficialEnvironmentPolicy`, `CameraFrameQualityPolicy`, `CrosswalkReferencePolicy`와 `MainActivity`를 연결해 측정 가능한 GPS·카메라 품질과 사용자 확인 조건을 분리하고, unknown·stale·epoch mismatch·미승인 production profile을 fail-closed 처리했다. 보행 중 품질 저하는 출력을 즉시 억제하고 제한된 재검사 뒤 중앙 안전정지하며, 횡단보도 안내는 신호·차량·안전한 횡단을 보장하지 않는 참고 정보로 제한했다.
- focused Android 시험과 전체 사용자 앱 JVM `511/511`, debug assemble, lint를 통과했다. 원출력은 FP-005 Goal result의 `focused-android-tests.log`, `full-android-verification.log`에 보존했다.
- v2.2 archive가 과거 완료 산출물을 현재 live bytes와 직접 비교하던 결함을 고쳤다. 동결 v2.2 checker·test·archive·과거 receipt는 바꾸지 않고, 정확한 완료 event·receipt·implementation record의 `before→after→live` successor chain만 허용한다. event·receipt 누락과 before hash 변조 회귀를 포함한 focused 7개 검사가 통과했다.
- FP-005 builder 10개 회귀와 `--check`가 통과했다. append-only Gap·Backlog r012를 생성해 `GAP-014=MISSING→PARTIAL`로 재평가했고, 실제 환경 현장·사용자·기기·생산 프로필·정식 시험은 계속 `NOT_RUN`, 출시는 `NOT_ELIGIBLE`로 유지했다.
- FP-006 Goal `WS-GOAL-EPIC-02-FP-006-R001`을 가슴형·목걸이형 정면 장착, 손-held·주머니 금지, 미승인 수치 추측 금지, 교정·안전정지, 기존 보조수단 비대체 경계로 작성했다.
- FP-005 canonical update·완료와 FP-006 materialize·ready를 담은 staged sequence 4~7을 만들었다. staged continuation과 regular-file temp-root Goal graph가 통과했고 두 제어 회귀 묶음은 `344 passed`였다. live checkpoint는 최종 daylog·working snapshot 재봉인 전까지 sequence 3으로 보존했다.
