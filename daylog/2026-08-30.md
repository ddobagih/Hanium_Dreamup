# 2026-08-30

## 사용자·관리자 앱 UI와 기능 통합

- 시각장애인 사용자 앱의 첫 화면·가입·로그인·기기점검·보행·신고 흐름을 단순화하고, 관리자 앱의 로그인·상태·신고 검토 화면을 일반 사용자가 이해하기 쉬운 구조로 정리했다.
- UI/UX 변경 commit `2c2fb260`을 `origin/current`에 push했다.
- 이메일 6자리 OTP 가입, 비밀번호 로그인, 만 14세 이상 자기입력 생년월일 gate, 가입 시 통합 동의, 로그인 뒤 사용자 시작 기기점검과 기능 잠금을 연결했다.
- 사용자 신고 queue·정정·삭제 요청, 관리자 원본 증거·검토 결정·수동 전달 패키지, 신고 삭제 tombstone과 격리 복원 재적용 경계를 보강했다.
- 기관 자동 제출, 자동 제어, 자동 복구는 구현하거나 실행하지 않았다.

주요 변경 경로:

- `apps/android/app/**`
- `apps/android/adminapp/**`
- `apps/android-gateway/**`
- `backend/app/**`, `backend/alembic/versions/202608300001_*.py`~`202608300005_*.py`, `backend/tests/**`
- `scripts/**`, `contracts/**`, `docs/**`

## 로컬·실기기 통합시험

- 전용 PostgreSQL/PostGIS와 Mailpit STARTTLS에서 가입→OTP 수신→계정 생성→로그인→동의→로그아웃을 Gateway 경유로 실행했다.
- Samsung SM-G981N Android 13에서 사용자 앱 가입·OTP·자동 로그인·기기점검 화면 이동을 확인했다.
- 같은 기기에서 관리자 AndroidKeyStore P-256 키 등록, device proof, 비밀번호·미사용 TOTP 로그인, 상태·세션 조회를 확인했다.
- 관리자 custody는 `UNATTESTED`, 운영 화면은 기본 `operations=false`로 유지했다. 승인 상태를 가장하거나 실제 기관 제출을 실행하지 않았다.
- faster-whisper STT와 Qwen3-TTS를 Gateway를 통해 실제 추론했다. 승인 호출어 모델이 없는 `길라잡이` wake word는 제외했다.
- 오프라인 한국어 TTS voice가 준비되지 않아 사용자 기기점검은 안전하게 실패했고 보행 기능을 열지 않았다. TalkBack·실외 보행·지속 발열 시험은 실행하지 않았다.

검증:

- Backend fresh PostgreSQL 전체 회귀: `1440 passed, 2 skipped`
- 별도 cluster 복원 재적용 시험: 이전 실행 `2 passed`
- Gateway: `144 passed`
- Voice: `165 passed`
- Android 사용자 앱: `1,313 passed`, lint 통과
- Android 관리자 앱: `183 passed`, lint·debug build 통과
- 실기기 계측시험: `6 passed`
- backup·환경 식별 회귀: 올바른 CPython 3.14 환경에서 `100 passed`
- Alembic head: `202608300005`
- `git diff --check`: 통과

## 관리자 DB 경계 감사와 중단 상태

- 관리자 원본 증거·검토·전달 경계의 role, ACL, mutation claim, 함수 definition digest와 downgrade 회귀를 fresh PostgreSQL에서 보강했다.
- 정상 기능 회귀는 통과했지만 독립 운영 보안 감사에서 데모를 막지는 않으나 실운영 전에 닫아야 하는 HIGH 6건·MEDIUM 1건을 확인했다.
- 사용자 지시에 따라 추가 구현을 중단하고 다음 문서에 필수 백로그, 완료 기준과 재개 순서를 기록했다.
  - `docs/planning/walksafe-production-database-hardening-backlog-20260830.md`
- 실환경 실행 보고서에서 위 백로그를 운영 배포 전 필수 항목으로 연결했다.

중단 시 주의:

- `backend/app/services/admin_report_integrity.py`의 session-lock 신뢰 테이블 ACL 추가와 `backend/tests/test_admin_report_integrity_postgres_integration.py`의 drift 회귀 추가는 중단 당시 미검증 상태였다.
- 중단 후 오류 여부를 확인해 `py_compile`, 표적 단위/readiness `56 passed`, fresh PostgreSQL 통합시험 `3 passed`를 확인했다. 실행 중인 휴대폰 데모 DB도 새 코드의 관리자 boundary가 `true`였다. 현재 작업트리는 문법·표적 DB 경로나 데모 DB 경계가 깨진 상태가 아니다.
- 이 최소 검증은 아래 운영 보강 백로그 전체 완료를 의미하지 않는다.
- 나머지 필수 보강은 role descendant 분리, `reports` 5개 열 UPDATE 제한, exact trigger·함수 결속, RLS/policy/rule 금지, constraint/index manifest, migration 자체 fail-closed다.

## 남은 외부 운영 입력

- 실제 SMTP 업체·발신 주소와 외부 수신 시험
- 실제 TMAP key와 운영 provider
- 오프라인 한국어 TTS, TalkBack, 실외 보행·지속 발열 시험
- 사업자·수탁자·보유기간·책임자·공개 URL과 개인정보 문안 승인
- custody 승인 뒤 관리자 실업무, 사람이 기관에 제출하고 실제 접수 확인
- 운영 HTTPS origin, Android release signing과 배포 채널

## 실행 자원과 기록

- 휴대폰용 PostgreSQL·Mailpit, Backend·Gateway·Voice와 ADB reverse는 다음 데모를 위해 실행 상태로 보존했다.
- 실운영 보강 작업은 문서화 후 병렬 에이전트를 중단했다.
- control/Goal graph 자동화와 장시간 watcher service·timer 시작은 수행하지 않았다.

## 길라잡이 호출어 기반 온디바이스 음성 통합

- 활성 보행 중 `길라잡이`를 기다리고 같은 발화 또는 6초 명령 창의 후속 발화를 기존 명령 처리 경로로 전달하도록 구현했다. 호출어와 명령은 동일한 Vosk 한국어 모델을 사용하고, 최종 transcript와 평균 confidence 0.60 이상만 허용한다.
- 마이크 foreground service, 보행·마이크·알림·버전 고지·모델 준비 gate, 앱 pause/권한 철회/보행 종료 시 중지, 기존 Gateway/플랫폼 STT와의 마이크 직렬화, TTS·TalkBack 출력 중 입력 억제를 연결했다.
- PCM과 transcript는 메모리에서만 처리하고 파일·서버·원문 로그에 남기지 않는다. 모델 asset은 고정 archive/file hash로 검증해 `noBackupFilesDir`에 원자적으로 준비하며, 누락된 모델로는 APK asset merge가 실패한다.
- 주요 변경: `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/voice/**`, `MainActivity.kt`, `AndroidManifest.xml`, Android Gradle dependency/verification 파일, `scripts/prepare_vosk_ko_small_model.py`, 음성 단위·정적 테스트와 상태·동의·실환경 문서.

검증:

- Android 전체 debug 단위·정적 회귀: `1,348 passed`, failure/error/skip 0
- `:app:lintDebug`, `:app:assembleDebug`: 성공
- Vosk 모델: 18 files, 264,377,676 bytes, hash manifest 검증 성공
- 최종 debug APK: 223,172,638 bytes, SHA-256 `09027b56400e191e09197585fc75d6b678d1eb868fb5ec2f809b0c4a220d2f6c`
- SM-G981N에 데이터 보존 설치·cold launch 성공, 기기 `base.apk` hash 일치, private 모델 252MB 확인, 즉시 crash 없음
- `git diff --check`: 호출어 변경 범위 통과
- 코드 문서 검사기는 이번 변경의 missing/empty 파일 없이, 기존 장문 모듈 주석 누락 21건 때문에 실패했다.

남은 실환경 검증:

- 기기가 로그아웃 상태이고 Android 13 알림 권한이 거부돼 실제 호출어 서비스는 의도대로 시작되지 않았다. 로그인→새 음성 고지 동의→알림 권한 허용→ACTIVE 보행 뒤 실제 발화, 소음별 오탐·미탐, TalkBack 중복, 15분 이상 발열·배터리·메모리는 `NOT_RUN`이다.
- 213MB급 개발 APK는 운영 배포 승인 근거가 아니다. 출시 전 AAB/asset delivery 등 모델 전달 방식을 확정하고 동일 release 후보로 재시험해야 한다.
- 음성 실기기 시험용 로컬 일회용 이메일 계정을 정상 가입 계약으로 생성하고 Gateway 비밀번호 로그인을 확인했다. 이메일·비밀번호·OTP는 작업 로그에 기록하지 않았다.

## 기종 독립 로그인 후 기기점검 보강

- 특정 제조사·모델 목록이나 현재 연결 단말을 통과 조건으로 쓰지 않고, 로그인한 모든 Android 기기에 같은 실제 기능시험을 실행하도록 기기점검을 보강했다.
- 공통 필수 항목은 로컬 한국어 voice 선택 뒤 실제 TTS 파일 합성, 사용자 시작 Vosk `길라잡이` 인식, fresh non-mock GPS fix, 실제 진동 요청과 사용자 체감 확인, CameraX 실제 프레임과 production detector 완료, 권한·알림·음성 고지·자원 상태다.
- 공통 항목과 실제 metric Depth가 모두 성공하면 `FULL`, Depth가 명시적으로 미지원이면서 공통 항목이 성공하면 `LIMITED`, 나머지는 `FAIL`로 닫는다. 탐지 결과가 0건이어도 실제 모델 timing과 completed model 증거가 있으면 pipeline 실행 성공으로 인정한다.
- 호출어 시험은 자동 청취하지 않는다. 사용자가 TalkBack 안내를 듣고 시작 버튼을 누른 뒤 안내 길이에 따른 quiet window 후에만 마이크를 열며, 청취 중 화면에서 literal 호출어를 숨긴다.
- 장문 항목 목록의 live-region/contentDescription 중복을 제거하고 짧은 상태 전용 live region, 진동 질문·맥락형 예/아니오 버튼·접근성 초점, 하드웨어별 실패 표시를 추가했다.
- timeout·background·session/attempt 변경·재시도에서 GPS, Vosk, 진동, CameraX, ARCore metric preflight와 TTS probe를 닫는다. metric 결과는 exact 로그인 session/attempt binding이 같은 경우에만 반영한다.
- 실환경 실행 보고서와 기능 체크리스트를 최신 기종 독립 판정·APK·잔여 다기종 시험 기준으로 갱신했다. 자동 제어와 외부 자동 전송은 추가하지 않았다.

주요 변경:

- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/device/AndroidStartupCapabilityProbe.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/device/PostLoginDeviceCheckPolicy.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/device/AndroidKoreanTextToSpeechSynthesisProbe.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/device/AndroidDeviceCheckHapticProbe.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/device/DeviceCheckDetectorExecutionPolicy.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/device/DeviceCheckLocationFixPolicy.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/voice/VoskWakePhraseProbe.kt`
- 대응 Android 단위·정적 테스트, `docs/testing/walksafe-real-environment-*.md`

검증:

- Android 사용자 앱 debug 단위·정적 회귀: `1,374 passed`, failure/error/skip 0
- `:app:lintDebug`, `:app:assembleDebug`: 성공
- Vosk bundled model asset 검증: 성공
- SM-G981N Android 13 실기기 TFLite·AndroidKeyStore 계측시험: `6 passed`
- 최신 debug APK: 223,172,638 bytes, SHA-256 `18876f3eca3539f982325887f9c65ea5933f87030312326dd2708d2ec96eef6e`
- 같은 APK를 `adb install -r`로 설치하고 기기 `base.apk` hash 일치, 로그인 화면 cold launch·프로세스 유지·즉시 FATAL 없음 확인
- 변경한 tracked Android 범위 `git diff --check`: 통과

남은 실환경 검증:

- 휴대폰은 최신 APK 로그인 화면에 있다. 로그인 뒤 진동 체감, 실제 `길라잡이` 발화, 로컬 TTS, GPS fix, CameraX detector, ARCore Depth와 TalkBack 종단점검은 사람 입력이 필요해 `NOT_RUN`이다.
- 현재 SM-G981N 결과는 첫 검증 증거일 뿐 허용 기준이 아니다. 서로 다른 제조사·Android 버전과 Depth 지원/미지원 조합에서 동일 기능시험을 반복해야 한다.

## 로컬 사용자 로그인 계정 재생성

- 실행 중인 Gateway·Backend·Mailpit의 정식 이메일 OTP 가입 흐름으로 로컬 테스트 계정 1개를 생성했다.
- OTP 요청 `202`, 계정 생성 `201`, 동일 자격정보 비밀번호 로그인 `200`과 세션 쿠키 발급을 확인했다.
- 이메일·비밀번호·OTP·세션 값은 작업 로그에 기록하지 않았다.

## 두 번째 실기기 사용자 앱 설치

- 새로 연결된 Samsung `SM-S931N`(Android 16, API 36, arm64-v8a)에 최신 사용자 debug APK를 기존 데이터 삭제 없이 설치했다.
- 호스트 Gateway용 `adb reverse tcp:8081 tcp:8081`을 설정하고 앱을 cold launch해 로그인 화면이 포그라운드에 표시되는 것을 확인했다.
- 기기 설치본과 로컬 APK SHA-256이 `18876f3eca3539f982325887f9c65ea5933f87030312326dd2708d2ec96eef6e`로 일치했고, 앱 프로세스가 유지되며 즉시 FATAL 로그가 없었다.
- 자동 로그인, 권한 승인, 기기점검 실행은 하지 않고 사용자 수동 시험을 위해 로그인 화면에 두었다.

## 두 번째 실기기 로그인 제한 복구

- 새 휴대폰 로그인 실패를 재현해 계정 오류가 아니라 Gateway의 `gateway_login_rate_limited` 429 응답임을 확인했다.
- ADB reverse를 거친 여러 실기기·호스트 로그인이 같은 `127.0.0.1`로 집계되고, 정상 시도를 포함한 5회가 5분 안에 누적돼 15분 제한이 적용된 것이 원인이었다.
- 운영 보안 코드와 전역 기록은 변경하지 않고 로컬 개발 IP의 제한 파일만 잠금 아래 백업 이동했다.
- 같은 계정의 비밀번호 로그인 `200`과 세션 쿠키 발급을 확인한 뒤 검증 세션을 `204`로 로그아웃하고 임시 쿠키 파일을 삭제했다.
- 검증 시도 기록도 백업 이동해 로컬 IP 시도 횟수를 0으로 만들고, `SM-S931N`의 Gateway reverse를 재확인한 뒤 사용자 앱 로그인 화면을 다시 포그라운드에 표시했다.
