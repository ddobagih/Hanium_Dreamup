# 2026-08-15 WalkSafe 작업 로그

## PR #3 병합 준비

- `agent/fp022-completion-20260814`에서 `current`로의 Git 이력은 5커밋 fast-forward 가능하고 충돌이 없음을 확인했다.
- 실패한 acceptance의 완료 전이 테스트 12개를 current unit에서 historical replay 계층으로 옮겼다.
- 복구 verifier 테스트가 호스트 Gradle cache에 의존하지 않도록 synthetic wrapper·module seed fixture를 만들고, Python verifier가 Android Gradle 실행보다 먼저 유지되도록 했다.
- 후속 migration에 맞춰 FP-046 PostgreSQL 회귀의 Alembic head와 runtime ACL 기대값을 현행화했다.

## 관리자 복구 보안 수정

- startup에서 새 TOTP 후보가 결속된 뒤 transaction이 만료되면 expiry RPC가 기존 TOTP만 허용해 복구가 교착되는 문제를 PostgreSQL에서 재현했다.
- 이미 적용된 `202608120001` migration은 원본 바이트로 복원하고, `202608150001` forward migration에서만 동일 transaction/token/expiry/issuer에 결속된 pending 후보의 만료 정리를 허용했다.
- proof middleware가 만료를 route보다 먼저 410으로 종료하는 추가 교착을 발견해 add-only `202608150002`에서 exact pending 결속과 active P-256 device key가 일치할 때만 서명 검증·cleanup route까지 진행하도록 했다.
- startup classifier는 exact 만료 후보만 `RECOVERY_EXPIRED_CANDIDATE`로 구분하고 readiness가 cleanup API를 노출하되, 다른 후보·issuer·device·token drift는 계속 fail closed한다.
- 두 downgrade는 recovery advisory lock, 관련 3개 테이블 write-conflicting lock, active/pending/incomplete preflight 순서로 TOCTOU를 막고 runtime `EXECUTE WITH GRANT OPTION` ACL drift도 거부한다.
- 실제 PostgreSQL runtime role과 P-256 키로 challenge 발급·서명·middleware·실제 service를 관통해 typed 410, challenge 소비, `RECOVERY_REQUIRED`, state version 증가, pending 3필드 제거와 credential 불변을 검증했다.
- PostgreSQL 전용 runtime ACL 테스트를 DB 없는 unit에서 functional 계층으로 옮겼다.

## 통제 후속 검토

- 기존 seq70/71 R014 세 문서는 변경하지 않고 별도 `control-successor-reviews/20260815/R001`에서 6개 통제 successor와 4개 acceptance evidence를 exact binding했다.
- actor-separated 독립 리뷰 결과는 `APPROVED`이고 blocking·major·minor open finding은 모두 0이다.
- R001 이후 발견된 managed closure의 verifier script/test digest 전이를 기존 문서 수정 없이 add-only `R002`에서 다시 봉인했다.
- R002는 R014·R001을 frozen replay하고 승인된 두 predecessor→successor만 strict overlay하며 누락·추가·중복·제3 digest를 fail closed 처리한다.
- 별도 reviewer의 R002 결과도 `APPROVED`이고 blocking·major·minor finding은 모두 0이다. Assignment/result/independent SHA-256은 각각 `e3a84b76…`, `6e2f9b27…`, `c7dff21f…`이다.
- 후속 의미 감사에서 발견된 stale migration head, R002 외부 seal, 만료 proof·downgrade 보강을 기존 문서 수정 없이 add-only `R003`에서 다시 봉인했다.
- R003는 R002 triplet을 exact pin으로 frozen replay하고 control 4개, acceptance evidence 12개, 신규 migration 2개와 verifier 2행 전이를 결속한다.
- 별도 `/root/control_successor_r003_alt` reviewer가 PostgreSQL 회귀까지 재실행한 결과 `APPROVED`, blocking·major·minor finding 0이며 assignment/result/independent SHA-256은 각각 `51c0350e…`, `178b53e1…`, `8babb666…`이다.
- 정식 시험·외부 검토·실기기·배포·출시 credit은 모두 0이며 release는 `NOT_ELIGIBLE`로 유지했다.

## 변경 파일

- 복구 보안: `backend/alembic/versions/202608150001_admin_recovery_expiry_candidate.py`, `202608150002_admin_recovery_expired_proof.py`, device proof service, health/readiness, ACL·관리자 보안·FP046 테스트와 관련 README.
- acceptance 계층: `scripts/run_walksafe_test_layers_current.sh`, 복구 verifier·DB preflight 테스트.
- 통제·문서: FP022 review builder/test, Goal graph checker, R001/R002/R003 review triplet, current security compatibility binding, continuation checkpoint·catalog.

## 검증

- Cacheless 복구 verifier: 50 PASS. Layer preflight: 21 PASS. R003 builder: 36 PASS.
- 실제 PostgreSQL 대상 보안·ACL·FP046 묶음: 202 PASS. 전체 functional Python: 806 PASS.
- 현행 unit Python: 1,021 PASS, 8 SKIP. Active-session control: 59 PASS. Model/data audit: 12 PASS.
- Goal graph, continuation, repository catalog, active-doc, OpenAPI, R003 assignment/result/post-review validator: PASS.
- Android source가 바뀌지 않은 앞선 실행에서 debug unit 1,000 PASS를 확인했다.
- 최종 로컬 Node/Gateway/Android 재실행은 제품 assertion이 아니라 `/tmp` 사용자 quota의 npm errno `-122` 때문에 완료하지 못했다. 임의 임시 파일은 삭제하지 않고 깨끗한 GitHub Actions에서 재확인한다.
