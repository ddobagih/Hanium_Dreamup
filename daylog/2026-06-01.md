# 2026-06-01

## Android native gate 실행 계획 수립

### 한 일
- Android Coordinate, Model/Data/Evidence, Backend/Ops/Report lane 조사 결과를 통합해 2026-06-01 기준 다음 스텝 계획서를 작성했다.
- 다음 작업을 P0~P6 gate로 분리했다.
  - P0: 실기기 overlay/depth 관찰 체크리스트
  - P1: ARCore overlay/depth coordinate mapper 구현 분기
  - P2: metadata-only capture log/evidence 보강
  - P3: static RGB/model threshold 정리
  - P4: ARCore RGB-D 검증 dataset 설계
  - P5: backend/report 연결 전 source/metadata 계약 정리
  - P6: gate 통과 후 TTS/haptic/report/navigation/voice 순서
- 문서 지도와 현재 상태/roadmap의 Android 다음 계획 링크를 새 계획서로 갱신했다.

### 변경 파일
- `plans/features/2026-06-01_android_native_gate_execution_plan.md`
- `docs/README.md`
- `docs/current_status.md`
- `product/roadmap.md`
- `daylog/2026-06-01.md`

### 검증
- 계획서 제목, 기준 APK sha256, P0~P6 섹션, `[단계]`/`-> 검증:` 형식 sanity check → PASS.
- trailing whitespace check → PASS.
- `git diff --check -- docs/README.md docs/current_status.md product/roadmap.md` → PASS.
- 신규 plan/daylog 파일은 Python trailing whitespace check로 확인 → PASS.

### 남은 리스크
- 이 작업은 계획서 작성과 문서 링크 갱신이다. 실기기 overlay/depth/`N보` 관찰은 아직 수행하지 않았고 PASS로 기록하지 않았다.

## Android native gate 문서 실행

### 한 일
- 팀 lane 병렬 조사 결과를 통합해 6/1 gate 계획의 Codex-only 문서 산출물을 작성했다.
- P0 실기기 overlay/depth/`N보` 체크리스트를 작성했다.
- P2 metadata-only capture schema를 작성했다.
- P4 ARCore RGB-D dataset schema를 작성했다.
- P3 static RGB threshold evidence 문서를 작성하고 full static rerun blocked 상태를 명시했다.
- P5 Android report source/metadata decision draft를 작성했다.
- ARCore 1.54.0 API 확인 결과를 coordinate mapping plan에 반영했다.
- 문서 지도, evidence index, feature matrix, current status, root README, WalkSafe v2 README, 6/1 gate 계획서를 새 문서 기준으로 갱신했다.

### 변경 파일
- `README.md`
- `plans/features/2026-06-01_android_native_gate_execution_plan.md`
- `docs/README.md`
- `docs/current_status.md`
- `docs/android/arcore_coordinate_mapping_plan.md`
- `docs/android/android_device_overlay_depth_checklist_20260601.md`
- `docs/android/android_metadata_capture_schema_20260601.md`
- `docs/android/arcore_rgbd_dataset_schema_20260601.md`
- `docs/evidence/android_static_threshold_evidence_20260601.md`
- `docs/evidence/final_report_index.md`
- `docs/evidence/feature_matrix.md`
- `docs/walksafe-v2/android_report_source_metadata_decision_20260601.md`
- `docs/walksafe-v2/README.md`
- `daylog/2026-06-01.md`

### 검증
- 문서 sanity/trailing whitespace check → PASS.
- `git diff --check -- README.md plans/features/2026-06-01_android_native_gate_execution_plan.md docs/README.md docs/current_status.md docs/android/arcore_coordinate_mapping_plan.md docs/evidence/final_report_index.md docs/evidence/feature_matrix.md docs/walksafe-v2/README.md` → PASS.
- 신규 문서는 Python trailing whitespace/sanity check로 확인 → PASS.
- `python scripts/check_static_dataset_readiness_20260531.py` → `blocked_for_full_static_eval_dataset_missing` 확인.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning 유지.

### 남은 리스크
- 실기기 overlay/depth/`N보` 관찰은 아직 수행하지 않았다.
- report upload 구현, source enum 확장, metadata allowlist 확장, 이미지/depth 저장 확대는 gate와 사용자 승인 전까지 보류한다.

## Android metadata-only log 좌표 정합 필드 보강

### 한 일
- `MetadataCaptureLogEntry`에 preview size, depth size, transform path, fallback reason 필드를 추가했다.
- 현재 좌표 경로가 아직 identity임을 UI/debug summary에서 `transform=identity fallback=arcore_mapper_not_connected`로 드러내도록 했다.
- `MainActivity`가 depth snapshot 크기와 GL surface 크기를 capture log에 넘기도록 연결했다.
- `MetadataCaptureLogTest`와 Android depth scaffold static check를 새 metadata 필드 기준으로 갱신했다.
- 최신 debug APK를 다시 빌드하고 SHA-256을 문서에 반영했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MetadataCaptureLog.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/MetadataCaptureLogTest.kt`
- `scripts/check_android_depth_scaffold_20260531.py`
- `README.md`
- `apps/android/README.md`
- `docs/current_status.md`
- `docs/android/arcore_depth_estimation_architecture.md`
- `docs/android/android_device_overlay_depth_checklist_20260601.md`
- `docs/android/android_metadata_capture_schema_20260601.md`
- `docs/evidence/final_report_index.md`
- `docs/evidence/feature_matrix.md`
- `plans/features/2026-06-01_android_native_gate_execution_plan.md`
- `daylog/2026-06-01.md`

### 검증
- `python -m py_compile scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning 유지.
- `cd apps/android && ./gradlew test assembleDebug --no-daemon` → BUILD SUCCESSFUL.
- 최신 debug APK: `apps/android/app/build/outputs/apk/debug/app-debug.apk`
- 최신 APK SHA-256: `1cfbc4087944eb2f92801f8eb958b346c83b3547a3c34bf4de2cf2d3b988bf23`
- touched file trailing whitespace/sanity check → PASS.
- `git diff --check -- <tracked touched files>` → PASS.

### 남은 리스크
- transform path는 아직 실제 ARCore mapper가 아니라 명시적 identity/fallback 표시다.
- 실기기 overlay/depth/`N보` 정합은 아직 체크리스트로 관찰해야 한다.

## Android server metadata debug log 시스템 구현

### 한 일
- Android 실기기 화면 공유 없이 depth/overlay 문제를 확인할 수 있도록 metadata-only server debug log 경로를 추가했다.
- backend에 `/android/debug/depth-logs`, `/android/debug/depth-logs/recent` endpoint를 추가했다.
- backend debug log는 기본 비활성(`ANDROID_DEBUG_LOG_ENABLED=false`)이며, local/dev에서 명시적으로 켰을 때만 JSONL로 저장한다.
- Android debug build에만 `INTERNET` permission과 local cleartext HTTP를 추가했다. release manifest에는 `INTERNET` permission이 없다.
- Android 앱에 `서버 로그 켜기/끄기` 버튼과 debug-only HTTP uploader를 추가했다.
- uploader는 metadata-only JSON만 전송하며 이미지, depth raw, confidence image, screenshot, GPS, audio, secret은 포함하지 않는다.
- 서버 debug log runbook을 작성하고 문서 인덱스/현재 상태/evidence/privacy 문서를 갱신했다.

### 변경 파일
- `.gitignore`
- `backend/.env.example`
- `backend/app/api/android_debug.py`
- `backend/app/config.py`
- `backend/app/main.py`
- `backend/app/schemas.py`
- `backend/tests/test_android_debug_logs.py`
- `apps/android/app/src/debug/AndroidManifest.xml`
- `apps/android/app/src/debug/java/kr/co/hanium/dreamup/walksafe/debuglog/DebugMetadataLogUploaderFactory.kt`
- `apps/android/app/src/debug/java/kr/co/hanium/dreamup/walksafe/debuglog/HttpMetadataLogUploader.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/debuglog/MetadataLogEndpointPolicy.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/debuglog/MetadataLogJsonEncoder.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/debuglog/MetadataLogUploader.kt`
- `apps/android/app/src/release/java/kr/co/hanium/dreamup/walksafe/debuglog/DebugMetadataLogUploaderFactory.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/debuglog/MetadataLogJsonEncoderTest.kt`
- `scripts/check_android_depth_scaffold_20260531.py`
- `README.md`
- `apps/android/README.md`
- `docs/README.md`
- `docs/current_status.md`
- `docs/android/android_device_overlay_depth_checklist_20260601.md`
- `docs/android/android_metadata_capture_schema_20260601.md`
- `docs/android/android_server_debug_log_runbook_20260601.md`
- `docs/android/arcore_depth_estimation_architecture.md`
- `docs/evidence/feature_matrix.md`
- `docs/evidence/final_report_index.md`
- `docs/evidence/privacy_checklist.md`
- `docs/walksafe-v2/android_report_source_metadata_decision_20260601.md`
- `plans/features/2026-06-01_android_native_gate_execution_plan.md`
- `daylog/2026-06-01.md`

### 검증
- `python -m py_compile backend/app/api/android_debug.py backend/app/config.py backend/app/schemas.py scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_android_debug_logs.py -q` → 4 passed.
- `cd apps/android && ./gradlew :app:testDebugUnitTest :app:assembleDebug :app:processReleaseMainManifest --no-daemon` → BUILD SUCCESSFUL. 중간에 Kotlin daemon disk quota warning이 있었지만 fallback 후 성공했다.
- `cd apps/android && ./gradlew :app:compileReleaseKotlin --no-daemon -Dkotlin.incremental=false` → BUILD SUCCESSFUL.
- debug merged manifest에서 `INTERNET`/`usesCleartextTraffic` 확인 → 있음.
- release merged manifest에서 `INTERNET`/`usesCleartextTraffic` grep → 없음.
- 최신 debug APK SHA-256: `17a1b0b4efe2b4fd9f85a619292d2fceb40f11c7530187201095fc1a36c71c8f`.
- touched file sanity/trailing whitespace check → PASS.
- `git diff --check -- <tracked touched files>` → PASS.

### 남은 리스크
- debug log upload 성공은 Device PASS가 아니다. overlay/depth/`N보` 정합은 저장된 metadata로 별도 판정해야 한다.
- local/dev server 실행과 `adb reverse` 설정은 사용자가 실기기 테스트할 때 필요하다.
- 외부 공개 서버, 인증/TLS, 보존기간, 운영 접근권한은 별도 승인 전 범위 밖이다.

## Android remote metadata log APK 임시 빌드

### 한 일
- 사용자가 밖에서 폰으로 APK를 설치/실행할 수 있도록 임시 Cloudflare download tunnel을 열었다.
- 사용자의 명시 승인 후 Android debug metadata log backend를 local/dev 모드로 실행하고 Cloudflare quick tunnel로 임시 노출했다.
- debug APK의 metadata log endpoint를 임시 backend tunnel URL로 바꿔 `:app:assembleDebug`를 실행했다.
- 빌드 후 source endpoint는 다시 기본 local endpoint(`http://127.0.0.1:8000/android/debug/depth-logs`)로 되돌렸다.
- 새 remote-log debug APK를 download tunnel에서 받을 수 있게 symlink를 추가했다.

### 변경/산출물
- 임시 빌드 산출물: `apps/android/app/build/outputs/apk/debug/app-debug.apk`
- 임시 download symlink: `/tmp/walksafe-apk-download/walksafe-remote-log-debug.apk`
- 원격 debug log 저장 폴더: `logs/android_remote_debug_20260601/`
- 소스 파일 `apps/android/app/src/debug/java/kr/co/hanium/dreamup/walksafe/debuglog/DebugMetadataLogUploaderFactory.kt`는 임시 패치 후 원복했다.

### 검증
- backend debug server `/health` tunnel smoke → PASS.
- backend debug server `/android/debug/depth-logs/recent` tunnel smoke → PASS, 현재 items empty.
- `cd apps/android && ./gradlew :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL.
  - 중간에 Kotlin daemon disk quota warning이 있었지만 fallback compile 후 성공했다.
- remote-log APK SHA-256: `4e3edf27989ccd0588104f440c34894260dd1639c30c794c19b4d3531f08d1f2`.
- download tunnel HEAD 확인 → HTTP 200, content-length 68678044.

### 남은 리스크
- Cloudflare quick tunnel은 임시 외부 노출이며, 링크를 아는 사용자는 APK/debug endpoint에 접근할 수 있다.
- remote-log APK는 임시 디버그 산출물이며 canonical APK hash 문서의 기존 기준 APK와 다르다.
- 실기기 로그 수신은 사용자가 remote-log APK를 재설치하고 앱에서 `서버 로그 켜기`를 눌러야 확인된다.
- 테스트 종료 후 tunnel/server를 닫고 필요하면 local endpoint 기준 debug APK를 다시 빌드해야 한다.
- 추가: 기존 download tunnel 만료 여유를 늘리기 위해 같은 APK symlink를 새 1시간 download tunnel에서도 확인했다.

## Android remote bbox/depth 로그 분석 및 mapped overlay 개선 APK

### 한 일
- 사용자가 원격 debug APK로 사람/보도블럭을 테스트한 metadata log를 분석했다.
- 로그 196건 기준 detector 갱신 지연, stale 구간, identity transform fallback, tactile block 낮은 confidence/큰 bbox 후보를 확인했다.
- `DebugBboxOverlayView`에 ARCore `IMAGE_PIXELS -> VIEW` 변환 결과를 pixel rect로 그릴 수 있는 mapped overlay 경로를 추가했다.
- `MainActivity`에서 detector bbox를 현재 ARCore frame의 `Frame.transformCoordinates2d(Coordinates2d.IMAGE_PIXELS, ..., Coordinates2d.VIEW, ...)`로 view rect에 매핑해 overlay에 전달하도록 했다.
- detector cadence가 `추론시간 + 1초`로 늘어지지 않도록 완료 시점에 `lastDetectionRunMs`를 다시 덮어쓰던 경로를 제거하고, debug interval을 500ms로 낮췄다.
- detector debug status에 detect duration과 camera image size를 표시하도록 했다.
- 원격 debug APK 빌드를 위해 endpoint를 임시 tunnel URL로 바꿔 빌드한 뒤, source는 다시 local endpoint로 원복했다.
- Android 빌드가 `/tmp` quota 때문에 실패해 `/tmp`의 오래된 AIHub 임시 다운로드/압축 파일을 정리했다. 또한 Android 빌드 재시도 중 Web generated cache/node_modules를 제거했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/DebugBboxOverlayView.kt`
- `daylog/2026-06-01.md`

### 임시 정리
- 삭제: `/tmp/aihub_dltest`, `/tmp/aihub_dltest2`, `/tmp/aihub_dltest3`, `/tmp/aihub_71626_511851.tar`, `/tmp/aihub_71626_511851_test.tar`
- 삭제: `apps/web/node_modules`, `apps/web/.next`, `apps/web/tsconfig.tsbuildinfo`
- 주의: Web 작업 전에는 `cd apps/web && npm install`이 다시 필요하다.

### 검증
- 원격 metadata log 수신 확인: `logs/android_remote_debug_20260601/android_depth_debug.jsonl` 196 rows.
- 로그 분석 결과:
  - `transform_path=identity`, `fallback_reason=arcore_mapper_not_connected` 196/196.
  - detector frame 갱신 간격 median 약 3.16초, max 약 5.7초.
  - `detector_age_ms` median 약 1449ms, max 약 3362ms, `age>2500ms` 28건.
  - `person` 35건, `normal_tactile_block` 35건. tactile confidence는 약 0.356~0.430으로 낮음.
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon --rerun-tasks` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --no-daemon` → BUILD SUCCESSFUL. 중간 `/tmp` quota로 Kotlin incremental warning 후 fallback.
- `/tmp` quota probe 64MB write → cleanup 후 PASS.
- `cd apps/android && ./gradlew :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL.
- mapped/fast remote-log APK SHA-256: `18e8fbbbbfa745a251030324da6462ebdbad000739af999dbd788cef5bbb33d3`.
- download tunnel HEAD 확인 → HTTP 200, content-length 68643598.
- `git diff --check -- apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/DebugBboxOverlayView.kt daylog/2026-06-01.md` → PASS.

### 남은 리스크
- 이번 APK는 overlay 표시를 ARCore view 좌표로 바꾸는 1차 수정이며, depth sampling은 아직 identity path다.
- metadata log만으로 G1/G2 Device PASS를 주장할 수 없다. 사용자의 육안 관찰로 bbox 위치 개선 여부를 확인해야 한다.
- detector 자체는 여전히 960 custom + 640 COCO float32 두 모델을 순차 실행하므로 근본 latency 개선에는 model scheduling/buffer reuse/delegate 검토가 필요하다.
- remote-log APK는 임시 debug 산출물이며 canonical APK hash와 다르다.

## Android remote manual image capture debug 추가

### 한 일
- 사용자의 요청에 따라 metadata log와 함께 원격으로 확인 가능한 수동 이미지 캡쳐 경로를 추가했다.
- 개인정보 노출을 줄이기 위해 자동 연속 캡쳐가 아니라 앱 버튼을 누를 때 다음 camera frame 1장을 저해상도 JPEG로 업로드하는 방식으로 제한했다.
- backend debug API에 `/android/debug/frame-captures`, `/android/debug/frame-captures/recent`를 추가했다.
- Android debug build에 `이미지 1장 캡쳐` 버튼을 추가했다.
- 업로드 metadata에는 frame timestamp, camera image size, detect duration, detection count, top detection class/confidence/bbox, capture policy를 기록한다.
- 이미지 업로드는 JPEG만 전송하며 GPS, audio, depth raw, screenshot, secret은 포함하지 않는다.
- 기존 backend server를 새 코드로 재시작하고, 새 backend/download Cloudflare quick tunnel을 열어 원격 APK를 다시 빌드했다.

### 변경 파일
- `backend/app/api/android_debug.py`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/debuglog/FrameCaptureUploader.kt`
- `apps/android/app/src/debug/java/kr/co/hanium/dreamup/walksafe/debuglog/DebugFrameCaptureUploaderFactory.kt`
- `apps/android/app/src/debug/java/kr/co/hanium/dreamup/walksafe/debuglog/HttpFrameCaptureUploader.kt`
- `apps/android/app/src/release/java/kr/co/hanium/dreamup/walksafe/debuglog/DebugFrameCaptureUploaderFactory.kt`
- `daylog/2026-06-01.md`

### 검증
- `python -m py_compile backend/app/api/android_debug.py` → PASS.
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon --rerun-tasks` → BUILD SUCCESSFUL.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_android_debug_logs.py -q` → 4 passed.
- backend tunnel `/android/debug/frame-captures/recent` smoke → PASS.
- backend tunnel `/android/debug/frame-captures` multipart smoke → PASS 후 test capture record/file은 제거.
- `cd apps/android && ./gradlew :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL.
- capture remote APK SHA-256: `56b834208489ad38078139a7feb95f4e51b5075d1d903b9ff9be55233f7f0023`.
- download tunnel HEAD 확인 → HTTP 200, content-length 68686438.
- `git diff --check -- <touched files>` → PASS.

### 남은 리스크
- 이미지 캡쳐는 수동 1장씩이어도 주변 사람/차량번호/주소가 찍힐 수 있다. 사용자는 민감한 장면을 피해서 버튼을 눌러야 한다.
- capture APK는 임시 remote debug 산출물이며 canonical APK와 다르다.
- Cloudflare quick tunnel은 임시 외부 노출이다. 테스트 종료 후 tunnel/server를 닫아야 한다.

## Android detector latency/stale/mapper 긴급 수정

### 한 일
- 실기기 로그와 팀 읽기 전용 분석을 기준으로 bbox 생성/소멸 지연 원인을 정리했다.
  - 기존 stale 판정이 detector 완료 시각 기준이라, 오래된 camera frame 결과가 완료 직후 fresh로 취급될 수 있었다.
  - 960 custom tactile + 640 COCO float32 모델을 매번 순차 실행해 detector interval이 1.5~3초대로 늘어졌다.
  - overlay mapping에서 `Frame`을 UI thread로 넘겼고, depth sampling은 identity mapper에 남아 있었다.
- Android detector를 COCO 우선 partial publish 구조로 바꿨다.
  - COCO 결과를 먼저 publish하고, custom tactile은 interval마다 실행한다.
  - detector timing을 YUV decode / COCO preprocess/inference/parse / custom preprocess/inference/parse로 분리 기록한다.
  - TFLite output/flat buffer와 model input buffer를 재사용해 반복 할당을 줄였다.
- stale guard를 완료 시각이 아니라 source camera image age 기준으로 바꿨다.
  - overlay는 source age 약 1.2초 초과 시 제외한다.
  - depth 입력은 source age 약 0.8초 초과 시 제외한다.
- overlay update와 status text update를 분리했다.
  - overlay는 GL frame에서 `IMAGE_PIXELS -> VIEW` 좌표를 계산한 뒤 UI에는 pixel rect만 전달한다.
  - status text는 기존 500ms throttle을 유지한다.
- depth sampling mapper를 주입 가능하게 바꾸고, ARCore `IMAGE_PIXELS -> TEXTURE_NORMALIZED` mapper를 우선 사용하게 했다.
  - mapper가 없으면 identity fallback으로 남긴다.
- metadata-only log schema를 latency/좌표 분석 필드로 확장했다.
  - camera image size, display rotation, overlay/depth transform path, detector timing, partial/skipped model 정보를 추가했다.
- bbox parser가 normalized 좌표의 미세한 범위 초과를 pixel 좌표로 오판하지 않도록 tolerance를 추가했다.
- Android README/current status/APK sha를 최신 빌드 기준으로 갱신했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MetadataCaptureLog.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/ObjectDepthRuntimePipeline.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/AndroidDetectorContracts.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TfliteAndroidFrameDetector.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/YuvImagePreprocessor.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/YoloEndToEndOutputParser.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/debuglog/MetadataLogJsonEncoderTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/ObjectDepthRuntimePipelineTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/YoloEndToEndOutputParserTest.kt`
- `backend/app/schemas.py`
- `backend/tests/test_android_debug_logs.py`
- `apps/android/README.md`
- `docs/current_status.md`
- `docs/android/android_metadata_capture_schema_20260601.md`
- `daylog/2026-06-01.md`

### 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon --rerun-tasks` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --no-daemon` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL.
- `python -m py_compile backend/app/schemas.py backend/app/api/android_debug.py` → PASS.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_android_debug_logs.py -q` → 4 passed.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning 유지.
- touched file trailing whitespace check → PASS.
- `git diff --check -- backend/app/schemas.py docs/current_status.md apps/android/README.md docs/android/android_metadata_capture_schema_20260601.md` → PASS.
- 최신 debug APK: `apps/android/app/build/outputs/apk/debug/app-debug.apk`
- 최신 APK SHA-256: `86cd52d88f05cb72c291e5edc703bba09c3a75c8052e9c9087bccf6d4a4d3c5b`

### 남은 리스크
- 이번 수정은 로컬 빌드/단위 테스트 기준이며, 실기기에서 detector timing과 bbox/depth 정합을 재확인해야 한다.
- custom tactile은 여전히 큰 960 float32 모델이라 COCO보다 느릴 수 있다. 로그의 `detector_custom_inference_ms`를 보고 float16/int8/작은 input/NNAPI 또는 별도 모델 교체를 후속 판단한다.
- depth mapper는 ARCore `TEXTURE_NORMALIZED` 경로를 사용하지만, depth image와 texture normalized 정합은 실기기에서 median/sample 위치로 검증해야 한다.
- 새 APK를 외부 임시 다운로드 링크로 공개하는 작업은 사용자 확인 전 실행하지 않았다.

## Android depth/frame 정합 재검토 및 보수 패치

### 한 일
- 최신 로그와 병렬 코드 리뷰 결과를 통합해 depth가 객체와 어긋나는 원인을 재판정했다.
  - 현재 수신 로그 363건은 최신 latency/mapper APK 이전 로그이며, 모두 `identity` depth transform과 `arcore_mapper_not_connected` fallback이었다.
  - 기존 로그의 `frame_timestamp_ms - detector_frame_timestamp_ms` gap은 2~3초대가 많아, 오래된 bbox로 현재 depth를 샘플링했을 가능성이 컸다.
  - top detection과 best depth selection 기준이 달라 multi-detection 구간에서 class mismatch가 발생할 수 있음을 확인했다.
- MainActivity depth 입력 guard를 보강했다.
  - source age뿐 아니라 ARCore frame timestamp delta도 stale 조건에 포함했다.
  - ARCore depth mapper가 없으면 MainActivity 실사용 경로에서 detection을 depth pipeline에 넘기지 않도록 했다.
  - `IMAGE_PIXELS -> TEXTURE_NORMALIZED` depth mapper가 bbox corner 일부만 변환하지 못하면 왜곡된 삼각형으로 샘플링하지 않고 metric sampling을 억제하도록 했다.
- depth pipeline/sampler를 보수적으로 수정했다.
  - 일부 객체만 miss된 프레임에서 이전 track bbox를 현재 depth로 계속 estimate하지 않도록 current update에서 본 track만 depth output으로 사용한다.
  - 큰 ROI에서 max sample cap이 걸릴 때 row-major 상단 편향이 생기지 않도록 stride 계산을 `ceil(sqrt(...))` 기준으로 바꿨다.
  - mask/polygon 없는 YOLO bbox fallback은 bbox 전체가 아니라 10% 안쪽 ROI로 샘플링한다.
- metadata-only log를 원인 판별용으로 보강했다.
  - `detectorFrameDeltaMs`, best depth track id, detection confidence, final score, p20, risk distance, IQR 필드를 추가했다.
  - backend Android debug schema와 단위 테스트를 새 필드에 맞췄다.
- Android README/current status/schema 문서를 최신 APK와 새 guard 기준으로 갱신했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MetadataCaptureLog.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/debuglog/MetadataLogJsonEncoder.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/ObjectDepthRuntimePipeline.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/DepthSampler.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/depth/MaskPolygonExtractor.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/MetadataCaptureLogTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/debuglog/MetadataLogJsonEncoderTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/DepthSamplerTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/depth/ObjectDepthRuntimePipelineTest.kt`
- `backend/app/schemas.py`
- `backend/tests/test_android_debug_logs.py`
- `scripts/check_android_depth_scaffold_20260531.py`
- `apps/android/README.md`
- `docs/current_status.md`
- `docs/android/android_metadata_capture_schema_20260601.md`
- `daylog/2026-06-01.md`

### 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon --rerun-tasks` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --no-daemon` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL.
- `python -m py_compile backend/app/schemas.py backend/app/api/android_debug.py` → PASS.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m pytest backend/tests/test_android_debug_logs.py -q` → 4 passed.
- `python -m py_compile scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning 유지.
- 최신 debug APK: `apps/android/app/build/outputs/apk/debug/app-debug.apk`
- 최신 APK SHA-256: `c8c19f8cd44c0cda0947202ba5397ef52ccb82a97d7ab80408402d1797d32810`

### 남은 리스크
- latest `c8c19f8...` APK로 실기기 재테스트한 로그는 아직 없다. 기존 로그만으로 최신 mapper/frame-delta guard의 Device PASS를 주장할 수 없다.
- custom tactile model은 여전히 큰 960 float32 모델이라 tactile bbox가 늦거나 miss/과대 bbox가 날 수 있다. 새 로그의 `detector_custom_inference_ms`와 `detectorFrameDeltaMs`를 보고 모델 경량화/입력 축소/quantization 여부를 결정해야 한다.
- `TEXTURE_NORMALIZED`를 depth normalized로 쓰는 ARCore mapper는 설계상 후보 경로이며, 실제 depth image 정합은 실기기에서 overlay와 depth median/sample 위치로 재검증해야 한다.
- 외부 임시 다운로드 링크나 원격 tunnel은 이번 작업에서 새로 열지 않았다.

## Android depthfix remote APK 링크 재발급

### 한 일
- 사용자의 요청에 따라 최신 depth/frame 정합 패치가 포함된 remote debug APK 다운로드 링크를 재발급했다.
- local/dev backend debug server를 `ANDROID_DEBUG_LOG_ENABLED=true`로 실행하고 Cloudflare quick tunnel로 임시 노출했다.
- Android debug metadata/frame capture endpoint를 임시 backend tunnel URL로 바꿔 remote APK를 빌드한 뒤, source endpoint는 다시 local endpoint로 원복했다.
- remote APK는 `/tmp/walksafe-apk-download/walksafe-depthfix-remote-debug-20260601.apk`로 복사해 download tunnel에 노출했다.
- source 원복 후 canonical local debug APK도 다시 빌드했다.

### 산출물
- remote backend tunnel: `https://youth-rrp-friday-donate.trycloudflare.com`
- remote APK download tunnel: `https://carmen-aka-secret-strategic.trycloudflare.com/walksafe-depthfix-remote-debug-20260601.apk`
- remote APK SHA-256: `7f3b33ab1a19f8db4f1ffddecf62c4689476cd615047480af4b99dae12bce939`
- source 원복 후 local debug APK SHA-256: `fc9c561f2434222317ffd3edd189c84c441fc8c78cef251007ea78bfd45aba09`

### 검증
- backend tunnel `/health` → 200.
- backend tunnel `/android/debug/depth-logs/recent` → 200.
- backend tunnel `/android/debug/frame-captures/recent` → 200.
- `cd apps/android && ./gradlew :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL.
- download tunnel HEAD → 200, `content-length=68686960`, `content-type=application/vnd.android.package-archive`.

### 남은 리스크
- Cloudflare quick tunnel은 임시 외부 링크이며 uptime 보장이 없다.
- remote APK에는 임시 backend tunnel URL이 포함되어 있으므로, tunnel/server가 살아 있는 동안만 원격 metadata/image capture 업로드가 가능하다.
- 링크를 아는 사람은 APK에 접근할 수 있다. 테스트 후 tunnel/server를 닫아야 한다.

## Android overlay flicker 원인 확인 및 visual hold 패치

### 한 일
- 사용자의 최신 테스트 로그를 확인해 박스가 나타났다 사라지는 원인을 판정했다.
  - 최신 session `android-debug-1780286123813` 231 rows에서 `detection_count=0`이 108 rows였고, detector snapshot이 0개를 내보낼 때 overlay가 즉시 비었다.
  - depth는 `frame_delta>800ms` 85 rows로 보수적으로 suppress되는 것이 맞지만, visual overlay까지 같은 raw detection snapshot을 그대로 따라가 UX flicker가 커졌다.
  - camera image는 ARCore CPU image 기준 640x480으로 들어오며, 수동 frame capture만 저해상도 JPEG 품질 70으로 업로드된다.
- overlay와 depth 정책을 분리했다.
  - depth 입력은 기존 strict source age/frame delta guard를 유지한다.
  - overlay는 fresh detection이 잠깐 0개가 되더라도 마지막 non-empty detection snapshot을 최대 1.5초까지만 유지해 단발 miss로 박스가 깜빡이지 않게 했다.
  - session stop 시 held overlay snapshot도 clear한다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `daylog/2026-06-01.md`

### 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon --rerun-tasks` → BUILD SUCCESSFUL.

### 남은 리스크
- overlay visual hold는 단발 detector miss를 숨기는 UX 보정이다. 실제 depth/거리 안내에는 적용하지 않았다.
- 물체가 실제로 사라졌을 때 박스가 최대 1.5초 남을 수 있다. 현장 테스트에서 길면 0.8~1.0초로 줄인다.
- 아직 이 visual hold 패치 APK는 원격 링크로 재발급하지 않았다.

## Android visual hold remote APK 재발급

### 한 일
- 사용자의 요청에 따라 overlay flicker 완화 visual hold 패치가 포함된 remote debug APK를 다시 빌드했다.
- metadata/frame capture endpoint를 기존 임시 backend tunnel URL로 바꿔 remote APK를 만들고, `/tmp/walksafe-apk-download/walksafe-depthfix-remote-debug-20260601.apk`를 새 APK로 덮어썼다.
- source endpoint는 다시 local endpoint로 원복했고, 원복 후 canonical local debug APK를 재빌드했다.

### 산출물
- remote APK download URL: `https://carmen-aka-secret-strategic.trycloudflare.com/walksafe-depthfix-remote-debug-20260601.apk`
- remote APK SHA-256: `539163d6e50694e8b9b17ab9b91162343cac2916892c1bc8d617892db8fe3133`
- source 원복 후 local debug APK SHA-256: `966a0e2e001a178fd640a175f3250843e1e896dd5a0900e3b2eb42877b81902e`

### 검증
- `cd apps/android && ./gradlew :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL(remote endpoint build).
- `cd apps/android && ./gradlew :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL(source 원복 후 local endpoint build).
- download tunnel HEAD → 200, `content-length=68726766`, `content-type=application/vnd.android.package-archive`.

### 남은 리스크
- Cloudflare quick tunnel은 임시 외부 링크이며 uptime 보장이 없다.
- visual hold는 화면 bbox 깜빡임 완화용이며 depth/거리 판단에는 strict guard를 계속 사용한다.

## Android tactile block 미검출 로그 판정 및 capture 진단 보강

### 한 일
- 사용자가 점자블록 이미지 캡쳐 후 미검출을 보고해 최신 frame capture/log를 확인했다.
- 최신 캡쳐 `3f9bf2ba-e61f-46c5-9433-e4bbfee7a175.jpg`에는 노란 점자블록이 보이지만 metadata는 `detection_count=1`, `top_detection_class_name=person`이었다.
- 해당 캡쳐 시점 전후 depth log는 대부분 `detector_completed_models=['coco_general']`, `detector_skipped_models=['custom_tactile']`였다. 즉 점자블록을 담당하는 custom tactile 모델이 캡쳐 frame에서 실행되지 않았다.
- custom tactile이 실행된 구간도 약 1.0초 이상 걸려 overlay/depth freshness gate에서 stale이 되기 쉬움을 확인했다.
- 수동 이미지 캡쳐 요청 frame에서는 custom tactile 모델을 강제로 실행하도록 detector 계약을 확장했다.
- frame capture metadata에 detector completed/skipped model, custom/coco inference ms, 전체 detection list를 추가했다.
- custom tactile 결과 중 tactile class overlay는 느린 custom inference를 고려해 화면 표시 허용/hold 시간을 별도로 늘렸다. depth/거리 판단은 기존 strict guard를 유지한다.
- Android runtime config에서 `normal_tactile_block` threshold를 0.35에서 0.25로 낮춰 정상 점자블록 debug recall을 높였다.

### 변경 파일
- `apps/android/app/src/main/assets/model-config/two_model_runtime.json`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/AndroidDetectorContracts.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TfliteAndroidFrameDetector.kt`
- `daylog/2026-06-01.md`

### 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon --rerun-tasks` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --no-daemon` → BUILD SUCCESSFUL.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning 유지.

### 남은 리스크
- normal tactile threshold를 낮춰 false positive가 늘 수 있다. remote debug 로그에서 class별 false positive를 보고 다시 조정해야 한다.
- force custom capture는 진단 정확도를 높이지만 캡쳐 시 detector가 약 1초 이상 더 걸릴 수 있다.
- 이 패치 APK는 아직 remote download 링크로 재발급하지 않았다.

## Android full-test tactile APK 재발급

### 한 일
- 사용자의 지적에 따라 이번 APK는 임시 skip/force 혼합이 아니라 full-test 정책으로 고정했다.
- debug full-test APK에서는 custom tactile 모델을 매 detector cycle마다 실행한다.
- 수동 frame capture metadata에는 전체 detection list, completed/skipped model, COCO/custom inference time을 기록한다.
- frame capture JPEG quality를 70에서 90으로 올렸다.
- `normal_tactile_block` threshold는 0.25로 낮춘 상태를 유지해 정상 점자블럭 recall을 우선 확인한다.
- remote APK를 기존 download URL에 덮어썼고, source endpoint는 local endpoint로 원복했다.

### 산출물
- remote APK download URL: `https://carmen-aka-secret-strategic.trycloudflare.com/walksafe-depthfix-remote-debug-20260601.apk`
- remote APK SHA-256: `94afb87f3b857531b00981e882a00bfcd406e812c83474d65c7415de940536d9`
- source 원복 후 local debug APK SHA-256: `753bf8a06106477ac4ae7b8d8613171777bed0ee3cb31da57ff2e40f0fcc39e5`

### 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon --rerun-tasks` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --no-daemon` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:assembleDebug --no-daemon` → BUILD SUCCESSFUL.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS, backend threshold 차이는 warning 유지.
- download tunnel HEAD → 200, `content-length=68698193`, `content-type=application/vnd.android.package-archive`.

### 남은 리스크
- custom tactile을 매 cycle 실행하므로 박스 갱신은 느려질 수 있다. 이번 APK는 속도보다 점자블럭 검출 여부와 후보 bbox 확인을 위한 진단용이다.
- 정상 점자블럭 threshold를 낮췄기 때문에 false positive가 늘 수 있다. 테스트 로그로 다시 조정해야 한다.

## Android overlay partial merge 정책 분리

### 한 일
- COCO partial 결과가 `latestDetectionSnapshot`을 덮으면서 이전 custom tactile 박스가 즉시 사라지는 문제를 줄이기 위해 overlay 전용 tactile snapshot을 분리했다.
- `latestDetectionSnapshot`은 depth/risk/log용 최신 detector raw 결과로 유지하고, `latestTactileOverlaySnapshot`은 tactile class가 실제로 나온 마지막 결과만 보관하도록 했다.
- overlay 계산에서 현재 fresh COCO/custom 결과에 tactile이 없으면, tactile 전용 hold 시간 안의 마지막 tactile 박스를 화면 표시용으로만 병합한다.
- depth/risk 입력은 기존처럼 `latestDetectionSnapshot.freshDetections(... MAX_DEPTH ...)`만 사용해 오래된 tactile hold 박스가 거리/위험 계산에 들어가지 않게 유지했다.

### 변경 파일
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/MainActivity.kt`
- `daylog/2026-06-01.md`

### 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon` 실행.
- 결과: FAILED. 실패 지점은 이번 작업 범위 밖인 `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TfliteAndroidFrameDetector.kt:92,99`의 `No parameter with name 'runtime' found` 컴파일 오류였다.

### 남은 리스크
- 이번 변경은 화면 overlay 깜빡임 완화용이며 custom tactile 자체의 1초대 inference 지연/미검출 문제는 해결하지 않는다.
- 마지막 tactile 박스를 짧게 표시 유지하므로 빠르게 시야가 바뀌는 상황에서는 화면 표시 박스가 실제 위치보다 늦게 남을 수 있다. depth/risk에는 이 hold 결과를 넣지 않았다.

## Android custom_tactile runtime config/fallback scaffold

### 한 일
- inference 범위에서 TFLite interpreter runtime 설정을 모델별 config로 분리했다.
- `two_model_runtime.json`에 `runtime.delegate`, `runtime.num_threads`, `runtime.fallback_to_cpu`를 추가했다.
- 현재 기본값은 기존 동작과 같은 `cpu`, `4 threads`, `fallback_to_cpu=true`로 두어 탐지 정책은 바꾸지 않았다.
- `nnapi` delegate 선택 시 interpreter 생성 실패 또는 invoke 실패가 발생하면 CPU interpreter로 재생성해 한 번 재시도하도록 fallback을 추가했다.
- `AndroidDetectorTiming`에 COCO/custom별 requested delegate, active delegate, thread 수, fallback 여부를 담을 runtime 필드를 추가했다. MainActivity 로그 직렬화는 담당 범위 밖이라 직접 수정하지 않았다.

### 변경 파일
- `apps/android/app/src/main/assets/model-config/two_model_runtime.json`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/AndroidDetectorContracts.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TfliteAndroidFrameDetector.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelRuntimeConfig.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelRuntimeConfigTest.kt`
- `daylog/2026-06-01.md`

### 검증
- `python scripts/check_android_tflite_contract_20260531.py` → PASS. backend threshold 차이 warning 유지.
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon --rerun-tasks` → FAILED. 실패는 현재 담당 범위 밖 `MainActivity.kt`의 overlay stabilizer 관련 unresolved reference/metadata parameter 오류이며, inference 변경 파일의 컴파일 오류는 출력되지 않았다.

### 남은 리스크
- GPU delegate는 현재 Gradle dependency가 없어 구현하지 않았다. 사용하려면 TensorFlow Lite GPU delegate dependency와 기기별 fallback 검증이 필요하다.
- runtime 필드는 contract에만 추가되어 실제 서버 로그/캡쳐 metadata에 남기려면 MainActivity에서 JSON key를 매핑해야 한다.
- 기본 config는 CPU 4 threads라 즉시 속도 개선을 강제하지 않는다. 다음 APK 후보에서 `custom_tactile.runtime.delegate=nnapi` 또는 `num_threads` 변형으로 실기기 benchmark가 필요하다.

## Unified YOLO26n COCO+tactile 학습/export 스크립트 초안

### 한 일
- 사용자가 COCO를 person-only로 축소하지 않겠다고 정정한 내용에 맞춰, 기존 WalkSafe COCO 일반 객체 allowlist를 유지하고 tactile class를 추가하는 unified dataset builder 초안을 추가했다.
- 기본 class 초안은 `person`, `bicycle`, `car`, `motorcycle`, `bus`, `truck`, `traffic light`, `bench`, `normal_tactile_block`, `damaged_tactile_block` 10-class로 잡았다.
- `tactile_damage_area`는 기본 제외하고, 필요 시 builder 옵션으로 11-class 비교안을 만들 수 있게 했다.
- legacy two-model Android exporter와 분리된 unified TFLite export 초안과 학습 실행 wrapper, 정적 계획 check script를 추가했다.
- `model/README.md`에 unified YOLO26n 640 전환 초안과 실행 명령을 기록했다.

### 변경 파일
- `data_sources/scripts/build_walksafe_unified_coco_tactile.py`
- `scripts/run_walksafe_unified_yolo26n_20260601.sh`
- `scripts/export_walksafe_unified_tflite_20260601.py`
- `scripts/check_walksafe_unified_training_plan_20260601.py`
- `model/README.md`
- `daylog/2026-06-01.md`

### 검증
- `python -m py_compile data_sources/scripts/build_walksafe_unified_coco_tactile.py scripts/export_walksafe_unified_tflite_20260601.py scripts/check_walksafe_unified_training_plan_20260601.py` → PASS.
- `python scripts/check_walksafe_unified_training_plan_20260601.py --allow-missing-dataset` → PASS, dataset 미생성 warning만 출력.
- `python data_sources/scripts/build_walksafe_unified_coco_tactile.py --dry-run` → PASS, 기본 10-class 계획 출력.
- `bash scripts/run_walksafe_unified_yolo26n_20260601.sh --print-only` → PASS, 학습 명령 출력만 수행.
- `python scripts/export_walksafe_unified_tflite_20260601.py --dry-run` → PASS, export 명령 출력만 수행.
- `bash -n scripts/run_walksafe_unified_yolo26n_20260601.sh` → PASS.

### 남은 리스크
- 실제 데이터셋 생성/학습/export는 수행하지 않았다. `TS8`, `TS9`, `VS1`, `VS2`, COCO train/val/annotations가 준비되어야 한다.
- COCO 전체 80-class도 builder 옵션으로 가능하지만, 기본은 기존 앱 allowlist 보존이다. 제품 범위가 80-class로 확정되면 class 수와 Android/backend 계약을 다시 맞춰야 한다.
- 새 TFLite가 Android parser가 기대하는 NMS output `[1,300,6]`로 나오는지는 실제 export 후 별도 확인해야 한다.

## Android unified COCO+tactile model candidate scaffold

### 한 일
- 사용자의 지적에 따라 단일 모델 방향을 `person` 전용이 아니라 기존 COCO class order 80개를 유지하고 `normal_tactile_block`, `damaged_tactile_block`를 뒤에 추가하는 82-class 후보로 정리했다.
- Android runtime config에 disabled 상태의 `unified_walksafe` 후보를 추가했다. 실제 unified TFLite asset이 아직 없으므로 `primary_model`은 `legacy_two_model`로 유지해 현재 앱 빌드/실행 fallback이 깨지지 않게 했다.
- `TwoModelRuntimeConfig`가 `primary_model`, optional `unified_walksafe`, model `enabled` flag를 파싱하도록 확장했다.
- `TfliteAndroidFrameDetector`에 unified single-model 실행 경로를 추가했다. `primary_model=unified_walksafe`이고 asset이 준비되면 1회 inference로 COCO allowlist 객체 + tactile 결과를 반환하고, asset load 실패 시 legacy two-model로 fallback한다.
- `TwoModelClassMap`에 unified 82-class mapping과 allowlist를 추가했다.
- Android TFLite contract/export/check script가 `unified_walksafe` 후보를 인식하도록 갱신했다.

### 변경 파일
- `apps/android/app/src/main/assets/model-config/two_model_runtime.json`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/AndroidDetectorContracts.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TfliteAndroidFrameDetector.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelClassMap.kt`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelRuntimeConfig.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelRuntimeConfigTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/YoloEndToEndOutputParserTest.kt`
- `scripts/check_android_tflite_contract_20260531.py`
- `scripts/check_android_depth_scaffold_20260531.py`
- `scripts/export_android_tflite_models_20260531.py`
- `daylog/2026-06-01.md`

### 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --no-daemon` → BUILD SUCCESSFUL.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS. `unified_walksafe` asset missing은 disabled candidate warning으로 처리됨.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/export_android_tflite_models_20260531.py --dry-run --unified yolo26n.pt` → unified asset export command 확인.

### 남은 리스크
- 실제 `walksafe_unified_yolo26n_640_float32.tflite` asset은 아직 없으므로 unified path는 disabled 상태다.
- unified 모델 class order는 COCO 80개 뒤에 tactile 2개를 붙이는 82-class 기준으로 고정했다. 학습/export도 이 순서를 반드시 맞춰야 한다.
- MainActivity의 상세 metadata schema는 아직 COCO/custom 중심 필드를 유지한다. unified 활성화 후 서버 로그까지 깔끔하게 보려면 generic detector timing 필드 매핑을 추가해야 한다.
- APK 빌드/배포, 서버 조작, 모델 학습, 대용량 다운로드는 수행하지 않았다.

## Backend/Web unified_walksafe contract compatibility

### 한 일
- `unified_walksafe` model_key를 backend detect.v2 schema, pure runtime helper, YOLO result adapter, web parser/type에 추가했다.
- 기존 COCO 일반 객체를 person-only로 축소하지 않고 `person`, `car`, `bus`, `truck`, `bicycle`, `motorcycle`, `traffic light`, `bench` allowlist에 tactile class를 더한 unified class 계약을 추가했다.
- backend real/yolo 모드에서 `DETECT_V2_UNIFIED_MODEL_PATH`가 있으면 unified single model을 lazy load하고, 없으면 기존 `custom_tactile + coco_general` two-model fallback을 유지하도록 했다.
- `/reports/v2`는 기존 custom damaged tactile 신고를 유지하면서 `unified_walksafe`의 `damaged_tactile_block`도 허용하도록 했다. unified class id는 학습 class order가 최종 고정되기 전까지 고정값 검사를 하지 않는다.
- web detect.v2 parser, auto-report, risk policy, ROI general-object 판정, navigation normal tactile 판정, admin model key label이 unified 결과를 버리지 않도록 갱신했다.
- configs에 unified_walksafe threshold/classes/class_categories 예시를 추가했다.

### 변경 파일
- `model/two_model_runtime.py`
- `model/test_two_model_runtime.py`
- `configs/walksafe_two_model_runtime_20260522.yaml`
- `configs/walksafe_two_model_runtime_stage1_mvp_20260523.json`
- `backend/app/config.py`
- `backend/app/schemas.py`
- `backend/app/services/detect_v2.py`
- `backend/app/services/report_policy.py`
- `backend/app/services/yolo_inference_adapter.py`
- `backend/tests/test_detect_v2.py`
- `backend/tests/test_yolo_inference_adapter.py`
- `apps/web/types/inference-v2.ts`
- `apps/web/lib/detect-api-v2.ts`
- `apps/web/lib/report-api.ts`
- `apps/web/lib/auto-report-v2.ts`
- `apps/web/app/_walksafe/risk-evaluator.ts`
- `apps/web/app/_walksafe/risk-roi.ts`
- `apps/web/app/_walksafe/hooks/useNavigationGuidance.ts`
- `apps/web/app/_walksafe/components/CameraSurface.tsx`
- `apps/web/app/_walksafe/components/AssistPanel.tsx`
- `apps/web/app/admin/page.tsx`
- `apps/web/tests/auto-report-v2-policy.test.ts`
- `apps/web/tests/risk-evaluator-policy.test.ts`
- `daylog/2026-06-01.md`

### 검증
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m pytest model/test_two_model_runtime.py backend/tests/test_yolo_inference_adapter.py backend/tests/test_detect_v2.py backend/tests/test_reports_v2.py -q` → PASS, 57 passed.
- `cd apps/web && npm ci --ignore-scripts` → PASS, dependency 설치 후 검증 환경 준비.
- `cd apps/web && npm run typecheck` → PASS.
- `cd apps/web && npm run lint` → PASS.

### 남은 리스크
- 실제 unified 모델 학습/export는 수행하지 않았다. backend/web은 `unified_walksafe` payload를 받을 준비만 끝난 상태다.
- backend unified real runtime의 class filtering은 현재 기존 앱 COCO allowlist + tactile class 기준이다. 새 모델을 COCO 80-class 전체로 학습하기로 확정하면 config/classes와 web label/priority 정책을 추가 확장해야 한다.
- `DETECT_V2_UNIFIED_MODEL_PATH`가 명시됐는데 파일이 없으면 unified 설정 오류로 본다. unified env를 비워두면 기존 two-model fallback은 유지된다.

## Unified COCO+tactile 전환 통합 검증

### 한 일
- 사용자의 정정에 따라 단일 모델을 `person` 전용이 아닌 기존 COCO 일반 객체 유지 방향으로 재정리했다.
- Android 쪽은 `unified_walksafe` 후보가 COCO 80개 class order 뒤에 `normal_tactile_block`, `damaged_tactile_block`를 붙인 82-class 모델을 받을 수 있게 준비되어 있음을 확인했다.
- Dataset/export 쪽은 기존 앱 COCO allowlist 8개 일반 객체 + tactile 2개 기준 10-class 학습 스크립트 초안이 추가된 상태를 확인했다.
- Backend/Web 쪽은 `unified_walksafe` model_key를 받아도 결과를 버리지 않도록 확장된 상태를 확인했다.
- 실제 학습, 대용량 다운로드, APK 배포, 서버 재시작은 수행하지 않았다.

### 추가 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --no-daemon` → BUILD SUCCESSFUL.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS. `unified_walksafe` asset missing은 disabled candidate warning.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/check_walksafe_unified_training_plan_20260601.py --allow-missing-dataset` → PASS, dataset 미생성 warning.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m pytest model/test_two_model_runtime.py backend/tests/test_yolo_inference_adapter.py backend/tests/test_detect_v2.py backend/tests/test_reports_v2.py -q` → PASS, 57 passed.
- `cd apps/web && npm run typecheck && npm run lint` → PASS.

### 남은 결정/리스크
- Android config는 82-class(COCO80+tactile2), dataset builder 기본은 10-class(기존 allowlist8+tactile2)라 최종 학습 class order를 하나로 확정해야 한다.
- 사용자가 COCO 전체 80개 유지까지 원하면 dataset builder/export/backend/web 우선순위를 82-class로 맞춰야 한다.
- 실제 unified TFLite asset이 아직 없어 Android primary는 `legacy_two_model` 유지 중이다.

## Unified model class order 확정: 10-class

### 한 일
- 사용자가 최종 class order를 아래 10개로 확정했다.
  1. `person`
  2. `bicycle`
  3. `car`
  4. `motorcycle`
  5. `bus`
  6. `truck`
  7. `traffic light`
  8. `bench`
  9. `normal_tactile_block`
  10. `damaged_tactile_block`
- 이전에 남아 있던 Android `unified_walksafe` 82-class(COCO80+tactile2) 후보를 10-class 기준으로 정정했다.
- `model/two_model_runtime.py`, backend/web 테스트, Android config/class map/check script, runtime config 예시를 10-class 기준으로 맞췄다.
- legacy `custom_tactile`의 `tactile_damage_area`는 기존 two-model fallback 호환용으로 유지하지만, unified 1차 모델에서는 제외했다.

### 변경/정렬 파일
- `apps/android/app/src/main/assets/model-config/two_model_runtime.json`
- `apps/android/app/src/main/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelClassMap.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/TwoModelRuntimeConfigTest.kt`
- `apps/android/app/src/test/java/kr/co/hanium/dreamup/walksafe/inference/YoloEndToEndOutputParserTest.kt`
- `scripts/check_android_tflite_contract_20260531.py`
- `model/two_model_runtime.py`
- `model/test_two_model_runtime.py`
- `configs/walksafe_two_model_runtime_20260522.yaml`
- `configs/walksafe_two_model_runtime_stage1_mvp_20260523.json`
- `backend/tests/test_yolo_inference_adapter.py`
- `backend/tests/test_detect_v2.py`
- `apps/web/tests/risk-evaluator-policy.test.ts`
- `apps/web/tests/auto-report-v2-policy.test.ts`

### 검증
- `cd apps/android && ./gradlew :app:compileDebugKotlin --no-daemon` → BUILD SUCCESSFUL.
- `cd apps/android && ./gradlew :app:testDebugUnitTest --no-daemon` → BUILD SUCCESSFUL.
- `python scripts/check_android_tflite_contract_20260531.py` → PASS. unified asset missing은 disabled candidate warning.
- `python scripts/check_android_depth_scaffold_20260531.py` → PASS.
- `python scripts/check_walksafe_unified_training_plan_20260601.py --allow-missing-dataset` → PASS, dataset 미생성 warning.
- `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. .venv/bin/python -m pytest model/test_two_model_runtime.py backend/tests/test_yolo_inference_adapter.py backend/tests/test_detect_v2.py backend/tests/test_reports_v2.py -q` → PASS, 57 passed.
- `cd apps/web && npm run typecheck && npm run lint` → PASS.
- `python -m py_compile data_sources/scripts/build_walksafe_unified_coco_tactile.py scripts/export_walksafe_unified_tflite_20260601.py scripts/check_walksafe_unified_training_plan_20260601.py scripts/check_android_tflite_contract_20260531.py` → PASS.
- `python data_sources/scripts/build_walksafe_unified_coco_tactile.py --dry-run` → PASS, 10-class order 출력.
- `bash scripts/run_walksafe_unified_yolo26n_20260601.sh --print-only` → PASS, 학습 명령 출력.
- `python scripts/export_walksafe_unified_tflite_20260601.py --dry-run` → PASS, export 명령 출력.

### 남은 작업
- 실제 unified 학습용 데이터셋 materialize는 아직 하지 않았다. `TS8.zip`, `TS9.zip`, `VS1.zip`, `VS2.zip`, COCO train/val/annotations가 필요하다.
- 실제 `walksafe_unified_yolo26n_640_float32.tflite` asset이 없으므로 Android primary는 아직 `legacy_two_model`이다.
- 새 TFLite export 후 output contract `[1,300,6]`와 class order가 위 10-class와 일치하는지 확인해야 한다.
