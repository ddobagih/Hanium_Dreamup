# AI 기반 시각장애인 보행 지원 시스템 프로젝트 구현 계획

작성 기준일: 2026-05-11  
기준 문서:

- `2026 ICT 한이음 드림업 프로젝트 개요서.pdf`
- `2026년 한이음 드림업 프로젝트 수행계획서.pdf`

## 1. 프로젝트 개요

### 목표

스마트폰 카메라와 센서를 활용해 시각장애인의 보행 위험 요소를 실시간으로 감지하고, 음성으로 안내하며, 불량 보행 인프라를 정밀 위치 정보와 함께 신고 데이터로 축적하는 PWA 기반 보행 지원 시스템을 구현한다.

### 핵심 사용자

- 시각장애인 및 저시력 보행자
- 시각장애인 복지관, 교통약자 이동 지원 기관
- 보행 인프라 유지보수를 담당하는 지자체 및 공공기관

### 최종 산출물

- 모바일 브라우저에서 동작하는 PWA
- YOLO 기반 장애물 및 불량 인프라 탐지 모델
- 카메라 입력 기반 실시간 추론 모듈
- IMU+GPS 기반 위치 보정 및 이동 방향 추정 모듈
- STT/TTS 기반 음성 안내 인터페이스
- FastAPI 백엔드 및 PostgreSQL/PostGIS 신고 데이터 저장 구조
- 현장 수집 데이터 업로드 및 모델 재학습을 위한 MLOps 파이프라인
- 실서비스 도메인 배포 결과물과 최종 보고서

## 2. 구현 범위

### 필수 구현 범위

1. YOLO 비전 모델
   - 보행 위험 요소와 불량 인프라를 탐지한다.
   - 모바일 웹 환경에서 사용할 수 있도록 경량화와 브라우저 추론 방식을 검토한다.
   - 1차 목표는 모델 v1을 먼저 완성하는 것이다.

2. PWA
   - 스마트폰 카메라 입력을 받아 실시간 추론에 연결한다.
   - 시각장애인 사용을 전제로 화면 조작 의존도를 낮추고 음성 안내를 우선한다.
   - 저신호 환경을 고려해 기본 오프라인 캐시를 지원한다.

3. FastAPI/PostGIS 백엔드
   - 탐지 결과, 위치, 이미지 증거, 신고 상태를 저장한다.
   - 위치 기반 조회와 히트맵 분석을 위해 PostGIS 공간 데이터를 사용한다.

4. IMU+GPS 센서퓨전
   - DeviceMotion, DeviceOrientation, Geolocation 데이터를 수집한다.
   - 사용자의 이동 방향과 속도를 추정해 충돌 가능성이 높은 객체만 선별한다.
   - 불량 인프라 신고 좌표의 GPS 오차를 줄이는 데 사용한다.

5. STT/TTS 음성 내비게이션
   - 목적지 설정, 경로 안내, 위험 경고를 음성 중심으로 제공한다.
   - 초기 버전은 Web Speech API를 우선 검토하고, 품질이 부족하면 Whisper 또는 외부 STT/TTS API를 검토한다.

6. MLOps
   - 현장 수집 데이터를 클라우드 저장소에 업로드한다.
   - 모델 재학습, 성능 검증, 배포 과정을 자동화한다.
   - 초기 버전에서는 완전 자동화보다 데이터 업로드와 재학습 절차 문서화를 우선한다.

### 후순위 또는 확장 범위

- 지자체 민원 시스템과의 실제 API 연동
- Blue-Green 방식의 완전 무중단 모델 배포
- 월 1회 이상 자동 재학습 운영
- 안내견 로봇, 자율주행 휠체어, 스마트 안경, 흰 지팡이 센서 모듈 연동
- 전국 단위 공개 데이터셋 운영

## 3. 모델 우선 구현 계획

모델은 전체 서비스의 선행 작업이다. PWA, 서버, 음성 안내, MLOps는 모델 v1의 클래스와 추론 결과 형식이 정해진 뒤 연결한다.

### 모델 v1 목표

- PDF에 명시된 보행 위험 요소를 우선 탐지한다.
- 실시간 서비스 가능성을 확인할 수 있는 baseline 모델을 만든다.
- 모델 v1은 과도한 범위 확장을 피하고, 실제 수집 가능한 데이터 중심으로 구성한다.
- 최종 학습/검증 기준은 한국 보행 환경으로 둔다. 해외 공개 데이터는 smoke test 또는 pretrain 후보로만 사용한다.

### 모델 v1 클래스

| 클래스 ID | 클래스명 | 설명 |
| --- | --- | --- |
| 0 | `damaged_tactile_block` | 파손, 단절, 규격 불량 점자블록 |
| 1 | `parked_kickboard_bicycle` | 보행로에 방치된 전동 킥보드 또는 자전거 |
| 2 | `construction_obstacle` | 공사 구조물, 안전콘, 임시 펜스, 불법 적치물 |
| 3 | `pothole` | 보행로 포트홀, 꺼짐, 깊게 패인 구간 |

v1에서는 일반 차량, 사람, 신호등, 횡단보도, 상점 간판, OCR 대상 텍스트는 제외한다. 필요하면 v2에서 별도 기능으로 분리한다.

점자블록은 한국 점형/선형 점자블록과 국내 보도 환경을 기준으로 판단한다. 정상 점자블록은 positive label을 붙이지 않고, negative 이미지로 별도 확보해 오탐을 줄인다.

### 데이터셋 구성

권장 구조:

```text
datasets/walksafe_kr_v1/
  images/
    train/
    val/
    test/
  labels/
    train/
    val/
    test/
  data.yaml
```

데이터 수집 기준:

- 스마트폰 세로 화면 기준의 보행자 1인칭 시점 이미지를 우선한다.
- 밝은 낮, 흐린 날, 야간, 역광, 흔들림이 있는 이미지가 모두 포함되어야 한다.
- 각 클래스별 최소 200장 이상을 1차 목표로 잡는다.
- 모델 v1 baseline은 클래스별 데이터가 부족해도 먼저 학습하고, 부족한 클래스는 리스크로 기록한다.
- 개인정보 보호를 위해 얼굴, 차량번호, 민감 정보는 라벨링 또는 학습 전 비식별 처리한다.
- validation/test에는 한국 직접 촬영 또는 한국 인도보행 데이터셋만 포함한다.

라벨링 기준:

- 객체 전체가 보이면 전체 영역을 박스로 표시한다.
- 객체 일부만 보여도 보행 위험 판단이 가능하면 보이는 영역을 표시한다.
- 너무 작거나 흐려서 사람이 구분하기 어려운 객체는 라벨링하지 않는다.
- 하나의 객체가 여러 클래스에 걸쳐 보이면 실제 위험 원인에 가장 가까운 하나의 클래스로만 라벨링한다.

### 학습 및 평가

학습 순서:

1. 데이터셋 조사 및 수집 후보 정리
2. v1 클래스 기준 라벨링
3. YOLO baseline 학습
4. validation set 기준 mAP, precision, recall 확인
5. 실제 보행 영상 또는 테스트 이미지로 추론 지연시간 확인
6. 브라우저 추론 가능성을 위해 ONNX 또는 TensorFlow.js 변환 검토

모델 완료 기준:

- validation 기준 전체 mAP 0.9 이상을 목표로 한다.
- 실제 모바일 또는 모바일에 가까운 환경에서 경보 지연 1초 이내를 목표로 한다.
- 클래스별 성능이 낮은 경우 전체 평균만 보지 않고 낮은 클래스와 원인을 문서화한다.
- 테스트 이미지 20장 이상에서 사람이 보기에도 위험 객체 박스가 납득 가능해야 한다.

### 브라우저 추론 연결 기준

- 1차 후보: ONNX Runtime Web
- 2차 후보: TensorFlow.js
- 변환 후 확인할 항목:
  - 모델 로드 시간
  - 단일 프레임 추론 시간
  - 스마트폰 발열과 배터리 부담
  - 카메라 프레임 처리 중 UI 멈춤 여부

### 모델 v1 작업 체크리스트

- [x] 모델 클래스 4개 확정
- [x] 데이터셋 후보 출처 정리
- [x] 직접 촬영 데이터 수집 기준 정리
- [x] 라벨링 규칙 문서화
- [x] YOLO 학습 환경 구성
- [x] 공개 baseline 데이터셋 수집 및 YOLO 구조 변환
- [x] 학습 smoke test 실행
- [x] 한국 기준 데이터 전략 및 seesun 참고사항 반영
- [ ] baseline 학습 실행
- [ ] validation 성능 기록
- [ ] 테스트 이미지/영상 추론 결과 저장
- [ ] ONNX 또는 TensorFlow.js 변환 검토
- [ ] 모델 v1 완료 여부 판단

## 4. 전체 일정

PDF의 원래 수행기간은 2026-04-01부터 2026-10-30까지다. 현재 기준일이 2026-05-11이므로 4월 일정은 요구사항과 계획 기준선으로 취급하고, 실제 실행 일정은 5월 이후로 재정렬한다.

| 기간 | 주요 작업 | 산출물 |
| --- | --- | --- |
| 2026-05-11 ~ 2026-05-17 | 모델 요구사항 확정, 클래스 정의, 데이터셋 조사, 학습 환경 구성 | 모델 v1 클래스 정의서, 데이터셋 조사서, 학습 환경 기록 |
| 2026-05-18 ~ 2026-05-31 | 데이터 수집/라벨링, YOLO baseline 학습, 1차 평가 | 라벨링 데이터셋 v1, baseline 모델, 1차 성능 기록 |
| 2026-06-01 ~ 2026-06-14 | 모델 경량화, 브라우저 추론 방식 검토, 테스트 이미지/영상 추론 검증 | 경량 모델 후보, 변환 결과, 추론 샘플 |
| 2026-06-15 ~ 2026-06-30 | PWA 카메라 입력과 모델 추론 모듈 연결, 지연시간 측정 | PWA 추론 프로토타입, 지연시간 측정표 |
| 2026-07 | PWA UI, FastAPI 서버, PostGIS 신고 데이터 구조 구현 | PWA v0.9, FastAPI v0.9, DB ERD, 신고 API |
| 2026-08 | IMU+GPS 센서퓨전, 음성 안내, MLOps 데이터 업로드/재학습 파이프라인 구현 | 센서퓨전 모듈, STT/TTS 프로토타입, 데이터 업로드 파이프라인 |
| 2026-09 | 통합 테스트, 실환경 검증, 정확도/지연시간 개선 | 테스트 보고서, 현장 검증 데이터, 개선된 모델 |
| 2026-10-01 ~ 2026-10-30 | 최종 배포, 실서비스 도메인 연동, 최종 보고서/시연 자료 정리 | 배포 URL, 최종 보고서, 발표 자료, 소스코드 정리본 |

## 5. 검증 기준

### 모델

- 객체 인식 정확도: 90% 이상 목표
- 경보 지연: 1초 이내 목표
- 클래스별 성능 편차 확인
- 실제 보행 환경 이미지 또는 영상으로 수동 검수

### PWA

- 모바일 브라우저에서 카메라 권한 요청 및 영상 입력이 정상 동작해야 한다.
- 카메라 프레임이 모델 추론 입력으로 연결되어야 한다.
- 추론 중 화면이 멈추거나 브라우저가 종료되지 않아야 한다.
- 기본 화면은 세로형 모바일 사용을 기준으로 한다.

### 백엔드 및 데이터베이스

- 탐지 결과, 좌표, 이미지 증거, 신고 시간이 저장되어야 한다.
- PostGIS 좌표 데이터로 반경 검색이 가능해야 한다.
- 신고 목록과 단건 조회 API가 동작해야 한다.

### 센서퓨전

- DeviceMotion, DeviceOrientation, Geolocation 데이터 수집 가능 여부를 확인한다.
- 사용자의 이동 방향 추정값이 카메라 방향과 크게 어긋나지 않아야 한다.
- GPS 오차가 큰 상황은 보정값과 원본값을 함께 기록한다.

### 음성 안내

- 위험 객체 탐지 시 TTS 안내가 재생되어야 한다.
- 목적지 설정 또는 기본 명령이 STT로 인식되어야 한다.
- 음성 안내가 너무 자주 발생해 사용자의 인지 부담을 높이지 않아야 한다.

### 배포

- PWA가 HTTPS 환경에서 동작해야 한다.
- 실서비스 도메인으로 접속 가능해야 한다.
- 배포 후 카메라, 위치, 마이크 권한 흐름을 확인해야 한다.

## 6. 주요 리스크와 대응

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| 데이터 부족 | 특정 클래스의 인식률 저하 | v1 클래스 수를 4개로 제한하고 직접 촬영 데이터를 우선 확보 |
| 해외 데이터 도메인 차이 | 한국 점자블록과 보행 환경에서 오탐/미탐 증가 | 해외 공개 데이터는 smoke/pretrain으로만 쓰고 한국 validation/test로 최종 판단 |
| 모바일 브라우저 성능 부족 | 실시간 추론 지연 | 작은 YOLO 모델, 프레임 샘플링, ONNX Runtime Web 또는 TensorFlow.js 비교 |
| GPS 오차 | 신고 위치 신뢰도 저하 | 원본 GPS와 보정 좌표를 함께 저장하고, 오차 반경을 데이터에 포함 |
| 음성 인식 품질 부족 | 목적지 설정 실패 | Web Speech API로 시작하되 한국어 품질이 부족하면 외부 STT API 검토 |
| 일정 지연 | 통합 테스트 부족 | 모델 v1을 5월 말까지 먼저 완성하고, 나머지 기능은 단계적으로 연결 |
| 기능 과확장 | 구현 난이도 상승 | v1에서는 탐지, 카메라 연결, 신고 저장의 핵심 흐름을 우선 구현 |

## 7. 당장 시작할 작업

1. 모델 v1 클래스 4개를 최종 확정한다.
2. 각 클래스별 데이터셋 후보와 직접 촬영 계획을 정리한다.
3. YOLO 학습 환경을 만든다.
4. 소량 데이터라도 baseline 학습을 먼저 실행한다.
5. 성능이 낮은 클래스와 데이터 부족 원인을 기록한다.
6. 모델 변환과 브라우저 추론 가능성을 확인한다.

## 8. 운영 원칙

- 구현은 모델 v1부터 시작한다.
- 요청되지 않은 기능은 추가하지 않는다.
- 한 번에 전체 시스템을 완성하려 하지 않고, 검증 가능한 산출물을 주차별로 만든다.
- 기능 추가보다 정확도, 지연시간, 접근성 검증을 우선한다.
- 일정과 성능 수치는 실제 결과가 나오면 문서에 계속 갱신한다.
