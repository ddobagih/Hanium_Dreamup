# WalkMate YOLO11n 21-class primary model

- Source archive: user-provided `WalkMate_21cls_768_final_model.zip`, SHA256 `93102ed502a7966a193733725a0d17908c1481e4ffc02ed7e9f83b13d7259bdc`.
- Selected checkpoint: archive `model/best.pt`; architecture inspected as `yolo11n.yaml`, detection head `nc=21`, `end2end=false`. Exact labels are in `data.yaml` and `export_manifest.json`.
- Checkpoint SHA256: `6f8ae9d2bb82f91e4391e42957a1b374bd4b14efa4702a86f7283454bc38cee0`.
- Android artifact: `apps/android/app/src/main/assets/models/walkmate_yolo11n_21cls_768_float32.tflite`, SHA256 `d10aa174a2ceb8ae116a09b7b570e27ff71c296fbd98f240c11e69dbdb434b57`, 10,715,788 bytes.
- Export: existing isolated Python 3.12 conversion environment, Ultralytics 8.4.48, TensorFlow 2.19.1; FP32, batch 1, input 768, no embedded NMS. Full versions and arguments are recorded in `export_manifest.json`.
- Actual FlatBuffer input `[1,768,768,3]`, output `[1,25,12096]`, both FLOAT32; 366 operators. No opcode rewrite applied.
- Output channels: **normalized** center-x, center-y, width, height followed by 21 sigmoid probabilities, without objectness. Correction on 2026-09-13: the previous pixel-unit claim inspected the source head but missed normalization in the exported artifact. Direct identical-input comparison found TFLite boxes equal to PyTorch boxes / 768 (maximum normalized difference 2.3842e-6 on neutral input), with maximum score difference 6.1468e-8. The app now declares `yolo_raw_xywh_normalized`; the model bytes are unchanged.
- App postprocessing: best class per anchor, per-class configured score thresholds, class-aware NMS with IoU 0.7, maximum 300 detections, then clipping and letterbox removal. Initial thresholds were carried by equivalent class meaning or defaulted to 0.15; they are not tuned for this model.
- The primary runtime keeps logical key `unified_walksafe`; config version, artifact hash, class order, output contract, NMS settings and fixture hash invalidate old tuning bindings. `fallback_model` is null. Old assets remain preserved for explicit rollback; FastSAM auxiliary is unchanged.
- Calibration candidate images preserve original JPEG and annotation geometry. Class IDs are remapped by names. A current baseline must actually produce an allowed top-class score within ±0.02 of its configured threshold on a declared candidate image with a finite valid bbox; otherwise calibration is INCONCLUSIVE. No prior model's near-threshold scores are asserted for this model.
- Completed here: conversion, archive/checkpoint/class metadata and FlatBuffer inspection, scoped Kotlin compilation. No functional test, benchmark, APK build/install, device action, field accuracy or cross-format numerical equality test was run, per user instruction. Runtime CPU/GPU compatibility, latency and memory cost remain for user validation.
