# 보안 정책

## 지원 범위

보안 수정의 대상은 기본 브랜치 `current`의 최신 코드와 그 코드가 사용하는 Android 사용자·관리자 앱, Android Gateway, Backend, 모델·데이터 처리, 계약·배포 도구입니다.

`main`, `LEGACY_REFERENCE` 자료, 과거 증거 snapshot, 임의 fork는 지원 범위가 아닙니다. 현재 저장소는 정식 출시 승인을 받지 않았으며, 이 지원 범위가 정식 시험 완료나 출시 적격을 의미하지는 않습니다.

## 비공개 신고

보안 문제를 공개 GitHub Issue, Discussion, Pull Request, 커밋 메시지에 올리지 마십시오. 비밀값·취약점 상세·개인 식별 정보(PII)·실제 위치·사진·신고 자료도 공개 채널에 첨부하지 마십시오.

저장소 소유자 `@ddobagih` 또는 프로젝트에서 이미 사용 중인 비공개 팀 채널로 신고합니다. 비공개 채널에 접근할 수 없다면 세부 내용을 보내기 전에 저장소 접근을 관리하는 경로로 소유자에게 안전한 전달 방법을 요청하십시오. 이 저장소는 공개 신고용 이메일이나 응답 시간을 별도로 약속하지 않습니다.

## 최소 신고 내용

실제 사용자 자료 대신 합성·가림 처리한 자료로 다음만 전달합니다.

- 영향을 받는 브랜치·커밋과 구성요소
- 관찰한 동작, 기대한 동작, 예상 영향
- 최소 재현 절차와 필요한 환경
- 민감값을 제거한 오류·로그·화면 자료
- 이미 시행한 임시 완화가 있다면 그 내용

토큰, 비밀번호, 개인키, 서명키, 복구코드, 운영 데이터베이스 내용, 실제 사용자 식별자와 원본 증거는 보내지 않습니다. 재현에 꼭 필요하면 소유자와 별도 보호 전달 방법을 먼저 합의합니다.

## 조사 경계

명시적으로 허가받은 로컬·시험 환경에서만 재현합니다. 운영 서비스, 실제 기기·사용자·기관, 제3자 시스템을 대상으로 한 무단 검사, 서비스 방해, 데이터 변경·반출은 허용되지 않습니다.

소유자는 신고를 비공개로 분류하고 영향과 재현 여부를 확인한 뒤 수정·비밀값 회전·공개 시점을 조율합니다. 수정 전에는 취약점 상세를 공개하지 마십시오.
