# 2026-05-14 Voice STT/TTS Execution

작업 기준: `docs/voice_stt_tts_3day_execution_plan.md`의 `2026-05-14 Checklist: Schema and PWA STT Wiring`

제약 준수:

- 음성 서버를 새로 실행하지 않았다.
- STT/TTS 대형 모델을 새로 다운로드하거나 장시간 추론을 재실행하지 않았다.
- 기존 CSV/WAV/health smoke 결과만 확인했다.

## 실행 명령

```bash
pwd
git status --short
sed -n '1,240p' docs/voice_stt_tts_3day_execution_plan.md
rg --files docs voice scripts samples
stat -c '%F %n' .venv-voice requirements-voice.txt voice/server.py scripts/test_stt.py scripts/test_tts.py samples/voice/stt/myvoice
rg --files outputs samples/voice
sed -n '1,240p' voice/intents.py
rg -n "speech|voice|MediaRecorder|microphone|audio|intent|NEXT_PUBLIC_VOICE|VOICE_API|speechSynthesis|getUserMedia|RISK_ALERTS|lastSpoken" apps/web/app/page.tsx apps/web
find samples/voice/stt/myvoice -maxdepth 2 -type f -printf '%s %p\n'
find outputs/voice -maxdepth 2 -type f -printf '%s %p\n'
sed -n '1,80p' outputs/voice/stt_myvoice_results.csv
sed -n '1,240p' scripts/test_stt.py
sed -n '1,240p' scripts/test_tts.py
sed -n '1,240p' voice/server.py
sed -n '1,120p' outputs/voice/server_health_smoke.json
.venv-voice/bin/python --version
sed -n '1,200p' requirements-voice.txt
sed -n '110,150p' apps/web/app/page.tsx
sed -n '228,248p' apps/web/app/page.tsx
sed -n '430,450p' apps/web/app/page.tsx
sed -n '339,365p' apps/web/app/page.tsx
sed -n '1,80p' outputs/voice/tts_qwen3_0_6b_custom_sohee.csv
sed -n '1,80p' outputs/voice/tts_qwen3_0_6b_custom_probe.csv
sed -n '1,80p' outputs/voice/stt_myvoice_results_before_rule_update.csv
sed -n '1,80p' outputs/voice/stt_intent_dry_run.csv
awk -F, 'NR>1 {total++; if ($9=="success") success++; if ($9=="failure") failure++; if ($9=="needs_manual_review") manual++; if ($10 ~ /expected intent/) known++; if ($8=="True") known_success++; sum+=$7; times[++n]=$7} END {for(i=1;i<=n;i++) for(j=i+1;j<=n;j++) if(times[i]>times[j]){t=times[i];times[i]=times[j];times[j]=t}; p95_index=int(0.95*n); if(p95_index<1)p95_index=1; if(p95_index<n && p95_index<0.95*n)p95_index++; printf("total=%d known=%d success=%d failure=%d manual=%d known_success=%d avg=%.3f p95=%.3f\n", total, known, success, failure, manual, known_success, sum/n, times[p95_index])}' outputs/voice/stt_myvoice_results.csv
awk -F, 'NR>1 {total++; if ($5=="True") success++; sum+=$4; if($4>max)max=$4} END {printf("total=%d success=%d avg=%.3f max=%.3f\n", total, success, sum/total, max)}' outputs/voice/tts_qwen3_0_6b_custom_sohee.csv
find outputs/voice/cache -maxdepth 1 -type f -printf '%s %p\n'
find outputs/voice/tts -maxdepth 1 -type f -name '*.wav' -printf '%s %p\n'
```

## 결과

### 통과

- 작업 위치는 `/home/ddobagi/Code/hanium-dreamup`이다.
- `git status --short` 출력이 비어 있어 확인 시점에 작업 트리는 clean 상태였다.
- 필수 파일/디렉터리 존재 확인:
  - `.venv-voice`: directory
  - `requirements-voice.txt`: regular file
  - `voice/server.py`: regular file
  - `scripts/test_stt.py`: regular file
  - `scripts/test_tts.py`: regular file
  - `samples/voice/stt/myvoice`: directory
- `.venv-voice/bin/python --version`: `Python 3.14.4`
- `requirements-voice.txt`에는 `fastapi`, `uvicorn`, `python-multipart`, `faster-whisper`, `qwen-tts`, `torch`, `torchaudio` 등 로컬 음성 서버 의존성이 있다. 유료/클라우드 API 클라이언트 항목은 보이지 않는다.
- `voice/server.py`는 계획과 같은 API 초안을 제공한다:
  - `GET /health`
  - `POST /speech/intent`
  - `POST /speech/stt`
  - `POST /speech/tts`
- 기존 `outputs/voice/server_health_smoke.json` 확인 결과:
  - `status`: `ok`
  - `server`: `voice`
  - `cuda_available`: `true`
  - `gpu`: `NVIDIA GeForce RTX 5070 Ti`
  - `stt_model`: `medium`
  - `tts_model`: `Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice`
  - `tts_mode`: `custom`

### 실제 사람 음성 STT 결과

샘플 경로: `samples/voice/stt/myvoice`

확인된 파일:

- `samples/voice/stt/myvoice/길 안내 시작해.m4a`
- `samples/voice/stt/myvoice/다시말해줘.m4a`
- `samples/voice/stt/myvoice/목적지 서울역으로 설정해.m4a`
- `samples/voice/stt/myvoice/신고해.m4a`
- `samples/voice/stt/myvoice/음성꺼.m4a`
- `samples/voice/stt/myvoice/음성켜.m4a`
- `samples/voice/stt/myvoice/지금어디야.m4a`
- `samples/voice/stt/myvoice/현재 위험 신고해.m4a`

결과 CSV:

- 현재 결과: `outputs/voice/stt_myvoice_results.csv`
- 규칙 업데이트 전 결과: `outputs/voice/stt_myvoice_results_before_rule_update.csv`

현재 결과 요약:

- 총 파일: 8
- 현재 schema상 known intent 파일: 7
- 성공: 7
- 실패: 0
- manual review: 1
- known-intent 성공: 7/7
- 평균 지연: 0.457 sec
- p95 지연: 0.572 sec

manual review 항목:

- `지금어디야.m4a`
- transcript: `지금 어디야?`
- 현재 intent: `unknown`
- 사유: 현재 지원 intent set에 위치 질의가 매핑되어 있지 않음

### TTS 기존 결과

결과 CSV:

- `outputs/voice/tts_qwen3_0_6b_custom_sohee.csv`
- `outputs/voice/tts_qwen3_0_6b_custom_probe.csv`

`outputs/voice/tts_qwen3_0_6b_custom_sohee.csv` 요약:

- 총 문장: 6
- 성공: 6
- 평균 생성 시간: 2.377 sec
- 최대 생성 시간: 2.950 sec
- sample rate: 24000 Hz
- mode: `custom`

생성 WAV는 `outputs/voice/tts/*.wav`에 존재한다. `outputs/voice/cache`에는 현재 `.gitkeep`만 있어 캐시 WAV는 아직 없다.

### Intent Schema 상태

현재 `voice/intents.py` 지원 intent:

- `set_destination`
- `create_report`
- `voice_off`
- `voice_on`
- `repeat_last`
- `start_navigation`
- `unknown`

`get_current_location` 상태:

- 계획 문서에는 2026-05-14 결정/추가 후보로 포함되어 있다.
- 실제 사람 음성 `지금어디야.m4a`가 이미 존재하고 현재 `unknown`으로 남아 있다.
- `voice/intents.py`에는 아직 `get_current_location` 분류 규칙이 없다.
- PWA handler도 아직 없으므로 추가는 대기 상태다.
- 판단: 2026-05-14 범위에서 `get_current_location`을 같은 이름으로 추가하는 것이 적절하다. 다만 이번 작업의 쓰기 범위가 문서 1개로 제한되어 코드 반영은 하지 않았다.

### PWA 연결 상태

현재 `apps/web/app/page.tsx` 확인 결과:

- `speak()`는 브라우저 `speechSynthesis`만 사용한다.
- 위험 감지 시 `RISK_ALERTS` 문구를 말하고 진동을 실행한다.
- 카메라 권한 요청은 `getUserMedia({ audio: false, video: ... })`라 마이크 권한과 분리되어 있다.
- 현재 버튼은 음성 안내 on/off 토글이다. 음성 명령 녹음 버튼이 아니다.
- `MediaRecorder` 사용 흔적은 없다.
- `NEXT_PUBLIC_VOICE_API_BASE` 또는 유사한 음성 서버 주소 환경변수 사용 흔적은 없다.
- `/speech/stt` multipart 업로드 연결은 아직 없다.
- `lastSpokenMessageRef`, `destination` state, `start_navigation` 상태 처리 등 intent handler 기반 상태는 아직 없다.

## 통과/대기/막힘

| 항목 | 상태 | 메모 |
| --- | --- | --- |
| 음성 전용 가상환경 확인 | 통과 | `.venv-voice` 존재, Python 3.14.4 |
| 음성 서버 파일 확인 | 통과 | `voice/server.py` 존재, 계획 API 엔드포인트 구현 |
| STT 테스트 스크립트 확인 | 통과 | `scripts/test_stt.py` 존재 |
| TTS 테스트 스크립트 확인 | 통과 | `scripts/test_tts.py` 존재 |
| 실제 사람 음성 샘플 확인 | 통과 | `samples/voice/stt/myvoice`에 8개 m4a |
| 실제 사람 음성 CSV 확인 | 통과 | `outputs/voice/stt_myvoice_results.csv`, known-intent 7/7 성공 |
| TTS 기존 결과 확인 | 통과 | 0.6B custom 6/6 성공 CSV와 WAV 존재 |
| TTS 캐시 WAV 확인 | 대기 | `outputs/voice/cache`에는 `.gitkeep`만 있음. 2026-05-15 범위 |
| `get_current_location` schema 추가 | 대기 | 필요성 확인. 코드 반영은 쓰기 범위 밖 |
| PWA STT wiring | 대기 | `MediaRecorder`, env var, `/speech/stt` 호출, intent handler 없음 |
| 음성 서버 재실행 | 대기 | 제약에 따라 실행하지 않음 |
| 대형 모델 재다운로드/재추론 | 대기 | 제약에 따라 실행하지 않음 |

막힘:

- 이번 세션의 쓰기 범위가 `docs/execution/2026-05-14_voice.md` 하나로 제한되어 있어 `voice/intents.py`와 `apps/web/app/page.tsx`에 직접 반영할 수 없다.
- PWA 음성 서버 주소 환경변수 이름은 아직 프론트/백엔드 담당자 확정이 필요하다. 계획상 후보는 `NEXT_PUBLIC_VOICE_API_BASE=http://127.0.0.1:9001`이다.

## 다음 액션

1. 프론트/백엔드 담당자와 음성 서버 base URL env 이름을 확정한다. 후보: `NEXT_PUBLIC_VOICE_API_BASE`.
2. 모델/음성 담당 범위에서 `voice/intents.py`에 `get_current_location` 규칙을 추가한다.
3. 프론트 담당 범위에서 `MediaRecorder` 기반 명령 녹음 버튼을 기존 음성 안내 토글과 분리해 추가한다.
4. PWA에서 `POST /speech/stt` multipart field 이름을 `audio`로 맞춘다.
5. PWA intent handler에 `create_report`, `voice_on`, `voice_off`, `repeat_last`, `set_destination`, `start_navigation`, `get_current_location`, `unknown`을 같은 이름으로 연결한다.
6. STT timeout은 5초 이내 실패 처리로 고정한다.
7. 2026-05-15 작업에서 `RISK_ALERTS`와 맞춘 TTS cache WAV 생성/재생/fallback 정책을 진행한다.
