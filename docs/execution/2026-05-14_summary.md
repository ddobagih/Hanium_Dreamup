# 2026-05-14 Execution Summary

기준 계획:

- `docs/project_3day_execution_plan.md`
- `docs/frontend_3day_execution_plan.md`
- `docs/backend_3day_execution_plan.md`
- `docs/model_training_3day_execution_plan.md`
- `docs/voice_stt_tts_3day_execution_plan.md`

## 전체 결론

2026-05-14 1일차 계획 중 로컬에서 실행 가능한 항목은 대부분 완료했다.

- 프론트엔드 정적 검증과 dev server HTTP 확인 통과.
- 백엔드 PostGIS/Alembic/pytest/API health 확인 통과.
- 모델 v2 baseline freeze, dataset validation, test split 결과 확인 통과.
- STT/TTS 로컬 자산과 실제 사람 음성 결과 확인 통과.

실폰, 카메라 권한, GPS/방향 센서, TTS/진동 체감, PWA 설치/오프라인 shell, AI Hub 외부 validation 다운로드는 수동 조건이 있어 다음 단계로 남긴다.

## Frontend

상세 로그: `docs/execution/2026-05-14_frontend.md`

확인 결과:

- `npm run lint`: 통과.
- `npm run typecheck`: 통과.
- Next dev server HTTP 확인:
  - `/`: HTTP 200.
  - `/admin`: HTTP 200.
  - `/manifest.webmanifest`: HTTP 200.
  - `/sw.js`: HTTP 200.
- fake detector, report API client, admin UI, PWA shell 구조는 문서와 일치.

대기:

- 브라우저 카메라 권한 기반 fake detection E2E.
- 실폰 카메라, GPS, 방향 센서, TTS, 진동.
- PWA 설치와 오프라인 shell.

## Backend

상세 로그: `docs/execution/2026-05-14_backend.md`

확인 결과:

- `docker compose up -d db`: PostGIS 컨테이너 healthy.
- Alembic `upgrade head`: 통과.
- Alembic current: `202605120001 (head)`.
- `.venv/bin/python -m pytest backend/tests -q`: `16 passed`.
- `/health`: HTTP 200, `{"status":"ok"}`.
- `/detect/health`: HTTP 200, `model_status: "unavailable"`, `reason: "model_not_configured"`.
- `/reports?limit=5`: HTTP 200, 테스트 DB 신고 목록 반환.
- API 계약과 `/detect` placeholder는 문서와 코드가 일치.

주의:

- 전체 pytest는 로컬 개발 DB row와 `backend/uploads/test` 테스트 이미지를 생성했다.
- 해당 산출물은 Git 추적 대상이 아니다.

## Model

상세 로그: `docs/execution/2026-05-14_model.md`

확인 결과:

- v2 artifact 존재 확인.
- freeze manifest `SHA256SUMS.txt` 검증 통과.
- `model/validate_yolo_dataset.py --data datasets/walksafe_kr_v2/data.yaml`: 통과.
- dataset count:
  - train: `16,433`
  - val: `4,695`
  - test: `2,347`
- test split 결과:
  - precision: `0.728`
  - recall: `0.581`
  - mAP50: `0.657`
  - mAP50-95: `0.481`
  - validation 대비 mAP50-95 delta: `-0.01094`

판단:

- v2는 test split 기준 baseline frozen 가능.
- recall이 아직 낮아 실제 서비스 후보 확정은 AI Hub 513 외부 validation 이후로 보류한다.

## STT/TTS

상세 로그: `docs/execution/2026-05-14_voice.md`

확인 결과:

- `.venv-voice`, `requirements-voice.txt`, `voice/server.py`, `scripts/test_stt.py`, `scripts/test_tts.py` 존재 확인.
- 실제 사람 음성 폴더: `samples/voice/stt/myvoice`.
- 실제 사람 음성 결과: `outputs/voice/stt_myvoice_results.csv`.
- known intent 7개 기준 7/7 성공.
- 평균 지연: `0.457s`.
- p95 지연: `0.572s`.

대기:

- `지금 어디야?`는 현재 `unknown`이므로 `get_current_location` intent 추가 결정 필요.
- PWA `MediaRecorder`, `NEXT_PUBLIC_VOICE_API_BASE`, `/speech/stt` 연결은 아직 미구현.

## 2026-05-15 우선순위

1. Android 실폰에서 PWA 카메라, GPS, 방향 센서, TTS, 진동, 신고 E2E를 확인한다.
2. 백엔드 테스트 DB/업로드 산출물 정리 절차를 확정한다.
3. AI Hub 513 validation zip 확보 조건을 확인한다.
4. `get_current_location` intent를 지원할지 결정하고, 지원한다면 voice/PWA schema를 맞춘다.
5. 실제 모델 adapter 전환 전 `MODEL_ARTIFACT_PATH`, model version, class order, threshold 계약을 백엔드/모델 사이에 고정한다.
