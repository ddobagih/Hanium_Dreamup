# 단일 관리자 휴대전화 분실 복구훈련 절차

- 문서 ID: `WS-SINGLE-ADMIN-RECOVERY-DRILL-PROTOCOL-20260722-001`
- 버전: `0.1.0`
- 상태: `DRAFT_PROCEDURE_NOT_EXECUTED`
- 실행 결과: **NOT_RUN**

이 문서는 실행 절차 초안이다. 이 문서를 만들었다는 사실만으로 복구훈련 gate를 닫거나 면제하지 않는다.

## 실행 순서

1. **고정 형상과 정상 초기상태를 기록한다.**
   - 기대: 보안 상태 NORMAL, 기존 기기 A 세션 목록, 고위험 작업은 최근 비밀번호·TOTP 재확인 뒤에만 허용된다.
   - 증거: 비밀을 제외한 앱·서버·DB migration·설정 지문, 시각, 세션 ID·기기 ID의 부분 마스킹 목록
2. **기기 A를 사용할 수 없다고 선언하고 기기 B에서 외부 복구코드로 복구 시작을 요청한다.**
   - 기대: 보안 상태가 RECOVERY_IN_PROGRESS로 바뀌고 기존 모든 세션이 폐기되며, 복구코드는 아직 사용 완료 처리되지 않는다.
   - 증거: 상태 버전, 기존 세션 폐기 시각, 비밀을 제외한 감사 사건
3. **기기 A의 기존 세션과 다른 기존 세션으로 상태조회 및 고위험 작업을 시도한다.**
   - 기대: 기존 세션은 모두 거부되고 권한 변경·자료 삭제·출시 승인은 실패 닫힘으로 동결된다.
   - 증거: HTTP 상태·오류 코드와 오프라인 gate 실패 결과; 원문 증명값 제외
4. **첫 복구 시작 응답을 유실했다고 가정하고 같은 미사용 코드·같은 기기 B로 다시 시작한다.**
   - 기대: 복구 만료시각은 늘어나지 않고 새 복구 증명값만 발급되며 이전 복구 증명값은 사용할 수 없다.
   - 증거: 마스킹된 transaction ID, 변하지 않은 만료시각, 이전 증명값 거부 결과
5. **다른 기기 또는 다른 코드로 진행 중 복구를 가로채려 시도한다.**
   - 기대: 요청은 거부되고 RECOVERY_IN_PROGRESS와 고위험 동결이 유지된다.
   - 증거: 오류 코드, 상태 버전, 비밀 없는 감사 사건
6. **기기 B에서 새 비밀번호, 새 외부 TOTP 비밀의 현재 코드, 최신 복구 증명값으로 복구를 완료한다.**
   - 기대: 복구코드가 이 성공 시점에만 사용 처리되고 기존 세션은 계속 폐기된 채 새 기기 B 세션만 만들어지며 상태가 NORMAL로 돌아온다.
   - 증거: 완료 시각, 사용 처리 시각, 새 세션 ID의 부분 마스킹, 상태 버전
7. **이전 비밀번호·이전 TOTP·사용한 복구코드·이전 복구 증명값·기기 A 세션을 각각 재사용한다.**
   - 기대: 모두 거부되고 성공한 TOTP 시각구간을 다시 사용해도 거부된다.
   - 증거: 입력 원문을 저장하지 않은 거부 결과와 감사 사건
8. **새 자격정보로 로그인하고 최근 비밀번호·TOTP 재확인 전후의 고위험 작업을 비교한다.**
   - 기대: 재확인 전에는 차단되고 재확인 뒤 정해진 짧은 시간 안에서만 허용된다.
   - 증거: 마스킹된 세션 ID, 재확인 만료시각, 전후 결과
9. **복구자료의 휴대전화 외부 암호화 보관과 실제 열람·복원 가능성을 확인하고 시험환경을 정리한다.**
   - 기대: 복구자료는 기기 B 안에만 남지 않고, 원문 비밀은 증거 묶음에 포함되지 않으며 시험용 세션과 자료가 정해진 절차로 폐기된다.
   - 증거: 보관 매체 식별자·암호화/접근통제 확인·삭제 영수증; 비밀 원문 제외

## 합격조건

- 모든 단계의 기대 결과가 같은 고정 형상에서 충족되고 P0/P1 결함이 없다.
- 복구 시작부터 완료까지 기존 세션과 고위험 작업이 우회 없이 차단된다.
- 복구코드는 성공 완료 전에는 소비되지 않고 성공 뒤에는 재사용되지 않는다.
- 응답 유실 재개는 같은 코드·같은 기기에만 허용되며 만료를 연장하지 않고 이전 증명값을 무효화한다.
- 원문 비밀이나 실제 개인정보가 시험 증거·로그·저장소에 남지 않는다.
- 원자료·환경·실행자·시각·결함·결론을 SHA-256 manifest로 결속하고 지정 승인 절차를 마친다.

현재 gate는 `GATE-SINGLE-ADMIN-RECOVERY-DRILL = NOT_RUN`, 미면제이며 출시는 `NOT_ELIGIBLE`이다.

내용 지문: `ebb009d9ce7471dd23d3632590fbb1ab1739d8d2e08e0cb7352fdd2f227e973a`
